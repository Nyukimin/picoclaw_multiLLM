# レビュー結果

- status: draft
- owner: 未定
- source: `docs/archive/RenCrow_docs_handoff_20260701.zip` のみ
- last_reviewed: 2026-07-01
- scope: 引き継ぎ ZIP 最小セットの初回レビュー

## 読んだ範囲

元 `docs.zip` は読んでいない。今回読んだのは `RenCrow_docs_handoff_20260701.zip` 内の次の範囲。

- `00_引き継ぎ/`
- `00_読む順番.md`
- `README.md`
- `README_引き継ぎ版.md`
- `01_理解/`
- `02_正本仕様/`
- `03_記憶検索/`
- `04_構築指標/`
- `05_運用/`
- `99_整理/`

## 分かったこと

この ZIP は履歴の完全保存ではなく、RenCrow / PicoClaw のドキュメント整理を再開するための最小入口である。正本は `02_正本仕様/`、理解用は `01_理解/`、構築判断は `04_構築指標/`、整理根拠は `99_整理/` として分けられている。

現行の基本線は、Mio / Shiro / Aka / Ao / Gin / Kin / Kuro / Midori の Agent 体系である。Chat / Worker / Coder / Heavy / Wild の責務境界を維持し、Coder は直接実行せず、Worker が採用・適用・ログ記録・安全確認を担う。

Runtime は `rencrow.llm` / `rencrow.tts` / `rencrow.stt` を中心に、Viewer / picoclaw から backend を直接呼ばない方針で整理されている。実運用 Config の正本はユーザー環境側 `.picoclaw/config.yaml` で、Repo 内 `run/local-windows-runtime.yaml` は明示指定時だけ使うスナップショットであり、暗黙 fallback 先ではない。

記憶・検索は検索 API 常用ではなく、L0 / L1 / L2 の毎ターン想起、staging、validator、promoter を中心にする構成である。検索 API は未知語、最新確認、公式情報、裏取りなどの fallback として扱う。

## 分からなかったこと

承認フローの存廃は未解決である。`05_運用/02_Codebase_Run_Summary.md` では、承認フロー維持仕様と廃止プランが競合している可能性が示されている。正本へ反映する前に、対象仕様、実装、履歴の確認が必要。

`risk=high` 時の停止ルールは、仕様上の要求と実装状況が一致しているか未確定である。実装未確認のまま「実装済み」とは書けない。

サニタイズ責務が `pkg/security` 側か `materials` 側かは、ZIP 内レビューだけでは確定できない。これも正本変更ではなく、まずチケット化して確認する対象。

## 矛盾または更新漏れの可能性

`02_正本仕様/01_仕様.md` と `02_正本仕様/02_実装仕様.md` の冒頭に、旧配置の `docs/01_正本仕様/...` や除外済みの `docs/02_v2統合分割仕様/...` への参照が残っている。引き継ぎ ZIP 内では正本が `02_正本仕様/` に再配置されているため、入口文書としては混乱要因になる。

`01_理解/02_キャラクター・エージェント仕様.md` の主な参照にも `docs/01_正本仕様/実装仕様.md` が複数残っている。内容そのものより、参照先のパス修正が必要。

`02_正本仕様/04_IdleChat.md` には、除外済みの `09_IdleChat仕様/` 詳細資料へのリンクが複数残っている。詳細仕様への参照自体は有用だが、最小セット内には存在しないため、「必要時に元 docs.zip から一時復帰する資料」と明記した方がよい。

`01_理解/02_キャラクター・エージェント仕様.md` では Kin / `CODE4` が説明されている一方、`02_正本仕様/01_仕様.md` のルーティングカテゴリは `CODE / CODE1 / CODE2 / CODE3` までを主に扱っている。Kin が補助 Coder 枠なら、正本側の route 表現と理解用文書の整合性を確認する必要がある。

## 初回判断

現時点で元 `docs.zip` からすぐ恒久復帰すべき資料はない。まずは参照パス修正、未解決論点のチケット化、除外ファイルの条件付き復帰候補整理を行うのが妥当。
