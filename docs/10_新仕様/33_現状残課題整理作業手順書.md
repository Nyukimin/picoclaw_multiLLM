# RenCrow 現状残課題整理 作業手順書

## 1. 目的

この手順書は、`docs/10_新仕様` と現行実装を照合して確認した残課題を、次の作業者が 1 件ずつ処理するための実務手順に落としたものである。

現状は「主要機能の入口が未実装」という段階ではない。残課題の中心は、以下である。

- 接続残
- Human approval 後の適用残
- 実運用 E2E 未確認
- 外部環境待ちの E2E
- 保留 / 対象外の明確化

## 2. この文書の使い方

この文書は、残課題の一覧表ではなく、作業者が次に何を確認し、どの条件で pass / fail / skipped / blocked と判定し、どの docs に結果を戻すかを決めるための手順書である。

作業者は、次の流れで進める。

1. `3. 作業前に必ず読むもの` を確認する。
2. `4. 作業ルール` で、今回の作業が調査だけか、実装変更を含むかを分ける。
3. `5. 現在の残課題と処理順` から 1 件を選ぶ。
4. `7. 課題別作業手順` の該当節に従って確認する。
5. `10. 結果記録テンプレート` で証跡を残す。
6. `11. docs 更新先` に従って台帳へ反映する。

1 回の作業で複数項目をまとめて完了扱いしない。複数項目を触る場合でも、判定と証跡は項目ごとに分ける。

## 3. 作業前に必ず読むもの

作業開始前に、以下を順に確認する。

1. `AGENTS.md`
2. `CLAUDE.md`
3. `docs/01_正本仕様/実装仕様.md`
4. `docs/10_新仕様/13_実装項目インベントリ.md`
5. `docs/10_新仕様/17_E2E残課題.md`
6. `docs/10_新仕様/31_未実装項目実装仕様.md`
7. `docs/10_新仕様/32_E2E_runtime確認チェックリスト.md`
8. 必要に応じて `docs/01_正本仕様/12_実装状況_20260505.md`

実装判断で迷った場合は、`docs/01_正本仕様/実装仕様.md` と `docs/10_新仕様/31_未実装項目実装仕様.md` を優先する。

## 4. 作業ルール

### 4.1 編集可否

- この手順書を使う段階では、まず調査と確認を行う。
- 実装変更に入る場合は、ユーザーの明示的な OK を得る。
- 実装変更では、先に再現テスト、失敗テスト、または代替検証手順を明文化する。
- 無関係な cleanup は行わない。

### 4.2 開始条件

作業を始める前に、次を明確にする。

| 確認項目 | 内容 |
|---|---|
| 対象 | どの残課題を扱うか |
| 作業種別 | 調査 / E2E確認 / 設計 / 実装変更 / docs反映 |
| 外部依存 | API key、実ブラウザ、実マイク、複数マシン、外部チャネルの有無 |
| Human approval | 外部送信、PR作成、正式環境適用、sandbox promotion が必要か |
| 記録先 | `17_E2E残課題.md`、`31_未実装項目実装仕様.md`、または別の該当仕様 |

外部依存が不足している場合は、実行前に `skipped` または `blocked` の候補として扱い、成功扱いにしない。

### 4.3 終了条件

作業を終える前に、次を確認する。

| 確認項目 | 内容 |
|---|---|
| 判定 | pass / fail / skipped / blocked のいずれかに分類したか |
| 証跡 | config、route、session、job、event log、response を残したか |
| fallback | fallback を成功扱いしていないか |
| docs反映 | 結果を該当 docs に反映したか、または反映不要の理由を記録したか |
| 次アクション | 未完了の場合、次に何を準備すれば進むかを書いたか |

終了条件を満たせない場合は、完了扱いにせず `blocked` または `fail` として理由を残す。

### 4.4 判定ルール

- skip は成功ではなく、環境未準備または未確認として扱う。
- fallback は成功ではなく、期待 route 不成立として扱う。
- health ok だけで user flow 成立扱いにしない。
- handler / stub / unit test の成功だけで実 API E2E 完了扱いにしない。
- 実ブラウザ UI は DOM 存在ではなく、visible state と実操作の成立で判定する。
- 外部送信、PR 作成、正式環境適用は Human approval 必須として扱う。
- 削除済みの旧 docs / archive / old / codebase-map は現行判断の一次参照にしない。

### 4.5 証跡として残すもの

各作業では、最低限以下を記録する。

| 証跡 | 内容 |
|---|---|
| 日時 | 実行日と時刻 |
| 実行環境 | local / live service / browser / distributed / external API |
| config source | `~/.picoclaw/config.yaml`、runtime-config、または一時 config |
| 実行コマンド / 操作 | `go test`、curl、Viewer 操作、外部イベント送信など |
| route / session / job | route、job_id、session_id、channel id |
| event log | routing、agent response、attachment、warning、error |
| response | ユーザーに返った本文または失敗表示 |
| 判定 | pass / fail / skipped / blocked |
| skip / fail 理由 | secret 不足、実機不足、device 不足、route 不成立など |

### 4.6 Goal 実行時の自律進行ルール

この手順書を Goal に設定して長時間実行する場合は、不要な確認待ちで作業を止めず、検証済み commit 単位で自律的に進める。ただし、未確認差分を混ぜたまま進めてはいけない。

Goal 用プロンプトには、以下を必ず含める。

```text
この作業手順書に沿って、未完了項目を小さな検証済み commit 単位で順に処理してください。

各単位で、対象・変更範囲・検証コマンド・完了条件を自分で定義してから実装してください。
実装後、該当テストと必要な runtime / Viewer 確認を行ってください。
確認済みの関連ファイルだけを選択的に stage し、日本語 commit message で commit してください。
commit 後は push してください。
push できたら、ユーザー確認を待たずに次の未完了項目へ進んでください。

ただし、次の場合だけ停止して報告してください。
- テスト失敗や runtime 確認失敗が、現在の作業範囲内で短時間に解消できない。
- 変更が複数領域に広がり、1 commit の責務が曖昧になった。
- 外部 secret / 実機 / 外部 API / 複数マシンなど、こちらで準備できない依存が必要。
- destructive operation、依存追加、CI/deploy 設定変更、ファイル削除、セーフガード変更が必要。
- push 対象に未確認差分や live E2E 生成物が混ざりそうになった。

禁止:
- worktree 全体を一括 stage / commit しない。
- `vault/` や live E2E 生成物を明示指示なく commit しない。
- 失敗したテストを無視して push しない。
- blocked 項目を成功扱いしない。
- Viewer / domain / persistence / runtime / docs を、責務が曖昧なまま 1 commit に混ぜない。

報告:
- 各 push 後に、commit hash、検証コマンド、次に進む対象を短く報告してください。
- ユーザー確認は不要です。止まる条件に当たった場合だけ質問または報告して停止してください。
```

この Goal 用プロンプトを手順書へ組み込む、または既存手順書を Goal 実行向けに変換する場合は、repo skill `.agents/skills/goal-workflow-hardening/SKILL.md` を使用する。

## 5. 現在の残課題と処理順

| 順序 | 項目 | 状態 | 作業種別 | 外部依存 | Human approval |
|---:|---|---|---|---|---|
| 1 | SuperAgent Harness | local wiring / live route / client manual ledger / pause-resume queue reentry / scheduler terminal failure 確認済み / 真の長時間再開 E2E確認残 | 実運用 E2E | live runtime | 操作内容次第 |
| 2 | AI Native Engineering Workflow | local handler flow / live route / client command-context-SuperAgent trace 同一 run flow 確認済み / scheduler 正常完了 E2E確認残 | 実運用 E2E | live runtime | 正式環境変更は必須 |
| 3 | Skill Governance | local 確認済み / external PR submit audit live確認済み / 実PR作成残 | 外部 PR 接続設計 | GitHub / 外部 repo | 必須 |
| 4 | Revenue Operating Workflow | draft-only / Human Decision Gate / apply audit live確認済み / 実外部送信残 | 外部チャネル送信接続設計 | 外部チャネル | 必須 |
| 5 | Complexity Hotspot Skill | report-only / proposal / concrete diff review-only / Sandbox Gate live 部分確認済み | Coder diff provider 到達確認、実改善は別 Goal | Coder provider | 適用するなら必須 |
| 6 | E2E / runtime 要確認群 | E2E未確認 | 外部依存つき確認 | API key、実ブラウザ、実マイク、複数マシン、外部チャネル | 外部送信系は必須 |
| 保留 | 6人格の完全なキャラ会話 runtime | 未実装 / 保留寄り | 仕様判断 | LLM runtime | 仕様化時に確認 |
| 対象外 | 旧 LangGraph / FAISS / 旧キャラ体系 | 保留 / 対象外 | 作業しない | なし | 必要なら再承認 |

処理順は「外部依存が少なく、確認結果を台帳へ戻しやすいもの」を先にしている。外部環境が準備済みの場合は、`E2E / runtime 要確認群` の該当項目を先に処理してよい。ただし、その場合も `17_E2E残課題.md` と `32_E2E_runtime確認チェックリスト.md` に従う。

## 6. 共通の確認コマンド

### 6.1 実装ファイルの存在確認

```bash
rg --files internal application cmd pkg | rg "(skillgovernance|revenue|complexity|superagent|aiworkflow|workstream|sandbox|toolharness|dci|persona|browsertrace|knowledge|channel|attachment)"
```

### 6.2 docs 上の残課題確認

```bash
rg -n "部分実装|未実装|要確認|残作業|未確認|外部PR|外部送信|run_queue|scheduler|長時間再開|E2E|保留|対象外" docs/10_新仕様
```

### 6.3 live service / Viewer 基本確認

live service が起動済みの場合のみ実行する。

```bash
curl -fsS http://127.0.0.1:18790/health
curl -fsS http://127.0.0.1:18790/viewer/runtime-config
```

`/health` だけで完了扱いにしない。Viewer、runtime config、event log、対象フローの成立まで確認する。

## 7. 課題別作業手順

### 7.1 SuperAgent Harness

#### 目的

run queue scheduler の実運用 E2E と、長時間再開 E2E の確認を行う。

#### 主な関連実装

- `internal/application/superagent`
- `internal/domain/superagent`
- `internal/infrastructure/persistence/superagent`
- `internal/adapter/viewer/superagent_handler.go`
- `internal/application/orchestrator/superagent_runtime.go`

#### 手順

1. `docs/10_新仕様/31_未実装項目実装仕様.md` の P11 を読む。
2. `superagent_harness.enabled`、`run_queue_scheduler_enabled`、`run_queue_scheduler_interval_sec`、`run_queue_scheduler_claim_limit` の実効 config を確認する。
3. `/viewer/superagent/run-queue` で小さい queued item を作る。
4. scheduler が有効な場合、due item が claim されるか確認する。
5. local / distributed orchestrator に `superagent` channel の work item として渡るか確認する。
6. `trace_event` と `run_queue` の status が `completed` / `failed` / `cancelled` のいずれかに収束するか確認する。
7. fallback route に落ちていないか確認する。
8. 結果を `docs/10_新仕様/17_E2E残課題.md` に追記する。

#### 完了条件

- queued item が scheduler によって claim される。
- route / run_id / trace_event / queue status が同一フローとして追える。
- 完了または失敗が明示状態で記録される。
- fallback を成功扱いしていない。

#### 失敗扱い

- queued のまま止まる。
- claimed のまま終了状態にならない。
- local fallback / CHAT fallback で処理されたものを成功扱いする。
- event log が run と紐づかない。

#### 2026-05-19 実施済み

- `cmd/picoclaw` で run queue scheduler processor をテスト可能な単位に分離した。
- `RunQueueItem` が `superagent` channel の `ProcessMessageRequest` へ変換されることを local wiring test で確認した。
- 未対応 action は orchestrator へ渡さず error にすることを local wiring test で確認した。
- `internal/application/superagent` の scheduler claim / complete / failed test と、`internal/adapter/viewer` の run queue handler test を再確認した。
- 2026-05-19 08:27 UTC に live service を現ソースで build した binary へ更新し、`/viewer/superagent?limit=1` が 200 を返すことを確認した。
- 2026-05-19 08:42 UTC に live service で `/viewer/superagent/run-queue`、`claim`、`complete` を実行し、`rq_live_20260519083900` が queued -> claimed -> completed に遷移することを確認した。
- `pkg/rencrowclient` に `CreateRunQueueItem` / `ClaimRunQueueItem` / `CompleteRunQueueItem` を追加し、`TestE2E_SuperAgentRunQueueClientManualLedgerFlow` で同じ手動台帳 flow を live service に対して再実行できるようにした。
- `pkg/rencrowclient` の `PauseRun` / `ResumeRun` response に `runtime_control_applied` / `runtime_control_action` を追加し、`TestE2E_SuperAgentPauseResumeAndQueueReentryClientFlow` で pause -> resume marker clear、同一 `run_id` / `workstream_id` の queue 再投入、`lead_agent_paused` / `lead_agent_resumed` trace、completed queue current view を live service で確認できるようにした。
- JSONL store の status API が同一 `queue_id` の追記履歴を複数返していたため、SQLite store と同じ current view として `ListRunQueueItems` が最新 state per queue を返すよう修正した。
- 2026-05-19 08:44 UTC に live service を再 build / restart し、`/viewer/superagent?limit=20` が `rq_live_20260519083900` を `completed` 1件だけ返すことを確認した。
- `/viewer/superagent` status API に実効 `runtime_config` を返すようにし、2026-05-19 09:10 UTC に live service で `run_queue_scheduler_enabled=false`、`run_queue_scheduler_interval_sec=60`、`run_queue_scheduler_claim_limit=1` を確認した。当時の scheduler 実運用 E2E 未確認理由は実効 config 上 disabled である。
- 2026-05-19 09:28 UTC に一時 config で `run_queue_scheduler_enabled=true`、`run_queue_scheduler_interval_sec=1`、`run_queue_scheduler_claim_limit=1` を有効化し、live service の `runtime_config` で確認した。
- `rq_scheduler_live_20260519092900` が scheduler により claimed され、local orchestrator へ `channel=superagent` / `chat_id=rq_scheduler_live_20260519092900` として渡り、context pack / LeadAgent run / trace_event が保存されることを確認した。
- local LLM `http://192.168.1.13:8081` が `no route to host` のため、queue は `failed`、LeadAgent run は `failed`、trace は `run_queue_claimed` / `run_queue_failed` に収束した。これは正常応答成功として扱わない。
- 2026-05-19 に `cmd/picoclaw` の scheduler processor が、`resume` / `process_message` action の `CHAT` route、空 route、空 `job_id` を成功扱いせず error にする local test を追加した。明示 `chat` action の `CHAT` route は `job_id` 付きなら許可する。scheduler は processor error を queue `failed` として保存するため、CHAT fallback は `completed` に進まない。
- 2026-05-19 に `pkg/rencrowclient.ClaimRunQueueItem` が claim API の 2xx だけでは成功扱いせず、`claimed=true` 時に `queue_id` と `status=claimed` が揃うことを検証するようにした。`claimed=false` なのに claimed item state を含む malformed response も direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CompleteRunQueueItem` が complete API の 2xx だけでは成功扱いせず、`completed=true`、対象 `queue_id`、要求した terminal status が response item と一致することを検証するようにした。malformed complete response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CompleteRunQueueItem` が、`queue_id` 欠落や `completed` / `failed` / `cancelled` 以外の terminal status を送信前に拒否するようにした。空 status は `completed` に正規化する。これは scheduler 正常完了 E2E の代替ではなく、外部 client 側の terminal state 境界確認とする。
- 2026-05-20 05:40 UTC に `pkg/rencrowclient.ClaimRunQueueItem` / `CompleteRunQueueItem` の direct response validation を補強し、claimed item の `created_at` 欠落、terminal complete item の `created_at` / `completed_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(RunQueueClientFlow|CompleteRunQueueItemDefaultsCompletedStatus|CompleteRunQueueItemRejectsMalformedResponse|ClaimRunQueueItemRejectsMalformedResponse)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の run queue direct response を SuperAgent run queue 台帳証跡として扱わないための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-20 07:00 UTC に SuperAgent 保存前 domain validation を補強し、agent_run の `started_at` 欠落、subagent_task / context_pack / message_channel / trace_event / run_queue の `created_at` 欠落、terminal agent_run / run_queue の `completed_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/superagent -run 'TestValidate(SubagentTask|ContextPack|TraceEvent|SuperAgent)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/superagent ./internal/infrastructure/persistence/superagent ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(Validate.*SuperAgent|ValidateSubagent|ValidateContextPack|ValidateTraceEvent|JSONLStore.*SuperAgent|JSONLStoreListAgentRuns|JSONLStoreListRunQueue|HandleSuperAgent|SuperAgent|CreateAgentRun|CreateTraceEvent|CreateRunQueue|ClaimRunQueue|CompleteRunQueue|PauseRun|ResumeRun)' -count=1` は pass。これは timestamp 欠落の ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.CreateAgentRun` / `CreateTraceEvent` / `CreateRunQueueItem` が、必須 ID / type / status / goal / action が欠けた request を送信前に拒否するようにした。run queue create は空 status を `queued` に正規化し、`queued` 以外の create status は送らない local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.PauseRun` / `ResumeRun` が pause / resume API の 2xx だけでは成功扱いせず、run_id、期待 status、event_id、runtime_control_action の整合性を検証するようにした。malformed pause / resume response は direct client error とする local client test を追加した。これは真の長時間再開 live E2E の代替ではなく、外部 client 側の成功判定境界確認とする。
- JSONL store の status API が同一 `run_id` の `agent_run` 履歴を複数返していたため、SQLite store と同じ current view として `ListAgentRuns` が最新 state per run を返すよう修正した。
- 2026-05-19 に `pkg/rencrowclient.SuperAgentStatus` が status API の 2xx だけでは current view 成立扱いせず、`agent_runs` の重複 `run_id`、`run_queue` の重複 `queue_id`、必須 ID 欠落を error にする local client test を追加した。これは status view を E2E 証跡として読むための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.SuperAgentStatus` の current view validation を追加補強し、terminal `agent_run` (`completed` / `failed` / `cancelled` / `paused`) と terminal `run_queue` (`completed` / `failed` / `cancelled`) が `completed_at` を持たない場合は E2E 証跡として受け取らない local client test を追加した。これは status 文字列だけの完了を成功扱いしないための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 同補強後、`TestE2E_SuperAgentRunQueueClientManualLedgerFlow` と `TestE2E_SuperAgentPauseResumeAndQueueReentryClientFlow` を live service に対して再実行し、手動台帳 / pause-resume queue reentry の status view は新しい validation でも読めることを確認した。これは scheduler 実行ではなく、既存 live 台帳証跡の current view validation 確認として扱う。
- 2026-05-19 19:19 UTC に `pkg/rencrowclient.SuperAgentStatus` の current view validation を追加補強し、`agent_run.status` を `running` / `paused` / `completed` / `failed` / `cancelled`、`run_queue.status` を `queued` / `claimed` / `completed` / `failed` / `cancelled` に限定する local client test を追加した。同時に live `TestE2E_SuperAgentRunQueueClientManualLedgerFlow` と `TestE2E_SuperAgentPauseResumeAndQueueReentryClientFlow` を再実行して pass。これは `done` などの曖昧な status 文字列を scheduler 正常完了や真の長時間再開成功と誤認しないための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-19 19:53 UTC に `pkg/rencrowclient.SuperAgentStatus` の current view validation を追加補強し、`agent_run.status=failed` は `summary`、`run_queue.status=failed` は `reason` がない場合に E2E 証跡として受け取らない local client test を追加した。同時に live `TestE2E_SuperAgentRunQueueClientManualLedgerFlow` と `TestE2E_SuperAgentPauseResumeAndQueueReentryClientFlow` を再実行して pass。これは failed terminal を証跡なしで成功扱いしないための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-20 01:31 UTC に `pkg/rencrowclient.SuperAgentStatus` の current view validation を追加補強し、`subagent_task` / `context_pack` / `message_channel` / `trace_event` の必須 field、重複 ID、`context_pack.token_estimate` 負数を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSuperAgentStatus' -count=1 -v` は pass。これは status view を E2E 証跡として読むための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-20 03:05 UTC に `pkg/rencrowclient.SuperAgentStatus` の current view validation を追加補強し、`agent_run.started_at`、`subagent_task.created_at`、`context_pack.created_at`、`message_channel.created_at`、`trace_event.created_at`、`run_queue.created_at` 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSuperAgentStatus' -count=1 -v` は pass。これは timestamp 欠落の current view を SuperAgent status 証跡として扱わないための境界確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 証跡取得後、一時 config は削除し、live service 再起動後の `runtime_config` が `run_queue_scheduler_enabled=false`、`run_queue_scheduler_interval_sec=60`、`run_queue_scheduler_claim_limit=1` に戻ることを確認した。
- 2026-05-19 に local orchestrator の runtime pause cancellation contract test を追加し、RunController 由来の pause request で実行中 context が cancel された場合、LeadAgent run が `failed` ではなく `paused` terminal として保存され、trace が `lead_agent_paused` になることを確認した。これは真の長時間再開 live E2E の完了扱いではなく、事前の local runtime 契約確認とする。
- 2026-05-19 15:11 UTC に live service の現状態を再確認した。`/health` は `local_llm_chat` / `local_llm_worker` が `http://192.168.1.13:8081` / `:8082` へ `no route to host` のため `status=down`、`/viewer/superagent?limit=5` は 200 だが `runtime_config.run_queue_scheduler_enabled=false`、`/viewer/llm-ops/status` は `upstream unreachable`。このため scheduler 正常完了 E2E は `blocked` とし、LLM endpoint 復旧と scheduler enabled の一時確認環境が揃うまで未実施として維持する。health fail / upstream unreachable / scheduler disabled は成功扱いしない。
- 2026-05-19 17:00 UTC に現 binary の live service で `TestE2E_SuperAgentRunQueueClientManualLedgerFlow` と `TestE2E_SuperAgentPauseResumeAndQueueReentryClientFlow` を再実行し pass。これは手動台帳 / pause-resume queue reentry の再確認であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-19 18:37 UTC に `pkg/rencrowclient.SuperAgentStatus` が `runtime_config` を読み、scheduler enabled なのに interval / claim limit が不正な current view を拒否する local client test を追加した。Viewer Ops card は通常 config の scheduler disabled を `scheduler:disabled` / `blocked: scheduler disabled` として表示する。現 binary へ `make install` / service restart 後、browser E2E で同表示を確認し、`TestE2E_SuperAgentRunQueueClientManualLedgerFlow` も再 pass。`/health` は 503 のままであり、scheduler 正常完了 E2E の代替ではない。
- 2026-05-19 21:20 UTC に Viewer Ops へ `SuperAgent Terminal Audits` 表を追加し、LeadAgent run の failed terminal `summary` と run queue terminal `reason` を同じ画面で確認できるようにした。live `/viewer/superagent?limit=3` は最新 LeadAgent run が `failed`、summary が local Chat endpoint `connection refused` を保持し、run queue は completed reason を保持する。browser E2E では `superagent terminal audits:` / `terminal runs` / `failed runs` / `missing evidence:` を visible state として確認した。これは failed terminal を証跡なしで成功扱いしないための表示境界であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。
- 2026-05-19 21:49 UTC に Viewer Ops へ `SuperAgent Resume Audits` 表を追加し、`action=resume` の run queue と `lead_agent_paused` / `lead_agent_resumed` trace を run_id 単位で照合する visible state を追加した。live `/viewer/superagent?limit=20` は `rq_resume_client_e2e_*` の reason に `pause/resume queue reentry ledger E2E completed without scheduler execution` を保持し、通常 config は `run_queue_scheduler_enabled=false` のまま。browser E2E では `superagent resume audits:` / `manual-ledger` / `blocked: true long-running resume not verified` を確認した。これは client 台帳上の resume reentry を実行中 goroutine を伴う真の長時間再開 E2E と誤認しないための表示境界である。
- 2026-05-19 22:33 UTC に `SuperAgent Resume Audits` 表へ `runtime control` 列と `runtime-control applied` 集計を追加した。trace payload の `runtime_control=` action を表示し、manual ledger resume は `runtime-control:none` / `0 runtime-control applied` として browser E2E で確認済み。これは pause/resume trace の存在だけで実行中 goroutine への制御適用や真の長時間再開成功と誤認しないための追加境界である。
- 2026-05-20 09:50 UTC に Viewer Ops の `/viewer/superagent` fetch failure 境界を補強し、non-2xx body を `SuperAgent terminal audits unavailable: ...` / `SuperAgent resume audits unavailable: ...` として表示し、古い terminal run / resume queue や空 audit 表示を成功証跡にしない Node contract test を追加した。これは status API failure を scheduler 正常完了なし / 真の長時間再開なし / 成功として誤認しないための visible-state 境界であり、scheduler 正常完了 E2E や真の長時間再開 E2E の代替ではない。

実行コマンド:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/superagent ./cmd/picoclaw
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/complexity ./internal/application/complexity ./internal/infrastructure/persistence/complexity ./internal/adapter/viewer ./internal/infrastructure/persistence/revenue ./internal/infrastructure/persistence/superagent ./cmd/picoclaw
GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/superagent ./internal/application/superagent ./cmd/picoclaw ./internal/adapter/viewer
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 go test -tags=e2e ./test/e2e -run TestE2E_SuperAgentRunQueueClientManualLedgerFlow -count=1 -v
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 go test -tags=e2e ./test/e2e -run 'TestE2E_SuperAgent(PauseResumeAndQueueReentryClientFlow|RunQueueClientManualLedgerFlow)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/orchestrator -run 'TestProcessMessage_(RecordsLeadAgentRun|RecordsPausedLeadAgentRunWhenRuntimePauseCancelsContext)' -count=1
GOCACHE=/tmp/picoclaw-gocache go test ./cmd/picoclaw -run TestNewSuperAgentRunQueueProcessor -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestRunQueueClientFlow|TestClaimRunQueueItemRejectsMalformedResponse|TestCompleteRunQueueItemRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(RunQueueClientFlow|CreateRunQueueItem|CompleteRunQueueItem|ClaimRunQueueItem)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCreateAgentRun|TestCreateTraceEvent|TestCreateRunQueueItem|TestRunQueueClientFlow' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestPauseAndResumeRun|TestPauseRunRejectsMalformedResponse|TestResumeRunRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSuperAgentStatus|TestSuperAgentStatusRejectsDuplicateCurrentView' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSuperAgentStatus|TestRunQueueClientFlow|TestCompleteRunQueueItem|TestClaimRunQueueItem' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 go test -tags=e2e ./test/e2e -run 'TestE2E_SuperAgent(RunQueueClientManualLedgerFlow|PauseResumeAndQueueReentryClientFlow)' -count=1 -v
node internal/adapter/viewer/viewer_memory_panel.test.mjs
PICOCLAW_BROWSER_E2E=1 PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_Phase25BrowserViewerSessionContract -count=1 -v
curl -sS -m 5 http://127.0.0.1:18790/health
curl -sS -m 5 http://127.0.0.1:18790/viewer/runtime-config
curl -sS -m 5 'http://127.0.0.1:18790/viewer/superagent?limit=5'
curl -sS -m 5 'http://127.0.0.1:18790/viewer/llm-ops/status'
```

残り:

- LLM 到達可能状態での scheduler 正常完了 E2E。
- queued item 作成から scheduler claim、orchestrator request、route / job_id、trace_event、queue completed までの同一 flow 確認。
- 実行中 goroutine を伴う長時間 run の pause / resume / queue 再投入を含む真の再開 E2E。client 台帳上の pause / resume / queue 再投入は確認済み。

### 7.2 AI Native Engineering Workflow

#### 目的

run queue / context / event の実運用 E2E を確認する。

#### 主な関連実装

- `internal/domain/aiworkflow`
- `internal/application/aiworkflow`
- `internal/infrastructure/persistence/aiworkflow`
- `internal/application/superagent`
- `internal/infrastructure/llm/middleware`
- `internal/infrastructure/tools`

#### 手順

1. `docs/10_新仕様/31_未実装項目実装仕様.md` の P12 を読む。
2. `ai_workflow.enabled`、`ai_workflow.context_tracking_enabled`、`ai_workflow.context_budget_*` の実効 config を確認する。
3. `/viewer/ai-workflow/context-budget/check` で小さい context usage を記録する。
4. `/viewer/ai-workflow/commands/run` で read-only の registered command を起動する。
5. `ai_workflow_event`、`ai_context_usage`、`command_registry` の記録を確認する。
6. SuperAgent run queue と同じ run として追える場合は、run_id / trace_event も照合する。
7. 正式環境変更、外部送信、PR 作成に進む場合は、Promotion Request、Gate approve、Human approval、post-apply verification path が揃っていることを確認する。

#### 完了条件

- command / context / event が同一フローとして保存される。
- context budget warning / stop が期待どおり記録される。
- 正式環境変更は Human approval なしに進まない。

#### 失敗扱い

- context budget stop を無視して LLM context に大きな tool result を戻す。
- external control policy を通さず正式環境変更へ進む。
- GitHub Actions 追加や main 直接変更に進む。

#### 2026-05-19 実施済み

- `/viewer/ai-workflow/commands/run` と `/viewer/ai-workflow/context-budget/check` を同じ store へ保存する local handler flow test を追加した。
- `/viewer/ai-workflow` status API から command invocation、context budget warning、context usage、command registry をまとめて確認できることを test で確認した。
- 2026-05-19 08:27 UTC に live service を現ソースで build した binary へ更新し、`/viewer/ai-workflow?limit=1` が 200 を返すことを確認した。
- 2026-05-19 08:34 UTC に live service で `/viewer/ai-workflow/commands/run` を実行し、`command_invoked:tool-harness-check:20260519083402.385073595` が `/viewer/ai-workflow?limit=20` に出ることを確認した。
- 2026-05-19 08:34 UTC に live service で `/viewer/ai-workflow/context-budget/check` を実行し、`ctx_live_20260519083353` が context usage として保存されることを確認した。ただし live config は `context_budget_tokens` 未設定のため decision は `context budget disabled` で、warning / stop 成功扱いにはしない。
- 2026-05-19 08:34 UTC に live service で `/viewer/ai-workflow/external-control/check` を実行し、`promotion_apply` が Human approval なしでは `needs_approval` になることを確認した。正式環境変更は実行していない。
- `/viewer/ai-workflow` status API に実効 `context_budget_policy` を返すようにし、2026-05-19 09:05 UTC に live service で `max_context_tokens=0`、`warn_at_ratio=0.8`、`stop_at_ratio=0.95` を確認した。warning / stop 未確認の理由は実効 config 上 disabled である。
- 2026-05-19 09:35 UTC に一時 config で `context_budget_tokens=1000`、`context_budget_warn_ratio=0.5`、`context_budget_stop_ratio=0.9` を有効化し、live service の `context_budget_policy` で確認した。
- `ctx_budget_warn_live_20260519093600` が `status=warn` / `event_type=context_budget_warning` として保存され、`ctx_budget_stop_live_20260519093600` が `status=stop` / `event_type=context_budget_exceeded` として保存されることを確認した。
- 証跡取得後、一時 config は削除し、live service 再起動後の `context_budget_policy` が `max_context_tokens=0`、`warn_at_ratio=0.8`、`stop_at_ratio=0.95` に戻ることを確認した。
- 正式環境変更、外部送信、PR 作成は実行していない。
- 2026-05-19 09:43 UTC に `pkg/rencrowclient.CheckExternalControl` から live service の `/viewer/ai-workflow/external-control/check` を呼び、Human approval なしの `promotion_apply` が `needs_approval` で止まり、`workflow_events` に `external_control_policy_checked` が保存されることを確認した。
- 2026-05-19 に `pkg/rencrowclient.CheckExternalControl` が external-control API の 2xx だけでは成功扱いせず、request echo、`allowed` / `needs_approval` / `blocked` status、approval-required 整合性、blocked reason を検証するようにした。malformed external-control response は direct client error とする local client test を追加した。
- `pkg/rencrowclient` に `AIWorkflowStatus` / `CheckContextBudget` と、実レスポンスの `event` オブジェクトを扱える `RunCommand` response を追加した。
- `WorkflowEvent` に `run_id` / `workstream_id` を追加し、`/viewer/ai-workflow/commands/run` が command event へ保存できるようにした。
- `TestE2E_AIWorkflowCommandContextAndSuperAgentTraceSameRun` を追加し、`pkg/rencrowclient` から live service の command invocation、context usage、SuperAgent run / trace を同一 `run_id` と `workstream_id` で保存し、AI Workflow / SuperAgent の status view から再取得できることを確認した。
- 2026-05-19 に `pkg/rencrowclient.AIWorkflowStatus` が status API の 2xx だけでは current view 成立扱いせず、workflow event / project memory / worktree / command / context usage の重複 ID と必須 ID 欠落を error にする local client test を追加した。これは AI Workflow status view を E2E 証跡として読むための境界確認であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-19 19:28 UTC に `pkg/rencrowclient.AIWorkflowStatus` の current view validation を追加補強し、project memory の repo / file_path / memory_type、worktree の repo / path / branch / status、command の file_path、context usage の非負 token / count を検証する local client test を追加した。あわせて JSONL AI Workflow registry list が command / project memory / worktree を最新 state per key で返すよう修正し、現 binary へ install / restart 後に live `TestE2E_AIWorkflowCommandContextAndSuperAgentTraceSameRun` が再 pass。これは AI Workflow status view の証跡境界確認であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-20 01:20 UTC に `pkg/rencrowclient.AIWorkflowStatus` を補強し、context usage の `estimated_cost` / `kv_cache_estimate` が負数の current view を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestAIWorkflowStatus' -count=1 -v` は pass。これは malformed numeric estimate を AI Workflow status 証跡として扱わないための境界確認であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-20 02:55 UTC に `pkg/rencrowclient.AIWorkflowStatus` を補強し、workflow event / worktree / context usage の `created_at`、project memory / command の `updated_at` 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestAIWorkflowStatus' -count=1 -v` は pass。これは timestamp 欠落の current view を AI Workflow status 証跡として扱わないための境界確認であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.RunCommand` が commands API の 2xx だけでは成功扱いせず、command registry、`command_invoked` event、requested status、run_id / workstream_id の整合性を検証するようにした。malformed command response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CheckContextBudget` が context-budget API の 2xx だけでは成功扱いせず、context usage、decision status、warning / stop event linkage の整合性を検証するようにした。malformed context-budget response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CreateWorkstreamArtifact` が Workstream Artifact API の 2xx だけでは成功扱いせず、artifact_id / workstream_id / artifact_type / status の整合性を検証するようにした。malformed artifact response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.ReviewWorkstreamVaultUpdate` が Workstream Vault review API の 2xx だけでは成功扱いせず、update_id / workstream_id / file_path / review_status / applied flag と applied_path の整合性を検証するようにした。2026-05-20 には `CreateWorkstreamVaultUpdate` / `PreviewWorkstreamVaultUpdate` も追加し、malformed vault create / preview / review response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.RunCommand` / `CheckContextBudget` / `CheckExternalControl` / `CreateWorkstreamArtifact` / `ReviewWorkstreamVaultUpdate` / `PauseRun` / `ResumeRun` が、必須 ID / command / agent / action / artifact metadata 欠落 request を送信前に拒否するようにした。これは scheduler 起点 E2E や正式 apply E2E の代替ではなく、外部 client 側の事前境界確認とする。
- 2026-05-19 に `pkg/rencrowclient.WorkstreamStatus` を追加し、`/viewer/workstreams` が 2xx を返しても workstream / goal / artifact / annotation / steering / heartbeat / vault update の同一 ID 重複や必須 ID 欠落を current view として成功扱いしない local client test を追加した。これは Workstream status view を E2E 証跡として読むための境界確認であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.WorkstreamStatus` の current view validation を追加補強し、`status=completed` の Goal が `completed_at` を持たない場合は E2E 証跡として受け取らない local client test を追加した。これは Workstream Goal の status 文字列だけの完了を成功扱いしないための境界確認であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-19 17:16 UTC に `TestE2E_WorkstreamStatusClientCurrentView` を追加し、live service の `/viewer/workstreams` を `pkg/rencrowclient.WorkstreamStatus` 経由で読み、waiting goal と pending_review artifact が current view として取得できることを確認した。これは status 証跡読み取り E2E であり、scheduler 起点 E2E、Vault approved review による実ファイル適用、正式 apply E2E の代替ではない。
- 2026-05-19 19:08 UTC に現 binary へ再 install / restart 後、live `/viewer/workstreams?limit=3` が waiting goal と pending_review artifact を返すこと、Viewer Ops card が `waiting goals:` / `pending-review:` / `mode: review-only` / `blocked: no vault apply` を visible state として表示することを `TestE2E_Phase25BrowserViewerSessionContract` で確認した。これは Workstream artifact / goal の存在を Vault apply や正式 apply 成功と誤認しないための表示境界であり、Vault approved review による実ファイル適用、scheduler 起点 E2E、正式 apply E2E の代替ではない。
- 2026-05-19 19:13 UTC に `pkg/rencrowclient.WorkstreamStatus` の Vault update current view validation を追加補強し、`review_status` が `pending` / `approved` / `rejected` 以外の場合は E2E 証跡として受け取らない local client test を追加した。同時に live `TestE2E_WorkstreamStatusClientCurrentView` を再実行して pass。これは `applied` などの曖昧な status 文字列を Vault 正式 apply 成功と誤認しないための境界確認であり、Vault approved review による実ファイル適用、scheduler 起点 E2E、正式 apply E2E の代替ではない。
- 2026-05-20 01:52 UTC に `pkg/rencrowclient.WorkstreamStatus` の Vault update current view validation を追加補強し、`applied=true` が `review_status=approved` と `applied_path` を満たさない場合、および `applied=false` が `applied_path` を持つ場合は local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(WorkstreamStatus|CreateWorkstreamVaultUpdate|PreviewWorkstreamVaultUpdate|ReviewWorkstreamVaultUpdate)' -count=1 -v` は pass。これは applied flag / path だけを Vault apply 証跡として扱わないための境界確認であり、外部送信、PR 作成、scheduler 正常完了、本 repo patch 適用の代替ではない。
- 2026-05-20 03:15 UTC に `pkg/rencrowclient.WorkstreamStatus` の current view validation を追加補強し、workstream / goal / artifact / annotation / steering / heartbeat / vault update の `created_at` 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestWorkstreamStatus' -count=1 -v` は pass。これは timestamp 欠落の current view を Workstream status 証跡として扱わないための境界確認であり、Vault approved review による実ファイル適用、scheduler 起点 E2E、正式 apply E2E の代替ではない。
- 2026-05-20 05:05 UTC に `pkg/rencrowclient.CreateWorkstreamArtifact` / `CreateWorkstreamVaultUpdate` / `ReviewWorkstreamVaultUpdate` を補強し、direct response の artifact / vault_update が `created_at` を持たない場合は local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(CreateWorkstreamArtifact|CreateWorkstreamVaultUpdate|ReviewWorkstreamVaultUpdate)' -count=1 -v` は pass。これは timestamp 欠落の direct response を Workstream Artifact / Vault Update 証跡として扱わないための境界確認であり、Vault approved review による実ファイル適用、scheduler 起点 E2E、正式 apply E2E の代替ではない。
- 2026-05-20 06:50 UTC に Workstream 保存前 domain validation を補強し、workstream / goal / artifact / annotation / steering / heartbeat / vault update の `created_at` 欠落と completed goal の `completed_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/workstream -run 'TestValidate(WorkstreamRejectsMissingCreatedAt|GoalRejectsCompletedWithoutCompletedAt|Goal|Workstream|Artifact|ArtifactAnnotation|Steering|Heartbeat|Vault)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/workstream ./internal/infrastructure/persistence/workstream ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(Validate.*Workstream|ValidateGoal|Workstream|CreateWorkstream|ReviewWorkstream|Vault)' -count=1` は pass。これは timestamp 欠落の ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、Vault approved review による実ファイル適用、scheduler 起点 E2E、正式 apply E2E の代替ではない。
- 2026-05-19 21:16 UTC に Workstream Vault Review の Viewer 表示へ `applied` / `applied_path` と `pending` / `approved` / `rejected` / `applied` 集計を追加し、vault update が空の live `/viewer/workstreams?limit=3` でも `0 applied` / `blocked: no vault apply` を visible state として表示することを `node internal/adapter/viewer/viewer_memory_panel.test.mjs` と live browser `TestE2E_Phase25BrowserViewerSessionContract` で確認した。これは pending_review artifact / waiting goal と Vault apply 済みを混同しないための表示証跡であり、Vault approved review による実ファイル適用や正式 apply E2E の代替ではない。
- 2026-05-20 00:27 UTC に `pkg/rencrowclient.CreateWorkstreamVaultUpdate` / `PreviewWorkstreamVaultUpdate` を追加し、`ReviewWorkstreamVaultUpdate` は approved proposed_content の apply response で top-level と `vault_update` 本体の `applied` / `applied_path` 整合性を要求するよう補強した。Workstream JSONL store は同一 `update_id` の pending -> approved 追記履歴を最新 state per ID の current view として返す。
- 同時刻に live `TestE2E_WorkstreamVaultUpdatePreviewAndApprovedApply` を実行し、pending create、preview diff、approved review apply、status current view の `applied=true` / `applied_path` を確認した。証跡は `update_id=vault_apply_20260520002739.458203088`、`workstream_id=ws_vault_apply_20260520002739.458203088`、`applied_path=/home/nyukimi/picoclaw_multiLLM/vault/workstreams/live-e2e/ws_vault_apply_20260520002739.458203088/STATUS.md`。これは Workstream Vault の隔離 live-e2e file apply 成功であり、外部送信、PR 作成、scheduler 正常完了、本 repo patch 適用ではない。
- Viewer Ops は Vault applied 件数がある場合に Workstreams card へ `vault applied:` を表示し、live browser `TestE2E_Phase25BrowserViewerSessionContract` で確認済み。apply がない状態では従来どおり `blocked: no vault apply` を表示する。
- 2026-05-20 10:00 UTC に Viewer Ops の `/viewer/workstreams` fetch failure 境界を補強し、non-2xx 時は stale Workstream / Vault update row を使わず `workstream status unavailable: ...` / `Workstream vault reviews unavailable: ...` / `blocked: vault apply state unreadable` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは status API failure を Vault apply 証跡として扱わないための local 表示境界であり、Vault approved review による live file apply、scheduler 起点 E2E、正式 apply E2E の代替ではない。
- `SubmitPromotionWorkflow` は `ApplyAfterApproval=true` の場合に `ExternalControl` を必須にした。ExternalControlPolicy を通らない正式 apply intent は、promotion request 作成前に `external control policy is required before apply` で停止する。
- `TestE2E_AIWorkflowPromotionWorkflowRequiresHumanApprovalBeforeApply` を追加し、一時 sandbox config 有効化中の live service で、external control policy が許可し Promotion Gate が approve しても、最終 apply の `HumanApproved=false` では `/viewer/sandbox/promotions/apply` へ進まず、promotion request / approve gate log / rollback artifact / post-apply verification artifact だけが保存されることを確認した。
- 2026-05-19 に `pkg/rencrowclient.CreatePromotionRequest` が Promotion Request API の 2xx だけでは成功扱いせず、promotion_id / sandbox_id / target_path / diff_path、gate event_id、decision status と gate status、review 時の missing requirements、artifact type / path の整合性を検証するようにした。malformed Promotion Request response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CreatePromotionRequest` が、`promotion_id` / `sandbox_id` / `target_path` のない request を送信前に拒否するようにした。これは Promotion Gate へ渡す最小識別子の境界確認であり、diff / test / rollback / human approval の不足判定は Gate 側で `needs_review` / `needs_more_tests` として扱う。
- `post_apply_verification_command` 指定時に verifier / ToolRunner が未接続の場合、`/viewer/sandbox/promotions/apply` は 503 で停止し、`promotion_applied` log / completed artifact を保存しないことを local handler test で確認した。
- `SubmitPromotionWorkflow` は apply API が 2xx を返しても、`decision.status` / `gate_log.gate_status` が `promotion_applied`、`post_apply_verification_artifact` が `completed`、証跡 path が一致しない場合は `Applied=true` にしない。malformed apply response を正式適用成功扱いにしない local client test を追加した。
- 2026-05-19 に低レベル `pkg/rencrowclient.ApplyPromotion` も apply API 2xx だけでは成功扱いせず、`decision.status` / `gate_log.gate_status` / Gate Log の `promotion_id` と `post_apply_verification` / completed `post_apply_verification_artifact` の `sandbox_id` と証跡 path / `diff_apply_result.status=applied` と `applied_files` を検証するようにした。malformed response は direct apply client では error、`SubmitPromotionWorkflow` では `Applied=false` と `SkippedReason` として扱う。
- 2026-05-19 に低レベル `pkg/rencrowclient.RollbackPromotion` も rollback API 2xx だけでは成功扱いせず、`decision.status` / `gate_log.gate_status` が `rollback_executed`、`rollback_result.status=rolled_back`、completed `rollback_execution` artifact、rollback plan path 一致を検証するようにした。malformed response は direct rollback client error とする。
- 2026-05-19 に低レベル `pkg/rencrowclient.ApplyPromotion` / `RollbackPromotion` が、Human approval 未承認、apply diff / post-apply verification path 不足、rollback plan 不足の request を送信前に拒否するようにした。これは正式 apply / rollback E2E の代替ではなく、直接 client から不正な正式環境変更 request を送らないための境界確認とする。
- 2026-05-19 に `pkg/rencrowclient.SandboxStatus` が status API の 2xx だけでは current view 成立扱いせず、sandbox / artifact / promotion / gate log の重複 ID と必須 ID 欠落、decision の status 欠落を error にする local client test を追加した。これは Promotion Request / Gate / artifact を E2E 証跡として読むための境界確認であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.SandboxStatus` の current view validation を追加補強し、`gate_status=promotion_applied` の Gate Log が `post_apply_verification` を持たない場合は E2E 証跡として受け取らない local client test を追加した。これは Gate Log の status 文字列だけで正式適用成功扱いしないための境界確認であり、Human approval あり正式 apply E2E の代替ではない。
- 2026-05-19 19:23 UTC に `pkg/rencrowclient.SandboxStatus` の current view validation をさらに補強し、`decision.status` / `gate_log.gate_status` が `approve` / `reject` / `needs_review` / `needs_more_tests` / `promotion_applied` / `rollback_executed` 以外の場合は E2E 証跡として受け取らない local client test を追加した。2026-05-19 20:23 UTC に通常 config の live `/viewer/sandbox?limit=1` は 404 ではなく 503 `sandbox store unavailable` を返すことを確認した。これは Sandbox disabled の blocked 証跡であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-20 04:05 UTC に `pkg/rencrowclient.SandboxStatus` の current view validation を補強し、sandbox / artifact / promotion / gate log の created_at 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSandboxStatus' -count=1 -v` は pass。これは timestamp 欠落の status view を Promotion Request / Gate / artifact や正式 apply / rollback 証跡として扱わないための境界確認であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-20 05:20 UTC に `pkg/rencrowclient.CreatePromotionRequest` / `ApplyPromotion` / `RollbackPromotion` を補強し、direct response の promotion / gate_log / post-apply artifact / rollback artifact が `created_at` を持たない場合は local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(CreatePromotionRequest|ApplyPromotion|RollbackPromotion)' -count=1 -v` は pass。これは timestamp 欠落の direct response を Promotion Request / apply / rollback 証跡として扱わないための境界確認であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-20 07:50 UTC に Sandbox 保存前 domain validation を補強し、sandbox / artifact / promotion / gate log の `created_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/sandbox ./internal/infrastructure/persistence/sandbox ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(ValidateSandbox|EvaluatePromotion|JSONLStore.*Sandbox|SQLiteStore.*Sandbox|HandleSandbox|SandboxStatus|CreatePromotion|ApplyPromotion|RollbackPromotion)' -count=1` は pass。これは timestamp 欠落の Promotion Request / Gate / artifact ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-19 21:08 UTC に Viewer Ops へ `Sandbox Promotion Gate Logs` 表を追加した。通常 config の live `/viewer/sandbox?limit=1` は 503 `sandbox store unavailable` のままだが、browser E2E では `sandbox promotion gate logs:` / `needs-review` / `post-apply evidence` summary が visible state として表示されることを確認した。これは Gate Log / post-apply evidence の可視化であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-19 22:41 UTC に同 summary へ `formal apply requires human approval` / `blocked: no promotion applied` と rollback 件数を追加した。Gate Log / post-apply evidence を正式 apply / rollback 成功と誤認しないための表示境界であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-19 23:09 UTC に `TestE2E_SandboxPromotionApplyAndRollbackWithHumanApproval` を追加し、一時 sandbox config と隔離 apply root `/tmp/picoclaw-sandbox-promotion-apply-root` で live service に対して実行して pass。`promo_sandbox_apply_rollback_20260519230949.441009237` は ExternalControl allow、Human approval granted、unified diff apply、post-apply verification command、`promotion_applied` gate log、rollback API、`rollback_executed` gate log、completed `post_apply_verification` / `rollback_execution` artifact を確認した。対象ファイルは apply 後 `two -> TWO`、rollback 後 `two` に戻り、post-apply verification file は `verified` を記録した。証跡取得後に `~/.picoclaw/config.yaml` を復元し、通常 config は `sandbox_enabled=false` / `/viewer/sandbox?limit=1` 503 `sandbox store unavailable` に戻した。これは隔離 apply root の正式 apply / rollback E2E であり、外部 PR 作成や本 repo worktree への patch 適用ではない。
- 2026-05-20 10:10 UTC に Viewer Ops の `/viewer/sandbox` fetch failure 境界を補強し、non-2xx 時は stale `promotion_applied` Gate Log を使わず `sandbox status unavailable: ...` / `Sandbox gate logs unavailable: ...` / `blocked: promotion apply state unreadable` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは status API failure を正式 apply / rollback 証跡として扱わないための local 表示境界であり、Human approval あり正式 apply / rollback E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.EvaluateHeavyWorker` / `HeavyWorkerRuntimeDiagnostics` を追加し、Heavy Worker evaluate API が 2xx を返しても request echo、decision status、requested event、parent event、agent が整合しない response を direct client success として返さない local client test を追加した。runtime diagnostics も `role=Heavy` / `route=ANALYZE` / `failure_is_error=true` と configured base/model、LLM Ops unavailable error を検証する。これは Heavy Worker 実 provider E2E や scheduler 正常完了 E2E の代替ではない。
- 2026-05-19 22:25 UTC に `LLMOpsIdleChatGate` の status probe timeout を read-only route と同じ短時間設定へ揃えた。`TestLLMOpsIdleChatGate_StatusTimesOutQuickly` と `TestHandleAIWorkflowHeavyWorkerRuntimeDiagnostics` は pass し、live `/viewer/ai-workflow/heavy-worker/runtime-diagnostics` は約 3 秒で 200 body を返す。body は `role=Heavy` / `route=ANALYZE` / configured base/model を含むが、`llm_ops.live_available=false` と `llm-ops GET /v1/status ... context deadline exceeded` を返すため、Heavy 実 provider 成功や LLM Ops 管理 API 到達成功とは扱わない。
- 証跡取得後、一時 sandbox config はバックアップから復元し、当時の live service 再起動後に `/viewer/sandbox?limit=1` が 404、`/viewer/ai-workflow?limit=1` が 200 へ戻ることを確認した。2026-05-19 20:23 UTC の runtime 更新後は Sandbox disabled 時も status route が 503 `sandbox store unavailable` を返す。
- 2026-05-19 15:14 UTC に live service の現状態を再確認した。`/viewer/ai-workflow?limit=5` は 200 で command registry / workflow event / context usage / `context_budget_policy` を返すが、`/health` は local Chat / Worker endpoint `no route to host`、`/viewer/superagent?limit=5` は `runtime_config.run_queue_scheduler_enabled=false` のため、scheduler 起点で SuperAgent run queue trace と同一 run として追う E2E は `blocked` として維持する。command / context status 200 や過去 event の存在は scheduler 起点 E2E 成功扱いにしない。
- 2026-05-19 16:25 UTC に live service の現状態を再確認した。`/health` は 503 で local Chat / Worker endpoint `http://192.168.1.13:8081` / `:8082` が `no route to host`、`/viewer/superagent?limit=3` は `runtime_config.run_queue_scheduler_enabled=false`、`/viewer/ai-workflow?limit=3` は 200、当時の `/viewer/sandbox?limit=1` は通常 config 復元後の 404。2026-05-19 20:23 UTC の runtime 更新後は `/viewer/sandbox?limit=1` が 503 `sandbox store unavailable` を返す。scheduler 正常完了 E2E / AI Native scheduler 起点同一 run E2E は blocked 継続とする。
- 2026-05-19 17:00 UTC に現 binary の live service で `TestE2E_AIWorkflowExternalControlClientRequiresApproval` を再実行し pass。Human approval なしの external control は停止することを再確認した。これは scheduler 起点 E2E や正式 apply E2E の代替ではない。
- 2026-05-19 18:43 UTC に `pkg/rencrowclient.AIWorkflowStatus` が `context_budget_policy` を検証し、context budget enabled なのに warn / stop ratio が不正な current view を拒否する local client test を追加した。Viewer Ops card は通常 config の context budget disabled を `context-budget:disabled` / `blocked: context budget disabled` として表示する。現 binary へ `make install` / service restart 後、`/viewer/ai-workflow?limit=1` が `max_context_tokens=0` を返すこと、browser E2E で同表示が見えること、`TestE2E_AIWorkflowExternalControlClientRequiresApproval` が再 pass することを確認した。`/health` は 503 のままであり、scheduler 起点 E2E、warning / stop 成功、正式 apply E2E の代替ではない。
- 2026-05-20 05:45 UTC に `pkg/rencrowclient.RunCommand` / `CheckContextBudget` の direct response validation を補強し、command invocation event、returned context usage、context budget warning / stop event の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(RunCommand|AIWorkflowStatusAndContextBudget|CheckContextBudgetRejectsMalformedResponse)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の direct response を AI Workflow command / context budget 証跡として扱わないための境界確認であり、scheduler 起点 E2E、warning / stop runtime 成功、正式 apply E2E の代替ではない。
- 2026-05-20 07:10 UTC に AI Workflow 保存前 domain validation を補強し、workflow event / worktree / context usage の `created_at` 欠落、project memory / command の `updated_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/aiworkflow -run 'TestValidateAIWorkflow|TestValidateContextUsageRejectsNegativeCounts' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/aiworkflow ./internal/infrastructure/persistence/aiworkflow ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(ValidateAIWorkflow|ValidateContextUsage|JSONLStore.*AIWorkflow|JSONLStoreSaves|SQLiteStore|HandleAIWorkflow|AIWorkflow|RunCommand|CheckContextBudget|CheckExternalControl|EvaluateHeavyWorker)' -count=1` は pass。これは timestamp 欠落の ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、scheduler 起点 E2E、warning / stop runtime 成功、正式 apply E2E の代替ではない。
- 2026-05-20 05:50 UTC に `pkg/rencrowclient.EvaluateHeavyWorker` の direct response validation を補強し、Heavy Worker requested event の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestEvaluateHeavyWorker' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の direct response を Heavy Worker requested 証跡として扱わないための境界確認であり、RouteANALYZE 実運用成功や scheduler 起点 E2E の代替ではない。
- 2026-05-19 21:26 UTC に Viewer Ops へ `AI Workflow Run Evidence` 表を追加し、command / context usage / SuperAgent trace を `run_id` 単位で照合する visible state を追加した。live `/viewer/ai-workflow?limit=5` は command registry、過去の `command_invoked` events、recent context usages、`context_budget_policy.max_context_tokens=0` を返すが、通常 config の scheduler 正常完了や正式 apply 成功は示さない。browser E2E では `ai workflow run evidence:` / `command-context-trace same-run` / `blocked: scheduler normal completion not verified` を確認した。これは status 200 や過去 event を scheduler 起点成功と誤認しないための表示境界であり、scheduler 正常完了 E2E や正式 apply E2E の代替ではない。
- 2026-05-20 09:55 UTC に Viewer Ops の `/viewer/ai-workflow` fetch failure 境界を補強し、non-2xx body を `AI Workflow run evidence unavailable: ...` として表示し、古い command / context / trace same-run row や空 evidence 表示を成功証跡にしない Node contract test を追加した。これは status API failure を scheduler 正常完了なし / 成功として誤認しないための visible-state 境界であり、scheduler 起点 E2E や正式 apply E2E の代替ではない。

実行コマンド:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/complexity ./internal/application/complexity ./internal/infrastructure/persistence/complexity ./internal/adapter/viewer ./internal/infrastructure/persistence/revenue ./internal/infrastructure/persistence/superagent ./cmd/picoclaw
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(CheckExternalControl|RunCommand|AIWorkflowStatusAndContextBudget|CheckContextBudget|PauseAndResumeRun|CreateWorkstreamArtifact|ReviewWorkstreamVaultUpdate)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestAIWorkflowStatusAndContextBudget|TestAIWorkflowStatusRejectsMalformedCurrentView' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestRunCommand|TestRunCommandRejectsMalformedResponse|TestAIWorkflowStatusAndContextBudget|TestCheckContextBudgetRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestWorkstreamStatus' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestWorkstreamStatus|TestCreateWorkstreamArtifact|TestReviewWorkstreamVaultUpdate' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCreateWorkstreamArtifact|TestCreateWorkstreamArtifactRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestReviewWorkstreamVaultUpdate|TestReviewWorkstreamVaultUpdateRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCheckExternalControl|TestSubmitPromotionWorkflow' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCreatePromotionRequest|TestCreatePromotionRequestRejectsMalformedResponse|TestSubmitPromotionWorkflow|TestApplyPromotion|TestRollbackPromotion' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(CreatePromotionRequest|ApplyPromotion|RollbackPromotion|SubmitPromotionWorkflow)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSubmitPromotionWorkflow(AppliesOnlyAfterGateAndApproval|DoesNotMarkMalformedApplyResponseApplied|RequiresExternalControlBeforeApply|CreatesRequestButDoesNotApplyWithoutApproval|StopsWhenExternalControlPolicyBlocks|DoesNotApplyWhenGateDoesNotApprove)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestApplyPromotion|TestSubmitPromotionWorkflow' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestApplyPromotion|TestSubmitPromotionWorkflow|TestSandboxStatus' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSandboxStatus|TestSandboxStatusRejectsMalformedCurrentView' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSandboxStatus|TestCreatePromotionRequest|TestApplyPromotion|TestRollbackPromotion|TestSubmitPromotionWorkflow' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(EvaluateHeavyWorker|HeavyWorkerRuntimeDiagnostics)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer ./internal/application/sandbox ./internal/domain/sandbox ./pkg/rencrowclient
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 go test -tags=e2e ./test/e2e -run 'TestE2E_AIWorkflow(CommandContextAndSuperAgentTraceSameRun|ExternalControlClientRequiresApproval)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_SANDBOX_E2E=1 go test -tags=e2e ./test/e2e -run TestE2E_AIWorkflowPromotionWorkflowRequiresHumanApprovalBeforeApply -count=1 -v
node internal/adapter/viewer/viewer_memory_panel.test.mjs
PICOCLAW_BROWSER_E2E=1 PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_Phase25BrowserViewerSessionContract -count=1 -v
curl -sS -m 5 http://127.0.0.1:18790/health
curl -sS -m 5 'http://127.0.0.1:18790/viewer/ai-workflow?limit=5'
curl -sS -m 5 'http://127.0.0.1:18790/viewer/superagent?limit=5'
```

残り:

- live service で scheduler 起点の SuperAgent run queue trace と command / context / event を同一 run として追う E2E。command / context / SuperAgent run/trace は同一 `run_id` と `workstream_id` で追えることを client E2E 済み。
- Promotion Request、Gate approve、explicit apply intent、post-apply verification path までの live client E2E は確認済み。最終 apply は Human approval なしで止まることを確認済み。
- `post_apply_verification_command` 指定時の verifier / ToolRunner 未接続拒否は local handler test 済み。これは正式 apply 成功ではなく、接続不足時に成功ログを残さない境界確認として扱う。
- apply response の malformed success は `pkg/rencrowclient` local test で成功扱いしないことを確認済み。direct apply client は error、workflow client は `Applied=false` / `SkippedReason` として扱う。これは正式 apply E2E の代替ではなく、client 側の成功判定境界確認として扱う。
- Human approval ありで `/viewer/sandbox/promotions/apply` を実行する正式環境変更 E2E。これは実適用を伴うため、対象 apply root、diff、rollback、post-apply verification command を決めた上で明示 Human approval を得てから実施する。

### 7.3 Skill Governance

#### 目的

PR gate の停止確認を行い、外部 PR 実作成フローへ進む条件を明確にする。

#### 主な関連実装

- `internal/domain/skillgovernance`
- `internal/application/skillgovernance`
- `internal/infrastructure/persistence/skillgovernance`
- `internal/adapter/viewer/skill_governance_handler.go`
- `internal/application/orchestrator/message_orchestrator_skill_bootstrap.go`

#### 手順

1. `docs/10_新仕様/31_未実装項目実装仕様.md` の P5 を読む。
2. `skill_governance.enabled`、`skill_governance.storage`、`skill_governance.sqlite_path` の実効 config を確認する。
3. 「PR を出して」相当の intent で PR gate skill が起動するか確認する。
4. 実問題、diff、承認、テスト証跡がない場合に停止するか確認する。
5. Coder Proposal 実行時に `workspace/logs/skill_governance/coder_evidence/{job_id}/skill_diff.md` と `agent_transcript.md` が保存されるか確認する。
6. `/viewer/skill-governance/recent` で `coder_transcripts` と trigger log を確認する。
7. `/viewer/skill-governance/external-pr-submit` で passed Contribution Gate と Human approval がない場合に停止するか確認する。
8. Human approval がある場合でも、現状は GitHub adapter 未接続として `blocked` audit を保存し、実 PR を作らないことを確認する。
9. 外部 PR 実作成へ進む場合は、GitHub 操作、Human approval、対象 repo、branch、diff、rollback、テスト結果、PR URL、post-submit verification を別途設計する。

#### 完了条件

- PR gate skill が適切に起動する。
- 証跡不足時に停止する。
- Coder evidence と transcript が保存される。
- 外部 PR submit audit は Human approval なしに保存 / 実行されない。
- 外部 PR 作成は adapter 未接続時に `blocked` audit として記録され、成功扱いされない。

#### 失敗扱い

- gate なしで PR 作成へ進む。
- raw transcript 全文を無制限保存する。
- diff / test / approval 不足を成功扱いする。

#### 2026-05-19 実施済み

- Contribution Gate が証跡不足を `blocked` にする domain / Viewer handler test を再確認した。
- Coder Proposal 実行時の `skill_diff.md` / `agent_transcript.md` 分離保存と、発話単位 `coder_transcript_log` 保存の application / persistence test を再確認した。
- `/viewer/skill-governance/recent` が `coder_transcripts` を返す handler test を再確認した。
- 外部 PR 実作成 API / client 接続は実装していないことを確認した。ここは Human approval 必須の別設計として残す。
- 2026-05-19 10:08 UTC に `ExternalPRSubmitRecord`、JSONL / SQLite persistence、`/viewer/skill-governance/external-pr-submit`、`pkg/rencrowclient.SubmitSkillGovernanceExternalPR`、`pkg/rencrowclient.EvaluateSkillGovernanceContributionGate`、Ops Viewer external PR audit 件数表示を追加し、local test と live service で確認した。
- live service で passed Contribution Gate `evt_skill_pr_gate_live_20260519103000` 後、Human approval なし request `submit_skill_pr_live_noapproval_20260519103000` は 403 を返し未保存、Human approval あり request `submit_skill_pr_live_20260519103000` は 202 を返し `submit_status=blocked`、`failure_reason="external PR adapter is not configured"`、`external_pr_created=false`、`post_submit_verified=false` の audit record として保存された。
- domain validation で `submit_status=created` と `external_pr_created=true` / `pr_url` / `post_submit_verified=true` / `post_submit_evidence` の整合性を必須化した。`blocked` / `failed` audit は PR 作成済みや post-submit verified として保存できない。
- 2026-05-19 に `post_submit_evidence` を audit model / external client model に追加し、created PR を post-submit verified とする場合は検証証跡が空では保存できないことを domain test で確認した。
- 2026-05-19 に external PR submit blocked audit は、request 側に `submit_status=created`、`pr_url`、`external_pr_created=true`、`post_submit_verified=true`、`post_submit_evidence`、`pr_adapter=github` が混入しても採用せず、保存値を `submit_status=blocked`、`external_pr_created=false`、`post_submit_verified=false`、`pr_url=""`、`post_submit_evidence=""`、`pr_adapter=unconfigured` に上書きすることを handler test で確認した。
- 2026-05-19 に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` が malformed success response を実 PR 作成成功扱いにしない validation を追加した。top-level `external_pr_created` / `post_submit_verified` と record 側の状態が矛盾する場合、または created success に `pr_url` / `post_submit_evidence` がない場合は error として扱う。
- 2026-05-19 に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` が response record の `submit_id` / `contribution_event_id` / `repo` を request と照合するようにした。2xx response でも別 gate / 別 repo の external PR audit record を成功扱いしない local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.EvaluateSkillGovernanceContributionGate` が malformed success response を Contribution Gate 成功扱いにしない validation を追加した。event_id / repo / gate_status / decision status / can_contribute / stop_reasons が一致しない場合は error として扱う。
- 2026-05-19 に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` / `EvaluateSkillGovernanceContributionGate` が、`submit_id` / `contribution_event_id` / `repo` / `title` などが欠けた request を送信前に拒否するようにした。2026-05-20 00:54 UTC 時点では `SubmitSkillGovernanceExternalPR` の `human_approved=false` も client 側で拒否する。これは実 PR 作成 E2E の代替ではない。
- 2026-05-19 に external PR submit が `contribution_event_id` の一致だけで別 repo の passed Contribution Gate を流用しないよう、gate repo と request repo の照合を追加した。repo mismatch 時は 409 で停止し、external PR submit audit を保存しない local handler test を追加した。
- 2026-05-19 に `pkg/rencrowclient.SkillGovernanceStatus` を追加し、`/viewer/skill-governance/recent` が 2xx を返しても manifest / trigger / change / contribution / external PR submit / coder transcript の同一 ID 重複や必須 ID 欠落を current view として成功扱いしない local client test を追加した。`external_pr_created=true` の record は `submit_status=created`、`pr_url`、`post_submit_evidence` なしでは status 証跡として受け取らない。これは実 PR 作成 E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.SkillGovernanceStatus` の current view validation を補強し、`external_pr_created=false` の submit record が `submit_status=created` / `pr_url` / `post_submit_verified=true` / `post_submit_evidence` を持つ場合も E2E 証跡として受け取らない local client test を追加した。これは PR URL や post-submit verified 文字列だけで実 PR 作成成功扱いしないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-19 19:16 UTC に `pkg/rencrowclient.SkillGovernanceStatus` の current view validation を追加補強し、external PR submit record の `submit_status` が `blocked` / `failed` / `created` 以外の場合は E2E 証跡として受け取らない local client test を追加した。同時に live `TestE2E_SkillGovernanceExternalPRClientRequiresApprovalAndAuditsBlockedSubmit` を再実行して pass。これは `submitted` などの曖昧な status 文字列を実 PR 作成成功と誤認しないための境界確認であり、実 PR 作成 E2E の代替ではない。
- `TestE2E_SkillGovernanceExternalPRClientRequiresApprovalAndAuditsBlockedSubmit` を追加し、`pkg/rencrowclient` から live service の Contribution Gate / external PR submit audit flow を再実行できるようにした。
- 2026-05-19 15:19 UTC に `gh` 認証と `origin` GitHub remote の存在を確認した。ただしこれは RenCrow 側 external PR adapter の接続確認ではない。
- 2026-05-19 15:19 UTC に live `/viewer/skill-governance/recent?limit=5` を再確認し、passed Contribution Gate はあるが、external PR submit record はすべて `submit_status=blocked`、`failure_reason="external PR adapter is not configured"`、`external_pr_created=false`、`post_submit_verified=false`、`pr_adapter=unconfigured` であることを確認した。
- 2026-05-19 15:19 UTC に `HandleSkillGovernanceExternalPRSubmit` が Human approval と passed Contribution Gate 後も `domainskill.NewBlockedExternalPRSubmitRecord` を保存し、response で `external_pr_created=false`、`post_submit_verified=false`、`external_pr_adapter_configuration=required` を返すことをコードで再確認した。
- 2026-05-19 17:00 UTC に現 binary の live service で `TestE2E_SkillGovernanceExternalPRClientRequiresApprovalAndAuditsBlockedSubmit` を再実行し pass。Human approval なし 403 と Human approval あり blocked audit を再確認した。実 PR 作成 / PR URL / post-submit verification は未接続のまま。
- 2026-05-19 17:41 UTC に `/viewer/skill-governance/recent?limit=3` が top-level readiness として `external_pr_adapter=unconfigured`、`external_pr_adapter_configured=false`、`human_approval_required_for_pr=true` を返すことを確認した。Ops Viewer でも external PR adapter 未設定と Human approval 必須を表示する。
- 初回 live status client 再実行では、JSONL store が起動ごとの同一 `skill_id` manifest append 履歴を複数返し、`pkg/rencrowclient.SkillGovernanceStatus` の重複 current view validation が fail した。JSONL `ListSkillManifests` を SQLite store と同じ最新 state per `skill_id` へ修正した。
- 修正後に `make install`、`picoclaw.service` restart、`PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_SkillGovernanceExternalPRClientRequiresApprovalAndAuditsBlockedSubmit -count=1 -v` を実行し pass。これは status current view / blocked audit の確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-19 21:02 UTC に Viewer Ops へ `Skill Governance External PR Submit Audits` 表を追加した。live `/viewer/skill-governance/recent?limit=3` は `external_pr_adapter=unconfigured` / `human_approval_required_for_pr=true` を返し、external PR submit records は `submit_status=blocked` / `failure_reason="external PR adapter is not configured"` / `external_pr_created=false` / `post_submit_verified=false` / `pr_adapter=unconfigured` のまま。browser E2E で audit 表と summary `skill external PR submit audits:` が visible state として表示されることを確認した。これは blocked audit の可視化であり、実 PR 作成や post-submit verification 成功ではない。
- 2026-05-19 21:29 UTC に `Skill Governance External PR Submit Audits` summary を補強し、`not created` 件数、`external PR adapter: unconfigured / configured: no / human approval: required` を visible state として表示するようにした。live `/viewer/skill-governance/recent?limit=3` は adapter 未接続、Human approval 必須、直近 records はすべて `blocked` / `external_pr_created=false` / `post_submit_verified=false` / `pr_adapter=unconfigured` のまま。browser E2E で `not created` と adapter 未接続表示を確認した。これは GitHub / 外部 repo への実 PR 作成、PR URL、post-submit verification の代替ではない。
- 2026-05-19 22:44 UTC に同 summary へ `blocked: no external PR created` を追加した。blocked audit を実 PR 作成成功と誤認しないための表示境界であり、GitHub / 外部 repo への実 PR 作成、PR URL、post-submit verification の代替ではない。
- 2026-05-19 22:06 UTC に Viewer Ops へ `Skill Evidence Audits` 表を追加し、live `/viewer/skill-governance/recent?limit=10` の trigger log / contribution gate / coder transcript 件数を同じ画面で確認できるようにした。現 live current view は `core.codebase-complexity-hotspot` の trigger と passed contribution gate を返す一方、`coder_transcripts=[]` のため、browser E2E では `skill evidence audits:` / `coder transcripts` / `blocked: coder evidence transcript not observed` を visible state として確認した。これは trigger / gate 証跡の可視化であり、Coder Proposal 実行時の `skill_diff.md` / `agent_transcript.md` 保存完了や実 PR 作成成功ではない。
- 2026-05-19 22:47 UTC に同 summary へ `blocked: passed contribution gate is not external PR evidence` を追加した。passed contribution gate を Coder evidence transcript 保存完了や実 PR 作成成功と誤認しないための表示境界である。
- 2026-05-20 00:36 UTC に live `/viewer/skill-governance/recent?limit=5` を再確認し、`coder_transcripts=[]`、`external_pr_adapter=unconfigured`、external PR submit records は `blocked` / `external_pr_created=false` / `post_submit_verified=false` のままであることを確認した。同時刻の `/health` は `local_llm_chat` / `local_llm_worker` が `http://192.168.1.13:8081` / `:8082` へ `no route to host` で 503。CODE 系 `/viewer/send` は成功時に Worker 実行へ進み得るため、本 repo worktree に対して無害な Coder Proposal 実行とは判断せず、自然発生 transcript E2E は blocked とした。
- 2026-05-20 00:38 UTC に `Skill Evidence Audits` の evidence complete 判定を `CoderTranscriptEntry` の実 schema へ修正した。Viewer は同一 job/session 内の `segment=patch_evidence` と `segment=transcript_evidence` の `evidence_path` ペアを数える。`pkg/rencrowclient.SkillGovernanceStatus` は evidence path 欠落、traversal path、片側だけの evidence pair を拒否する。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSkillGovernanceStatus' -count=1 -v` と `node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。`make install` / service restart 後、service は pid `948276` で `:18790` listen、live `/viewer/skill-governance/recent?limit=5` は 200、`PICOCLAW_BROWSER_E2E=1 PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_Phase25BrowserViewerSessionContract -count=1 -v` は pass。これは表示 / client 契約の修正であり、Coder Proposal provider 成功、`workspace/logs/skill_governance/coder_evidence/{job_id}/skill_diff.md` / `agent_transcript.md` の live 自然発生、実 PR 作成成功の代替ではない。
- 2026-05-20 00:54 UTC に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` / `SkillGovernanceStatus` を補強し、`human_approved=false` request と、PR 未作成の external PR submit record が `failure_reason` を持たない direct response / current view を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|RevenueStatus|SubmitSkillGovernanceExternalPR|SkillGovernanceStatus)' -count=1 -v` は pass。これは blocked / failed audit の理由欠落を実 PR 作成証跡として扱わないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 01:37 UTC に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` / `SkillGovernanceStatus` を追加補強し、direct response / current view の external PR submit record が `title`、`human_approved=true`、`approval_status=approved`、top-level `human_approval_required_for_pr=true` を満たさない場合は local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(SubmitSkillGovernanceExternalPR|SkillGovernanceStatus)' -count=1 -v` は pass。これは Human approval と approved audit が揃わない record を実 PR 作成証跡として扱わないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 02:32 UTC に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` / `SkillGovernanceStatus` を追加補強し、external PR adapter required / unconfigured 状態なのに direct response / current view の record 側だけ `pr_adapter=github` など設定済みに見える場合は local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/revenue ./pkg/rencrowclient -run 'Test(ValidateExternalSendApplyRecord|RevenueStatus|ApplyRevenueExternalSend|SkillGovernanceStatus|SubmitSkillGovernanceExternalPR)' -count=1 -v` は pass。これは blocked audit を adapter 接続済みや実 PR 作成証跡として扱わないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 04:15 UTC に `pkg/rencrowclient.SkillGovernanceStatus` を補強し、trigger log / change log / contribution / external PR submit / coder transcript の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(RevenueStatus|SkillGovernanceStatus)' -count=1 -v` は pass。これは timestamp 欠落の current view を external PR audit / Coder evidence 証跡として扱わないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 04:35 UTC に `pkg/rencrowclient.SubmitSkillGovernanceExternalPR` を補強し、direct response の external PR submit record が `created_at` を持たない場合は local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|SubmitSkillGovernanceExternalPR)' -count=1 -v` は pass。これは timestamp 欠落の direct audit response を実 PR 作成 / blocked audit 証跡として扱わないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 04:50 UTC に `pkg/rencrowclient.EvaluateSkillGovernanceContributionGate` を補強し、direct response の gate log が `created_at` を持たない場合は local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(EvaluateRevenueHumanDecision|ReviewRevenueHumanDecision|CreateRevenueDailyRoutineReport|CreateRevenueChannelDraft|EvaluateSkillGovernanceContributionGate)' -count=1 -v` は pass。これは timestamp 欠落の direct gate response を Contribution Gate 証跡として扱わないための境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 07:40 UTC に Skill Governance 保存前 domain validation を補強し、manifest の `updated_at` 欠落、trigger / change / contribution / external PR submit / coder transcript の `created_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/skillgovernance ./internal/infrastructure/persistence/skillgovernance ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(ValidateSkillGovernance|ValidateExternalPR|NewBlockedExternalPR|JSONLStore.*SkillGovernance|SQLiteStore.*SkillGovernance|HandleSkillGovernance|SkillGovernance)' -count=1` は pass。これは timestamp 欠落の external PR audit / Coder evidence ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、実 PR 作成 E2E の代替ではない。
- 2026-05-20 09:45 UTC に Viewer Ops の `/viewer/skill-governance/recent` fetch failure 境界を補強し、non-2xx body を `Skill external PR submit audits unavailable: ...` / `Skill evidence audits unavailable: ...` として表示し、古い external PR submit row や空 audit 表示を成功証跡にしない Node contract test を追加した。これは status API failure を実 PR 未作成 / 実 PR 作成成功として誤認しないための visible-state 境界であり、実 PR 作成 E2E の代替ではない。
- 実 PR 作成、PR URL 保存、post-submit verification は実行していない。

実行コマンド:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/skillgovernance ./internal/application/skillgovernance ./internal/infrastructure/persistence/skillgovernance ./internal/adapter/viewer
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/skillgovernance ./internal/infrastructure/persistence/skillgovernance ./internal/adapter/viewer ./pkg/rencrowclient ./cmd/picoclaw
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/skillgovernance ./internal/infrastructure/persistence/skillgovernance ./internal/adapter/viewer ./pkg/rencrowclient
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run TestHandleSkillGovernanceExternalPRSubmitSavesBlockedAudit -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleSkillGovernanceExternalPRSubmit(RequiresPassedGate|RejectsGateRepoMismatch|SavesBlockedAudit)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestEvaluateSkillGovernanceContributionGate|TestEvaluateSkillGovernanceContributionGateRejectsMalformedResponse|TestSubmitSkillGovernanceExternalPR' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run TestSubmitSkillGovernanceExternalPR -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(SubmitSkill|EvaluateSkill)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|SubmitSkillGovernanceExternalPR)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSkillGovernanceStatus' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/skillgovernance -run 'TestJSONLStore.*Skill|TestSQLiteStoreSaveAndListSkillGovernanceRecords' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleSkillGovernanceRecent|TestHandleSkillGovernanceExternalPRSubmit' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSkillGovernanceStatus|TestSubmitSkillGovernanceExternalPR' -count=1 -v
node internal/adapter/viewer/viewer_memory_panel.test.mjs
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 go test -tags=e2e ./test/e2e -run TestE2E_SkillGovernanceExternalPRClientRequiresApprovalAndAuditsBlockedSubmit -count=1 -v
PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_SkillGovernanceExternalPRClientRequiresApprovalAndAuditsBlockedSubmit -count=1 -v
command -v gh
gh auth status
git remote -v
curl -sS -m 5 'http://127.0.0.1:18790/viewer/skill-governance/recent?limit=5'
curl -sS -m 5 'http://127.0.0.1:18790/viewer/skill-governance/recent?limit=3'
sed -n '243,292p' internal/adapter/viewer/skill_governance_handler.go
rg -n 'NewBlockedExternalPRSubmitRecord|external PR adapter is not configured|SubmitSkillGovernanceExternalPR|ValidateExternalPRSubmitRecord' internal/domain/skillgovernance internal/adapter/viewer pkg/rencrowclient
```

残り:

- GitHub / 外部 repo へ実 PR を作る adapter 接続。
- 対象 repo、branch、diff、rollback、テスト結果、PR 本文、PR URL、監査ログの保存形式。
- 外部 PR 作成後の post-submit verification。

### 7.4 Revenue Operating Workflow

#### 目的

Human Decision Gate 承認後の apply audit と実外部送信接続を扱い、現在の draft-only 境界を壊さない。

#### 主な関連実装

- `internal/domain/revenue`
- `internal/application/revenue`
- `internal/infrastructure/persistence/revenue`
- `internal/adapter/viewer/revenue_handler.go`
- `internal/application/heartbeat`
- `pkg/rencrowclient`

#### 手順

1. `docs/10_新仕様/31_未実装項目実装仕様.md` の P7 を読む。
2. `/viewer/revenue/channel-drafts` が draft-only で保存されることを確認する。
3. `approval_status=pending` が既定であることを確認する。
4. `/viewer/revenue/human-decision-gate/review` で approve / reject が記録されることを確認する。
5. 実外部送信を接続する前に、送信先 channel adapter、audit log、送信結果、失敗時 rollback / retry 方針を設計する。
6. 外部送信ボタンや自動送信を追加する場合は、Human approval の明示 UI / API を必須にする。

#### 完了条件

- channel draft は承認前に外部送信されない。
- 承認後の送信適用が audit と送信結果を残す。
- 失敗時に通常成功として隠さない。

#### 失敗扱い

- pending draft を自動送信する。
- 成功保証文や無承認メッセージを送る。
- 外部送信結果を記録しない。

#### 2026-05-19 実施済み

- `ChannelDraft` が `external_send_applied=true` を拒否する domain test を再確認した。
- `/viewer/revenue/channel-drafts` が下書きを `approval_status=pending` で保存し、外部送信しない handler test を再確認した。
- `/viewer/revenue/human-decision-gate/review` の approve / reject 記録 test を再確認した。
- 2026-05-19 の追加実装前は、`pkg/rencrowclient` は Human Decision Gate 評価 / review / Daily Routine Report 作成 API までで、外部送信適用 API を持たなかった。
- 2026-05-19 08:47 UTC に live service で `/viewer/revenue/channel-drafts` を実行し、入力で `external_send_applied=true` を渡しても保存結果は `external_send_applied=false`、`external_actions_applied=false`、`human_approval_required_for_external_send_apply=true` になることを確認した。
- 2026-05-19 08:47 UTC に live service で `/viewer/revenue/human-decision-gate` と `/viewer/revenue/human-decision-gate/review` を実行し、`closed_channel_send` decision が pending / needs_review から approved へ遷移することを確認した。
- JSONL store の status API が同一 `decision_id` の pending / approved 履歴を複数返していたため、SQLite store と同じ current view として `ListHumanDecisionGateRecords` が最新 state per decision を返すよう修正した。
- 2026-05-19 08:49 UTC に live service を再 build / restart し、`/viewer/revenue?limit=20` が `rev_decision_live_20260519084700` を approved 1件だけ返し、`pending_decision_count=0`、`external_actions_applied=false` になることを確認した。
- 2026-05-19 に `ExternalSendApplyRecord`、JSONL / SQLite persistence、`/viewer/revenue/channel-drafts/external-send-apply`、`pkg/rencrowclient.CreateRevenueChannelDraft`、`pkg/rencrowclient.ApplyRevenueExternalSend` を追加した。
- 2026-05-19 09:20 UTC に live service で、承認済み `closed_channel_send` decision と `human_approved=true` を渡した `rev_apply_live_20260519092100` が `apply_status=blocked`、`send_result=not_sent`、`external_send_applied=false`、`post_send_verified=false` の audit record として保存されることを確認した。外部 channel adapter は未接続のため、実送信成功扱いにはしない。
- 2026-05-19 09:20 UTC に live service で、`human_approved` なしの `rev_apply_live_no_approval_20260519092100` は 403 を返し、status API に保存されないことを確認した。
- domain validation で `apply_status=sent` と `external_send_applied=true` / `post_send_verified=true` / `post_send_evidence` の整合性を必須化した。`blocked` / `failed` audit は送信済みや post-send verified として保存できない。
- 外部 channel adapter 未接続時の blocked audit は、request に `channel_adapter` が指定されても `channel_adapter=unconfigured` として保存する。未接続 adapter を設定済み adapter のように見せない local handler test を追加した。
- 2026-05-19 に external send apply が承認済み `closed_channel_send` decision を別 draft へ流用しないことを local handler test で確認した。`decision.subject_id` と `draft_id` が一致しない場合は 409 で停止し、external send apply audit を保存しない。
- 2026-05-19 に `post_send_evidence` を audit model / external client model に追加し、sent 成功状態を post-send verified とする場合は検証証跡が空では保存できないことを domain test で確認した。
- 2026-05-19 に `pkg/rencrowclient.EvaluateRevenueHumanDecision` / `ReviewRevenueHumanDecision` が Human Decision Gate API の 2xx だけでは成功扱いせず、decision_id / decision_type / approval_status / gate_status / requires_approval の整合性を検証するようにした。malformed Human Decision Gate response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CreateRevenueDailyRoutineReport` が Daily Routine API の 2xx だけでは draft report 成功扱いせず、report_id / workstream_id / date、`status=draft_report`、`external_actions_applied=false`、`report.external_send_applied=false`、Human approval requirement の整合性を検証するようにした。malformed Daily Routine response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.CreateRevenueChannelDraft` が Channel Draft API の 2xx だけでは draft-only 成功扱いせず、draft_id / channel、`external_actions_applied=false`、`draft.external_send_applied=false`、Human approval requirement、`approval_status=pending` の整合性を検証するようにした。malformed Channel Draft response は direct client error とする local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.ApplyRevenueExternalSend` が malformed success response を実外部送信成功扱いにしない validation を追加した。top-level `external_actions_applied` / `post_send_verified` と record 側の状態が矛盾する場合、または sent success に `post_send_evidence` がない場合は error として扱う。
- 2026-05-19 に `pkg/rencrowclient.ApplyRevenueExternalSend` が response record の `apply_id` / `draft_id` / `decision_id` を request と照合するようにした。2xx response でも別 draft / 別 decision の external send apply record を成功扱いしない local client test を追加した。
- 2026-05-19 に `pkg/rencrowclient.EvaluateRevenueHumanDecision` / `ReviewRevenueHumanDecision` / `CreateRevenueDailyRoutineReport` / `CreateRevenueChannelDraft` / `ApplyRevenueExternalSend` が、必須 decision / draft / apply / channel / body などが欠けた request を送信前に拒否するようにした。2026-05-20 00:54 UTC 時点では `ApplyRevenueExternalSend` の `human_approved=false` も client 側で拒否する。これは実外部送信 E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.RevenueStatus` を追加し、`/viewer/revenue` が 2xx を返しても Human Decision / Daily Routine / Channel Draft / External Send Apply の同一 ID 重複や必須 ID 欠落を current view として成功扱いしない local client test を追加した。`external_send_applied=true` の record は `apply_status=sent` と `post_send_evidence` なしでは status 証跡として受け取らず、summary の `external_actions_applied=true` も verified send record なしでは error とする。これは実外部送信 E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.RevenueStatus` の current view validation を補強し、`external_send_applied=false` の apply record が `post_send_verified=true` / `post_send_evidence` / `apply_status=sent` を持つ場合も E2E 証跡として受け取らない local client test を追加した。これは post-send verified や sent 文字列だけで実外部送信成功扱いしないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-19 19:16 UTC に `pkg/rencrowclient.RevenueStatus` の current view validation を追加補強し、external send apply record の `apply_status` が `blocked` / `failed` / `sent` 以外の場合は E2E 証跡として受け取らない local client test を追加した。同時に live `TestE2E_RevenueExternalSendClientRequiresApprovalAndAuditsBlockedApply` を再実行して pass。これは `applied` などの曖昧な status 文字列を実外部送信成功と誤認しないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 00:54 UTC に `pkg/rencrowclient.ApplyRevenueExternalSend` / `RevenueStatus` を補強し、`human_approved=false` request と、未送信・未適用の external send apply record が `failure_reason` を持たない direct response / current view を local client error として拒否するようにした。これは blocked / failed audit の理由欠落を実外部送信成功や適用済み証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 01:18 UTC に `pkg/rencrowclient.RevenueStatus` を補強し、dashboard summary の pending decision / daily report / channel draft / external send apply count が負数の current view を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(RevenueStatus|ApplyRevenueExternalSend)' -count=1 -v` は pass。これは malformed summary count を dashboard 証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 01:43 UTC に `pkg/rencrowclient.ApplyRevenueExternalSend` / `RevenueStatus` を追加補強し、direct response / current view の external send apply record が `human_approved=true`、`approval_status=approved`、未送信時の top-level `human_approval_required_for_retry=true` を満たさない場合は local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|RevenueStatus)' -count=1 -v` は pass。これは Human approval と approved audit が揃わない record を実外部送信証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 02:32 UTC に `internal/domain/revenue.ValidateExternalSendApplyRecord` / `pkg/rencrowclient.ApplyRevenueExternalSend` / `RevenueStatus` を追加補強し、未送信・未適用 record の `send_result=sent` と、external channel adapter required / unconfigured 状態なのに direct response / current view の record 側だけ `channel_adapter` が設定済みに見える状態を local error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/revenue ./pkg/rencrowclient -run 'Test(ValidateExternalSendApplyRecord|RevenueStatus|ApplyRevenueExternalSend|SkillGovernanceStatus|SubmitSkillGovernanceExternalPR)' -count=1 -v` は pass。これは blocked audit を送信成功や adapter 接続済み証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 04:15 UTC に `pkg/rencrowclient.RevenueStatus` を補強し、Human Decision / Daily Routine / Channel Draft / External Send Apply の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(RevenueStatus|SkillGovernanceStatus)' -count=1 -v` は pass。これは timestamp 欠落の current view を dashboard / external send audit 証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 04:35 UTC に `pkg/rencrowclient.ApplyRevenueExternalSend` を補強し、direct response の external send apply record が `created_at` を持たない場合は local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|SubmitSkillGovernanceExternalPR)' -count=1 -v` は pass。これは timestamp 欠落の direct audit response を実外部送信 / blocked audit 証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 04:50 UTC に `pkg/rencrowclient.EvaluateRevenueHumanDecision` / `ReviewRevenueHumanDecision` / `CreateRevenueDailyRoutineReport` / `CreateRevenueChannelDraft` を補強し、direct response の record / report / draft が `created_at` を持たない場合は local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(EvaluateRevenueHumanDecision|ReviewRevenueHumanDecision|CreateRevenueDailyRoutineReport|CreateRevenueChannelDraft|EvaluateSkillGovernanceContributionGate)' -count=1 -v` は pass。これは timestamp 欠落の direct response を Human Decision Gate / draft-only 証跡として扱わないための境界確認であり、実外部送信 E2E の代替ではない。
- 2026-05-20 07:30 UTC に Revenue 保存前 domain validation を補強し、market research / SNS post metric / product / customer voice / revenue event / daily routine / channel draft / external send apply / Human Decision Gate record の `created_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/revenue -count=1` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/revenue ./internal/infrastructure/persistence/revenue ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(Validate.*Revenue|ValidateProduct|ValidateCustomerVoice|ValidateExternalSend|BuildHumanDecision|BuildDailyRoutine|JSONLStore.*Revenue|SQLiteStore.*Revenue|HandleRevenue|Revenue)' -count=1` は pass。これは timestamp 欠落の revenue ledger item を保存時点で拒否し、後続 dashboard / external send audit current view を malformed にしないための local domain 境界確認であり、実外部送信 E2E の代替ではない。
- `TestE2E_RevenueExternalSendClientRequiresApprovalAndAuditsBlockedApply` を追加し、`pkg/rencrowclient` から live service の draft-only / Human Decision Gate / external send apply audit flow を再実行できるようにした。
- 2026-05-19 15:20 UTC に live `/viewer/revenue?limit=10` を再確認し、channel drafts は `external_send_applied=false`、summary は `external_actions_applied=false`、external send apply records はすべて `apply_status=blocked`、`send_result=not_sent`、`failure_reason="external channel adapter is not configured"`、`channel_adapter=unconfigured`、`post_send_verified=false`、`external_send_applied=false` であることを確認した。
- 2026-05-19 15:20 UTC に `HandleRevenueExternalSendApply` が Human approval、approved `closed_channel_send` decision、draft / decision subject 一致を確認した後も、外部送信 adapter を呼ばず blocked audit を保存することをコードで再確認した。
- 2026-05-19 15:20 UTC に `ValidateExternalSendApplyRecord` が sent / applied / post-send verified / evidence の整合性を要求し、blocked audit を送信成功扱いにできないことを再確認した。
- 2026-05-19 17:00 UTC に現 binary の live service で `TestE2E_RevenueExternalSendClientRequiresApprovalAndAuditsBlockedApply` を再実行し pass。Human approval なし 403 と Human approval あり blocked audit を再確認した。実外部送信 / post-send verification は未接続のまま。
- 2026-05-19 17:47 UTC に `/viewer/revenue?limit=3` が top-level readiness として `external_channel_adapter=unconfigured`、`external_channel_adapter_configured=false`、`human_approval_required_for_external_send=true` を返すことを確認した。Ops Viewer でも external channel adapter 未設定と Human approval 必須を表示する。
- `TestE2E_RevenueExternalSendClientRequiresApprovalAndAuditsBlockedApply` は status 確認を raw HTTP decode ではなく `pkg/rencrowclient.RevenueStatus` 経由に変更し、external channel readiness と blocked audit current view を client validation 付きで確認して pass した。これは実外部送信 E2E の代替ではない。
- 2026-05-19 20:58 UTC に Viewer Ops へ `Revenue External Send Apply Audits` 表を追加した。live `/viewer/revenue?limit=3` は `external_channel_adapter=unconfigured` / `human_approval_required_for_external_send=true` を返し、apply audit は `apply_status=blocked` / `send_result=not_sent` / `channel_adapter=unconfigured` / `post_send_verified=false` / `external_send_applied=false` のまま。browser E2E で audit 表と summary `revenue external send apply audits:` が visible state として表示されることを確認した。これは blocked audit の可視化であり、実外部送信や post-send verification 成功ではない。
- 2026-05-19 21:32 UTC に `Revenue External Send Apply Audits` summary を補強し、`not sent` 件数、`external channel adapter: unconfigured / configured: no / human approval: required` を visible state として表示するようにした。live `/viewer/revenue?limit=3` は adapter 未接続、Human approval 必須、直近 apply records はすべて `blocked` / `not_sent` / `channel_adapter=unconfigured` / `post_send_verified=false` / `external_send_applied=false` のまま。browser E2E で `not sent` と adapter 未接続表示を確認した。これは実外部送信、送信成功結果、post-send verification の代替ではない。
- 2026-05-19 22:44 UTC に同 summary へ `blocked: no external send applied` を追加した。blocked audit を実外部送信成功と誤認しないための表示境界であり、実外部送信、送信成功結果、post-send verification の代替ではない。
- 2026-05-19 22:37 UTC に `Revenue Channel Drafts` summary へ `draft-only` 件数と `external send requires human approval: yes` を追加した。browser E2E で visible state を確認し、channel draft 台帳の存在を外部送信済みや post-send verification 成功と誤認しない表示境界として扱う。
- 2026-05-20 09:40 UTC に Viewer Ops の `/viewer/revenue` fetch failure 境界を補強し、non-2xx body を `Revenue external send apply audits unavailable: ...` として表示し、古い external send apply row や空台帳表示を成功証跡にしない Node contract test を追加した。これは status API failure を外部送信なし / 実外部送信成功として誤認しないための visible-state 境界であり、実外部送信 E2E の代替ではない。

実行コマンド:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/revenue ./internal/application/revenue ./internal/infrastructure/persistence/revenue ./internal/adapter/viewer ./pkg/rencrowclient
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/revenue ./internal/infrastructure/persistence/revenue ./internal/adapter/viewer ./pkg/rencrowclient
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run TestHandleRevenueExternalSendApplyRecordsBlockedAuditWhenAdapterUnavailable -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleRevenueExternalSendApply(RequiresHumanApproval|RecordsBlockedAuditWhenAdapterUnavailable|RejectsUnapprovedDecision|RejectsDecisionSubjectMismatch)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCreateRevenueDailyRoutineReport|TestCreateRevenueDailyRoutineReportRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCreateRevenueChannelDraft|TestCreateRevenueChannelDraftRejectsMalformedResponse|TestApplyRevenueExternalSend' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestEvaluateRevenueHumanDecision|TestEvaluateRevenueHumanDecisionRejectsMalformedResponse|TestReviewRevenueHumanDecision|TestReviewRevenueHumanDecisionRejectsMalformedResponse' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(EvaluateRevenue|ReviewRevenue|CreateRevenue|ApplyRevenue)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|SubmitSkillGovernanceExternalPR)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestRevenueStatus' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(ApplyRevenueExternalSend|RevenueStatus)' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleRevenueStatus|TestHandleRevenueExternalSendApply' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestRevenueStatus|TestApplyRevenueExternalSend' -count=1 -v
node internal/adapter/viewer/viewer_memory_panel.test.mjs
GOCACHE=/tmp/picoclaw-gocache PICOCLAW_LIVE_E2E=1 go test -tags=e2e ./test/e2e -run TestE2E_RevenueExternalSendClientRequiresApprovalAndAuditsBlockedApply -count=1 -v
PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_RevenueExternalSendClientRequiresApprovalAndAuditsBlockedApply -count=1 -v
curl -sS -m 5 'http://127.0.0.1:18790/viewer/revenue?limit=10'
curl -sS -m 5 'http://127.0.0.1:18790/viewer/revenue?limit=3'
sed -n '641,731p' internal/adapter/viewer/revenue_handler.go
sed -n '154,205p' internal/domain/revenue/validation.go
rg -n 'external-send|ExternalSend|external_send|channel adapter|channel_adapter|not_sent|post_send|ApplyRevenueExternalSend' internal/domain/revenue internal/adapter/viewer pkg/rencrowclient
```

残り:

- 実外部送信を行う場合の送信先 channel adapter 接続。
- 送信成功結果、失敗時 retry / rollback 方針。
- 外部送信後の post-send verification と成功 / 失敗表示。

### 7.5 Complexity Hotspot Skill

#### 目的

report-only / proposal / Sandbox Gate の範囲で安全に確認する。patch 適用や外部 PR 作成は対象外とする。

#### 主な関連実装

- `internal/domain/complexity`
- `internal/application/complexity`
- `internal/infrastructure/persistence/complexity`
- `internal/adapter/viewer/complexity_hotspot_handler.go`
- `skills/core/codebase-complexity-hotspot`

#### 手順

1. `docs/10_新仕様/31_未実装項目実装仕様.md` の P10 を読む。
2. 小さい fixture または限定ディレクトリで scan を行う。
3. hotspot report artifact が保存されることを確認する。
4. proposal API が Workstream Goal / pending_review Artifact を作ることを確認する。
5. concrete diff API が対象 hotspot file に scoped されない diff を拒否することを確認する。
6. `sandbox_id` を渡した場合、Sandbox Promotion Request と Gate Log が作成されることを確認する。
7. patch は適用しない。外部 PR も作らない。

#### 完了条件

- hotspot を検出できる。
- report / proposal / concrete diff proposal が review-only artifact として保存される。
- 対象外 diff を拒否する。
- 証跡不足の場合は `needs_review` / `needs_more_tests` になる。

#### 失敗扱い

- 自動で patch を適用する。
- ベンチなしに高速化成功を断言する。
- 対象外ファイルの diff を通す。

#### 2026-05-19 実施済み

- scan が report artifact を保存する test を再確認した。
- proposal API が Workstream Goal / pending_review Artifact を作る test を再確認した。
- concrete diff API が review-only artifact を保存し、`patch_applied=false` / Human approval required として返す test を再確認した。
- 対象 hotspot file に scoped されない diff を拒否する application test を再確認した。
- `sandbox_id` 付き proposal が Sandbox Promotion Request / Gate Log を作るが、patch は適用しない test を再確認した。
- 2026-05-19 08:38 UTC に live service で `/viewer/complexity-hotspots/scan` を限定 scope `internal/application/complexity`、`max_hotspots=3`、`scan_id=scan_live_20260519083500` として実行した。
- scan は 201。`mode=report_only`、`files_scanned=8`、`hotspots_found=3`、`status=completed`。
- `/viewer/complexity-hotspots?limit=20` から `scan_live_20260519083500`、`art_complexity_scan_live_20260519083500`、hotspot、evidence を確認した。
- `/viewer/complexity-hotspots/proposals` を `hotspot_id=scan_live_20260519083500_nested_loop_1` で実行し、`complexity_patch_proposal`、`complexity_coder_diff_request`、Workstream Goal / pending_review Artifact が保存されることを確認した。
- `/viewer/complexity-hotspots/concrete-diffs` を対象 file `internal/application/complexity/analyzer.go` に scoped した unified diff で実行し、`art_complexity_concrete_live_20260519085000` が `complexity_concrete_diff_proposal` / `pending_review` として保存され、`patch_applied=false` / `human_approval_required=true` になることを確認した。
- 対象外 file `internal/application/complexity/other.go` の concrete diff は `400 concrete_diff must be scoped to hotspot file internal/application/complexity/analyzer.go` で拒否されることを確認した。
- 2026-05-19 09:56 UTC に一時 sandbox config で `/viewer/sandbox` route を有効化し、`sandbox_id` 付き proposal `promo_complexity_sandbox_live_20260519095600` が Promotion Request / Gate Log を作り、`diff_path` / `test_result_path` / `rollback_plan_path` / `human_approval` 不足で `needs_more_tests` になることを確認した。
- 同じ一時 config で `sandbox_id` 付き concrete diff `promo_complexity_sandbox_concrete_live_20260519095630` が Promotion Request / Gate Log を作り、diff / test / rollback はあるが Human approval 未承認のため `needs_review` で止まることを確認した。
- `/viewer/sandbox?limit=10` から両 promotion と `evt_complexity_promotion_gate_1779184573811590017` / `evt_complexity_concrete_diff_gate_1779184588820294958` を確認した。
- 証跡取得後、一時 sandbox config は削除し、当時の live service 再起動後に `/viewer/sandbox` が 404 へ戻ることを確認した。2026-05-19 20:23 UTC の runtime 更新後は Sandbox disabled 時も status route が 503 `sandbox store unavailable` を返す。
- sandbox store 未接続の 503 応答前に proposal / concrete diff の review artifact が部分保存される挙動を test で再現し、sandbox store が必要な request では保存前に接続可否を検査するよう修正した。
- 2026-05-19 09:02 UTC に live service を再 build / restart し、sandbox store 未接続の proposal / concrete diff が 503 を返した場合、新規 `goal_id` / `artifact_id` / `promotion_id` が status API に残らないことを確認した。
- 2026-05-19 に `coder-diffs` の未接続 generator は 503 で止まり、generator error / unified diff 不備 / 対象 hotspot file 外 diff は保存前に拒否され、`complexity_concrete_diff_proposal` / Workstream Artifact / Sandbox Promotion Request を部分保存しないことを test で確認した。
- 2026-05-19 に runtime wiring でも `/viewer/complexity-hotspots/coder-diffs` route を常に登録し、Coder provider 未接続時は 404 ではなく 503 `complexity coder diff mode unavailable` で明示停止するようにした。これは実 Coder provider live E2E の成功扱いではなく、未接続を成功・route欠落として隠さないための境界確認である。
- 2026-05-19 15:23 UTC に live `/viewer/complexity-hotspots/coder-diffs` が 5 秒 / 15 秒の client timeout で応答できないことを確認した。これを成功扱いせず、生成 timeout の明示停止が必要な境界として扱った。
- 2026-05-19 に `coder-diffs` 生成へ 10 秒 timeout を追加した。timeout 時は `503 complexity coder diff generation timed out` で停止し、`complexity_concrete_diff_proposal` / Workstream Artifact / Sandbox Promotion Request を部分保存しない local handler test を追加した。
- 2026-05-19 15:26 UTC に現行 binary へ更新後、live `/viewer/complexity-hotspots/coder-diffs` は Coder provider 応答に unified diff がなかったため `400 coder output did not contain unified diff` で停止することを確認した。status API に `job_complexity_coder_diff_recheck_20260519152600`、自動生成 artifact ID、同 error は残らなかった。当時の `/viewer/sandbox?limit=5` は 404。2026-05-19 20:23 UTC の runtime 更新後は Sandbox disabled 時も status route が 503 `sandbox store unavailable` を返す。
- 2026-05-20 00:10 UTC に Coder diff generation prompt へ同一 hotspot の evidence snippets と diff-only system prompt を追加し、local test で prompt / handler evidence 経路を確認した。現 binary へ `make install` / service restart 後、live `/viewer/complexity-hotspots/coder-diffs` を `job_complexity_coder_diff_evidence_20260520001000` で再実行したが、実 Coder provider は `503 complexity coder diff generation timed out` で停止した。status current view に同 job / artifact / timeout error は残らず、review artifact / Workstream artifact / Sandbox Promotion Request は保存されなかった。これは Coder provider 成功 E2E ではなく、入力証跡改善後も timeout blocked が残る証跡である。
- 2026-05-20 00:45 UTC に provider error / timeout が current view から消える弱点を補強し、`/viewer/complexity-hotspots/coder-diffs` が unified diff 不備または timeout を受けた場合に `complexity_coder_diff_failure` / `status=failed` report artifact を保存するようにした。failure artifact は `Patch applied: false` と Human approval required を明記するが、review diff artifact、Workstream artifact、Sandbox Promotion Request は作らない。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleComplexityHotspotCoderDiff' -count=1 -v`、`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestComplexityStatus' -count=1 -v`、`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。`make install` / clean restart 後、live `/viewer/complexity-hotspots/coder-diffs` を `job_id=job_complexity_coder_diff_failure_audit_20260520004900` で実行し、response は `503 complexity coder diff generation timed out`。`/viewer/complexity-hotspots?limit=10` は `art_complexity_coder_diff_failure_job_complexity_coder_diff_failure_audit_20260520004900`、`artifact_type=complexity_coder_diff_failure`、`status=failed`、`Failure reason: complexity coder diff generation timed out`、`Patch applied: false` を返した。live `TestE2E_ComplexityStatusClientCurrentView` と browser `TestE2E_Phase25BrowserViewerSessionContract` は pass。これは failure audit の可視化であり、Coder provider 成功、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-20 02:14 UTC に `pkg/rencrowclient.ComplexityStatus` の `complexity_coder_diff_failure` current view validation を追加補強し、failure artifact は `status=failed`、`Patch applied: false`、`Failure reason:` を満たさない場合に local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestComplexityStatus' -count=1 -v` は pass。これは failed artifact 文字列だけを Coder provider timeout / unified diff 不備の証跡として扱わないための境界確認であり、実 Coder provider live E2E、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-20 03:55 UTC に `pkg/rencrowclient.ComplexityStatus` の current view validation を補強し、scan / hotspot / evidence / report の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestComplexityStatus' -count=1 -v` は pass。これは timestamp 欠落の ledger item を report-only scan、review-only proposal、Coder diff failure audit の証跡として扱わないための境界確認であり、実 Coder provider live E2E、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-20 06:45 UTC に Complexity Hotspot の保存前 domain validation を補強し、scan / hotspot / evidence / report の `created_at` 欠落と completed scan の `completed_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/complexity -run 'TestValidate(ScanEvent|Hotspot|Complexity|Report)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/complexity ./internal/infrastructure/persistence/complexity ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(Validate.*Complexity|ValidateScanEvent|ValidateHotspot|JSONLStore|SQLiteStore|ComplexityStatus|HandleComplexity|CreateComplexity)' -count=1` は pass。これは timestamp 欠落の ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、実 Coder provider live E2E、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-20 06:00 UTC に `pkg/rencrowclient.CreateComplexityConcreteDiff` / `CreateComplexityCoderDiff` の direct response validation を補強し、response hotspot / concrete diff artifact / Workstream Artifact / Sandbox Promotion / Sandbox Gate Log の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestCreateComplexity(ConcreteDiff|CoderDiff)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の direct response を review-only concrete diff / coder diff 証跡として扱わないための境界確認であり、実 Coder provider live E2E、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-20 10:20 UTC に Viewer Ops の `/viewer/complexity-hotspots` fetch failure 境界を補強し、non-2xx 時は stale `Patch applied: true` 風の review artifact を使わず `complexity hotspot status unavailable: ...` / `Complexity review artifacts unavailable: ...` / `blocked: patch apply state unreadable` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは status API failure を patch 適用、Sandbox apply、外部 PR 作成証跡として扱わないための local 表示境界であり、実 Coder provider live E2E の代替ではない。
- 2026-05-19 に Sandbox Promotion apply は、`post_apply_verification_command` 指定時に verifier / ToolRunner が未接続なら 503 で停止し、`promotion_applied` log / completed artifact を保存しないことを test で確認した。
- 2026-05-20 01:48 UTC に `pkg/rencrowclient.SandboxStatus` を追加補強し、`promotion_applied` / `rollback_executed` Gate Log は `human_approval_status=granted`、対応 promotion の Human approval、apply diff / post-apply verification / rollback plan path、completed `post_apply_verification` / `rollback_execution` artifact が揃わない場合に local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(SandboxStatus|CreatePromotionRequest|ApplyPromotion|RollbackPromotion|SubmitPromotionWorkflow)' -count=1 -v` は pass。これは current view の terminal status 文字列だけを正式 apply / rollback 証跡として扱わないための境界確認であり、実 Coder provider 成功、main repo patch 適用、外部 PR 作成の代替ではない。
- 2026-05-20 04:05 UTC に `pkg/rencrowclient.SandboxStatus` を追加補強し、sandbox / artifact / promotion / gate log の created_at 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSandboxStatus' -count=1 -v` は pass。これは timestamp 欠落の status view を正式 apply / rollback 証跡として扱わないための境界確認であり、実 Coder provider 成功、main repo patch 適用、外部 PR 作成の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.ComplexityStatus` / `CreateComplexityConcreteDiff` / `CreateComplexityCoderDiff` を追加し、concrete diff / coder diff API が 2xx を返しても `patch_applied=false`、Human approval required、`complexity_concrete_diff_proposal` / `pending_review`、sandbox gate、coder result の整合性が取れない malformed response を direct client success として返さない local client test を追加した。これは実 Coder provider live E2E、patch 適用、外部 PR 作成の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.ComplexityStatus` が status API の 2xx だけでは current view 成立扱いせず、scan / hotspot / evidence / report の重複 ID と必須 ID 欠落を error にする local client test を追加した。これは report-only / review-only / Sandbox Gate の status view を E2E 証跡として読むための境界確認であり、実 Coder provider live E2E、patch 適用、外部 PR 作成の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.ComplexityStatus` の current view validation を補強し、`status=completed` の scan が `completed_at` を持たない場合は E2E 証跡として受け取らない local client test を追加した。これは report-only scan の完了文字列だけで成功扱いしないための境界確認であり、実 Coder provider live E2E、patch 適用、外部 PR 作成の代替ではない。
- 2026-05-20 01:26 UTC に `pkg/rencrowclient.ComplexityStatus` の current view validation を追加補強し、scan count 負数、hotspot / evidence line range 不整合、hotspot confidence 範囲外を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestComplexityStatus' -count=1 -v` は pass。これは malformed numeric evidence を Complexity status 証跡として扱わないための境界確認であり、実 Coder provider live E2E、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-19 17:08 UTC に `TestE2E_ComplexityStatusClientCurrentView` を追加し、live service の `/viewer/complexity-hotspots` を `pkg/rencrowclient.ComplexityStatus` 経由で読んだ。初回は JSONL store が同一 `artifact_id` の report 履歴を複数返し、client validation が fail したため、Complexity JSONL store の List API を SQLite store と同じ最新 state per ID の current view に修正した。
- 修正後に `make install`、`picoclaw.service` restart、`PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_ComplexityStatusClientCurrentView -count=1 -v` を実行し、completed report-only scan、hotspot、evidence、pending_review report artifact を live current view として確認した。これは status 証跡読み取りの確認であり、実 Coder provider 成功、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-19 18:49 UTC に `pkg/rencrowclient.ComplexityStatus` の current view validation を補強し、proposal / coder diff request / concrete diff proposal report が `pending_review` 以外の場合、また concrete diff proposal content が `Patch applied: false` と Human approval required を示さない場合は E2E 証跡として受け取らない local client test を追加した。Viewer Ops card は live current view の review-only report を `mode: review-only` / `blocked: no patch applied` として表示する。現 binary へ `make install` / service restart 後、browser E2E と `TestE2E_ComplexityStatusClientCurrentView` を再実行して pass。これは patch 未適用の可視化であり、実 Coder provider 成功、patch 適用、Sandbox apply、外部 PR 作成の代替ではない。
- 2026-05-19 21:38 UTC に Viewer Ops へ `Complexity Review Artifacts` 表を追加し、live `/viewer/complexity-hotspots?limit=5` の `pending_review` reports を `1 pending-review` / `0 patch applied` / `human approval required` / `mode: review-only blocked: no patch applied` として visible state にした。`art_complexity_sandbox_concrete_live_20260519095630` は `Patch applied: false` / `Human approval required: true` を返す。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` と live browser `TestE2E_Phase25BrowserViewerSessionContract` は pass。これは review artifact の存在を patch 適用成功や外部 PR 作成と誤認しないための表示境界である。
- `coder-diffs` は 2026-05-19 15:26 UTC に live service で呼び出し、Coder output に unified diff がない場合に 400 で停止し部分保存しないことを確認した。Sandbox Promotion apply と外部 PR 作成は呼び出していない。

実行コマンド:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/complexity ./internal/application/complexity ./internal/infrastructure/persistence/complexity ./internal/adapter/viewer
GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/complexity ./internal/application/complexity ./internal/infrastructure/persistence/complexity ./internal/adapter/viewer ./internal/infrastructure/persistence/revenue ./internal/infrastructure/persistence/superagent ./cmd/picoclaw
GOCACHE=/tmp/picoclaw-gocache go test ./cmd/picoclaw -run TestBuildComplexityHotspotCoderDiffHandlerReturns503WhenCoderUnavailable -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleComplexityHotspotCoderDiff' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestComplexity|TestCreateComplexity' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/complexity ./pkg/rencrowclient -run 'TestJSONLStoreSavesAndListsComplexityRecords|TestComplexityStatus|TestSandboxStatus' -count=1 -v
PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_ComplexityStatusClientCurrentView -count=1 -v
node internal/adapter/viewer/viewer_memory_panel.test.mjs
PICOCLAW_BROWSER_E2E=1 PICOCLAW_LIVE_E2E=1 PICOCLAW_LIVE_BASE_URL=http://127.0.0.1:18790 GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run TestE2E_Phase25BrowserViewerSessionContract -count=1 -v
```

今回追加確認:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/complexity ./internal/adapter/viewer ./internal/domain/complexity ./internal/infrastructure/persistence/complexity
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestComplexityStatus|TestComplexityStatusRejectsMalformedCurrentView' -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/complexity ./internal/adapter/viewer -run 'TestCoderDiffService|TestHandleComplexityHotspotCoderDiff' -count=1 -v
make install
systemctl --user start picoclaw.service
curl -sS -i -m 15 http://127.0.0.1:18790/viewer/complexity-hotspots/coder-diffs -H 'Content-Type: application/json' -d '{"hotspot_id":"scan_live_20260519083500_nested_loop_1","job_id":"job_complexity_coder_diff_recheck_20260519152600","workstream_id":"ws_live_20260519083500"}'
curl -sS -m 5 'http://127.0.0.1:18790/viewer/complexity-hotspots?limit=50' | rg 'job_complexity_coder_diff_recheck_20260519152600|art_complexity_concrete_diff_scan_live_20260519083500_nested_loop_1|coder output did not contain unified diff' || true
curl -sS -i -m 20 http://127.0.0.1:18790/viewer/complexity-hotspots/coder-diffs -H 'Content-Type: application/json' -d '{"hotspot_id":"scan_live_20260519083500_nested_loop_1","job_id":"job_complexity_coder_diff_evidence_20260520001000","workstream_id":"ws_live_20260519083500","artifact_id":"art_complexity_coder_diff_evidence_20260520001000"}'
curl -sS -m 5 'http://127.0.0.1:18790/viewer/complexity-hotspots?limit=80' | rg 'job_complexity_coder_diff_evidence_20260520001000|art_complexity_coder_diff_evidence_20260520001000|complexity coder diff generation timed out' || true
curl -sS -i -m 5 'http://127.0.0.1:18790/viewer/sandbox?limit=5'
```

残り:

- Sandbox Promotion apply / rollback は隔離 apply root に対する Human approval あり live E2E 済み。本 repo worktree への patch 適用、Complexity Coder diff からの apply、外部 PR 作成は実行していない。実 repo patch / PR が必要な場合は別 Goal とし、Human approval、diff、test、rollback、post-apply verification、ToolRunner 接続を揃える。
- `coder-diffs` は live provider 呼び出し境界まで確認したが、2026-05-19 の Coder output は unified diff を含まず 400 で停止し、2026-05-20 の evidence snippet 付き prompt 再実行は 503 timeout で停止した。in-scope unified diff を返す provider 成功 E2E と Sandbox Gate 連携は未確認として残す。
- 実際の performance 改善は別 Goal、別 patch proposal、Human approval に分離する。
- 外部 PR 作成は対象外のまま維持する。

### 7.5.1 Persona Lore / Mutual Observation 保存前境界

2026-05-20 07:20 UTC に Persona 保存前 domain validation を補強し、discomfort / trigger / canonical response / observation / meta profile update / interface session の `created_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/persona -run 'TestValidate(Discomfort|Trigger|Observation|MetaProfile|Interface|Persona)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/persona ./internal/infrastructure/persistence/persona ./internal/adapter/viewer -run 'Test(Validate.*Persona|ValidateDiscomfort|ValidateTrigger|ValidateObservation|ValidateMetaProfile|ValidateInterface|JSONLStorePersona|JSONLStoreRejectsSensitive|JSONLStoreApplyMeta|SQLiteStorePersona|SQLiteStoreRejectsSensitive|HandlePersona)' -count=1` は pass。これは timestamp 欠落の observation ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、Human Review ありの meta 適用 live E2E や long-term personality update E2E の代替ではない。

2026-05-20 10:50 UTC に Viewer Ops の `/viewer/persona-observation` fetch failure 境界を補強し、status API が non-2xx の場合は stale approved Meta 更新候補や空台帳ではなく `persona observation status unavailable: ...` / `blocked: persona meta review state unreadable` / `blocked: long-term personality update state unreadable` を表示する Node contract test を追加した。これは status API failure を Human Review ありの meta 適用や long-term personality update 成功として誤読しないための visible-state 境界確認であり、meta 適用 live E2E の代替ではない。

### 7.6 E2E / runtime 要確認群

#### 目的

外部依存が必要な E2E 項目を、skip / blocked / fail / pass に分けて台帳化する。

#### 対象

- 外部 API provider E2E
  - Claude
  - DeepSeek
  - OpenAI
  - Google Search Chat
  - Google Search Worker
- Ollama / local LLM E2E
- Slack / Discord / Telegram file payload 実 API E2E
- Wild SSH / 複数マシン transport
- 分散全経路
- browser microphone STT
- TTS playback
- lip sync

#### 手順

1. `docs/10_新仕様/17_E2E残課題.md` を読む。
2. `docs/10_新仕様/32_E2E_runtime確認チェックリスト.md` の該当節を読む。
3. 外部依存が揃っているか確認する。
4. 揃っていない場合は `skipped` または `blocked` とし、理由を記録する。
5. 揃っている場合は実機 / 実 API / 実ブラウザで 1 セッションを追う。
6. route、job_id、session_id、response、event log を照合する。
7. fallback に落ちた場合は fail として記録する。
8. 結果を `docs/10_新仕様/17_E2E残課題.md` に反映する。
9. 実装不足が判明した場合は、`docs/10_新仕様/31_未実装項目実装仕様.md` の該当 Phase に戻す。

#### 完了条件

- 各項目が pass / fail / skipped / blocked のいずれかに分類されている。
- skip と fail が成功扱いされていない。
- 証跡、config、route、session、log が残っている。
- `13_実装項目インベントリ.md` と `17_E2E残課題.md` の状態が実態と一致している。

#### 2026-05-19 追加済み client 境界確認

- `pkg/rencrowclient` の non-2xx response は成功扱いせず、`APIError` として method / path / status / body を保持する local client test を追加した。
- これは Human approval なし 403、adapter 未接続 503、外部 API fail を fail / blocked 証跡として扱うための境界確認であり、実 API E2E、正式 apply、外部送信、PR 作成の完了条件にはしない。

実行コマンド:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run TestAPIErrorIncludesStatus -count=1 -v
GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestBrowserTraceAPI' -count=1 -v
```

## 8. 外部環境がないと進められないもの

| 項目 | 必要な外部環境 |
|---|---|
| 外部 API provider E2E | Claude / DeepSeek / OpenAI / Google Search の実 key |
| Ollama / local LLM E2E | Ollama は 2026-05-19 に `http://100.83.207.6:11434` で確認済み。local OpenAI-compatible Chat / Worker は `http://192.168.1.13:8081` / `:8082` 不達のため復旧待ち |
| Slack / Discord / Telegram file payload | 実 API event、token、webhook、channel 環境 |
| Wild SSH / 複数マシン transport | 実機分散環境 |
| browser microphone STT | HTTPS Viewer、実ブラウザ、マイク、STT bridge |
| TTS playback / lip sync | 実ブラウザ音声再生、audio event / chunk 確認 |

### 8.1 2026-05-19 実施済み E2E / runtime 確認

- 2026-05-19 08:26 UTC に live service を現ソースで build した binary へ更新し、`:18790` listen を確認した。
- `/viewer/runtime-config` は取得できた。
- `/viewer/superagent?limit=1`、`/viewer/ai-workflow?limit=1`、`/viewer/revenue?limit=1`、`/viewer/complexity-hotspots?limit=1` は 200。更新前の installed binary は該当 route を含まず 404 だったため、古い binary の結果は現行判定に使わない。
- AI Workflow live API で command invocation、context usage 保存、external control `needs_approval` policy event を確認した。通常 live config では context budget disabled だが、一時 config で warning / stop handler flow、primary LLM provider middleware の runtime warning / stop event 保存、DCI search route からの Worker ToolRunner `file_read` warning / stop event 保存を部分確認済み。ToolRunner wrapper は `buildToolRuntime` の runtime integration でも warning / stop、JSONL 保存、stop 時 offload を確認済み。証跡取得後、一時 config は削除し、live service 再起動後の policy は disabled に戻した。
- 2026-05-19 に `pkg/rencrowclient.ToolHarnessStatus` を追加し、`/viewer/tool-harness/recent` の 2xx だけでは mediation event view を成功扱いせず、event_id 重複、必須 field 欠落、未知の `validation_status`、repair / relation default の識別子欠落を local client test で拒否するようにした。これは Tool Harness event を E2E 証跡として読むための境界確認であり、provider 固有 tool protocol recovery や実運用 E2E の代替ではない。
- 2026-05-20 01:32 UTC に `pkg/rencrowclient.ToolHarnessStatus` の status / evidence 整合性を補強し、`valid` event に repair evidence がある場合と、`repaired` event に repair / relation default evidence がない場合を direct client success として返さない local client test を追加した。これは mediation event current view の malformed response 拒否であり、provider 固有 tool protocol recovery の代替ではない。
- 2026-05-20 02:40 UTC に `pkg/rencrowclient.ToolHarnessStatus` の current view validation を追加補強し、mediation event の `created_at` 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestToolHarnessStatus' -count=1 -v` は pass。これは timestamp のない Tool Harness event を実行証跡として扱わないための境界確認であり、provider 固有 tool protocol recovery の代替ではない。
- 2026-05-20 08:10 UTC に Tool Harness 保存前 domain validation を追加し、mediation event の `event_id` / `tool_name` / `raw_input_hash` / `validation_status` / `created_at` 欠落、未知 status、`valid` と repair evidence の矛盾、`repaired` の evidence 欠落を JSONL 保存前に拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/toolharness ./internal/infrastructure/persistence/toolharness ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(ValidateEvent|ToolHarness|HandleToolHarness|JSONLRecorder)' -count=1` は pass。これは malformed mediation event を保存時点で拒否し、後続 current view を壊さないための local domain 境界確認であり、provider 固有 tool protocol recovery の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.DCIRecent` / `DCISearch` を追加し、`/viewer/dci/recent` / `/viewer/dci/search` の 2xx だけでは DCI trace / evidence 成立扱いせず、trace 重複、必須 field 欠落、step 重複、request query / Evidence Pack / Trace event_id / evidence count / evidence path-line-snippet の不整合を local client test で拒否するようにした。さらに terminal trace (`completed` / `failed`) が `ended_at` を持たない場合も E2E 証跡として受け取らない local client test を追加した。これは DCI evidence / trace を E2E 証跡として読むための境界確認であり、実機 VectorDB / Qdrant E2E や大規模 corpus tuning の代替ではない。
- 2026-05-19 19:34 UTC に `pkg/rencrowclient.DCIRecent` / `DCISearch` の current view validation を追加補強し、trace status は `completed` / `failed`、step status は `ok` / `error` / `stopped` / `completed` に限定した。`failed` trace と `error` step は `error_message` を必須にし、未知 status や error 証跡なしの失敗を DCI 成功証跡として受け取らない local client test を追加した。同時に live `TestE2E_ToolHarnessAndDCIStatusClientCurrentView` を再実行して pass。これは DCI read-only search / trace current view の証跡境界確認であり、実機 VectorDB / Qdrant E2E や大規模 corpus tuning の代替ではない。
- 2026-05-20 02:47 UTC に `pkg/rencrowclient.DCIRecent` / `DCISearch` の trace validation を追加補強し、`started_at` 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'Test(DCIRecent|DCISearch)' -count=1 -v` は pass。これは開始時刻のない DCI trace を read-only search / evidence 実行証跡として扱わないための境界確認であり、実機 VectorDB / Qdrant E2E や ranking tuning の代替ではない。
- 2026-05-20 04:25 UTC に `pkg/rencrowclient.DCIRecent` / `DCISearch` の trace validation を追加補強し、`actor` / `mode` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestDCI' -count=1 -v` は pass。これは実行主体と探索 mode が不明な DCI trace を read-only search / evidence 実行証跡として扱わないための境界確認であり、実機 VectorDB / Qdrant E2E や ranking tuning の代替ではない。
- 2026-05-20 05:55 UTC に `pkg/rencrowclient.DCIRecent` / `DCISearch` の step validation を補強し、search step の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestDCI(Recent|Search)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の step を DCI read-only search / evidence 実行証跡として扱わないための境界確認であり、実機 VectorDB / Qdrant E2E や ranking tuning の代替ではない。
- 2026-05-20 08:00 UTC に DCI 保存前 domain validation を追加し、search trace の `started_at` / terminal `ended_at` 欠落、step の `created_at` 欠落、未知 status、証跡なし failed / error、重複 step を JSONL / SQLite 保存前に拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/dci ./internal/infrastructure/persistence/dci ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(ValidateSearchTrace|DCI|HandleDCI|JSONLStore.*DCI|SQLiteStore.*DCI|SQLiteStoreSaveSearch|JSONLStoreSave)' -count=1` は pass。これは malformed DCI trace を保存時点で拒否し、後続 current view を壊さないための local domain 境界確認であり、実機 VectorDB / Qdrant E2E や ranking tuning の代替ではない。
- 2026-05-20 01:25 UTC に `pkg/rencrowclient.DCIRecent` / `DCISearch` の数値証跡境界を補強し、step `result_count < 0`、Evidence Pack `confidence` 範囲外、Evidence `confidence` 範囲外を direct client success として返さない local client test を追加した。これは DCI result current view の malformed response 拒否であり、実機 VectorDB / Qdrant E2E や ranking tuning の代替ではない。
- 2026-05-19 17:12 UTC に `TestE2E_ToolHarnessAndDCIStatusClientCurrentView` を追加し、live service の `/viewer/tool-harness/recent` から valid mediation event を `pkg/rencrowclient.ToolHarnessStatus` 経由で確認した。同じ E2E で `pkg/rencrowclient.DCISearch` により `ToolRunner context budget` を read-only 検索し、completed trace / Evidence Pack / non-empty evidence を確認した後、`DCIRecent` が同一 `event_id` の completed trace を返すことを確認した。これは Tool Harness / DCI の証跡読み取りと read-only DCI search の live E2E であり、provider 固有 tool protocol recovery、実機 VectorDB / Qdrant E2E、大規模 corpus tuning、外部送信の代替ではない。
- 2026-05-19 22:54 UTC に Viewer Ops の Tool Harness / DCI Trace card へ `provider protocol recovery not verified` / `VectorDB/Qdrant E2E not verified` を追加した。valid mediation event や read-only DCI evidence を provider 固有 tool protocol recovery、実機 VectorDB / Qdrant E2E、大規模 corpus tuning 成功と誤認しないための表示境界である。
- 2026-05-20 11:05 UTC に Viewer Ops の Tool Harness / DCI fetch failure 境界を補強し、`/viewer/tool-harness/recent` または `/viewer/dci/recent` が non-2xx の場合は stale mediation event / DCI trace ではなく `tool harness status unavailable: ...` / `dci trace status unavailable: ...` と blocked 理由を表示する Node contract test を追加した。これは status API failure を provider 固有 tool protocol recovery、read-only DCI evidence 成功、実機 VectorDB / Qdrant E2E 成功として誤読しないための visible-state 境界確認であり、provider recovery や実機 VectorDB / Qdrant E2E の代替ではない。
- 2026-05-20 12:45 UTC に Viewer Ops の DCI manual search fetch failure 境界を補強し、`/viewer/dci/search` が non-2xx の場合は generic `dci search failed` ではなく body 付き `HTTP <status>: ...` を DCI manual search result の `error:` 行へ表示し、stale completed search result を残さない Node contract test を追加した。これは read-only DCI search route failure を evidence pack 成功や実機 VectorDB / Qdrant E2E 成功として扱わないための local 表示境界であり、実機 VectorDB / Qdrant E2E の代替ではない。
- 2026-05-20 11:15 UTC に Viewer Evidence / Verification fetch failure 境界を補強し、`/viewer/evidence/recent`、`/viewer/evidence/summary`、`/viewer/verification/recent`、`/viewer/verification/summary` が non-2xx の場合は stale execution report / verification report / summary count を使わず `Evidence / verification unavailable: ...` / `evidence summary unavailable: ...` / `blocked: execution evidence state unreadable` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは status API failure を Worker 実行証跡、verification 成功、post-apply verification 成功として扱わないための local 表示境界であり、live Worker 実行 E2E や post-apply verification E2E の代替ではない。
- 2026-05-20 13:45 UTC に Viewer Evidence detail fetch failure 境界を補強し、`/viewer/evidence/detail?job_id=...` が non-2xx の場合は generic `evidence detail fetch failed` ではなく body 付き `HTTP <status>: ...` を Selected Evidence Detail に表示する Node contract test を追加した。これは detail route failure を Worker 実行証跡詳細取得成功や post-run verification 成功として扱わないための local 表示境界であり、live Worker 実行 E2E や post-apply verification E2E の代替ではない。
- 2026-05-19 に `pkg/rencrowclient.KnowledgeMemoryStatus` / `ReviewKnowledgeMemory` を追加し、`/viewer/knowledge-memory` / `/viewer/knowledge-memory/review` の 2xx だけでは Knowledge Memory 台帳 / review 成立扱いせず、Personal Archive 原本の unprotected、同一 ID 重複、必須 field 欠落、Dream Run の auto-approved、review request と response の `detail_type` / `id` / `review_status` / promote intent / target status / formal target 不整合を local client test で拒否するようにした。これは Knowledge Memory 台帳と review/promote comparison を E2E 証跡として読むための境界確認であり、正式 memory / Source Registry への自動 promote 成功の代替ではない。
- 2026-05-20 02:01 UTC に Dream Consolidation Run の `status` / `review_status` 整合性を保存前 validation と `pkg/rencrowclient.KnowledgeMemoryStatus` current view validation の両方で補強した。`pending` は `draft` / `proposal`、`approved` は `reviewed` / `promoted`、`rejected` は `rejected` の組み合わせだけを受け取り、矛盾した Dream review / promote 台帳状態は local client/domain error とする。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(ValidateDreamConsolidation|DreamConsolidation|KnowledgeMemoryStatus)' -count=1 -v` は pass。これは Dream review / promote の current view 境界確認であり、正式 memory / Source Registry 自動 promote 成功の代替ではない。
- 2026-05-19 19:39 UTC に `pkg/rencrowclient.KnowledgeMemoryStatus` の current view validation を追加補強し、Creative / News / Daily Intake / Dream の status、Dream review status、Temporal Marker layer を許可値に限定する local client test を追加した。同時に live `TestE2E_KnowledgeMemoryAndBrowserTraceStatusClientCurrentView` を再実行して pass。これは未知 status や不正 layer を正式 memory / review / promote 成功証跡として受け取らないための境界確認であり、Knowledge Memory 作成・review・promote や Source Registry への正式同期完了の代替ではない。
- 2026-05-20 01:22 UTC に `pkg/rencrowclient.KnowledgeMemoryStatus` を補強し、Temporal Marker の `access_count` が負数の current view を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestKnowledgeMemoryStatus' -count=1 -v` は pass。これは malformed temporal marker count を Knowledge Memory 台帳証跡として扱わないための境界確認であり、正式 memory / Source Registry 自動同期の代替ではない。
- 2026-05-20 03:25 UTC に `pkg/rencrowclient.KnowledgeMemoryStatus` の current view validation を追加補強し、Personal Archive / Creative / News / Daily Intake / Temporal Marker / Dream Run の `created_at` 欠落を local client error として拒否するようにした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestKnowledgeMemoryStatus' -count=1 -v` は pass。これは timestamp 欠落の current view を Knowledge Memory 台帳証跡として扱わないための境界確認であり、正式 memory / Source Registry 自動同期の代替ではない。
- 2026-05-20 06:20 UTC に Knowledge Memory create handler を補強し、Personal Archive / Creative / News / Daily Intake / Temporal Marker / Dream Run 作成 payload が `created_at` を省略した場合も保存前に server timestamp を付与する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run TestKnowledgeMemoryCreateHandlers -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer ./internal/domain/knowledgememory ./internal/infrastructure/persistence/knowledgememory ./pkg/rencrowclient -run 'Test(KnowledgeMemory|DreamConsolidation|CreateKnowledgeMemory|ReviewKnowledgeMemory)' -count=1` は pass。これは create API が timestamp 欠落の台帳 item を生成しないための local handler 境界確認であり、正式 memory / Source Registry 自動同期の代替ではない。
- 2026-05-20 06:25 UTC に Knowledge Memory の保存前 domain validation を補強し、Creative / News / Daily Intake / Dream の未知 status / review_status を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory -run TestValidateKnowledgeMemoryRejectsUnknownStatus -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory ./internal/adapter/viewer ./internal/infrastructure/persistence/knowledgememory ./pkg/rencrowclient -run 'Test(Validate.*Knowledge|ValidateDream|KnowledgeMemory|DreamConsolidation|CreateKnowledgeMemory|ReviewKnowledgeMemory)' -count=1` は pass。これは unknown status の台帳 item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、正式 memory / Source Registry 自動同期の代替ではない。
- 2026-05-20 06:30 UTC に Knowledge Memory の保存前 domain validation を補強し、Temporal Marker の `access_count` が負数の item を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory -run 'TestValidateTemporalMemoryMarker' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory ./internal/adapter/viewer ./internal/infrastructure/persistence/knowledgememory ./pkg/rencrowclient -count=1` は pass。これは negative count の台帳 item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、正式 memory / Source Registry 自動同期の代替ではない。
- 2026-05-20 06:35 UTC に Knowledge Memory の保存前 domain validation を補強し、Personal Archive / Creative / News / Daily Intake / Temporal Marker / Dream Run の `created_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory -run 'TestValidateKnowledgeMemoryRejectsMissingCreatedAt|TestValidateKnowledgeMemoryRejectsUnknownStatus|TestValidateDreamConsolidation' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/knowledgememory ./internal/adapter/viewer ./internal/infrastructure/persistence/knowledgememory ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の台帳 item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、正式 memory / Source Registry 自動同期の代替ではない。
- 2026-05-20 10:40 UTC に Viewer Ops の `/viewer/knowledge-memory` fetch failure 境界を補強し、status API が non-2xx の場合は stale promoted/enabled 台帳や空台帳ではなく `knowledge memory status unavailable: ...` / `blocked: memory promote state unreadable` / `blocked: source registry sync state unreadable` を表示する Node contract test を追加した。これは status API failure を正式 memory promote / Source Registry 自動同期成功として誤読しないための visible-state 境界確認であり、正式 memory / Source Registry 自動同期 E2E の代替ではない。
- 2026-05-20 12:05 UTC に Memory tab の `refreshKnowledgeMemoryLedger` を補強し、`/viewer/knowledge-memory` が non-2xx の場合は stale Personal Archive / Creative Knowledge / News Knowledge / Daily Intake / Temporal Marker / Dream Run row を使わず `Knowledge memory ledger unavailable: ...` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは Knowledge Memory route failure を正式 memory promote / Source Registry 自動同期 / Dream consolidation 成功として扱わないための local 表示境界であり、正式 memory / Source Registry 自動同期 E2E の代替ではない。
- 2026-05-19 23:43 UTC に `pkg/rencrowclient.CreateKnowledgeNewsItem` / `CreateKnowledgeDailyIntakeRule` と `TestE2E_KnowledgeMemoryCreateReviewPromoteCurrentView` を追加し、通常 live config で News Knowledge と Daily Intake Rule の create -> review/promote -> current view を確認した。証跡は `news_id=news_e2e_20260519234334.871771383` (`promoted`) と `rule_id=rule_e2e_20260519234334.871771383` (`enabled`)。この作業で JSONL store の同一 ID 追記履歴を最新 state per ID に集約し、typed-nil Conversation L1 store が L1 staging 連携で panic しないよう `WithL1Connection` を補強した。通常 config は `conversation_enabled=false` / `l1_sqlite_config_present=false` のため、正式 memory / Source Registry 自動同期成功の代替にはしない。
- 2026-05-19 に `pkg/rencrowclient.SourceRegistryStatus` / `SourceRegistryStaging` / `ValidateSourceRegistryStaging` / `PromoteSourceRegistryStaging` を追加し、`/viewer/source-registry` の 2xx だけでは Source Registry 台帳 / staging validate / promote 成立扱いせず、source entry / staging item の同一 ID 重複、必須 field 欠落、trust score 範囲外、validate response の request id / passed-status-issues / auto-promote intent 不整合、promote response の target / staging_id / category / domain / namespace 不整合を local client test で拒否するようにした。これは Source Registry staging review / validate / promote API を E2E 証跡として読むための境界確認であり、外部 fetch 成功、正式 memory / news / knowledge 自動確定、または外部情報の正当性確認の代替ではない。
- 2026-05-20 01:18 UTC に `ValidateSourceRegistryStaging` の validate response 境界を補強し、`pending` response、`validated` だが `passed=false`、`validated` だが issues あり、`rejected` だが `passed=true` を direct client success として返さない local client test を追加した。`validated` は `passed=true` かつ issues 空、`rejected` は `passed=false` かつ issues ありでなければ staging validation 成功証跡として扱わない。これは client response 契約確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 02:08 UTC に `pkg/rencrowclient.SourceRegistryStaging` の current view validation を補強し、terminal `validation_status` (`validated` / `rejected`) は `meta.validated_at` を必須、`rejected` は `meta.validation_issues` も必須にした。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSourceRegistry' -count=1 -v` は pass。これは terminal status 文字列だけを Source Registry staging review / validate 成立証跡として扱わないための境界確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 02:11 UTC に `pkg/rencrowclient.SourceRegistryStatus` の current view validation を補強し、`last_status` がある source entry は `last_fetched_at` を必須、`last_error` 単独は拒否する local client test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSourceRegistry' -count=1 -v` は pass。これは fetch status 文字列だけを外部 fetch 成功 / 失敗証跡として扱わないための境界確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 03:35 UTC に `pkg/rencrowclient.SourceRegistryStaging` の current view validation を補強し、staging item の `created_at` / `updated_at` 欠落または RFC3339 不正を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSourceRegistry' -count=1 -v` は pass。これは timestamp 欠落 / 不正の staging item を Source Registry staging review / validate / promote 証跡として扱わないための境界確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 08:20 UTC に `/viewer/source-registry` の source entry response へ `created_at` / `updated_at` を追加し、`pkg/rencrowclient.SourceRegistryStatus` が欠落または RFC3339 不正を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer ./pkg/rencrowclient ./internal/infrastructure/persistence/conversation -run 'Test(HandleSourceRegistry|SourceRegistry|L1SQLiteStore_.*SourceRegistry|L1SQLiteStore_.*Staging|ValidateL1Namespace)' -count=1` は pass。これは timestamp 欠落 / 不正の source entry を Source Registry 台帳、外部 fetch、staging review の証跡として扱わないための境界確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 06:10 UTC に `pkg/rencrowclient.PromoteSourceRegistryStaging` の direct response validation を補強し、news / knowledge / memory promotion item の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSourceRegistry(Status|Staging|ValidateAndPromote)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは timestamp 欠落の promotion response を Source Registry promote 証跡として扱わないための境界確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 06:15 UTC に `pkg/rencrowclient.PromoteSourceRegistryStaging` の direct response validation を追加補強し、news / knowledge / memory promotion item の `ID` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestSourceRegistry(Status|Staging|ValidateAndPromote)' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -count=1` は pass。これは ID 欠落の promotion response を Source Registry promote 成功証跡として扱わないための境界確認であり、通常 config の live Source Registry blocked 状態は継続する。
- 2026-05-20 11:35 UTC に Memory tab の Source Registry fetch failure 境界を補強し、`/viewer/source-registry` または `action=staging` が non-2xx の場合は stale source / staging row を使わず `Source Registry unavailable: ...` / `Source Registry staging unavailable: ...` / `staging unavailable: ...` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは Source Registry route failure を source 台帳、staging review、validate / promote 成功として扱わないための local 表示境界であり、live Source Registry E2E の代替ではない。
- 2026-05-20 13:05 UTC に Memory tab の Source Registry action failure 境界を補強し、validate / promote / run 操作が non-2xx の場合は generic error ではなく body 付き `HTTP <status>: ...` を staging status line または run status に表示する Node contract test を追加した。これは validate / promote / run route failure を Source Registry staging review、promote、外部 fetch/run 成功として扱わないための local 表示境界であり、live Source Registry E2E の代替ではない。
- 2026-05-20 13:15 UTC に Memory tab の Source Registry save / export / import action failure 境界を補強し、source save、YAML export、YAML import が non-2xx の場合は console-only generic error ではなく body 付き `HTTP <status>: ...` を Source Registry action status に表示する Node contract test を追加した。これは source 登録、YAML export/import、Source Registry 設定反映成功として扱わないための local 表示境界であり、live Source Registry E2E の代替ではない。
- 2026-05-20 13:25 UTC に Memory tab の memory state / promote action failure 境界を補強し、`/viewer/memory/state` と `/viewer/memory/promote` が non-2xx の場合は generic error ではなく body 付き `HTTP <status>: ...` を Memory table に表示する Node contract test を追加した。これは memory state 更新や memory promote route failure を Memory Layers 反映、Source Registry 自動同期、正式 memory promote 成功として扱わないための local 表示境界であり、live memory promote E2E の代替ではない。
- 2026-05-20 08:30 UTC に `pkg/rencrowclient.MemoryLayers` を追加し、`/viewer/memory/layers` の L0 / L1 / L3 memory event、L2 summary、L3 Qdrant document の必須 field と timestamp を current view validation で確認するようにした。`TestE2E_SourceRegistryStagingValidatePromoteAndMemoryLayers` も raw JSON map ではなく typed client 経由で promoted memory を読む。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestMemoryLayers' -count=1` と `GOCACHE=/tmp/picoclaw-gocache go test -tags=e2e ./test/e2e -run 'TestE2E_SourceRegistryStagingValidatePromoteAndMemoryLayers' -count=1` は pass。これは malformed Memory Layers current view を E2E 証跡として扱わないための境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や正式 memory 自動同期成功の代替ではない。
- 2026-05-20 08:40 UTC に Conversation L1 memory event の保存前 validation を補強し、`SaveMessage` の空 `session_id` / `thread_id<=0` / 空 speaker / 空 message と、保存・promote 直前の L1 memory event 必須 field / timestamp 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation -run 'TestL1SQLiteStore_(SaveMessage|DefaultNamespace|RejectsInvalid|UpdateMemoryState|PromoteValidatedStagingItemToMemory|ValidateStagingItemAutoPromotesMemoryCandidate)' -count=1` は pass。これは malformed L1 memory event を Memory Layers current view に流さないための保存境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や live E2E の代替ではない。
- 2026-05-20 08:50 UTC に Conversation L1 memory event の読み出し validation を補強し、既存 DB row が malformed な場合も `RecentByNamespace` / `RecentByState` / `RecentBySession` scan 時に拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation -run 'TestL1SQLiteStore_(RecentMemoryRejectsMalformedRows|SaveMessageRejectsMalformedMemoryEvent|SaveMessageAndRecentByNamespace|PromoteValidatedStagingItemToMemory|ValidateStagingItemAutoPromotesMemoryCandidate)' -count=1` は pass。これは Memory Layers handler が壊れた既存 row を current view として返さないための読み出し境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や live E2E の代替ではない。
- 2026-05-20 09:00 UTC に DuckDB L2 ThreadSummary の保存・読み出し validation を補強し、`SaveThreadSummary` と `GetSessionHistory` / `SearchByDomain` scan 時に `thread_id<=0` と空 `summary` を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation -run 'TestDuckDBStore_.*ThreadSummary' -count=1` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(DuckDBStore_.*ThreadSummary|HandleMemoryLayers|MemoryLayers)' -count=1` は pass。これは Memory Layers の L2 summary が malformed row を current view として返さないための読み出し境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や live E2E の代替ではない。
- 2026-05-20 09:10 UTC に VectorDB KB Document の保存・変換 validation を補強し、`SaveKB` は `id` / `domain` / `content` / `created_at` / `updated_at` / `embedding` 欠落を拒否し、Qdrant point 変換は `id` / `domain` / `content` / `created_at` / `updated_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation -run 'TestVectorDBStore_.*Document|TestSaveL1KnowledgeItemSavesVectorKBDocument|TestListKBDocuments' -count=1` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(VectorDBStore_.*Document|SaveL1KnowledgeItemSavesVectorKBDocument|ListKBDocuments|HandleMemoryLayers|MemoryLayers)' -count=1` は pass。これは Memory Layers の L3 Qdrant document が malformed payload を current view として返さないための境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や実 Qdrant E2E の代替ではない。
- 2026-05-20 09:20 UTC に `/viewer/memory/layers` handler の snapshot validation を追加し、hot / cold store が malformed L0 / L1 / L3 memory、L2 summary、L3 Qdrant document を返した場合は 500 `invalid memory layers snapshot` で停止する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/viewer -run 'TestHandleMemoryLayers' -count=1` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/persistence/conversation ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(L1SQLiteStore_.*Memory|DuckDBStore_.*ThreadSummary|VectorDBStore_.*Document|HandleMemoryLayers|MemoryLayers)' -count=1` は pass。これは Viewer / browser 直アクセスで malformed Memory Layers current view を成功扱いしないための handler 境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や live E2E の代替ではない。
- 2026-05-20 09:30 UTC に Viewer Memory tab の `refreshMemoryLayers` を補強し、`/viewer/memory/layers` の non-2xx body を table 上に `Memory Layers unavailable: ...` として表示する Node contract test を追加した。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは handler failure を stale row や空表示として成功扱いしないための Viewer visible-state 境界確認であり、通常 config の Source Registry / Memory Layers blocked 状態や live browser E2E の代替ではない。
- 2026-05-20 11:25 UTC に News Pack tab の `refreshNewsPack` を補強し、`/viewer/memory/snapshot` が non-2xx の場合は stale news / digest / recall usage を使わず `News Pack unavailable: ...` / `News digests unavailable: ...` / `News recall usage unavailable: ...` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは Memory snapshot API failure を News Pack / Daily Digest / Recall usage 成功として扱わないための local 表示境界であり、live Source Registry / News ingestion E2E の代替ではない。
- 2026-05-20 11:45 UTC に Memory tab の `refreshMemoryEvents` / `refreshRecallTraces` を補強し、`/viewer/memory/events` または `/viewer/recall/traces` が non-2xx の場合は stale L1 event / search cache / recall trace を使わず `Memory events unavailable: ...` / `Search cache unavailable: ...` / `Recall traces unavailable: ...` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは memory event / Recall Trace route failure を L1 event log、search cache、recall usage 成功として扱わないための local 表示境界であり、live conversation memory / recall E2E の代替ではない。
- 2026-05-20 11:55 UTC に Memory tab の `refreshMemorySnapshot` を補強し、`/viewer/memory/snapshot` が non-2xx の場合は stale memory / news / digest / knowledge count を使わず `Memory snapshot unavailable: ...` / `Memory snapshot news unavailable: ...` / `Memory snapshot digests unavailable: ...` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは Memory snapshot route failure を L1 memory、News Pack、Daily Digest、Knowledge count 成功として扱わないための local 表示境界であり、live conversation memory / News ingestion E2E の代替ではない。
- 2026-05-19 19:45 UTC に Source Registry の current-view validation を追加補強し、source kind を `rss` / `atom` / `official_api` / `github` / `huggingface` / `pypi` / `mediawiki` / `search_fallback`、fetch `last_status` を空または `ok` / `error`、staging / validation status を `pending` / `validated` / `rejected` に限定した。`last_status=error` は `last_error` 必須とし、保存層の `MarkSourceRegistryFetched` も unknown status と error 証跡なしを拒否する local test を追加した。live `/viewer/source-registry` / `action=staging` は通常 config で 503 `source registry unavailable` のため、Source Registry staging review / validate / promote の live E2E は blocked として維持する。
- 2026-05-19 23:22 UTC に一時 Conversation L1 config と隔離 SQLite `/tmp/picoclaw-source-registry-live-e2e/l1.sqlite` で `TestE2E_SourceRegistryStagingValidatePromoteAndMemoryLayers` を実行して pass。local RSS fixture を live `/viewer/source-registry` に登録し、run による fetch / staging、validated staging current view、明示 validate、memory promote、`/viewer/memory/layers?namespace=kb:e2e` の promoted memory 表示まで確認した。証跡例は `source_id=rss:e2e-source-registry-20260519232213729923285`、`staging_id=kb:e2e:rss:e2e-source-registry-20260519232213729923285:20260519T222213Z:40a8c9126365:0a727bfec34c`、`promoted_memory_id=kb:e2e:kb:e2e:rss:e2e-source-registry-20260519232213729923285:20260519T222213Z:40a8c9126365:0a727bfec34c:1779232933750570361`。確認後は通常 config を復元し、`conversation_enabled=false` / `l1_sqlite_config_present=false` / `source_registry_available=false` / `memory_layers_available=false` を再確認した。
- 2026-05-19 に `pkg/rencrowclient.BrowserTraceAPIStatus` / `DiscoverBrowserTraceAPI` / `CreateBrowserTraceAPIFetcherProposal` を追加し、`/viewer/browser-trace-api` / `discover` / `fetcher-proposals` の 2xx だけでは Browser Trace API discovery / fetcher proposal 成立扱いせず、trace / candidate / schema / validation / coverage / artifact の同一 ID 重複や必須 field 欠落、write method、validation status / issues、discover response の request trace 整合、fetcher proposal の `official_promotion=false` / `implementation_apply=false` / `pending_review` artifact / validated candidate / Workstream artifact 整合不備を local client test で拒否するようにした。これは Browser Trace API discovery と fetcher proposal を E2E 証跡として読むための境界確認であり、実 Fetcher 実装、正式 API 採用、promoted DB write、外部 API 利用規約確認の代替ではない。
- 2026-05-20 00:01 UTC に `pkg/rencrowclient.ValidateBrowserTraceAPICandidate` と `/viewer/browser-trace-api/validations` を追加し、Human review evidence なしでは `needs_review`、terms / official API / PII / schema / risk / human approval の review evidence が揃った場合だけ `validated` として保存する境界を追加した。同時に live `TestE2E_BrowserTraceAPIDiscoverValidateAndFetcherProposal` を実行し、local trace fixture から discover、initial `needs_review`、validation review、review-only fetcher proposal、current view の pending_review proposal artifact まで確認した。証跡は `trace_run_id=trace_e2e_20260520000547.552107722`、`candidate_id=api_cand_d7c3f3f50a6a`、`validation_id=api_val_review_api_cand_d7c3f3f50a6a_1779235267925517562`、`proposal_artifact_id=art_fetcher_proposal_api_cand_d7c3f3f50a6a`。これは正式 API 採用、promoted DB write、実 Fetcher 実装、外部 API 利用規約の自動確認の代替ではない。
- 2026-05-19 19:41 UTC に Browser Trace API の current view validation を追加補強し、APICandidate status は `candidate`、APIValidation status は `validated` / `needs_review`、APIArtifact status は `generated` / `draft` / `pending_review` に限定した。`internal/domain/browsertrace` の保存前 validation と `pkg/rencrowclient.BrowserTraceAPIStatus` の E2E 証跡 validation の両方で未知 status を拒否する local test を追加した。これは unknown / promoted などの曖昧な status を正式 API 採用、fetcher proposal 承認、promoted DB write の証跡として受け取らないための境界確認である。
- 2026-05-20 01:01 UTC に Browser Trace API validation result の status / passed / issues 整合性を追加補強した。保存前 validation と `pkg/rencrowclient.BrowserTraceAPIStatus` は、`status=validated` なのに `passed=false` または issues あり、`status=needs_review` なのに `passed=true` または issues なしの証跡を拒否する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/browsertrace -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestBrowserTraceAPI' -count=1 -v` は pass。これは validation status 文字列だけで正式 API 採用、Fetcher 実装、promoted DB write を成功扱いしないための境界確認である。
- 2026-05-20 01:36 UTC に `pkg/rencrowclient.BrowserTraceAPIStatus` の数値証跡境界を補強し、candidate confidence 範囲外、schema `sample_count < 0`、schema confidence 範囲外を direct client success として返さない local client test を追加した。これは API discovery current view の malformed response 拒否であり、正式 API 採用、実 Fetcher 実装、外部 API 利用規約確認の代替ではない。
- 2026-05-20 02:05 UTC に Browser Trace API schema の `schema_json` を保存前 validation と `pkg/rencrowclient.BrowserTraceAPIStatus` current view validation の両方で JSON として検証するよう補強した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/browsertrace ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(BrowserTraceAPI|ValidateAPICandidateSchema|HandleBrowserTraceAPI)' -count=1 -v` は pass。これは壊れた schema 文字列を正式 API 採用、Fetcher proposal、実 Fetcher 実装の証跡として扱わないための境界確認である。
- 2026-05-20 03:45 UTC に `pkg/rencrowclient.BrowserTraceAPIStatus` の current view validation を補強し、trace run / API candidate / schema / validation / coverage / artifact の `created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestBrowserTraceAPI' -count=1 -v` は pass。これは timestamp 欠落の ledger item を Browser Trace discovery / validation review / fetcher proposal 証跡として扱わないための境界確認であり、正式 API 採用、実 Fetcher 実装、外部 API 利用規約確認の代替ではない。
- 2026-05-20 06:40 UTC に Browser Trace API の保存前 domain validation を補強し、trace run / API candidate / schema / validation / coverage / artifact の `created_at` 欠落を拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/browsertrace -run 'TestValidateBrowserTraceAPIRejectsMissingCreatedAt|TestValidateAPICandidate|TestValidateAPIArtifact' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/domain/browsertrace ./internal/infrastructure/persistence/browsertrace ./internal/adapter/viewer ./pkg/rencrowclient -run 'Test(BrowserTraceAPI|ValidateAPI|SourceCandidate|SQLiteStore|JSONLStore|HandleBrowserTraceAPI)' -count=1` は pass。これは timestamp 欠落の ledger item を保存時点で拒否し、後続 current view を malformed にしないための local domain 境界確認であり、正式 API 採用、実 Fetcher 実装、外部 API 利用規約確認の代替ではない。
- 2026-05-20 05:35 UTC に `pkg/rencrowclient.CreateBrowserTraceAPIFetcherProposal` の direct response validation を補強し、Workstream Artifact を返す fetcher proposal response は `workstream_artifact.created_at` 欠落を local client error として拒否する test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestBrowserTraceAPIStatusDiscoverAndFetcherProposal|TestBrowserTraceAPIDiscoverAndFetcherProposalRejectInvalidOrMalformed' -count=1 -v` は pass。これは timestamp 欠落の Workstream Artifact を Browser Trace fetcher proposal 証跡として扱わないための境界確認であり、正式 API 採用、実 Fetcher 実装、外部 API 利用規約確認の代替ではない。
- 2026-05-20 10:30 UTC に Viewer Ops の `/viewer/browser-trace-api` fetch failure 境界を補強し、non-2xx 時は stale trace candidate / fetcher proposal 件数や空台帳を使わず `browser trace api status unavailable: ...` / `blocked: official API adoption state unreadable` / `blocked: fetcher implementation state unreadable` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは status API failure を正式 API 採用、promoted DB write、実 Fetcher 実装証跡として扱わないための local 表示境界である。
- 2026-05-19 17:21 UTC に `TestE2E_KnowledgeMemoryAndBrowserTraceStatusClientCurrentView` を追加し、live service の `/viewer/knowledge-memory` と `/viewer/browser-trace-api` が空台帳の current view として `pkg/rencrowclient` validation を通ることを確認した。これは status route 到達確認であり、Knowledge Memory の create / review / promote、Browser Trace の実 trace discover / fetcher proposal / 正式 API 採用の代替ではない。
- 2026-05-19 18:57 UTC に現 binary へ再 install / restart 後、live `/viewer/knowledge-memory?limit=1` と `/viewer/browser-trace-api?limit=1` が空台帳を返すこと、配信中 `ops.js` が `blocked: empty ledger` / `blocked: no trace candidates` を含むこと、`TestE2E_Phase25BrowserViewerSessionContract` が live Viewer Ops card の空台帳 blocked 表示を visible state として確認することを確認した。これは route 200 / items=0 を Knowledge Memory 作成・review・promote や Browser Trace discovery 成立として誤認しないための表示境界であり、実 trace discover / fetcher proposal / 正式 API 採用の代替ではない。
- 2026-05-19 22:50 UTC に Viewer Ops の Knowledge Memory / Browser Trace API card へ `blocked: no memory promote verified` / `blocked: no official API adoption` を追加した。route present や空台帳を正式 memory promote や公式 API 採用成功と誤認しないための表示境界である。
- 2026-05-19 23:48 UTC に live create/review/promote 後の Viewer Ops card 契約を `blocked: empty ledger` から `daily intake:` / `news:` / `review-only: promote not verified` へ更新した。Knowledge Memory 台帳上の current view は非空になったが、Conversation L1 disabled の通常 config では正式 memory / Source Registry 自動同期成功とは扱わない。
- 2026-05-20 10:40 UTC に Knowledge Memory status fetch failure 時は stale promoted news / enabled daily intake rule を使わず `knowledge memory status unavailable: ...` を visible state として表示する。これは status API failure の見せ方であり、正式 memory promote / Source Registry 自動同期成功ではない。
- 2026-05-20 12:05 UTC に Memory tab の Knowledge Memory ledger fetch failure 時は stale Personal Archive / Creative / News / Daily Intake / Temporal Marker / Dream Run row を使わず `Knowledge memory ledger unavailable: ...` を visible state として表示する。これは ledger route failure の見せ方であり、正式 memory promote / Source Registry 自動同期 / Dream consolidation 成功ではない。
- 2026-05-20 00:02 UTC に live Browser Trace discover/validation/proposal 後の Viewer Ops card 契約を `blocked: no trace candidates` から `fetcher proposals:` / `review-only: no official API adoption` へ更新した。live browser `TestE2E_Phase25BrowserViewerSessionContract` を再実行して pass。Browser Trace current view は非空になったが、正式 API 採用、promoted DB write、実 Fetcher 実装とは扱わない。
- 2026-05-19 20:38 UTC に `/viewer/runtime-config` の `runtime_readiness` へ `knowledge_memory_enabled` / `knowledge_memory_status_available` / `browser_trace_api_enabled` / `browser_trace_api_status_available` / `browser_trace_api_fetcher_available` を追加した。live config ではすべて true で、`TestE2E_Phase25LiveViewerRuntimeConfigClient` と `TestE2E_Phase25BrowserViewerSessionContract` は pass。Ops readiness は `Knowledge Memory` / `/viewer/knowledge-memory`、`Browser Trace API` / `fetcher:present` / `review-only: discover and fetcher proposal require evidence` を visible state として表示する。これは route availability の可視化であり、Knowledge Memory 作成・review・promote、Browser Trace discover / fetcher proposal / 正式 API 採用の成功扱いにはしない。
- 2026-05-19 17:26 UTC に runtime を更新し、L1 store 無効時の live `/viewer/source-registry` は 404 ではなく 503 `source registry unavailable` を返すことを確認した。Source Registry staging review / validate / promote の live E2E は blocked として維持する。503 は依存不足の明示であり、route 成功扱いにしない。
- 2026-05-19 17:31 UTC に `/viewer/runtime-config` の `runtime_readiness` へ `conversation_enabled` / `l1_sqlite_config_present` / `source_registry_available` を追加し、live config ではすべて false であることを `TestE2E_Phase25LiveViewerRuntimeConfigClient` で確認した。さらに `PICOCLAW_BROWSER_E2E=1` の `TestE2E_Phase25BrowserViewerSessionContract` で Ops readiness に Source Registry blocked 表示が visible state として出ることを確認した。これは blocked 理由の可視化であり、Source Registry staging review / validate / promote 成功の代替ではない。
- 2026-05-19 20:18 UTC に L1 store 無効時も `/viewer/memory/layers` を route 未登録 404 にせず 503 `memory layers unavailable` として返すよう runtime wiring を更新した。local wiring test と live `TestE2E_Phase25LiveMemoryLayersUnavailableWhenL1Disabled` は pass。これは Memory Layers の L0/L1/L2/L3 横断表示成功ではなく、L1 store 無効を blocked route として明示する境界確認である。
- 2026-05-19 20:45 UTC に `/viewer/runtime-config` の `runtime_readiness` へ `memory_layers_available` / `memory_layers_status_available` / `source_registry_status_available` を追加した。live config では `memory_layers_available=false` / `memory_layers_status_available=true` / `source_registry_available=false` / `source_registry_status_available=true` で、`/viewer/memory/layers` は 503 `memory layers unavailable`、`/viewer/source-registry` は 503 `source registry unavailable` を返す。`TestE2E_Phase25LiveViewerRuntimeConfigClient`、`TestE2E_Phase25LiveMemoryLayersUnavailableWhenL1Disabled`、`TestE2E_Phase25BrowserViewerSessionContract` は pass。これは blocked route の可視化であり、Memory Layers 横断表示や Source Registry staging review / validate / promote 成功扱いにはしない。
- 2026-05-19 22:10 UTC に Viewer Ops へ `Runtime Blocked Route Audits` を追加し、live `/viewer/source-registry?action=staging&limit=3` と `/viewer/memory/layers` の 503 body を画面上で追えるようにした。browser E2E では `runtime blocked route audits:` / `source registry unavailable` / `memory layers unavailable` / `blocked: Source Registry staging and Memory Layers require Conversation L1 runtime` を visible state として確認した。これは route 登録や readiness 表示を Source Registry staging review / Memory Layers 横断表示の成功と誤認しないための表示境界である。
- 2026-05-19 22:13 UTC に `Runtime Blocked Route Audits` へ live `/viewer/sandbox?limit=1` も追加し、通常 config の `503 sandbox store unavailable` を同じ blocked route 表に出すようにした。browser E2E では `sandbox store unavailable` と `blocked: Source Registry staging, Memory Layers, and Sandbox require their runtime stores` を確認した。これは Sandbox route present を正式 apply / rollback 成功と誤認しないための表示境界である。
- 2026-05-19 22:19 UTC に LLM Ops proxy の read-only route timeout を短縮し、`/viewer/llm-ops/status` が現 live の upstream 不達時も約 3 秒で `502 upstream unreachable` body を返すようにした。start / stop / restart は model startup 待ちのため長い timeout を維持する。`Runtime Blocked Route Audits` へ LLM Ops status も追加し、browser E2E では `upstream unreachable` と `blocked: Source Registry staging, Memory Layers, Sandbox, and LLM Ops require their runtime dependencies` を確認した。これは LLM Ops 管理 API 不達を画面上の blocked 証跡にする改善であり、model start / stop / restart 成功や Chat / Worker endpoint 復旧ではない。
- 2026-05-19 22:25 UTC に Heavy Worker runtime diagnostics の LLM Ops status probe も同じ read-only timeout を使うようにし、live `/viewer/ai-workflow/heavy-worker/runtime-diagnostics` が約 3 秒で `llm_ops.live_available=false` と `llm-ops GET /v1/status ... context deadline exceeded` を返すことを確認した。Viewer Ops の `Heavy Runtime` card も `route: ANALYZE` と同 error を visible state として browser E2E で確認した。これは Heavy runtime config と LLM Ops 不達の表示境界であり、Heavy 実 provider 成功ではない。
- 2026-05-20 10:55 UTC に Viewer Ops の `/viewer/ai-workflow/heavy-worker/runtime-diagnostics` fetch failure 境界を補強し、status API が non-2xx の場合は stale Heavy provider / LLM Ops live state を使わず `heavy runtime diagnostics unavailable: ...` / `blocked: RouteANALYZE provider state unreadable` / `blocked: LLM Ops live state unreadable` を表示する Node contract test を追加した。これは runtime diagnostics API failure を Heavy 実 provider成功、RouteANALYZE 実運用成功、LLM Ops 管理 API 到達成功として誤読しないための visible-state 境界確認であり、Heavy 実 provider E2E や scheduler 正常完了 E2E の代替ではない。
- 2026-05-20 12:15 UTC に Viewer Ops の `refreshOpsData` を補強し、`/viewer/logs?scope=persisted` が non-2xx の場合は stale latest job / Mio last report / last error / operator feed を使わず `ops logs unavailable: ...` / `Ops logs unavailable: ...` を表示するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは persisted logs route failure を最新ジョブ、Mio報告、失敗イベント、operator feed 成功として扱わないための local 表示境界であり、live session E2E や runtime health 成功の代替ではない。
- 2026-05-20 12:25 UTC に Viewer monitor の `refreshViewerStatus` を補強し、`/viewer/status` が non-2xx の場合は stale agent running / idle / job state を使わず各 agent を `unavailable`、reason を `viewer status unavailable: ...` として表示更新するようにした。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは monitor snapshot route failure を agent 稼働成功や live session 成功として扱わないための local 表示境界であり、live session E2E や runtime health 成功の代替ではない。
- 2026-05-20 13:55 UTC に Viewer Chat の `/viewer/send` failure 境界を補強し、`/viewer/send` が non-2xx の場合は generic `send failed` ではなく body 付き `Viewer send unavailable: HTTP <status>: ...` を通常 chat timeline に表示する Node contract test を追加した。これは send route failure を通常 chat 応答成功や live session 成功として扱わないための local 表示境界であり、live Viewer send E2E の代替ではない。
- 2026-05-20 14:15 UTC に Viewer IdleChat control failure 境界を補強し、start / stop / mode control API が non-2xx の場合は console-only generic `idlechat control failed` ではなく body 付き `IdleChat control unavailable: HTTP <status>: ...` を IdleChat table に表示する Node contract test を追加した。これは control route failure を IdleChat 開始 / 停止 / mode 切替成功や live IdleChat session 成功として扱わないための local 表示境界であり、live IdleChat E2E の代替ではない。
- 2026-05-20 14:25 UTC に Viewer IdleChat status / logs fetch failure 境界を補強し、`/viewer/idlechat/status` と `/viewer/idlechat/logs?limit=20` が non-2xx の場合は stale summary row や空表示を使わず、body 付き `IdleChat status unavailable: HTTP <status>: ...` / `IdleChat logs unavailable: HTTP <status>: ...` を IdleChat table に表示する Node contract test を追加した。これは status / logs route failure を IdleChat 稼働状態、履歴取得、live IdleChat session 成功として扱わないための local 表示境界であり、live IdleChat E2E の代替ではない。
- 2026-05-20 14:35 UTC に Viewer live mode topic fetch failure 境界を補強し、`mode=live` の topic polling で `/viewer/idlechat/status` が non-2xx の場合は topic text を空や stale のままにせず、body 付き `IdleChat status unavailable: HTTP <status>: ...` を `#liveTopicText` に表示する Node contract test を追加した。これは live topic bar の status route failure を live IdleChat topic 更新成功や live session 成功として扱わないための local 表示境界であり、live IdleChat E2E の代替ではない。
- 2026-05-20 12:35 UTC に Viewer Ops の Knowledge Memory detail fetch failure 境界を補強し、`/viewer/knowledge-memory?detail_type=...&id=...` が non-2xx の場合は generic error ではなく body 付き `HTTP <status>: ...` を Knowledge Detail Result に表示する Node contract test を追加した。これは detail route failure を promoted/enabled item の詳細取得成功や正式 memory / Source Registry 自動同期成功として扱わないための local 表示境界であり、正式 memory / Source Registry 自動同期 E2E の代替ではない。
- 2026-05-20 12:55 UTC に Memory tab の Knowledge Memory detail fetch failure 境界を補強し、`fetchMemoryKnowledgeDetail` が `/viewer/knowledge-memory?detail_type=...&id=...` の non-2xx body を `HTTP <status>: ...` として Knowledge Memory Detail に表示し、stale detail を残さない Node contract test を追加した。これは Memory tab の detail route failure を台帳詳細取得成功、review/promote 成功、正式 memory / Source Registry 自動同期成功として扱わないための local 表示境界であり、正式 memory / Source Registry 自動同期 E2E の代替ではない。
- 2026-05-19 20:51 UTC に Viewer Ops readiness へ `/health` の Runtime Health card を追加した。live `/health` は 20:51 UTC に 503 `local_llm_chat` / `local_llm_worker` connection refused、20:54 UTC の再起動後は 5 秒 timeout で、browser E2E は `service:missing` / `chat:missing` / `worker:missing` / `blocked:` を visible state として確認して pass。これは health failure を画面上に残すための証跡であり、local LLM endpoint 復旧や `TestE2E_Phase25LiveRuntimeHealth` pass の代替ではない。
- 2026-05-19 21:44 UTC に `/health` / `/ready` handler の request timeout を短時間化し、local LLM endpoint 不達時も 5 秒 client timeout で証跡を失わず down JSON を返すようにした。現 binary へ `make install` / clean restart 後、`time curl -sS --max-time 5 http://127.0.0.1:18790/health` は約 1.04 秒で `status=down`、`local_llm_chat` / `local_llm_worker` の `connection refused`、各 `duration_ms` 約 1030 を返した。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` と live browser `TestE2E_Phase25BrowserViewerSessionContract` は pass。これは health failure の証跡を返す改善であり、health 成功や local LLM endpoint 復旧ではない。
- 2026-05-19 20:23 UTC に Sandbox disabled 時も `/viewer/sandbox` status route を route 未登録 404 にせず 503 `sandbox store unavailable` として返すよう runtime wiring を更新した。live `TestE2E_Phase25LiveSandboxStatusUnavailableWhenSandboxDisabled` は pass。これは Sandbox disabled の blocked 証跡であり、Human approval あり正式 apply / rollback の代替ではない。
- 2026-05-19 20:30 UTC に `/viewer/runtime-config` の `runtime_readiness` へ `sandbox_enabled` / `sandbox_status_available` を追加した。live config では `sandbox_enabled=false` / `sandbox_status_available=true` で、`TestE2E_Phase25LiveViewerRuntimeConfigClient` と `TestE2E_Phase25BrowserViewerSessionContract` は pass。Ops readiness は `Sandbox` / `enabled:missing` / `status:present` / `blocked: sandbox disabled` を visible state として表示する。これは blocked 状態の可視化であり、正式 apply / rollback 成功扱いにはしない。
- `/health` は 503。`local_llm_chat` / `local_llm_worker` が `http://192.168.1.13:8081` / `:8082` へ `no route to host` で失敗した。
- `TestE2E_Phase25LiveRuntimeHealth` は `/health status=503, want 200` で fail として記録した。`TestE2E_Phase25LiveViewerRuntimeConfigClient` は health と分離し、`pkg/rencrowclient.RuntimeConfig` で runtime-config readiness fields を検証する。
- 直接 shell では外部 API / Google Search の env は未設定だったが、`~/.picoclaw/.env` 読み込み後に Claude / DeepSeek / OpenAI / Google Search env は設定されていることを確認した。
- `~/.picoclaw/.env` 読み込み後も Slack / Discord / Telegram の実 token は未設定だった。
- Ollama endpoint `http://100.83.207.6:11434` は `/api/tags` と `Chat` generate が通った。
- `TestE2E_OllamaProvider_Generate`、`TestE2E_OllamaProvider_Japanese`、`TestE2E_OllamaProvider_Timeout`、`TestE2E_MioAgent_Chat_RealOllama`、`TestE2E_Routing_Chat_NaturalLanguage` は `PICOCLAW_CONFIG=$HOME/.picoclaw/config.yaml` 指定で pass した。
- `~/.picoclaw/.env` 読み込み後、Claude / DeepSeek provider E2E と Google Search Chat / Worker E2E は pass した。
- OpenAI provider E2E は `429 insufficient_quota` で fail として記録した。
- 2026-05-19 に現行 worktree で `GOCACHE=/tmp/picoclaw-gocache go test ./...`、`node internal/adapter/viewer/viewer_memory_panel.test.mjs`、`git diff --check` を再実行し、全て pass した。これは local regression の確認であり、実 API E2E、実ブラウザ音声、正式 promotion apply、実外部送信、実 PR 作成の完了扱いにはしない。
- 2026-05-19 15:30 UTC に secret 値を記録せず runtime dependency readiness を再確認した。Slack / Discord / Telegram の実 API token / webhook secret は missing のため file payload 実 API E2E は blocked。Google Search Chat / Worker は `GOOGLE_API_KEY_CHAT` / `GOOGLE_SEARCH_ENGINE_ID_CHAT` / `GOOGLE_API_KEY_WORKER` / `GOOGLE_SEARCH_ENGINE_ID_WORKER` が present で、`.env` 読み込み込みの `TestE2E_GoogleSearch_Chat` / `TestE2E_GoogleSearch_Worker` は pass。
- 2026-05-19 15:30 UTC に `STT_GATEWAY_URL` は present だが、runtime-config の `stt_base_url=http://192.168.1.33:8766` は `/health` と `/` が 5 秒 timeout であることを確認した。browser microphone STT は実ブラウザ / 実 mic 未実施のため blocked。
- 2026-05-19 15:30 UTC に `TTS_PROVIDER_URL` / `TTS_PROVIDER` / `IRODORI_BASE_URL` / `SBV2_BASE_URL` は missing であることを確認した。TTS playback / lip sync は provider env と実ブラウザ audio event 未準備のため blocked。
- 2026-05-19 15:30 UTC の `/health` は、local OpenAI-compatible Chat / Worker endpoint `http://192.168.1.13:8081` / `:8082` が `no route to host` のため `status=down`。SuperAgent / AI Workflow scheduler 正常完了 E2E の blocked 理由として維持する。
- 2026-05-19 15:38 UTC に現行 worktree で `GOCACHE=/tmp/picoclaw-gocache go test ./...`、`node internal/adapter/viewer/viewer_memory_panel.test.mjs`、`git diff --check` を再実行し、全て pass した。これはこの手順書に沿った実装差分の local regression / diff hygiene 確認であり、実 API E2E、実ブラウザ音声、正式 promotion apply、実外部送信、実 PR 作成の完了扱いにはしない。
- 2026-05-19 15:39 UTC に live runtime を再確認した。`/health` は `local_llm_chat` / `local_llm_worker` が `http://192.168.1.13:8081` / `:8082` へ `no route to host` のため 503、`/viewer/runtime-config` は同 endpoint を返し、`/viewer/superagent?limit=5` は 200 だが `runtime_config.run_queue_scheduler_enabled=false`、`/viewer/llm-ops/status` は `upstream unreachable`。このため SuperAgent / AI Workflow の scheduler 正常完了 E2E は引き続き blocked とする。
- 2026-05-19 15:55 UTC に live runtime と runtime dependency readiness を再確認した。`/health` は同 endpoint への `no route to host` で 503、`/viewer/runtime-config` は同 endpoint と `stt_base_url=http://192.168.1.33:8766` を返し、`/viewer/superagent?limit=5` は 200 だが `runtime_config.run_queue_scheduler_enabled=false`、`/viewer/llm-ops/status` は `upstream unreachable`。Slack / Discord / Telegram の実 token / webhook secret は missing、TTS provider env も missing。STT は `STT_GATEWAY_URL` present だが `http://192.168.1.33:8766/health` が `no route to host`。SuperAgent / AI Workflow scheduler 正常完了、file payload 実 API、browser microphone STT、TTS playback、lip sync は引き続き blocked とし、health fail / upstream unreachable / scheduler disabled / STT bridge 不達を成功扱いしない。
- 2026-05-19 に `/viewer/runtime-config` へ `tts_base_url` / `tts_health_path` と secret 値なしの `runtime_readiness` を追加した。Slack / Discord / Telegram credential pair、STT gateway env、TTS provider env の present / missing を boolean で確認できる。これは blocked / skipped 判定の証跡補助であり、実 API event、実ブラウザ mic、TTS playback、lip sync の成功扱いにはしない。
- 2026-05-19 に `pkg/rencrowclient.RuntimeConfig` を追加した。`/viewer/runtime-config` が 2xx を返しても、LLM Ops enabled / configured / base URL、local LLM provider / Chat / Worker endpoint、STT / TTS URL、TTS health path、`runtime_readiness` fields が整合しない response は direct client success としない。Phase25 live E2E は `TestE2E_Phase25LiveRuntimeHealth` と `TestE2E_Phase25LiveViewerRuntimeConfigClient` に分離し、health fail が runtime-config readiness 証跡を隠さないようにした。これは runtime readiness を client E2E 証跡として読むための境界確認であり、実 API event や実ブラウザ音声 E2E の代替ではない。
- 2026-05-19 19:02 UTC に現 binary へ再 install / restart 後、`/viewer/llm-ops/status` が 502 `upstream unreachable` を返すこと、Viewer Ops readiness が LLM Ops `configured:present` / `proxy:present` / `live:missing` / `blocked: HTTP 502: upstream unreachable` を visible state として表示することを `TestE2E_Phase25BrowserViewerSessionContract` で確認した。これは LLM Ops proxy 設定済みを管理 API 到達成功と誤認しないための表示境界であり、Chat / Worker local LLM endpoint 復旧や scheduler 正常完了 E2E の代替ではない。
- 2026-05-19 20:51 UTC に Runtime Health card を追加し、live `/health` 503 または timeout を Viewer Ops で `service:missing` / `chat:missing` / `worker:missing` / `blocked:` として確認した。health fail と LLM Ops 管理 API 502 は別の blocked 証跡として扱い、どちらも成功扱いしない。
- 2026-05-19 20:09 UTC に `pkg/rencrowclient.LLMOpsHealth` / `LLMOpsStatus` を追加し、`/viewer/llm-ops/health` / `status` が 2xx を返しても health status、`roles`、role 名、`health_ok`、halted / pid 整合性が不正な response を成功扱いしない local client test を追加した。live `TestE2E_Phase25LiveLLMOpsProxyClientBlockedOrLive` は現 runtime の health/status 502 `upstream unreachable` を `APIError` body として保持することを確認して pass。これは LLM Ops 管理 API 不達を blocked 証跡として読むための境界確認であり、Chat / Worker endpoint 復旧や scheduler 正常完了 E2E の代替ではない。
- 2026-05-19 20:13 UTC に `pkg/rencrowclient.StopLLMOps` / `StartLLMOps` / `RestartLLMOps` を追加し、control request の role / selection を送信前に検証し、502 `upstream unreachable` を `APIError` body として保持する local client test を追加した。live `TestE2E_Phase25LiveLLMOpsProxyClientBlockedOrLive` は health/status に加えて stop/start/restart も現 runtime の 502 blocked 証跡として確認して pass。これは LLM Ops control API 不達を操作成功と誤認しないための境界確認であり、実 model stop/start/restart 成功の代替ではない。
- 2026-05-20 01:55 UTC に `pkg/rencrowclient.LLMOpsStatus` の local validation を追加補強し、memory `llm_by_role` だけに role が存在して対応する `roles` state がない response、memory role 名不一致、port / rss_mib / pid 負数を direct client success として返さない test を追加した。`GOCACHE=/tmp/picoclaw-gocache go test ./pkg/rencrowclient -run 'TestLLMOps(Status|Health|Control)' -count=1 -v` は pass。これは process/memory 情報だけを LLM Ops 管理 API 到達や model control 成功の証跡として扱わないための境界確認であり、Chat / Worker endpoint 復旧や scheduler 正常完了 E2E の代替ではない。
- 2026-05-19 16:41 UTC に現 worktree を `make install` し、停止ルールに沿って `picoclaw.service` を再起動した。再起動後の `/viewer/runtime-config` は `runtime_readiness` を返し、`TestE2E_Phase25LiveViewerRuntimeConfigClient` は live service に対して pass。`/health` と `TestE2E_Phase25LiveRuntimeHealth` は local LLM endpoint `no route to host` のため 503 / fail のまま。runtime-config readiness pass は health 成功、実 API event、実ブラウザ mic、TTS playback、lip sync の成功扱いにはしない。
- 2026-05-19 16:46 UTC に `runtime_readiness` へ `stt_gateway_config_present` / `tts_provider_config_present` を追加した。現 worktree を再度 `make install` して live binary を更新し、`/viewer/runtime-config` が `stt_gateway_env_present=true` / `stt_gateway_config_present=true` / `tts_provider_env_present=false` / `tts_provider_config_present=true` を返すこと、`TestE2E_Phase25LiveViewerRuntimeConfigClient` が pass することを確認した。effective config が存在しても実 provider health / 実 browser playback / lip sync は未確認のため blocked のまま扱う。
- 2026-05-19 に `pkg/rencrowclient.RuntimeConfig` が STT / TTS endpoint と config-present field の矛盾を malformed response として拒否する local client test を追加した。補強後も live `TestE2E_Phase25LiveViewerRuntimeConfigClient` は pass。これは runtime-config readiness を E2E 証跡として読む境界確認であり、実 provider health / 実 browser playback / lip sync の代替ではない。
- 2026-05-19 16:54 UTC に Viewer Ops へ `runtimeReadinessCards` を追加し、Slack / Discord / Telegram credential、STT env/config、TTS env/config の readiness を表示するようにした。`viewer_memory_panel.test.mjs` は readiness card 表示を確認して pass。live `/viewer` と配信中 `ops.js` に該当 element / renderer が含まれることも確認済み。これは runtime readiness の可視化であり、実 API event / 実 browser mic / TTS playback / lip sync の代替ではない。
- 2026-05-19 に `TestE2E_Phase25BrowserViewerSessionContract` へ Ops `runtimeReadinessCards` の visible state と `STT` / `TTS` / `Slack` / `config:present` / `credentials:missing` の text 確認を追加し、live Viewer に対して pass した。これは readiness 表示の実ブラウザ確認であり、実 mic STT / TTS playback / lip sync の代替ではない。
- 2026-05-19 17:54 UTC に `/viewer/debug/system` の audio health probe を並列化し、STT / TTS endpoint 不達でも route が長時間固まらず blocked 証跡を返すようにした。live service で `time curl -sS -m 4 http://127.0.0.1:18790/viewer/debug/system` は約 1.2 秒で返り、`stt_base_url=http://192.168.1.33:8766`、`tts_base_url=http://192.168.1.13:7870`、`stt_ok=false`、`tts_live_ok=false`、`tts_ready_ok=false`、timeout `last_error` を返した。
- 2026-05-19 に `pkg/rencrowclient.DebugSystemSnapshot` を追加し、`/viewer/debug/system` が 2xx を返しても `updated_at` 欠落、invalid audio URL、base URL なしの ok state を current runtime 証跡として受け取らない local client test を追加した。`TestE2E_Phase25LiveDebugSystemSnapshotClient` は live service に対して pass。これは STT / TTS blocked 状態の可視化と client 境界確認であり、実ブラウザ mic、TTS playback、lip sync の成功扱いにはしない。
- 2026-05-19 19:49 UTC に `pkg/rencrowclient.DebugSystemSnapshot` の validation を補強し、`updated_at` は RFC3339 必須、STT / TTS endpoint 設定済みで down の場合は health body または `last_error` を必須、`tts_ready_ok=true` なら `tts_live_ok=true` も必須にした。live `/viewer/debug/system` は `stt_ok=false` / `tts_live_ok=false` / `tts_ready_ok=false` と、TTS connection refused / STT timeout の `last_error` を返し、`TestE2E_Phase25LiveDebugSystemSnapshotClient` は再 pass。これは down 状態を空の成功証跡として受け取らないための境界確認であり、実 browser mic / audio playback 成功ではない。
- 2026-05-20 13:35 UTC に Viewer Debug panel の `/viewer/debug/system` fetch failure 境界を補強し、non-2xx の場合は generic `fetch failed` ではなく body 付き `HTTP <status>: ...` を GPU / Audio summary に表示する Node contract test を追加した。これは debug system route failure を STT / TTS readiness、GPU readiness、実 mic STT、browser audio playback / lip sync 成功として扱わないための local 表示境界であり、live browser audio / STT E2E の代替ではない。
- 2026-05-20 14:05 UTC に Viewer STT artifact persistence の `/viewer/stt/log`、`/viewer/stt/wav`、`/viewer/stt/autotest` failure 境界を補強し、non-2xx の場合は generic `stt ... failed` ではなく body 付き `HTTP <status>: ...` を error として保持する Node contract test を追加した。これは STT log / WAV / autotest 保存失敗を実 mic STT 成功や STT provider readiness 成功として扱わないための local 保存境界であり、live browser audio / STT E2E の代替ではない。
- 2026-05-19 20:01 UTC に `pkg/rencrowclient.RuntimeHealth` を追加し、`/health` が 503 を返す場合も body の `status=down` / `checks` / `message` / `timestamp` を decode して検証するようにした。HTTP status と body status の矛盾、down/degraded check の message 欠落、overall status と checks の矛盾は local client test で拒否する。`TestE2E_Phase25LiveRuntimeHealth` は同 client 経由に更新し、live では `local_llm_chat` / `local_llm_worker` の `connection refused` を含む down checks を出して fail することを確認した。これは health fail を証跡化するための境界確認であり、health 成功や scheduler 正常完了 E2E の代替ではない。
- 2026-05-19 18:03 UTC に `/viewer/runtime-config` の `runtime_readiness` へ Slack / Discord / Telegram の webhook route 登録状態と file payload pipeline readiness を追加した。現 binary へ `make install` / restart 後、live では Slack / Discord / Telegram の credentials / webhook / file pipeline がすべて false。`TestE2E_Phase25LiveViewerRuntimeConfigClient` は pass したが、これは外部 channel file payload 実 API E2E 成功ではなく、実 token / webhook / event 未準備の blocked 証跡である。
- 2026-05-19 18:10 UTC に `TestE2E_Phase25BrowserViewerSessionContract` へ Slack / Discord / Telegram の visible readiness text を追加し、live Viewer で `credentials:missing` / `webhook:missing` / `file:missing` が表示されることを確認した。これは DOM 存在だけではなく visible state の確認だが、実 API event E2E の代替ではない。
- 2026-05-19 18:10 UTC に Viewer Ops の runtime readiness card が `/viewer/debug/system` の audio snapshot を表示するようにした。live `/viewer/debug/system` は `stt_ok=false` / `tts_live_ok=false` / `tts_ready_ok=false` と timeout `last_error` を返し、live browser E2E で `health:missing` / `live:missing` / `ready:missing` の visible state を確認した。これは STT/TTS blocked 表示の確認であり、実 mic STT、TTS playback、lip sync 成功の代替ではない。
- 2026-05-19 23:02 UTC に Viewer Ops の STT / TTS readiness card へ `blocked: real microphone STT E2E not verified` と `blocked: browser audio playback/lip sync E2E not verified` を明示した。Node Viewer contract と live browser E2E はこの文言を確認するが、実 mic / 実 audio device / lip sync の成功条件は未達のまま扱う。
- 2026-05-19 23:08 UTC に Viewer Ops の Slack / Discord / Telegram readiness card へ `blocked: real external API file event E2E not verified` を明示し、Distributed card へ `blocked: Wild SSH/multi-machine E2E not verified` を明示した。Node Viewer contract と live browser E2E はこの文言を確認するが、実 token / webhook / 外部 API event / file payload E2E と Wild SSH / 複数マシン E2E は未達のまま扱う。
- 2026-05-19 18:14 UTC に `/viewer/runtime-config` の `runtime_readiness` へ distributed transport readiness を追加した。live では `distributed_enabled=false`、`distributed_transports_present=true`、`distributed_ssh_configured=true`、`distributed_ssh_connected=false`、`distributed_local_transport=false`。`TestE2E_Phase25LiveViewerRuntimeConfigClient` と live browser E2E は pass し、Ops で `Distributed` / `enabled:missing` / `ssh-connected:missing` / `blocked: distributed disabled` が見えることを確認した。これは Wild SSH / 複数マシン transport E2E の blocked 理由の可視化であり、実機分散接続成功の代替ではない。

残り:

- local OpenAI-compatible Chat / Worker endpoint 復旧後の live service / Viewer 基本 E2E 再確認。
- 新仕様 API route 200 は確認済みだが、SuperAgent run queue scheduler、Revenue 外部送信、Complexity Sandbox Gate live の user flow 成立とは扱わない。各 flow は個別に実行する。
- OpenAI provider quota / billing 復旧後の再確認。
- Slack / Discord / Telegram file payload の実 key / 実 event E2E。
- browser microphone STT / TTS playback / lip sync の実ブラウザ・実デバイス E2E。STT は bridge timeout 復旧、TTS は provider env 準備も必要。
- Wild SSH / 複数マシン transport と分散全経路の追加 E2E。

2026-05-20 08:20 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer` を Playwright Chromium で開き、route fulfill により `/viewer/idlechat/status` / `/viewer/idlechat/logs?limit=20` の non-2xx を再現した。IdleChat table と `mode=live` の topic bar に body 付き failure が表示されることを確認したが、API failure 再現の visible-state 境界確認であり、live IdleChat 会話 E2E の代替ではない。スクリーンショットは `tmp/viewer-idlechat-failure-boundary.png` と `tmp/viewer-live-topic-failure-boundary.png`。

2026-05-20 08:23 UTC に Viewer Ops runtime config / debug system snapshot fetch failure 境界を補強した。`/viewer/runtime-config` と `/viewer/debug/system` が non-2xx の場合は stale local LLM config や単なる disabled / missing 表示にせず、body 付き `runtime config unavailable: HTTP <status>: ...` と `blocked: HTTP <status>: ...` を runtime readiness に表示する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは runtime dependency route failure を live runtime config 成功、STT / TTS readiness 成功、LLM Ops 到達成功として扱わないための visible-state 境界確認であり、実 browser audio / STT / TTS E2E の代替ではない。

2026-05-20 08:26 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=ops` を Playwright Chromium で開き、route fulfill により `/viewer/runtime-config` 503 と `/viewer/debug/system` 502 を再現した。Runtime Config / STT / TTS readiness に body 付き failure が表示されることを確認した。スクリーンショットは `tmp/viewer-runtime-config-failure-boundary.png` と `tmp/viewer-debug-system-failure-boundary.png`。これは配信中 Viewer JS の visible-state 境界確認であり、live runtime config 成功、実ブラウザ音声、STT / TTS / lip sync 成功の代替ではない。

2026-05-20 08:29 UTC に Viewer TTS audio unlock / playback failure 境界を補強した。browser autoplay block などで `Audio.play()` が reject した場合は console-only `tts audio ... failed` にせず、`#ttsNowPlaying` に `TTS audio unavailable: <error>` を表示し、audio button の blocked state に error detail を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは音声再生失敗を本文表示 fallback や口パク成功と混同しないための visible-state 境界確認であり、実 browser audio playback / lip sync E2E の代替ではない。

2026-05-20 08:31 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer` を Playwright Chromium で開き、`HTMLMediaElement.play()` reject を再現した。`#ttsNowPlayingText` に `TTS audio unavailable: NotAllowedError: browser blocked autoplay` が表示され、audio button title に error detail が残ることを確認した。スクリーンショットは `tmp/viewer-tts-audio-failure-boundary.png`。これは browser audio failure の visible-state 境界確認であり、TTS playback / lip sync 成功の代替ではない。

2026-05-20 08:33 UTC に Viewer STT capture log / session id copy failure 境界を補強した。Clipboard write が reject した場合は toast / console-only にせず、`debugSttSession` と session badge に `STT log copy unavailable: ...` / `STT session copy unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は pass。これは STT 証跡コピー失敗を保存済み・共有済み証跡や実 mic STT 成功として扱わないための visible-state 境界確認であり、実 browser microphone STT E2E の代替ではない。

2026-05-20 08:36 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=ops` を Playwright Chromium で開き、clipboard write reject を再現した。`debugSttSession` と session badge に `STT log copy unavailable: clipboard denied` が表示されることを確認した。スクリーンショットは `tmp/viewer-stt-copy-failure-boundary.png`。これは STT 証跡コピー失敗の visible-state 境界確認であり、実 browser microphone STT E2E の代替ではない。

2026-05-20 08:41 UTC に Viewer STT microphone start / artifact persistence failure 境界を補強した。`getUserMedia()` reject と STT log/WAV/autotest 保存 reject を toast / console-only にせず、`debugSttSession` と session badge に `STT microphone start unavailable: ...` / `STT artifact persistence unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 67件 pass。これは mic 権限拒否や保存失敗を実 mic STT 成功、保存済み証跡、STT provider readiness 成功として扱わないための visible-state 境界確認であり、実 browser microphone STT E2E の代替ではない。

2026-05-20 08:45 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=timeline` を Playwright Chromium で開き、`getUserMedia()` reject を再現した。session badge と `debugSttSession` に `STT microphone start unavailable: permission denied by test` が表示されることを確認した。スクリーンショットは `tmp/viewer-stt-microphone-start-failure-boundary.png`。これは browser microphone start failure の visible-state 境界確認であり、実 browser microphone STT E2E の代替ではない。

2026-05-20 08:48 UTC に Viewer STT WebSocket message / transport failure 境界を補強した。STT server message が `type=error`、JSON parse failure、WebSocket `onerror` の場合は toast / console-only にせず、`debugSttSession` と session badge に `STT recognition unavailable: ...` / `STT message parse unavailable: ...` / `STT websocket unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 68件 pass。これは STT server / stream failure を実 mic STT 成功や provider readiness 成功として扱わないための visible-state 境界確認であり、実 browser microphone STT E2E の代替ではない。

2026-05-20 08:50 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=timeline` を Playwright Chromium で開き、fake WebSocket から `type=error` を流した。session badge と `debugSttSession` に `STT recognition unavailable: provider timeout live` が表示されることを確認した。スクリーンショットは `tmp/viewer-stt-websocket-message-failure-boundary.png`。これは STT websocket server error の visible-state 境界確認であり、実 browser microphone STT E2E の代替ではない。

2026-05-20 08:54 UTC に Viewer Home send failure 境界を補強した。Home / Daily Desk の送信で `sendViewerMessage()` が reject した場合は toast / console-only にせず、`homeStatusCard` に `Home send unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 69件 pass。これは Home 入口の送信失敗を通常 chat 成功や fallback 成功として扱わないための visible-state 境界確認であり、live Viewer send E2E の代替ではない。

2026-05-20 08:56 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=home` を Playwright Chromium で開き、route fulfill により `/viewer/send` 502 を再現した。Home の `今日の状態` card に `Home send unavailable: HTTP 502: viewer send route unavailable live` が表示されることを確認した。スクリーンショットは `tmp/viewer-home-send-failure-boundary.png`。これは Home send route failure の visible-state 境界確認であり、live Viewer send 成功 E2E の代替ではない。

2026-05-20 09:01 UTC に Viewer evidence detail の JSON / summary copy failure 境界を補強した。clipboard write が reject した場合は `console.error` のみで終わらせず、copy button に `Evidence JSON copy unavailable: ...` / `Evidence summary copy unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 70件 pass。これは証跡コピー失敗を共有済み証跡や実行証跡取得成功として扱わないための visible-state 境界確認であり、live evidence copy E2E の代替ではない。

2026-05-20 09:05 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=jobs` を Playwright Chromium で開き、fake evidence detail と clipboard write reject を再現した。Copy JSON / Copy Summary button に `Evidence JSON copy unavailable: clipboard denied live` / `Evidence summary copy unavailable: clipboard denied live` が表示されることを確認した。スクリーンショットは `tmp/viewer-evidence-copy-failure-boundary.png`。これは evidence copy failure の visible-state 境界確認であり、実行証跡取得成功や共有成功の代替ではない。

2026-05-20 09:07 UTC に Viewer generic copy payload failure 境界を補強した。Reports / IdleChat / System などが共有する `copyTextPayload()` で clipboard write が reject した場合は toast / console-only にせず、押下した copy button に `Copy unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 71件 pass。これはコピー失敗を共有済み証跡や transcript 取得成功として扱わないための visible-state 境界確認であり、各画面の live copy E2E の代替ではない。

2026-05-20 09:09 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=reports` を Playwright Chromium で開き、clipboard write reject を再現した。Report Copy Summary button に `Copy unavailable: clipboard denied live` が表示され、button title にも同じ失敗理由が残ることを確認した。スクリーンショットは `tmp/viewer-generic-copy-failure-boundary.png`。これは generic copy failure の visible-state 境界確認であり、各画面の transcript / report 共有成功の代替ではない。

2026-05-20 09:12 UTC に Viewer unsupported attachment failure 境界を補強した。Viewer 添付で不許可 MIME / 拡張子の file を選んだ場合は toast-only にせず、attachment tray に `Attachment unavailable: unsupported file type: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 72件 pass。これは不許可添付を添付成功や通常送信成功として扱わないための visible-state 境界確認であり、live attachment upload E2E の代替ではない。

2026-05-20 09:14 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=timeline` を Playwright Chromium で開き、不許可添付 `payload.exe` を input に設定した。attachment tray に `Attachment unavailable: unsupported file type: payload.exe` が表示されることを確認した。スクリーンショットは `tmp/viewer-unsupported-attachment-failure-boundary.png`。これは unsupported attachment failure の visible-state 境界確認であり、live attachment upload 成功や通常送信成功の代替ではない。

2026-05-20 09:16 UTC に Viewer message copy failure 境界を補強した。通常 timeline / IdleChat message の `copyMsg()` で clipboard write が reject した場合は unhandled rejection や console-only にせず、押下した Copy button に `Copy unavailable: ...` を保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 73件 pass。これは message copy 失敗を transcript / message 共有成功として扱わないための visible-state 境界確認であり、live message copy E2E の代替ではない。

2026-05-20 09:18 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=timeline` を Playwright Chromium で開き、message Copy button の clipboard write reject を再現した。Copy button に `Copy unavailable: clipboard denied live` が表示され、button title にも同じ失敗理由が残ることを確認した。スクリーンショットは `tmp/viewer-message-copy-failure-boundary.png`。これは message copy failure の visible-state 境界確認であり、timeline / IdleChat message 共有成功の代替ではない。

2026-05-20 09:21 UTC に Viewer LLM Ops readiness failure 境界を補強した。alias route 送信前の `/viewer/llm-ops/health` / `status` / `stop` / `start` が non-2xx の場合、response body を `llm ops ... failed: HTTP <status>: ...` として保持する。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 74件 pass。これは LLM Ops readiness route failure を alias 送信成功や fallback 成功として扱わないための visible-state 境界確認であり、live LLM start / stop 成功 E2E の代替ではない。

2026-05-20 09:28 UTC に Viewer Timeline の local send failure 表示境界を補強した。`Viewer send unavailable: ...` の synthetic `agent.response` は TTS 同期話者 `mio` 由来でも Timeline に表示し、通常の TTS 同期応答は従来どおり本文表示側に重複させない。`node internal/adapter/viewer/viewer_memory_panel.test.mjs` は 75件 pass。これは LLM Ops readiness failure や send route failure を silent drop / fallback 成功として扱わないための visible-state 境界確認であり、live LLM start / stop 成功 E2E の代替ではない。

2026-05-20 09:31 UTC に現ソースを `make install` して `picoclaw.service` を再起動後、配信中 `/viewer?tab=timeline` を Playwright Chromium で開き、route fulfill により `/viewer/llm-ops/health` 503 を再現した。Timeline に `Viewer send unavailable: llm ops health failed: HTTP 503: llm ops health unavailable live` が表示されることを確認した。スクリーンショットは `tmp/viewer-llm-ops-readiness-failure-boundary.png`。これは LLM Ops readiness failure の visible-state 境界確認であり、live LLM start / stop 成功 E2E の代替ではない。

2026-05-19 15:45 UTC に Slack / Discord / Telegram file payload の adapter-level failure boundary を補強した。各 adapter の download failure test は、attachment download が失敗した場合に 502 で停止し、orchestrator を呼ばず、添付なし通常 chat 成功へ進まないことを検証する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/channels/slack ./internal/adapter/channels/discord ./internal/adapter/channels/telegram -count=1 -v` は pass。これは実 API event / token / webhook を使う E2E の代替ではなく、失敗を fallback 成功にしない local 境界確認として扱う。

2026-05-20 09:35 UTC に Slack / Discord / Telegram file payload の adapter-level failure boundary を追加補強した。各 adapter の download failure test は、attachment download が non-2xx の場合に webhook 502 response へ upstream body を保持し、orchestrator を呼ばず、添付なし通常 chat 成功へ進まないことも検証する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/channels/slack ./internal/adapter/channels/discord ./internal/adapter/channels/telegram -run 'TestAdapter_ServeHTTP_(File|Attachment|Document)DownloadFailureDoesNotFallbackToChat' -count=1 -v` は pass。これは実 API event / token / webhook を使う E2E の代替ではなく、download failure 証跡を status だけで失わないための local 境界確認として扱う。

2026-05-20 09:40 UTC に Slack / Discord / Telegram adapter の Send / Probe failure boundary を追加補強した。postMessage / sendMessage / auth.test / getMe / users@me が non-2xx の場合、error は response body を保持する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/channels/slack ./internal/adapter/channels/discord ./internal/adapter/channels/telegram -run TestAdapter_SendAndProbeFailuresIncludeResponseBody -count=1 -v` は pass。これは実 API event / token / webhook を使う E2E や外部送信成功の代替ではなく、送信 / probe failure 証跡を status だけで失わないための local 境界確認として扱う。

2026-05-20 09:45 UTC に LINE media download failure boundary を追加補強した。`MediaDownloader.DownloadContent` は LINE content API が non-2xx の場合、error に response body を保持する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/line -run TestMediaDownloader_DownloadContent_APIErrorIncludesResponseBody -count=1 -v` は pass。これは実 LINE media event E2E や外部チャネル file payload E2E の代替ではなく、LINE media download failure 証跡を status だけで失わないための local 境界確認として扱う。

2026-05-20 09:50 UTC に Telegram `getFile` failure boundary を追加補強した。file download 前段の `getFile` が non-2xx の場合、webhook 502 response に upstream body を保持し、orchestrator へ fallback しない。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/channels/telegram -run TestAdapter_ServeHTTP_GetFileFailureDoesNotFallbackToChat -count=1 -v` は pass。これは実 Telegram API event E2E の代替ではなく、Telegram file metadata failure 証跡を status だけで失わないための local 境界確認として扱う。

2026-05-20 09:50 UTC に audio router HTTP/SSE failure boundary を追加補強した。`HTTPDownloader.Download` と `SSEClient` は audio download / event stream 接続の non-2xx response body を error に保持する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/infrastructure/audiorouter -run 'Test(HTTPDownloaderDownload_Non2xxIncludesResponseBody|SSEClientRun_NonOKIncludesResponseBody)' -count=1 -v` は pass。これは実 TTS playback / lip sync E2E の代替ではなく、audio object expiration / stream unavailable の証跡を status だけで失わないための local 境界確認として扱う。

2026-05-20 09:56 UTC に Source Registry sweeper の HTTP fetch failure boundary を追加補強した。`sweepHTTPSource` は non-2xx response body を error に保持し、`MarkSourceRegistryFetched` の error 証跡へ失敗理由を残せる。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/sourcefetcher -run TestRunSourceHTTPFailureIncludesResponseBody -count=1 -v` は pass。これは live Source Registry fetch / staging / promote E2E の代替ではなく、外部 source fetch failure 証跡を status だけで失わないための local 境界確認として扱う。

2026-05-20 10:01 UTC に IdleChat external topic / trend source HTTP failure boundary を追加補強した。NHK RSS / Google News RSS の non-2xx response body を error に保持する local test を追加し、Wikipedia / Google Trends / Reddit / Hatena の status error も body 付き整形へ揃えた。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/idlechat -run 'TestFetch(NewsHeadlinesFrom|GoogleNewsRSS)NonOKIncludesResponseBody' -count=1 -v` と `GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/idlechat -count=1 -v` は pass。これは live IdleChat 会話 E2E や外部 RSS / API 到達成功の代替ではなく、外部 topic seed / trend fetch failure 証跡を status だけで失わないための local 境界確認として扱う。

2026-05-19 15:47 UTC に MIME 不許可 / size 超過の local 境界も補強した。common attachment store は unsupported MIME、max file size 超過、max total size 超過を error にし、Slack / Discord / Telegram adapter は `AttachmentSaver.SaveAll` rejection を 502 で停止し、orchestrator を呼ばず、通常 chat 成功へ進まないことを検証する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/attachment ./internal/adapter/channels/slack ./internal/adapter/channels/discord ./internal/adapter/channels/telegram -count=1 -v` は pass。これは実 API event E2E の代替ではなく、fallback 成功にしない local 境界確認として扱う。

2026-05-19 15:49 UTC に prompt injection warning metadata の保持も補強した。Slack / Discord / Telegram adapter の successful attachment path は real `internal/application/attachment.Store` を通し、prompt injection text を含む attachment の `security_warnings` metadata が orchestrator request の `Attachments` に残ることを検証する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/adapter/channels/slack ./internal/adapter/channels/discord ./internal/adapter/channels/telegram -count=1 -v` は pass。これは実 API event E2E の代替ではなく、warning を本文や fallback 表示で隠さない local 境界確認として扱う。

2026-05-19 15:43 UTC に local distributed `RouteWILD` の fallback 境界確認を強化した。`TestDistributedOrchestrator_ProcessMessage_WildRouteUsesWildAgentWithoutFallback` は、`agent.response wild -> mio` event が存在するだけでなく、event の `job_id` / `session_id` が `ProcessMessageResponse` と同一フローとして一致することも検証する。`GOCACHE=/tmp/picoclaw-gocache go test ./internal/application/orchestrator -run TestDistributedOrchestrator_ProcessMessage_WildRoute -count=1 -v` は pass。これは SSH / 複数マシン transport E2E の代替ではなく、local distributed 証跡境界の補強として扱う。

## 9. 保留 / 対象外

以下は、ユーザーが明示的に本線化するまで作業しない。

| 項目 | 扱い | 理由 |
|---|---|---|
| LangGraph Turn Controller / character subgraph | 対象外 | 現行は Go `MessageOrchestrator` 本線 |
| FAISS sidecar | 対象外 | 現行は Qdrant 想定 VectorDB と L1 SQLite / DuckDB |
| 旧ルミナ / クラリス / ノクス体系、旧 mention | 対象外 | 現行は Mio / Shiro / AO / Aka / Kin / Gin |
| IdleChat 6人対応 | 将来実装 | 現時点では本線外 |
| GitHub Actions / CI 自動化 | 対象外 | ユーザー承認なしに追加しない |
| Docker sandbox | 対象外 | 現行 MVP は worktree / sandbox root / Path Guard |
| 無承認の外部投稿 / 送信 / 販売 | 対象外 | Human approval 必須 |

2026-05-19 15:34 UTC に `docs/01_正本仕様/13_仕様統合判断_20260505.md` と現行実装を再確認した。

- 6人格の完全なキャラ会話 runtime は、現行の Mio / Shiro / AO / Aka / Kin / Gin の役割・persona・Viewer Roles / Persona Observation とは別物として扱う。全人格が常時会話参加する runtime は未実装だが、本線の即時残課題ではなく将来実装とする。
- LangGraph Turn Controller / character subgraph は本番ランタイムとして採用せず、現行 Go `MessageOrchestrator` 本線を維持する。
- FAISS sidecar は採用せず、現行の Qdrant 想定 VectorDB と L1 SQLite / DuckDB 方針を維持する。
- 旧ルミナ / クラリス / ノクス体系と `@ルミナ` 等 mention は、現行 Mio / Shiro / AO / Aka / Kin / Gin 体系へ移行済みとして扱い、旧 mention 復活は残課題にしない。

## 10. 結果記録テンプレート

`docs/10_新仕様/17_E2E残課題.md` または該当 Phase の作業メモに、以下の形式で記録する。

```markdown
## 確認結果: {項目名}

- 日時:
- 実行者:
- 環境:
- config source:
- コマンド / 操作:
- route:
- session_id:
- job_id:
- event log:
- response:
- 結果: pass / fail / skipped / blocked
- skip / fail 理由:
- 更新した docs:
- 次アクション:
```

## 11. docs 更新先

| 結果 | 更新先 |
|---|---|
| E2E が通った | `docs/10_新仕様/13_実装項目インベントリ.md`, `docs/10_新仕様/17_E2E残課題.md`, 必要なら `docs/10_新仕様/31_未実装項目実装仕様.md` |
| skipped / blocked | `docs/10_新仕様/17_E2E残課題.md` に環境未準備理由を残す |
| fail | `docs/10_新仕様/17_E2E残課題.md` に失敗証跡と次アクションを残す |
| 実装不足が判明 | `docs/10_新仕様/31_未実装項目実装仕様.md` の該当 Phase に戻す |
| 対象外 / 保留へ移す | `docs/10_新仕様/13_実装項目インベントリ.md` と `docs/10_新仕様/31_未実装項目実装仕様.md` に反映する |

## 12. 参照ファイル

- `AGENTS.md`
- `CLAUDE.md`
- `docs/01_正本仕様/実装仕様.md`
- `docs/10_新仕様/13_実装項目インベントリ.md`
- `docs/10_新仕様/17_E2E残課題.md`
- `docs/10_新仕様/31_未実装項目実装仕様.md`
- `docs/10_新仕様/32_E2E_runtime確認チェックリスト.md`
- `docs/01_正本仕様/12_実装状況_20260505.md`
