# 指示なしで進められる残課題 実装指示書

## 1. 目的

この文書は、現在の RenCrow / PicoClaw の残課題のうち、ユーザーの追加判断を待たずに進められる範囲を Goal 実行向けに整理した実装指示書である。

対象は、主に以下である。

- live runtime の blocked 状態の再確認
- E2E 残課題台帳の再分類
- 外部依存なしの regression 確認
- Viewer / runtime の blocked 証跡確認
- `vault/` の扱い確認

この文書は、外部 API、実 PR 作成、実外部送信、正式環境適用、依存追加、設定変更、再起動を無条件に許可するものではない。

## 2. 参照する正本と台帳

作業開始前に、以下を順に確認する。

1. `AGENTS.md`
2. `CLAUDE.md`
3. `docs/01_正本仕様/実装仕様.md`
4. `docs/10_新仕様/13_実装項目インベントリ.md`
5. `docs/10_新仕様/17_E2E残課題.md`
6. `docs/10_新仕様/31_未実装項目実装仕様.md`
7. `docs/10_新仕様/32_E2E_runtime確認チェックリスト.md`
8. `docs/10_新仕様/33_現状残課題整理作業手順書.md`
9. `docs/10_新仕様/34_現状残課題クリア実装手順書.md`

実装判断で迷った場合は、`docs/01_正本仕様/実装仕様.md` を一次参照とし、E2E 判定は `docs/10_新仕様/17_E2E残課題.md` と `docs/10_新仕様/32_E2E_runtime確認チェックリスト.md` に従う。

## 3. Goal に設定するプロンプト

Goal に設定する場合は、次を使う。

```text
docs/10_新仕様/35_指示なしで進められる残課題実装指示書.md に沿って、追加判断なしで進められる残課題を順に処理してください。

各作業は、小さな検証済み commit 単位で進めてください。
各単位で、対象・変更範囲・検証コマンド・完了条件を自分で定義してから実装または確認に入ってください。
実装や docs 更新を行った場合は、該当テストと必要な runtime / Viewer 確認を行ってください。
確認済みの関連ファイルだけを選択的に stage し、日本語 commit message で commit してください。
commit 後は push してください。
push できたら、ユーザー確認を待たずに次の未完了項目へ進んでください。

ただし、次の場合だけ停止して報告してください。
- テスト失敗や runtime 確認失敗が、現在の作業範囲内で短時間に解消できない。
- 変更が複数領域に広がり、1 commit の責務が曖昧になった。
- 外部 secret / 実機 / 外部 API / 複数マシンなど、こちらで準備できない依存が必要。
- destructive operation、依存追加、CI/deploy 設定変更、ファイル削除、セーフガード変更が必要。
- RenCrow / PicoClaw の再起動、runtime config 変更、一時 config 有効化が必要。
- push 対象に未確認差分、別責務の差分、`vault/`、または live E2E 生成物が混ざりそうになった。
- blocked / skipped / fail を pass に移すには、追加実装、環境準備、実機確認、または外部依存が必要だと分かった。

禁止:
- worktree 全体を一括 stage / commit しない。
- `vault/` や live E2E 生成物を明示指示なく commit しない。
- 失敗したテストを無視して push しない。
- blocked / skipped / fail を成功扱いしない。
- health ok、unit test pass、DOM 存在だけで user flow 成立扱いしない。
- Viewer / domain / persistence / runtime / docs を、責務が曖昧なまま 1 commit に混ぜない。
- 外部送信、実 PR 作成、正式環境適用、依存追加、再起動、設定変更を独断で行わない。

報告:
- 各 push 後に、commit hash、検証コマンド、次に進む対象を短く報告してください。
- 停止条件に当たらない限り、ユーザー確認は不要です。
```

## 4. 作業ルール

### 4.1 進めてよい範囲

この文書で追加判断なしに進めてよいのは、次の範囲である。

- read-only な状態確認
- local regression test の再実行
- live service が既に起動している場合の read-only HTTP 確認
- Viewer の read-only 表示確認
- docs 更新案の作成
- Goal 承認済みの場合の docs 更新 commit / push
- `vault/` の中身・生成元・commit 対象外確認

### 4.2 止める範囲

次は、追加指示なしに実行しない。

- `systemctl --user stop/start/restart picoclaw.service`
- `make install`
- `~/.picoclaw/config.yaml` の変更
- scheduler / sandbox / L1 / context budget などの一時 config 有効化
- 外部 API / 外部チャネルへの実送信
- GitHub / 外部 repo への実 PR 作成
- 実マイク STT / TTS 音声再生など、実機 device 前提の確認
- `vault/`、tmp screenshot、live E2E 証跡ファイルの commit

### 4.3 判定ルール

- `pass`: 対象フローの成功条件を満たし、証跡が揃っている。
- `fail`: 対象フローが失敗し、原因または失敗箇所を証跡付きで記録した。
- `blocked`: 外部依存、設定、service 状態、Human approval などが不足して進めない。
- `skipped`: 実行条件を満たさず、未確認として残す。

`blocked`、`skipped`、`fail` は成功ではない。docs 更新でも、これらを `pass` に移してはいけない。

## 5. 作業単位と処理順

### 5.1 現在の blocked 状態の再確認

対象:

- `/health`
- `/viewer/runtime-config`
- `/viewer/debug/system`
- `/viewer/llm-ops/status`

目的:

2026-05-19 時点の `/health 503`、Chat / Worker local OpenAI-compatible endpoint 不達、LLM Ops 502 が現在も継続しているかを確認する。

実行例:

```bash
curl -sS -i --max-time 5 http://127.0.0.1:18790/health
curl -sS -i --max-time 5 http://127.0.0.1:18790/viewer/runtime-config
curl -sS -i --max-time 5 http://127.0.0.1:18790/viewer/debug/system
curl -sS -i --max-time 5 http://127.0.0.1:18790/viewer/llm-ops/status
```

完了条件:

- HTTP status、body、主要 check 名、blocked 理由を記録する。
- 503 / 502 は、body と理由が明示されていれば blocked 証跡として扱う。
- `/health` が ok でも、user flow 成立扱いにはしない。

停止条件:

- service restart が必要。
- runtime config の変更が必要。
- downstream LLM endpoint の起動や外部ホスト操作が必要。

### 5.2 E2E 残課題台帳の最新化前チェック

対象:

- `docs/10_新仕様/17_E2E残課題.md`
- `docs/10_新仕様/32_E2E_runtime確認チェックリスト.md`
- `docs/10_新仕様/13_実装項目インベントリ.md`

目的:

`確認済み`、`blocked`、`未確認`、`外部依存待ち` を再分類し、更新すべき差分案を作る。

実行例:

```bash
rg -n "確認済み|blocked|未確認|外部依存|実PR|実外部送信|scheduler|長時間再開|/health|LLM Ops|Source Registry|Memory Layers|Sandbox" docs/10_新仕様/17_E2E残課題.md docs/10_新仕様/32_E2E_runtime確認チェックリスト.md docs/10_新仕様/13_実装項目インベントリ.md
```

完了条件:

- 更新候補を項目別に整理する。
- `pass` へ移せるものと、`blocked` のまま維持するものを分ける。
- docs 更新を行う場合は、対応する実装・runtime 証跡がある範囲だけに限定する。

停止条件:

- docs が未 push 実装を完了扱いしそうになる。
- 実装変更なしでは判定できない。
- 外部 API / 実機 / 複数マシン確認が必要。

### 5.3 外部依存なしの regression 再実行

対象:

- Go package tests
- Viewer Node contract tests
- diff whitespace check

実行例:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./...
node internal/adapter/viewer/viewer_memory_panel.test.mjs
git diff --check
```

必要に応じて、既存の Viewer Node tests を追加実行する。

完了条件:

- 実行コマンド、pass / fail、失敗 package、失敗 test 名を記録する。
- pass しても、実 API E2E、実ブラウザ音声、実マイク STT、外部チャネル成功の代替にしない。

停止条件:

- 失敗が現在の作業範囲を超える。
- flaky か仕様不一致かを短時間で切り分けられない。
- 修正が必要になる。

### 5.4 SuperAgent / AI Workflow の blocked 証跡確認

対象:

- `/viewer/superagent`
- `/viewer/ai-workflow`
- SuperAgent scheduler disabled / failed terminal / manual-ledger 表示
- AI Workflow scheduler 起点 E2E blocked 表示

目的:

LLM 到達不可、scheduler disabled、真の長時間再開未確認を成功扱いせず、現在の visible state を確認する。

実行例:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/superagent?limit=20'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/ai-workflow?limit=20'
```

完了条件:

- `scheduler:disabled`、failed terminal、manual-ledger、`blocked: true long-running resume not verified` などが現状と一致するか記録する。
- scheduler 正常完了 E2E と真の長時間再開 E2E は、LLM 到達可能状態で未確認なら blocked のまま維持する。

停止条件:

- scheduler 有効化が必要。
- LLM endpoint 復旧が必要。
- 長時間実行 run の作成や runtime 制御が必要。

### 5.5 Skill Governance / Revenue の未接続状態確認

対象:

- `/viewer/skill-governance/recent`
- `/viewer/revenue`
- external PR adapter
- external channel adapter
- Human approval required 表示

目的:

blocked audit、adapter unconfigured、Human approval required が正しく見えるか確認する。

実行例:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/skill-governance/recent?limit=10'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/revenue?limit=10'
```

完了条件:

- `external_pr_created=false`、`submit_status=blocked`、`pr_adapter=unconfigured` を実 PR 成功扱いしない。
- `external_send_applied=false`、`apply_status=blocked`、`send_result=not_sent`、`channel_adapter=unconfigured` を実外部送信成功扱いしない。

停止条件:

- 実 PR 作成が必要。
- 実外部送信が必要。
- GitHub / SNS / channel credential が必要。

### 5.6 Complexity Hotspot の review-only 状態確認

対象:

- `/viewer/complexity-hotspots`
- report-only scan
- pending-review artifact
- `Patch applied: false`
- Coder diff timeout / failure audit

目的:

review-only の証跡を、patch 適用、Sandbox apply、外部 PR 作成と混同しないよう確認する。

実行例:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/complexity-hotspots?limit=10'
```

完了条件:

- report-only / review-only / failed artifact の状態を記録する。
- `Patch applied: false` と Human approval required が維持されているか確認する。
- Coder diff timeout は成功扱いしない。

停止条件:

- Coder provider 実行が必要。
- patch 適用、Sandbox apply、外部 PR 作成が必要。

### 5.7 Source Registry / Memory Layers / Sandbox の通常 config blocked 確認

対象:

- `/viewer/source-registry`
- `/viewer/memory/layers`
- `/viewer/sandbox`
- `/viewer/runtime-config`

目的:

通常 config で L1 store disabled、sandbox disabled の 503 が、route 未登録ではなく blocked 証跡として返るか確認する。

実行例:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/source-registry?action=staging&limit=3'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/memory/layers'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/sandbox?limit=1'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/runtime-config'
```

完了条件:

- Source Registry / Memory Layers / Sandbox が 503 の場合、body と readiness の理由を記録する。
- 一時 config で pass した過去 E2E と、通常 config の blocked 状態を分ける。

停止条件:

- L1 store 有効化が必要。
- sandbox 有効化が必要。
- service restart や config 変更が必要。

### 5.8 未追跡 `vault/` の扱い確認

対象:

- `vault/`
- `git status --short`

目的:

`vault/` の中身、生成元、commit 対象外にすべきかを確認する。

実行例:

```bash
git status --short
find vault -maxdepth 4 -type f | sort | sed -n '1,120p'
```

完了条件:

- `vault/` 配下に live E2E 生成物、Workstream vault、runtime artifact があるか確認する。
- commit 対象外なら、docs / report 上で「明示指示なく commit しない」と記録する。
- `.gitignore` 変更が必要な場合は、勝手に編集せず停止して報告する。

停止条件:

- ファイル削除が必要。
- `.gitignore` 変更が必要。
- commit 対象に含める判断が必要。

## 6. commit / push 単位

Goal として実行し、docs 更新または実装修正が発生した場合は、以下の単位を混ぜない。

| commit 単位 | 含めてよいもの | 含めないもの |
|---|---|---|
| runtime blocked 状態の docs 更新 | `17` / `32` の blocked 証跡追記 | runtime code 変更 |
| local regression 結果の docs 更新 | test コマンド、pass / fail、未検証範囲 | E2E 成功扱いへの変更 |
| Viewer visible-state 修正 | Viewer JS / CSS / handler と対象 test | domain / persistence の横断修正 |
| client validation 修正 | `pkg/rencrowclient` と focused test | runtime route 変更 |
| domain / persistence validation 修正 | 対象 domain または store と focused test | Viewer 表示修正 |
| docs 整理 | 対応する docs のみ | `vault/` / tmp / live E2E artifacts |

commit message は日本語で書く。

例:

```text
docs: 指示なしで進められる残課題の状態を整理する
fix: Viewer の blocked 状態表示を保持する
fix: SuperAgent status の証跡検証を補強する
```

## 7. 証跡記録テンプレート

各作業後、次の形式で記録する。

```text
## 確認結果

- 日時:
- 対象:
- 作業種別: read-only / local regression / live read-only / docs update / implementation
- config source:
- 実行コマンド:
- route / job / session:
- response / visible state:
- 判定: pass / fail / blocked / skipped
- 成功扱いしないもの:
- 次アクション:
```

## 8. 最終完了条件

この指示書の完了条件は、以下である。

- 8 項目すべてについて、pass / fail / blocked / skipped のいずれかで現状が記録されている。
- blocked / skipped / fail を pass にしていない。
- 外部依存、Human approval、再起動、config 変更が必要な項目は停止条件として分離されている。
- docs 更新を行った場合、対応範囲だけを selective stage している。
- `vault/` と live E2E 生成物を明示指示なく commit していない。
- 実装変更が発生した場合、対象 test と必要な runtime / Viewer 確認が記録されている。
- push 済みの場合、commit hash、検証コマンド、次に進む対象が報告されている。

## 9. 2026-05-20 実行結果

### 9.1 実行概要

- 日時: 2026-05-20T13:28:55Z
- branch: `feature/RenCrow_Start`
- 作業種別: read-only runtime 確認 / local regression / docs update
- 実行しなかったこと:
  - service restart
  - `make install`
  - runtime config 変更
  - scheduler / sandbox / L1 / context budget の一時有効化
  - 外部 API 送信
  - 実 PR 作成
  - `vault/` commit

### 9.2 5.1 現在の blocked 状態の再確認

実行:

```bash
curl -sS -i --max-time 5 http://127.0.0.1:18790/health
curl -sS -i --max-time 5 http://127.0.0.1:18790/viewer/runtime-config
curl -sS -i --max-time 5 http://127.0.0.1:18790/viewer/debug/system
curl -sS -i --max-time 5 http://127.0.0.1:18790/viewer/llm-ops/status
```

結果:

| route | status | 判定 | 根拠 |
|---|---:|---|---|
| `/health` | 503 | blocked | `local_llm_chat` と `local_llm_worker` が `http://192.168.1.13:8081` / `:8082` へ `context deadline exceeded` |
| `/viewer/runtime-config` | 200 | blocked 含む read-only 証跡 | local LLM は `192.168.1.13:8081` / `:8082`、LLM Ops は `192.168.1.13:8079`、`conversation_enabled=false`、`sandbox_enabled=false` |
| `/viewer/debug/system` | 200 | blocked | `stt_ok=false`、`tts_live_ok=false`、`tts_ready_ok=false`。STT / TTS health probe は timeout |
| `/viewer/llm-ops/status` | 502 | blocked | body は `upstream unreachable` |

結論:

- 2026-05-19 時点の `/health 503`、LLM endpoint 不達、LLM Ops 502 は継続している。
- blocked 証跡は取得できたが、user flow 成功、LLM start / stop 成功、STT / TTS 成功、scheduler 正常完了の証跡ではない。

### 9.3 5.2 E2E 残課題台帳の最新化前チェック

実行:

```bash
rg -n "確認済み|blocked|未確認|外部依存|実PR|実外部送信|scheduler|長時間再開|/health|LLM Ops|Source Registry|Memory Layers|Sandbox" docs/10_新仕様/17_E2E残課題.md docs/10_新仕様/32_E2E_runtime確認チェックリスト.md docs/10_新仕様/13_実装項目インベントリ.md
```

結果:

- `17_E2E残課題.md`、`32_E2E_runtime確認チェックリスト.md`、`13_実装項目インベントリ.md` は、skip / blocked / local boundary / live E2E を分ける前提で記録されている。
- 今回の read-only 確認で `blocked` から `pass` へ移せる項目はない。
- `/health`、LLM Ops、STT / TTS、Source Registry、Memory Layers、Sandbox、SuperAgent scheduler、実 PR、実外部送信は blocked または未確認のまま維持する。

結論:

- docs 台帳へ反映する場合も、今回の結果は blocked 再確認と local regression pass の追記に限定する。
- blocked / skipped / fail を pass に移さない。

### 9.4 5.3 外部依存なしの regression 再実行

実行:

```bash
GOCACHE=/tmp/picoclaw-gocache go test ./...
node internal/adapter/viewer/viewer_memory_panel.test.mjs
git diff --check
```

結果:

| command | 判定 | 補足 |
|---|---|---|
| `GOCACHE=/tmp/picoclaw-gocache go test ./...` | pass | 全 Go package test 通過 |
| `node internal/adapter/viewer/viewer_memory_panel.test.mjs` | pass | 75 tests pass |
| `git diff --check` | pass | whitespace error なし |

成功扱いしないもの:

- 実 API E2E
- 実ブラウザ音声
- 実マイク STT
- TTS playback / lip sync
- 外部チャネル送信
- 実 PR 作成
- scheduler 正常完了

### 9.5 5.4 SuperAgent / AI Workflow の blocked 証跡確認

実行:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/superagent?limit=20'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/ai-workflow?limit=20'
```

結果:

- `/viewer/superagent` は 200。
- `runtime_config.run_queue_scheduler_enabled=false`、`run_queue_scheduler_interval_sec=60`、`run_queue_scheduler_claim_limit=1`。
- run queue には `manual ledger client E2E completed; scheduler execution not used` と `pause/resume queue reentry ledger E2E completed without scheduler execution` がある。
- agent run には LLM endpoint 不達による failed run が残っている。
- 直近の status view には `running` の agent run も含まれるため、真の長時間再開 E2E 成功とは扱わない。
- `/viewer/ai-workflow` は 200。
- `context_budget_policy.max_context_tokens=0` で context budget は disabled。
- `promotion_apply` は `needs_approval` の policy event が残る。

結論:

- SuperAgent manual ledger / AI Workflow status は read-only で取得できた。
- scheduler 正常完了 E2E、fallback でない user flow、実行中 goroutine を伴う真の長時間再開 E2E は blocked / 未確認のまま維持する。

### 9.6 5.5 Skill Governance / Revenue の未接続状態確認

実行:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/skill-governance/recent?limit=10'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/revenue?limit=10'
```

結果:

| route | status | 判定 | 根拠 |
|---|---:|---|---|
| `/viewer/skill-governance/recent` | 200 | blocked | `external_pr_adapter=unconfigured`、`external_pr_adapter_configured=false`、`human_approval_required_for_pr=true`。submit records は `submit_status=blocked`、`external_pr_created=false`、`post_submit_verified=false` |
| `/viewer/revenue` | 200 | blocked | `external_channel_adapter=unconfigured`、`external_channel_adapter_configured=false`、`human_approval_required_for_external_send=true`。apply records は `apply_status=blocked`、`send_result=not_sent`、`external_send_applied=false`、`post_send_verified=false` |

結論:

- 実 PR 作成と実外部送信は未接続のまま。
- blocked audit は成功証跡ではない。

### 9.7 5.6 Complexity Hotspot の review-only 状態確認

実行:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/complexity-hotspots?limit=10'
```

結果:

- `/viewer/complexity-hotspots` は 200。
- `scan_live_20260519083500` は `mode=report_only`、`status=completed`。
- reports には `complexity_coder_diff_failure` が `status=failed` として残る。
- `complexity_concrete_diff_proposal` と `complexity_patch_proposal` は `pending_review`。
- artifact content に `Patch applied: false` と `Human approval required: true` が残る。

結論:

- report-only / review-only / failure audit は確認できた。
- Coder provider 成功、patch 適用、Sandbox apply、外部 PR 作成は未確認のまま。

### 9.8 5.7 Source Registry / Memory Layers / Sandbox の通常 config blocked 確認

実行:

```bash
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/source-registry?action=staging&limit=3'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/memory/layers'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/sandbox?limit=1'
curl -sS -i --max-time 5 'http://127.0.0.1:18790/viewer/runtime-config'
```

結果:

| route | status | 判定 | body / readiness |
|---|---:|---|---|
| `/viewer/source-registry?action=staging&limit=3` | 503 | blocked | `source registry unavailable` |
| `/viewer/memory/layers` | 503 | blocked | `memory layers unavailable` |
| `/viewer/sandbox?limit=1` | 503 | blocked | `sandbox store unavailable` |
| `/viewer/runtime-config` | 200 | blocked 含む read-only 証跡 | `conversation_enabled=false`、`source_registry_available=false`、`memory_layers_available=false`、`sandbox_enabled=false` |

結論:

- 通常 config では Source Registry / Memory Layers / Sandbox は blocked のまま。
- 一時 config での過去 pass と通常 config の blocked 状態を混同しない。

### 9.9 5.8 未追跡 `vault/` の扱い確認

実行:

```bash
git status --short
find vault -maxdepth 4 -type f | sort | sed -n '1,160p'
```

結果:

```text
?? docs/10_新仕様/35_指示なしで進められる残課題実装指示書.md
?? vault/
vault/workstreams/live-e2e/ws_vault_apply_20260520002308.833495599/STATUS.md
vault/workstreams/live-e2e/ws_vault_apply_20260520002549.529385720/STATUS.md
vault/workstreams/live-e2e/ws_vault_apply_20260520002739.458203088/STATUS.md
```

結論:

- `vault/` は live-e2e の Workstream vault 生成物である。
- 明示指示なく commit しない。
- `.gitignore` 変更や削除が必要な場合は、別途判断が必要。

### 9.10 今回の総合判定

| 項目 | 判定 | 次アクション |
|---|---|---|
| 5.1 blocked 状態再確認 | blocked | LLM / STT / TTS / LLM Ops の実 endpoint 復旧が必要 |
| 5.2 台帳最新化前チェック | pass | 今回の追記範囲では pass へ移す項目なし |
| 5.3 local regression | pass | 実 E2E 成功とは分離して維持 |
| 5.4 SuperAgent / AI Workflow | blocked | scheduler enabled + LLM 到達可能状態での正常完了 E2E が必要 |
| 5.5 Skill Governance / Revenue | blocked | external PR adapter / external channel adapter と Human approval flow が必要 |
| 5.6 Complexity Hotspot | blocked | Coder provider 成功、patch 適用、Sandbox apply、外部 PR 作成は未確認 |
| 5.7 Source Registry / Memory Layers / Sandbox | blocked | L1 / sandbox 通常 config 有効化または一時 E2E 環境が必要 |
| 5.8 `vault/` | blocked | live-e2e 生成物として commit 対象外。削除 / ignore は別判断 |

今回の docs 更新は、この `35_指示なしで進められる残課題実装指示書.md` の実行結果追記に限定する。
