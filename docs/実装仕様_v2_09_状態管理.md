# 実装仕様 v2 - 09 状態管理仕様

## セッション
- `session_id` はチャネル単位で一意管理
- Slackはthread単位を推奨

## メモリ
- `short_memory` 中心
- `recent_turns` は最小限

## ANALYZE保存
- 保存先は実装で必ず一意に決める
  - `pkg/memory/store.go` 拡張
  - もしくは `pkg/analyze/store.go` 新設

## 参照
- 正本: `docs/実装仕様_v2_統合版.md`（9章）
