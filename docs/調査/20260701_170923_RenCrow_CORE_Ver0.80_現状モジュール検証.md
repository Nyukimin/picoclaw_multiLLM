# RenCrow_CORE Ver0.80 現状モジュール検証

作成日時: 2026-07-01 17:09:23 JST
対象: `picoclaw_multiLLM` / branch `feature/RenCrow_Start`
目的: Chat / IdleChat / Viewer / Agent などを機能単位で閉じ込める Ver0.80 化の前に、現行実装の境界・仕様ズレ・テスト状態を検証する。

## 結論

現行コードは Go の全体テストと `go vet` を通過しており、コンパイル可能性と既存テスト上の健全性は保たれている。

ただし、Ver0.80 で「機能ごとのフォルダに実体を閉じ込める」には、まだ未整合が大きい。特に以下は先に対処すべきである。

1. `AGENTS.md` / `CLAUDE.md` の正本参照先が、整理後の `docs/02_正本仕様/` ではなく旧 `docs/01_正本仕様/` / `docs/10_新仕様/` を指している。
2. Viewer 通常チャット仕様は `to=mio|shiro|kuro|midori` だけを送る前提だが、実装とテストはまだ `model_alias` / `route_prefix` による旧 route 変換を正としている。
3. `cmd/picoclaw` が composition root として自然な配線を超えて、Viewer route、IdleChat 起動、モジュール連携、各 feature handler を広く抱えている。
4. `modules/` は契約化が始まっているが、`browseractor` / `voicechat` / `webgather` は README と依存方向テストの対象から漏れている。
5. IdleChat は `internal/application/idlechat` と一部 `modules/chat` に分かれており、機能フォルダとして閉じている状態ではない。

Ver0.80 の第一段階は、大規模移動ではなく「仕様入口の修正」「境界テストの対象拡張」「Viewer 旧 alias 経路の扱い決定」「cmd 配線の feature registrar 化」から入るのが安全である。

## 実行した検証

| 検証 | 結果 |
|---|---|
| `git status -sb` | clean。`feature/RenCrow_Start...origin/feature/RenCrow_Start` |
| `GOCACHE=/tmp/picoclaw-gocache go test ./modules/...` | pass |
| `GOCACHE=/tmp/picoclaw-gocache go test ./cmd/picoclaw ./internal/adapter/modulebridge ./internal/application/idlechat ./internal/application/orchestrator ./internal/adapter/viewer` | pass |
| `GOCACHE=/tmp/picoclaw-gocache go test ./...` | pass |
| `GOCACHE=/tmp/picoclaw-gocache go vet ./...` | pass |

注意: 今回は live service の停止・再起動・ブラウザ E2E は行っていない。目的は Ver0.80 前の構造検証であり、稼働中サービスを触る変更はしていない。

## 正本仕様から見た必須境界

### 理解 docs の前提

`docs/01_理解/01_システム概要.md` は、PicoClaw の責務を「入口、セッション、Viewer、各 RenCrow module server への橋渡し」と整理している。

また、現行仕様で固定することとして、Viewer 通常チャットでは Mio / Shiro / Kuro / Midori を明示選択できるが、Viewer は route/model alias へ変換しないと明記している。

該当箇所:

- `docs/01_理解/01_システム概要.md:5`
- `docs/01_理解/01_システム概要.md:21-31`
- `docs/01_理解/01_システム概要.md:33-47`

### Viewer 通常チャット

`docs/02_正本仕様/02_実装仕様.md` は、Viewer 通常チャットの相手選択を以下のように定義している。

- Viewer は `to=mio|shiro|kuro|midori` だけを送る。
- Viewer が route/model alias へ変換してはいけない。
- `to` は会話相手 ID であり route ではない。
- Viewer は `to` を `/ops`、`/analyze`、`/wild`、`model_alias`、`route_prefix` などへ変換しない。
- Chat/Orchestrator 側で `to` を Chat 系の相手別経路へ解決する。
- 実装対象にも、Viewer send payload は `to=mio|shiro|kuro|midori` のみで、`model_alias` / `route_prefix` は使わないとある。

該当箇所:

- `docs/02_正本仕様/02_実装仕様.md:38-47`
- `docs/02_正本仕様/02_実装仕様.md:194-205`
- `docs/02_正本仕様/02_実装仕様.md:351-359`

### Runtime Config

`docs/02_正本仕様/03_Runtime_Config.md` は、Viewer や picoclaw server が LLM / TTS / STT backend を直接呼ばず、必ず RenCrow module server を経由すると定義している。

- LLM: `picoclaw -> RenCrow_LLM -> LLM backend`
- TTS: `picoclaw -> RenCrow_TTS -> TTS backend`
- STT: `picoclaw -> RenCrow_STT -> STT backend`
- Viewer は `to=mio|shiro|kuro|midori` だけを送る。
- route / model / backend の解決は Viewer では行わない。

該当箇所:

- `docs/02_正本仕様/03_Runtime_Config.md:11-18`
- `docs/02_正本仕様/03_Runtime_Config.md:122-124`

## 構築指標との照合

`docs/04_構築指標/02_受入基準・テスト観点.md` の MVP Definition of Done は、今回の Ver0.80 境界整理と一致している。

- Runtime: Viewer / picoclaw は RenCrow_LLM / RenCrow_TTS / RenCrow_STT 経由でのみ backend を呼ぶ。
- Chat: Mio または初期Chat Agent が最終返答を生成する。
- Safety: Coder は直接実行せず、Worker が採用・適用・ログ記録する。
- Runtime test: Chat 相手切替中は入力を停止し、ready 後に再開する。
- Runtime test: `local_llm` 推論経路と `llm_ops` 起動/切替管理経路が混ざらない。
- IdleChat test: IdleChat 中は外部検索を禁止し、Heavy / Wild が稼働中の場合は割り込ませない。

該当箇所:

- `docs/04_構築指標/02_受入基準・テスト観点.md:3-20`
- `docs/04_構築指標/02_受入基準・テスト観点.md:22-29`
- `docs/04_構築指標/02_受入基準・テスト観点.md:30-37`
- `docs/04_構築指標/02_受入基準・テスト観点.md:63-70`

`docs/04_構築指標/01_構築ロードマップ.md` も、Phase 0 の完了条件として正本 Config の場所、fallback 禁止、Agent 責務と安全境界を求めている。Ver0.80 ではコード移動より先に、この境界を docs / tests / runtime contract に固定する必要がある。

該当箇所:

- `docs/04_構築指標/01_構築ロードマップ.md:3-10`
- `docs/04_構築指標/01_構築ロードマップ.md:35-39`

## 現行コード構造の棚卸し

Go ファイル数が多い領域は以下。

| 領域 | Go files | 見立て |
|---|---:|---|
| `cmd/picoclaw` | 151 | composition root / route / runtime 配線が巨大 |
| `internal/infrastructure/persistence` | 143 | 永続化 adapter 群 |
| `internal/adapter/viewer` | 122 | Viewer adapter に feature handler が集積 |
| `internal/application/orchestrator` | 86 | Chat/Worker/LLM/TTS/Event 連携の中核 |
| `modules/tts` | 67 | TTS 契約・状態整理が進んでいる |
| `internal/application/idlechat` | 66 | IdleChat 本体が internal application に残る |
| `modules/stt` | 24 | STT 契約化あり |
| `modules/chat` | 20 | Chat 契約化あり |
| `modules/llm` | 19 | LLM 契約化あり |
| `modules/worker` | 17 | Worker 契約化あり |

## modules/ の現状

`modules/README.md` は、module layout として `core` / `chat` / `worker` / `llm` / `tts` / `stt` の 6 つを記載している。

一方、実際の `modules/` には以下も存在する。

- `modules/browseractor`
- `modules/voicechat`
- `modules/webgather`

この 3 つは README がない。

| module | README |
|---|---|
| `browseractor` | no |
| `chat` | yes |
| `core` | yes |
| `llm` | yes |
| `stt` | yes |
| `tts` | yes |
| `voicechat` | no |
| `webgather` | no |
| `worker` | yes |

`modules/dependency_rules_test.go` の `allowedModuleImports` も 6 module だけで、`browseractor` / `voicechat` / `webgather` は依存方向テストの対象外である。

該当箇所:

- `modules/README.md:7-15`
- `modules/README.md:37-49`
- `modules/DEPENDENCY_RULES.md:7-33`
- `modules/dependency_rules_test.go:15-22`

## module import 状況

`go list` で現行 package の module import を見た結果。

| module | import 元数 | import 元 |
|---|---:|---|
| `browseractor` | 3 | `cmd/picoclaw`, `internal/infrastructure/browseractor`, `internal/infrastructure/tools` |
| `chat` | 3 | `cmd/picoclaw`, `internal/adapter/modulebridge`, `internal/application/idlechat` |
| `core` | 7 | `cmd/picoclaw`, `internal/adapter/modulebridge`, `modules/chat`, `modules/llm`, `modules/stt`, `modules/tts`, `modules/worker` |
| `llm` | 5 | `cmd/picoclaw`, `internal/adapter/modulebridge`, `internal/infrastructure/llm`, `modules/chat`, `modules/worker` |
| `stt` | 5 | `cmd/picoclaw`, `internal/adapter/modulebridge`, `internal/adapter/viewer`, `internal/infrastructure/stt`, `modules/chat` |
| `tts` | 5 | `cmd/picoclaw`, `internal/adapter/modulebridge`, `internal/application/orchestrator`, `internal/infrastructure/tts`, `modules/chat` |
| `voicechat` | 1 | `cmd/picoclaw` |
| `webgather` | 5 | `cmd/picoclaw`, `internal/application/sourcefetcher`, `internal/application/webgather`, `internal/infrastructure/tools`, `internal/infrastructure/webgather` |
| `worker` | 5 | `cmd/picoclaw`, `internal/adapter/modulebridge`, `internal/application/orchestrator`, `internal/application/service`, `modules/chat` |

現在の module 契約は「存在する」が、import 元はまだ `cmd/picoclaw` と `internal/...` に強く依存している。これは段階移行としては妥当だが、Ver0.80 で機能フォルダに閉じるなら、どの `internal/...` が feature 実装で、どれが adapter / infrastructure かを再分類する必要がある。

## 主要リスク

### P0: 指示・仕様入口の参照先が古い

`AGENTS.md` は整理後の正本配置と一致していない。

- `docs/10_新仕様/82_Claude_Code指示配置ガバナンス仕様.md` を正本として参照しているが、現行トップレベルには `docs/10_新仕様/` がなく、旧文書は `docs/refs/10_新仕様/` に移動済み。
- 実装一次参照として `docs/01_正本仕様/実装仕様.md` を複数箇所で参照しているが、現行トップレベルの正本は `docs/02_正本仕様/02_実装仕様.md`。
- 旧 docs 参照ルールでも `docs/01_正本仕様/` または `docs/10_新仕様/` に統合と書かれているが、現行 docs 体系ではこの表現が混乱を招く。

該当箇所:

- `AGENTS.md:9`
- `AGENTS.md:19`
- `AGENTS.md:40-43`
- `AGENTS.md:236`
- `AGENTS.md:387-421`
- `AGENTS.md:469`

`CLAUDE.md` も旧配置を一次参照としている。

該当箇所:

- `CLAUDE.md:89-104`
- `CLAUDE.md:320-331`

影響:

- 後続 Worker / Coder が旧 refs / archive 側を現行仕様として読む危険がある。
- Ver0.80 の module 境界判断が、整理後の docs 実体とずれる。

推奨:

1. `AGENTS.md` と `CLAUDE.md` の最初に読む順序を `docs/02_正本仕様/02_実装仕様.md` / `docs/02_正本仕様/03_Runtime_Config.md` へ更新する。
2. `docs/refs/...` は履歴参照であり、現行仕様ではないと明記する。
3. 旧 `docs/10_新仕様` にしかない候補仕様は、必要なものだけ `docs/02_正本仕様/` または Ver0.80 用の新規正本へ昇格する。

### P0: Viewer 通常チャットの実装が正本仕様と逆方向

正本仕様は Viewer が `to` だけを送ると定義している。しかし `internal/adapter/viewer/handler_send.go` は以下を持つ。

- request field: `model_alias`, `base_url`, `model`, `route_prefix`
- hardcoded alias: `Worker -> http://127.0.0.1:8082 /ops`
- hardcoded alias: `Coder -> http://127.0.0.1:8082 /code2`
- hardcoded alias: `Heavy -> http://127.0.0.1:8083 /analyze`
- hardcoded alias: `Wild -> http://127.0.0.1:8084 /wild`
- explicit route がない場合、`route_prefix + " " + message` に変換する。

該当箇所:

- `internal/adapter/viewer/handler_send.go:33-39`
- `internal/adapter/viewer/handler_send.go:48-73`
- `internal/adapter/viewer/handler_send.go:122-128`
- `internal/adapter/viewer/handler_send.go:162-178`

さらに既存テストはこの旧挙動を期待している。

- `TestHandleSendAppliesViewerLLMAlias` は `model_alias=Worker` を `/ops ...` に変換することを期待。
- `TestHandleSendUsesRuntimeAliasFields` は `model_alias=Heavy` と `route_prefix=/heavy` を受け入れることを期待。

該当箇所:

- `internal/adapter/viewer/handler_send_test.go:18-61`
- `internal/adapter/viewer/handler_send_test.go:91-133`

影響:

- `go test ./...` が通っても、Viewer 通常チャット仕様と一致している証拠にはならない。
- Viewer が backend / route 解決を抱えており、Viewer module を疎にできない。
- `Runtime_Config` の「Viewer/picoclaw は backend を直接扱わない」方針と衝突する。

推奨:

1. 旧 `model_alias` / `route_prefix` 経路を「legacy explicit route support」として隔離するか、通常チャットから除外する。
2. `SendRequest` に `To string` を追加し、Viewer 通常チャットは `to` だけを渡すテストを先に作る。
3. `/ops` / `/analyze` / `/wild` はユーザー本文の明示 command としてだけ扱う。
4. 旧 alias テストは削除ではなく、legacy API として残すか廃止判断を明記してから更新する。

### P1: cmd/picoclaw が Ver0.80 の機能境界を超えて肥大化している

`cmd/picoclaw/runtime_dependencies.go` の `Dependencies` は 178 field を持つ。composition root として配線を持つこと自体は問題ではないが、Viewer、Memory、Sandbox、Skill、Revenue、Persona、BrowserTrace、Complexity、SuperAgent、AIWorkflow、Scheduler、KnowledgeMemory などの handler / store / controller が 1 struct に集約されている。

該当箇所:

- `cmd/picoclaw/runtime_dependencies.go:60-220`

`cmd/picoclaw/routes.go` も Viewer base route、LLM ops、STT/audio、dynamic route を横断登録している。

該当箇所:

- `cmd/picoclaw/routes.go:28-95`
- `cmd/picoclaw/routes.go:97-138`
- `cmd/picoclaw/routes.go:140-260`

影響:

- Chat / IdleChat / Viewer / Agent の機能単位に切り出しても、起動配線が `cmd/picoclaw` に残り続ける。
- feature を追加するたびに `Dependencies` と route 登録が膨らむ。
- module ではなく巨大 app singleton を中心に実装が伸びる。

推奨:

1. `cmd/picoclaw` は main と最小 composition root だけに寄せる。
2. 各 feature に `RegisterRoutes(mux, deps)` または `BuildRuntime(cfg, deps)` の小さい registrar を持たせる。
3. `Dependencies` を feature ごとの dependency bundle に分割する。
4. 初回は移動ではなく wrapper / registrar 抽出に留め、behavior diff を出さない。

### P1: IdleChat が機能フォルダとして閉じていない

`cmd/picoclaw/runtime_idlechat.go` は IdleChat runtime の構築で以下を直接行う。

- `idlechat.NewIdleChatOrchestrator`
- speaker provider map の構築
- topic generation config の変換
- forecast provider の選定
- persona recorder の接続
- topic store path の構築
- TTS prefetch と Viewer event 変換
- `idleChatOrch.Start()`

該当箇所:

- `cmd/picoclaw/runtime_idlechat.go:20-139`

また IdleChat runtime は `internal/application/idlechat` にあり、forecast policy などの一部だけ `modules/chat` を使う。

影響:

- IdleChat が Chat module の一部なのか、独立 feature なのかが曖昧。
- Viewer event / TTS / Persona / LLM provider 配線が runtime 起動関数に集まり、IdleChat の交換・停止・テスト単位が大きい。

推奨:

1. `features/idlechat` または `modules/idlechat` 相当の facade を設ける。
2. `cmd/picoclaw` からは `idlechatfeature.BuildRuntime(cfg, ports)` のような 1 呼び出しにする。
3. provider 選定、topic store、TTS event bridge、Viewer event 変換を IdleChat feature 内の port として整理する。
4. `internal/application/idlechat` の中核ロジックはすぐ移動せず、facade で境界を固定してから段階移動する。

### P1: module boundary test の対象が実体と一致していない

`modules/dependency_rules_test.go` は `core` / `llm` / `tts` / `stt` / `worker` / `chat` だけを対象にしている。

実体として存在する `browseractor` / `voicechat` / `webgather` は対象外で、README もない。

影響:

- 新 module が増えても依存方向ルールが自動で効かない。
- Ver0.80 で module を疎にしたつもりでも、後から逆依存が混入する。

推奨:

1. 3 module の README を追加し、責務・禁止 import・外部 port を明記する。
2. `allowedModuleImports` に追加する。
3. `TestModulePackagesAreDocumented` が全 `modules/*` を自動列挙するように変える。
4. `modules/DEPENDENCY_RULES.md` の layout と forbidden dependencies を実体に合わせて更新する。

### P2: Viewer adapter が feature 集積場になっている

`internal/adapter/viewer` は 122 Go files あり、Chat UI / TTS / STT / Memory / Domain Graph / Sandbox / Skill / Revenue / Persona / BrowserTrace / Complexity / SuperAgent / AIWorkflow など、多数の feature handler を同一 package に持つ。

影響:

- Viewer shell と feature-specific viewer API が分離されていない。
- 機能単位で閉じ込める場合、Viewer 側だけが横断 package のまま残る。

推奨:

1. Viewer core は page / asset / SSE / shell state に絞る。
2. feature-specific handler は `features/<name>/viewer` または `internal/adapter/viewer/<name>` 相当に分ける。
3. 最初は package 分割より route registrar 分割を優先する。

### P2: module/chat はまだ独立 Chat 実装ではなく legacy orchestrator adapter

`cmd/picoclaw/module_chat_route.go` は `/viewer/modules/chat/route` を提供するが、これは generic route decision API であり、Viewer 通常チャットの `to=mio|shiro|kuro|midori` とは別の contract である。

`internal/adapter/modulebridge/chat.go` の `Respond` は `chat.RuntimePorts` を引数として受けるが使用せず、legacy orchestrator の `ProcessMessage` に委譲する。

該当箇所:

- `cmd/picoclaw/module_chat_route.go:12-32`
- `internal/adapter/modulebridge/chat.go:65-75`
- `internal/adapter/modulebridge/chat.go:75-95`

影響:

- `modules/chat` があることは、Chat 機能が module 内に閉じたことを意味しない。
- Ver0.80 で Chat を独立 feature にする場合、`to` 解決、history、response event、LLM recipient selection を Chat module 側へ寄せる必要がある。

推奨:

1. `modules/chat` に Viewer recipient contract を追加する。
2. `modulebridge` は legacy adapter として明記する。
3. Chat の新 contract を先にテストし、orchestrator から段階的に呼び出す。

## Ver0.80 向けの推奨構造

大移動より先に、以下の役割分担を固定する。

```text
cmd/picoclaw/
  main と最小 composition root

modules/
  core/          cross-module contract と状態所有ルール
  chat/          Chat の公開 contract、recipient/to 解決、route policy
  idlechat/      IdleChat の公開 contract と runtime ports
  viewer/        Viewer shell contract、SSE/event contract
  agent/         Agent identity / role / capability contract
  llm/
  tts/
  stt/
  worker/
  browseractor/
  voicechat/
  webgather/

internal/features/
  chat/
  idlechat/
  viewer/
  agent/
  memory/
  sandbox/
  skillgovernance/
  revenue/
  persona/
  aiworkflow/

internal/adapter/
  modulebridge/  legacy 実装を modules contract に接続する互換層
  viewer/        最終的には shell / common adapter へ縮小

internal/infrastructure/
  persistence/
  llm/
  tts/
  stt/
  browseractor/
  webgather/
```

ただし、上記を一気に移動しない。Ver0.80 の実装順は「contract / tests / registrar / facade / 実体移動」の順にする。

## 推奨移行順

1. **仕様入口修正**
   - `AGENTS.md` / `CLAUDE.md` を現行 docs 実体へ合わせる。
   - 正本は `docs/02_正本仕様/`、履歴は `docs/refs/` と明記する。

2. **module boundary test 更新**
   - `browseractor` / `voicechat` / `webgather` を依存方向テストへ追加する。
   - README 必須チェックを実体列挙型にする。

3. **Viewer 通常チャット contract 修正**
   - `to=mio|shiro|kuro|midori` の request / test を追加する。
   - `model_alias` / `route_prefix` の扱いを legacy として隔離する。
   - 明示 `/ops` などの command は本文 route として残す。

4. **cmd/picoclaw の registrar 分割**
   - Viewer base / Viewer feature APIs / STT / TTS / LLM Ops / modules / IdleChat を registrar に分ける。
   - `Dependencies` を feature dependency bundle に分ける。

5. **IdleChat facade 化**
   - `cmd/picoclaw/runtime_idlechat.go` の構築責務を `features/idlechat` へ移す。
   - TTS / Viewer / Persona / LLM provider は ports として渡す。

6. **Chat module の recipient 対応**
   - Chat `to` 解決を module contract に入れる。
   - Orchestrator は legacy route decider ではなく Chat contract を使って相手別モデルを解決する。

7. **Viewer adapter の feature API 分割**
   - package 移動は最後でよい。
   - まず route registrar と handler owner を分離する。

## Ver0.80 の完了条件案

- `go test ./...` が通る。
- `go vet ./...` が通る。
- `modules/dependency_rules_test.go` が全 `modules/*` を対象にする。
- `modules/*/README.md` が全 module に存在する。
- Viewer 通常チャットの送信テストが `to=mio|shiro|kuro|midori` を期待する。
- `model_alias` / `route_prefix` は通常チャット経路では使われない。
- `cmd/picoclaw` に feature 固有の policy が増えず、registrar / facade 呼び出しに寄る。
- `AGENTS.md` / `CLAUDE.md` が整理後 docs 実体を指す。

## 現時点の判定

RenCrow_CORE Ver0.80 化の土台はある。

- `modules/` 契約化は始まっている。
- `modulebridge` により legacy 実装との互換層もある。
- 依存方向テストも一部存在する。
- 全体テストと vet は通っている。

しかし、機能単位に「閉じ込めた」と呼ぶにはまだ早い。

- Viewer は旧 alias 変換を持つ。
- Chat module は legacy orchestrator adapter であり、相手別 Chat contract ではない。
- IdleChat は runtime 起動が `cmd/picoclaw` に強く残る。
- 新 module の依存テストと README が不足している。
- docs 入口が実体とずれている。

したがって、次の作業は refactor 本体ではなく、Ver0.80 の境界を守るための「仕様入口修正 + 依存テスト拡張 + Viewer Chat contract の失敗テスト追加」から始めるのが妥当である。
