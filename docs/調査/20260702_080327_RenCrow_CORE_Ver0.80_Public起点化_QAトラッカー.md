# RenCrow_CORE Ver0.80 Public 起点化 QA トラッカー

## Scope

`picoclaw_multiLLM` 現ブランチの push 済み HEAD を、新規 Public repository `RenCrow_CORE` Ver0.80 の seed / staging source にするための QA tracker。

## Status Summary

| Requirement | Status | Evidence | Notes |
| --- | --- | --- | --- |
| cmd/picoclaw から feature registrar 起点へ寄せる | inventory_done | `cmd/picoclaw/feature_registrars.go`, `cmd/picoclaw/feature_registrars_test.go` | Handler body は移動せず route registration handoff を維持 |
| internal/features scaffold | test_passed | feature scaffold audit `audit_status=0` | 全 30 feature に README / ports.go / registrar.go と必須見出しあり |
| modules contract README / manifest / dependency | test_passed | `GOCACHE=/tmp/picoclaw-gocache go test ./modules/...` | 現存 module 9 package は README あり |
| Viewer Chat recipient contract | retest_passed | 2026-07-01 live test `mio/shiro/kuro/midori` short/long PASS | `to=mio|shiro|kuro|midori` owner/from/token/current-state を確認済み |
| Public repo initial README | inventory_done | root `README.md` updated for RenCrow_CORE Ver0.80 | 旧 v3 branch / PR 前提を外した起点 README |
| Public repo 起点化仕様 | inventory_done | `docs/02_正本仕様/08_RenCrow_CORE_Ver0.80_Public_Repo起点化仕様.md` | 公開範囲、除外候補、preflight command を定義 |
| docs index sync | inventory_done | `docs/README.md`, `docs/00_読む順番.md` | 08 を正本入口へ追加 |
| representative tests | test_passed | `go test ./modules/...`, `go test ./cmd/picoclaw ./internal/features/... ./internal/adapter/viewer ./modules/...`, `git diff --check` | 現時点で通過 |
| full repository test / vet after docs sync | not_started | - | 次ステップで実行 |
| all module / feature live verification | not_started | - | API / route / Viewer / background job の broad live pass が未完了 |
| Public repo export exclusion | test_failed | public preflight scan | tracked local/artifact candidatesあり。削除ではなく export 除外または owner 判断が必要 |
| New Public repo RenCrow_CORE creation | blocked | external target repo not created in this turn | GitHub repo 作成と投入は Human approval / target remote が必要 |

## Public Preflight Findings

### Tracked local / secret / artifact path candidates

- `.env.example`: sample。公開可否レビュー対象。
- `config.yaml`: local IP、local path、SSH host、runtime endpoint を含む machine-local config。Public repo から除外する。
- `config.yaml.example`, `config/config.yaml.example`: sample。公開可否レビュー対象。
- `tmp/stt_inputs/*`, `tmp/stt_test_*`, `tmp/viewer_test_recording_script*.md`: test artifact / fixture。Public repo では除外または fixture として明示する。
- `docs/archive/unreferenced_20260701/STT_TTS/tmp/*`, `docs/refs/STT_TTS/tmp/*`: tmp path 配下 docs。公開可否レビュー対象。
- `artifact` / `cache` を名前に含む docs / source: 文脈確認が必要。

### Large tracked file candidates

- `kb-admin`: 70M ELF binary。Public repo から除外する。
- `go1.25.0.linux-amd64.tar.gz`: 57M Go distribution archive。Public repo から除外する。
- `docs/refs/understand-anything/intermediate/structure-output.json`: 8.9M generated analysis artifact。Public repo から除外候補。
- `docs/refs/understand-anything/knowledge-graph.json`: 4.7M generated analysis artifact。Public repo から除外候補。
- `assets/figma/*.png`, Viewer character images, gif/mp4 assets: public artifact として必要か owner 判断が必要。

### Textual secret keyword candidates

Scan output is mostly test fixtures, detector tests, code variable names, docs examples, and redaction tests. No real `sk-...` secret was confirmed in the sampled first 200 lines. This remains a review item before Public repo export.

## Next QA Actions

1. Run full `GOCACHE=/tmp/picoclaw-gocache go test ./...` and `GOCACHE=/tmp/picoclaw-gocache go vet ./...` after docs sync.
2. Create an export exclusion manifest for `RenCrow_CORE` Public seed.
3. Decide whether tracked binaries / local config should be removed from this staging repo or excluded only during Public export.
4. Perform broad live route/module/feature verification against the rebuilt service.
5. After owner approval, create / configure new Public repository `RenCrow_CORE`, export seed, and verify clean clone.

## Verification Update

2026-07-02 JST update:

| Check | Result |
| --- | --- |
| feature scaffold audit | PASS (`audit_status=0`) |
| `GOCACHE=/tmp/picoclaw-gocache go test ./modules/...` | PASS |
| `GOCACHE=/tmp/picoclaw-gocache go test ./cmd/picoclaw ./internal/features/... ./internal/adapter/viewer ./modules/...` | PASS |
| `GOCACHE=/tmp/picoclaw-gocache go test ./...` | PASS |
| `GOCACHE=/tmp/picoclaw-gocache go vet ./...` | PASS |
| `git diff --check` | PASS |
| `.rencrow-core-exportignore` | ADDED |

## Updated Status Notes

- Public repo initial README is now represented by root `README.md`.
- Public repo exclusion manifest is now represented by `.rencrow-core-exportignore`.
- `docs/README.md` and `docs/00_読む順番.md` now point to the Public repo 起点化仕様.
- Full repository static validation has passed after docs / README / export manifest changes.

## Remaining Non-Static Work

- Broad live verification for every module endpoint / feature group route is still required before final completion.
- New Public repository `RenCrow_CORE` has not been created or populated in this turn.
- Owner decision is still required on whether tracked local/binary/artifact candidates are removed from staging repo history, excluded only during export, or moved to release artifacts.

## Live Broad Check Update

After rebuilding and restarting the service through `picoclaw.service`, broad route/module/feature availability check passed.

Result file: `/tmp/rencrow_core_live_broad_check.tsv`

Summary:

- `core`: `/health` PASS.
- `modules`: manifest, health, chat route, LLM, Worker, TTS, STT diagnostics PASS.
- `channels`: `/webhook` route present; optional Telegram / Discord / Slack return 503 unavailable when token is not configured, which is expected visible-state behavior.
- `viewer`: page, runtime config, SSE, status, jobs, logs, character / Live2D routes PASS.
- `idlechat`: status route PASS.
- `ops`: backlog, scheduler, workstream, revenue, job detail routes PASS.
- `voice`: TTS audio proxy, playback ACK, active control, audio SSE, STT routes, VoiceChat websocket routes PASS.
- `web`: browser trace and complexity routes PASS.
- `knowledge / memory`: source registry, knowledge memory, memory snapshot/layers/recall routes PASS.
- `governance`: evidence, skill governance, tool harness, DCI, SuperAgent, AI workflow routes PASS. Verification and Sandbox return 503 unavailable in this runtime, treated as route-present visible unavailable rather than 404 route loss.


## Final Static Validation Update For This Work Unit

After channel unavailable-route fix and live broad route check:

| Check | Result |
| --- | --- |
| `GOCACHE=/tmp/picoclaw-gocache go test ./internal/features/channels` | PASS |
| `GOCACHE=/tmp/picoclaw-gocache go test ./cmd/picoclaw ./internal/features/... ./internal/adapter/viewer ./modules/...` | PASS |
| `GOCACHE=/tmp/picoclaw-gocache go test ./...` | PASS |
| `GOCACHE=/tmp/picoclaw-gocache go vet ./...` | PASS |
| `git diff --check` | PASS |

The remaining completion work is not code/test failure. It is the external Public repo creation/export decision and clean clone verification.

## Local Export Snapshot Verification

Local export snapshot was created from pushed HEAD `2bf3788b` using `git archive HEAD` and Public seed exclusions.

Export directory:

```text
/tmp/RenCrow_CORE_Ver0.80_seed_2bf3788b
```

Removed from the snapshot before verification:

- `config.yaml`
- `kb-admin`
- `go1.25.0.linux-amd64.tar.gz`
- `tmp/`
- generated understand-anything intermediate / knowledge graph artifacts
- runtime state, caches, DB-like files, secret/key-like files

Remaining path keyword candidates after exclusion are false-positive source/docs names such as `artifact_cleanup` source, `artifact_policy.md`, `cache-risk.md`, and `config.yaml.example`.

Snapshot verification:

| Check | Result |
| --- | --- |
| `GOCACHE=/tmp/rencrow-core-export-gocache go test ./modules/...` in export snapshot | PASS |
| `GOCACHE=/tmp/rencrow-core-export-gocache go test ./cmd/picoclaw ./internal/features/... ./internal/adapter/viewer ./modules/...` in export snapshot | PASS |

This proves the local Public seed snapshot can run the module contract and representative composition / feature / Viewer tests after excluding local config and generated artifacts.

## Remaining External Step

The remaining step is external: create / configure the new Public GitHub repository `RenCrow_CORE`, push the export snapshot, then clean clone that public repo and rerun the same checks. This should be done only after owner approval of the public target and export exclusion policy.
