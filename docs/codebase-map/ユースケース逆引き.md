---
generated_at: "2026-05-15T08:05:48Z"
run_id: run_20260515_080548
phase: 2
step: "11"
profile: rencrow-core-map
artifact: usecase_reference
---

# ユースケース逆引き

## 概要

主要ユースケースから読むべきコードを逆引きする地図である。  
行番号ではなく責務と結合点を示し、詳細確認時は Serena の `get_symbols_overview` / `find_symbol` で対象 symbol を追う。

## 関連ドキュメント

- `docs/codebase-map/結合ポイントマップ.md`
- `docs/codebase-map/modules/*.md`

## ユースケース一覧

### chat_message: ユーザー入力から Chat/Worker/Coder ルーティング

読む順:

1. `internal/adapter/line/handler.go` または `internal/adapter/viewer/handler.go`
2. `internal/application/orchestrator/message_orchestrator.go`
3. `internal/domain/routing/route.go`
4. `internal/domain/agent/mio.go`
5. route に応じて Shiro/Coder/Wild/Heavy/Worker 実装

注意:

- 入口ごとに session/channel/chatID の作り方が異なる。
- route 判定だけで完了ではなく、route-specific execution と response assembly まで追う。

### code_execution: Coder proposal から Worker 実行

読む順:

1. `internal/application/orchestrator/message_orchestrator.go`
2. `internal/domain/proposal/`
3. `internal/domain/patch/command.go`
4. `internal/application/service/worker_execution_service.go`
5. `internal/infrastructure/security/` と `internal/infrastructure/tools/`

注意:

- Coder は plan/patch を作る役、実行主体は Worker。
- file edit/shell/git operation は protected pattern と workspace 制約の確認が必須。

### idlechat: IdleChat セッションと Viewer 表示

読む順:

1. `internal/application/idlechat/orchestrator.go`
2. `cmd/picoclaw/main.go` の IdleChat handler 配線
3. `internal/adapter/viewer/handler.go` と `internal/adapter/viewer/assets/js/tabs/idlechat.js`
4. TTS/VTuber 関連: `internal/application/orchestrator/tts_support.go`, `internal/infrastructure/tts/`

注意:

- LLM raw response、表示文、TTS chunk、口パク trigger を混同しない。
- fallbackIdleResponse はエラー経路として扱う。

### memory_source_registry: 記憶・Source Registry・staging/validator

読む順:

1. `internal/infrastructure/persistence/conversation/l1_sqlite_store.go`
2. `internal/application/sourcefetcher/registry_sweeper.go`
3. `internal/adapter/viewer/source_registry_handler.go`
4. `internal/adapter/viewer/memory_*_handler.go`
5. `internal/domain/memory/` と `internal/domain/conversation/`

注意:

- observed/candidate/confirmed の state を勝手に飛ばさない。
- Source Registry の無審査追加や confirmed/pinned memory への直接昇格は禁止。

### audio_viewer: Viewer 音声/STT/TTS

読む順:

1. `cmd/picoclaw/stt_runtime_factory.go`, `cmd/picoclaw/tts_runtime_factory.go`
2. `internal/infrastructure/stt/`, `internal/infrastructure/tts/`
3. `internal/adapter/viewer/stt_capture_handler.go`, `audio_router_sse.go`
4. Viewer JS/CSS の該当 tab

注意:

- ブラウザ権限、HTTPS、SSE、provider timeout が絡む。
- Goテストだけでなく実ブラウザの録音/再生を確認する。
