# ストーリーモード — 仕様と実装状況

**作成日**: 2026-03-21
**ブランチ**: `feature/rencrow`
**更新時は「実装状況」欄のみ書き換えること（仕様欄は意思決定の記録）**

---

## 1. 実装したい仕様

### 1.1 品質目標（最上位）

「生成が成功する」ではなく「ユーザーに見せる価値のある物語」を安定して出力する。

**合格基準（全条件を満たすこと）**:

| # | 条件 |
|---|---|
| Q1 | タイトルを読まずに元話が認識できる |
| Q2 | 出来事として描写される（テーマの説明・説教でなく） |
| Q3 | 機能する twist が見える（元話とどこが違うかが分かる） |
| Q4 | 余韻で終わる（教訓の言い直し・総括でなく） |
| Q5 | `StorySource.Text` の単純な再話より明らかに良い |
| Q6 | 人が「面白かった」と言いそう |

**判定基準**: 3回連続ライブプローブが Q1〜Q6 をすべて通過するまで成功とみなさない。

---

### 1.2 パイプライン仕様

```
RunStorySession()
├── Step 1: スタイル選択          [決定論的] chooseStoryRewriteStyle()
└── loop (最大3ソース試行)
    ├── Step 2-6: prepareStory()  [決定論的・LLMなし]
    │   ├── 2. ソース選択          selectStorySourceExcluding()
    │   ├── 3. ソース分析          analyzeStorySource()
    │   ├── 4. 改変プラン生成      buildStoryRewritePlan()
    │   ├── 5. ビートプラン生成    groundedStoryBeatPlan()
    │   └── 6. アダプテーション    buildStoryAdaptationPlan()
    │
    │   [Step 6.5: ヒューマンレビューゲート — preview-run モードのみ]
    │
    ├── Step 7: 第1稿生成          [LLM: shiro × 4ビート, MaxTokens=300, Temp=0.6]
    │   └── 失敗時: deterministicStoryDraft → safeStoryRetelling（安全装置）
    └── Step 8: 改稿               [LLM: shiro, MaxTokens=1800, Temp=0.5]
        └── 失敗時: repairStoryDraft → deterministicStoryDraft（安全装置）
```

**設計原則**:
- Step 2〜6 は決定論的。LLM は Step 7・8 のみ
- フォールバック（deterministicStoryDraft / safeStoryRetelling）は**安全装置**であり**目標ではない**
- フォールバックで生成された再話は品質合格とみなさない

---

### 1.3 データ仕様

**コーパス**: 17作品（アクティブ）+ 10作品（アーカイブ）

| 区分 | 件数 | 場所 |
|------|------|------|
| アクティブ | 17 | `data/story/*.json` |
| アーカイブ（データ欠損） | 10 | `data/story/archive/*.json` |

**アーカイブ対象作品**（`data/story/archive/`）:
`aladdin`, `ali40`, `beauty`, `bremen`, `bunbuku`, `frogprince`, `hansel`, `kintaro`, `puss`, `sleepingbeauty`

アーカイブ理由: `text` フィールドの scene 密度不足など、必須フィールドのデータが基準を満たさない。
アーカイブ作品は生成対象外。データを補完次第アクティブに復帰可能。

**各 JSON の必須フィールド**:
- `text`: 場面順序が分かる長めの scene-aware synopsis（梗概ではなく、主要イベントの流れを含む）
- `spec.skeleton.required_beats`: 4ビート（導入・逸脱・反転・着地相当）
- `spec.skeleton.recognition_cues`: 元話認識に必要な固有語（2個以上）
- `spec.twists`: 5スタイル全て（1文の抽象的構造観察）

**各 JSON のオプションフィールド**（空の場合はデフォルト値を使用）:
- `opening_seed`: 第1ビートの起点文。空の場合 `"{Title}は{Setting}で足を止め…"` を自動生成
- `setting`: 舞台設定（例: `"大晦日の路地と商店街"`）。空の場合 `"現代の地方都市とその周辺"` を使用

> **設計原則**: 物語固有のデータ（起点文・舞台）は JSON に持つ。Go コードに switch/case で埋め込まない。

**5スタイル**: `role_shift` / `view_shift` / `value_shift` / `inversion` / `scale_shift`

**4ジャンル**: ノワール / ホラー / コメディ / ノーマル

---

### 1.4 Step 7 プロンプト仕様（第1稿）

- 各ビートを**2〜4文の散文**として個別生成し結合
- 第1ビートは `openingSeed`（固定文）で始める
- 禁止事項をプロンプトで明示:
  - 「もし〜だったら」等で前置きせず、直接場面を書く
  - 情景描写だけで始めない（行動か対話で始める）
  - 元話テキストを一文そのまま使わない
  - 現代ハイテク・企業名・プロジェクト名を新作しない
  - メタ的な設計説明を本文に書かない

---

### 1.5 テスト仕様

**必須カバレッジ**:

| テスト対象 | 仕様 |
|---|---|
| Step 1 | 全5スタイルから直前と異なるスタイルが選ばれること |
| Step 2 | excluded / blocked セットによるソース除外が正しく機能すること |
| Step 3 | アクティブ17作品で `analyzeStorySource()` が非空の Motifs / TabooOrRule を返すこと |
| Step 4 | 5スタイル × 4ジャンルの全20通りで `StoryRewritePlan` の必須フィールドが非空であること |
| Step 5 | RequiredBeats が 0〜4件の各ケースで `StoryBeatPlan` が非空文字列を返すこと |
| Step 2-6 | アクティブ17作品 × 5スタイルで `prepareStory()` がパニックせず非空の `StoryPrep` を返すこと |

### 1.6 Step 6.5 仕様（ヒューマンレビューゲート）

Step 6（決定論的プラン生成）と Step 7（LLM 第1稿生成）の間に入るヒューマンレビューゲート。
`preview-run` モード専用。通常の `RunStorySession()` では省略される。

**フロー**:
```
preview-run SOURCE [STYLE]
├── Step 6:   BuildStoryPrepForSource()  → StoryPrep 生成
├── Step 6.5: 情報収集フェーズ（必ずこの順で全出力する）
│   ├── [必須] DumpStoryPrep() — ANALYSIS / PLAN / BEAT PLAN / ADAPTATION を全表示
│   ├── [必須] ScanStoryPlanProblems() — 既知アンチパターンを警告
│   ├── [必須] GenerateRawBeats() — BEAT 0〜3 の生文章（バリデーションなし）を表示
│   └── 確認プロンプト（Enter: 続行 / q: 中止）← ユーザーの OK が出るまで先に進まない
└── Step 7+8: GenerateFromPrep()  ← OK が出た場合のみ実行
    ├── DRAFT (Step 7 出力) 表示
    └── STORY (Step 8 出力) 表示
```

**目的**: プランが意図通りか人が確認してから LLM 生成コストをかける。品質問題の切り分けにも使用する。

---

## 2. 実装状況

### 2.1 パイプライン構造

| 項目 | 状況 | 備考 |
|---|---|---|
| Step 2〜6 を `prepareStory` に集約 | ✅ 完了 | 2026-03-18 |
| Step 4〜6 の LLM 呼び出しを削除 | ✅ 完了 | 決定論的処理に置換済み |
| `retryStoryBeatPlan` 等の死コード削除 | ✅ 完了 | |
| Go コード内 per-story skeleton 削除 | ✅ 完了 | JSON `spec.skeleton` に一元化 |

### 2.2 データ（data/story/*.json）

| 項目 | 状況 | 備考 |
|---|---|---|
| アクティブ17作品の JSON 化 | ✅ 完了 | |
| twists を新形式（map[string]string）に移行 | ✅ 完了 | アクティブ17作品 |
| skeleton / required_beats / recognition_cues | ✅ 完了 | アクティブ17作品 |
| `opening_seed` / `setting` フィールド追加 | ✅ 完了 | 2026-03-21。Go switch/case から JSON に移行 |
| `text` を scene-aware synopsis に拡充 | **⚠️ 未完** | 一部作品で梗概のまま。scene密度が低いと model が場面を飛ばす |
| データ欠損10作品をアーカイブへ移動 | ✅ 完了 | 2026-03-23。`data/story/archive/` に格納。補完後に復帰可能 |

### 2.3 品質保護（バリデーション）

| 項目 | 状況 | 備考 |
|---|---|---|
| `reviseStoryNarrative` の reject 強化 | ✅ 完了 | settling / prose / skeleton チェック |
| `retryStoryDraft` のフォールバック追加 | ✅ 完了 | deterministicStoryDraft → safeStoryRetelling |
| draft が再話に近い場合 revision をスキップ | ✅ 完了 | |
| `storyHasOutlineLanguage` / `storyHasOverblownSetting` | ✅ 完了 | |
| `storyHasDistractingDigression` | ✅ 完了 | |
| ビートごとの文数過多チェック（> 8文 → reject） | ✅ 完了 | 2026-03-21 |
| `storyOpensWithActionOrDialogue` 削除 | ✅ 完了 | 2026-03-21。トークンリスト方式は終わりが見えないと判断 |
| `storyStartsWithAtmosphere` 削除 | ✅ 完了 | 2026-03-21。同上 |
| **「もし〜だったら」前置き禁止のバリデーション** | **❌ 未実装** | プロンプトで禁止しているが validator なし |

### 2.4 第1稿品質（Step 7 の LLM 出力）

| 項目 | 状況 | 備考 |
|---|---|---|
| ビートごとの個別生成 | ✅ 実装済み | |
| `openingSeed` による第1文固定 | ✅ 実装済み | アクティブ17作品に固定文あり |
| **一次出力の品質** | **❌ 未達** | draft が3回失敗してフォールバックへ流れるケースが多い |
| **「もし〜」「（情景）」開幕が出続ける** | **❌ 未解決** | セッションログの7件中3件（43%）が問題開幕 |

### 2.5 テスト

| 項目 | 状況 | 備考 |
|---|---|---|
| Step 1（スタイル選択）| ⚠️ 部分的 | 直前スタイル除外のみ。全スタイル有効・空履歴は未テスト |
| Step 2（ソース選択）| ❌ 未テスト | excluded / blocked の動作検証なし |
| Step 3（17作品分析）| ⚠️ 部分的 | 4作品のみ個別テスト、残13作品は JSON フィールド存在確認のみ |
| Step 4（5スタイル × 4ジャンル）| ❌ 未テスト | `view_shift` のみ他テストの前段として使用 |
| Step 5（ビートプラン）| ❌ 未テスト | `view_shift` + `issun` のみ |
| Step 2-6 全組み合わせ（17 × 5）| ❌ 未テスト | `prepareStory` のパニック確認テストなし |

### 2.6 Step 6.5（ヒューマンレビューゲート）

| 項目 | 状況 | 備考 |
|---|---|---|
| `preview-run` モード実装 | ✅ 完了 | 2026-03-21 |
| `GenerateFromPrep()` メソッド | ✅ 完了 | `StoryGenerateResult` を返す |

### 2.7 品質合格判定

| 項目 | 状況 |
|---|---|
| 3回連続ライブプローブ合格 | **❌ 未達成** |

### 2.8 簡易ストーリーモード（story-simple）

`internal/application/idlechat/story_mode_simple.go` に実装された軽量版物語生成機能。
通常のストーリーモード（Step 2〜8）とは独立したワンショット生成フロー。

| 項目 | 状況 | 備考 |
|---|---|---|
| `RunSimpleStorySession()` 実装 | ✅ 完了 | `story_mode_simple.go` |
| `StartSimpleStoryMode()` 手動起動 | ✅ 完了 | |
| ランダム選択（10作品 × 14主人公候補） | ✅ 完了 | |
| forecastLLM（Coder2 相当）での生成 | ✅ 完了 | |
| 段落単位 Viewer 配信 | ✅ 完了 | `groupStoryIntoViewerParagraphs` 共用 |
| バリデーション・リトライ | ❌ なし | 意図的な非適用（ワンショット設計のため） |
| テスト | ❌ 未テスト | |

---

## 3. 残タスク（優先順）

| 優先 | タスク | 理由 |
|---|---|---|
| 1 | **「もし〜だったら」validator 追加** | プロンプト禁止だけでは漏れる |
| 2 | **`StorySource.Text` の scene 密度確認・補完（アクティブ17作品）** | text が薄い作品で model が場面を飛ばす |
| 3 | **Step 1-6 の組み合わせテスト追加** | prepareStory の 17 × 5 パニック確認が最小コスト |
| 4 | **3回連続ライブプローブで合格確認** | 成功の定義 |

### 完了済み（参考）

| 完了日 | タスク |
|---|---|
| 2026-03-23 | `[Story] beat N raw` デバッグログ削除（Q6） |
| 2026-03-23 | twists の生成核（`改変の核`）をビートプロンプトに渡す（Q2） |
| 2026-03-23 | Beat 3 プロンプトに EndingFlavor 具体指示を追加（Q3） |
| 2026-03-23 | revision MaxTokens を 1800 に固定、Temperature=0.5 で安定化（Q4） |
| 2026-03-23 | `storyNarrativeLooksLikeProse` の actionHits チェック削除（Q5） |

> 品質問題の詳細（症状・なぜそうなったか）は `docs/story_quality_issues.md` を参照。

---

## 4. ライブプローブ手順

```bash
# テスト
env GOCACHE=/home/nyukimi/picoclaw_multiLLM/.gocache \
    GOMODCACHE=/home/nyukimi/picoclaw_multiLLM/.serena/gomodcache \
    go test ./internal/application/idlechat

# ビルド
env GOCACHE=/home/nyukimi/picoclaw_multiLLM/.gocache \
    GOMODCACHE=/home/nyukimi/picoclaw_multiLLM/.serena/gomodcache \
    go build -o test-story ./cmd/test-story

# 全ソース一覧
./test-story list

# プランのみ確認（LLM不使用）
./test-story dump-plan SOURCE [STYLE]
./test-story dump-all SOURCE

# プランを確認してから LLM 生成（ヒューマンレビューあり）
./test-story preview-run SOURCE [STYLE]

# アンチパターンスキャン
./test-story scan SOURCE STYLE
./test-story scan-all

# フル実行（特定ソース強制）
STORY_SOURCE=momotaro timeout 180s ./test-story
```

**合否の確認ポイント**:
- `story_text` が `story_draft_text` より良いか
- タイトルを隠して読んで元話が分かるか
- 「もし〜だったら」「（情景）」で始まっていないか
- 出来事として書かれているか（説明・説教でないか）
- ひねり（twist）が文章に現れているか（倉庫に変えただけ、ではないか）

---

**参照**:
- `docs/実装仕様_ストーリーモード_v1.md` — データ構造・全関数詳細
- `internal/application/idlechat/story_mode.go` — 実装本体
- `sessions/idlechat_topics.jsonl` — 実際の生成ログ
- `docs/story_quality_issues.md` — 観察された品質問題の蓄積（症状・なぜそうなったか）
- `~/.codex/skills/idlechat-story-tuning/` — チューニング作業フロー
