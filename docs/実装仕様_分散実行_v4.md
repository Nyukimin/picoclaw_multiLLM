# PicoClaw 実装仕様 v4.0 分散実行対応版

**作成日**: 2026-03-02
**バージョン**: 4.0
**ベース**: v3.0（Clean Architecture）+ Chat_Worker_Coder_アーキテクチャ.md（分散実行設計）
**ステータス**: 実装仕様（v3.0からの拡張）

---

## 目次

- [0. エグゼクティブサマリ](#0-エグゼクティブサマリ)
- [1. 背景と目的](#1-背景と目的)
- [2. アーキテクチャ概要](#2-アーキテクチャ概要)
- [3. Phase 1: ローカル通信層の確立](#3-phase-1-ローカル通信層の確立)
- [4. Phase 2: リモート通信基盤](#4-phase-2-リモート通信基盤)
- [5. Phase 3: 分散環境対応](#5-phase-3-分散環境対応)
- [6. 通信フォーマット仕様](#6-通信フォーマット仕様)
- [7. Session/Memory分散管理](#7-sessionmemory分散管理)
- [8. セキュリティとエラーハンドリング](#8-セキュリティとエラーハンドリング)
- [9. 実装プラン](#9-実装プラン)
- [10. テスト戦略](#10-テスト戦略)
- [11. 移行パス](#11-移行パス)
- [付録A: JSON Schema定義](#付録a-json-schema定義)
- [付録B: SSH接続設定例](#付録b-ssh接続設定例)
- [付録C: 用語集](#付録c-用語集)

---

## 0. エグゼクティブサマリ

### 0.1 v4.0の位置付け

**背景**:
- v3.0（Clean Architecture）で単一プロセス内の実装が完成
- `docs/Chat_Worker_Coder_アーキテクチャ.md` で分散実行の理想像が定義
- しかし、**実装可能な技術仕様が不足**し、分散実行機能は未実装

**v4.0の目的**:
- v3.0の上に**分散実行機能を追加**
- Agent間通信をSSH + JSON化
- 複数CPU/マシンでAgentを並列実行
- **v3.0との完全な互換性**を維持（設定フラグで切り替え）

### 0.2 期待効果

| 効果 | 詳細 | KPI |
|------|------|-----|
| **負荷分散** | LLM呼び出しを複数CPUに分散 | CPU使用率 70% → 40% |
| **並列実行** | Worker内PatchCommand並列実行（v4.0）、タスク分割は将来（v4.1） | 応答時間 20-30%短縮（v4.0）、50%短縮（v4.1目標） |
| **スケーラビリティ** | マシン追加でAgent数を増やせる | 1マシン → 4マシン対応 |
| **独立性** | Agent単位でのアップデート・再起動 | ダウンタイム 90%削減 |
| **保守性** | Agent単位の障害分離 | 障害影響範囲 -75% |
| **透明性** | すべての通信がログに記録 | デバッグ時間 -60% |

### 0.3 実装期間

**8週間**（Phase 1-3）+ 本番移行4週間

| Phase | 期間 | 内容 | マイルストーン |
|-------|------|------|--------------|
| Phase 1 | 2週間 | ローカル通信層 | Transport抽象化完成、v3.0共存確認 |
| Phase 2 | 3週間 | リモート通信基盤 | SSH通信動作、E2Eテスト成功 |
| Phase 3 | 2週間 | 分散環境対応 | Session同期、本番移行準備完了 |
| 本番移行 | 4週間 | 段階的移行 | 本番環境で分散実行稼働 |

### 0.4 主要設計決定

1. **Transport層抽象化**: Local/SSH透過的切り替え（`internal/domain/transport/`）
2. **SSH + JSON通信**: SSH経由でstdin/stdoutにJSON送受信
3. **Agent スタンドアロンモード**: リモートマシンで独立起動（`cmd/picoclaw-agent/`）
4. **階層的Session同期**: Coder → Worker → Mio の会話ログ伝播
5. **v3.0互換性**: 設定フラグ（`distributed.enabled`）で切り替え
6. **段階的移行**: 4ステップ、12週間で本番環境に展開

### 0.5 参照ドキュメント

- **`docs/Chat_Worker_Coder_アーキテクチャ.md`** （484-661行目）: 分散実行の設計思想
- **`docs/実装仕様_v3.md`**: v3.0のClean Architecture基盤
- **`docs/仕様.md`**: 上位要件

---

## 1. 背景と目的

### 1.1 現状の課題

#### 1.1.1 単一プロセスでのボトルネック

**v3.0の制約**:
- すべてのAgent（Mio、Shiro、Aka、Ao、Gin）が同一プロセス内で動作
- LLM呼び出し（重い処理）が順次実行され、並列化不可能
- Ollama常駐による単一マシンへの負荷集中

**具体的な問題**:
```
ユーザー: 「3つのファイルを同時に作成して」
  ↓
Mio → Shiro → Gin（ファイル1作成）→ 完了
            → Gin（ファイル2作成）→ 完了  ← 順次実行
            → Gin（ファイル3作成）→ 完了  ← 並列化できない
  ↓
合計時間: 15秒（5秒 × 3ファイル）
```

**v4.0の並列実行（Worker内PatchCommand並列）**:
```
ユーザー: 「3つのファイルを同時に作成して」
  ↓
Mio → Worker → Coder3（Proposal生成: 3ファイル分のPatchCommand）
  ↓
Worker: PatchCommand並列実行
  ├─ [goroutine1] file_edit/create: ファイル1
  ├─ [goroutine2] file_edit/create: ファイル2
  └─ [goroutine3] file_edit/create: ファイル3
  ↓
合計時間: Coder3生成5秒 + ファイル作成（並列）1秒 = 6秒
v3.0比: Coder3生成5秒 + ファイル作成（直列）3秒 = 8秒 → 25%改善
```

**v4.1の並列実行（タスク分割、将来）**:
```
ユーザー: 「3つのサービスを設計して」
  ↓
Mio → タスク分割 → [CPU1] Coder1（サービスA）並列実行
                  → [CPU2] Coder2（サービスB）並列実行
                  → [CPU3] Coder3（サービスC）並列実行
  ↓
合計時間: max(Coder1, Coder2, Coder3) = 5秒（並列実行）
```

#### 1.1.2 スケーラビリティの限界

| 項目 | v3.0の制約 | 影響 |
|------|----------|------|
| **メモリ** | 単一プロセスで全Agentを管理 | メモリ使用量 >100MB（目標<10MB破綻） |
| **CPU** | LLM呼び出しがシリアル実行 | CPU使用率 70%でボトルネック |
| **ネットワーク** | Ollama接続が単一マシンに集中 | ネットワーク帯域がボトルネック |
| **拡張性** | マシン追加してもスケールしない | 性能向上の上限 |

#### 1.1.3 保守性の課題

- Agent単位のアップデート不可能（全体を再起動）
- 障害時の影響範囲が全Agent（1つが落ちると全停止）
- ログが単一プロセスに集約され、デバッグ困難

### 1.2 分散実行のメリット

#### 1.2.1 負荷分散

```
[v3.0] 単一プロセス
  CPU1: Mio + Shiro + Gin + Aka + Ao (100%負荷)
  CPU2: (未使用)
  CPU3: (未使用)
  CPU4: (未使用)

[v4.0] 分散実行
  CPU1: Mio (40%負荷)
  CPU2: Shiro (40%負荷)
  CPU3: Gin (40%負荷)
  CPU4: Aka + Ao (40%負荷)
```

#### 1.2.2 並列実行

**v4.0スコープ**: Transport層の並列化 + Worker内PatchCommand並列実行

```
[v4.0] Worker内PatchCommand並列実行:

タスク: 「hello.goを作成してテストも実行して」
  ↓
Mio → Worker → Coder3（Proposal生成）→ patch返却
  ↓
Worker: PatchCommand並列実行
  ├─ [goroutine1] file_edit: hello.go作成
  ├─ [goroutine2] file_edit: hello_test.go作成
  └─ 依存関係のあるコマンドは順次実行（shell_command: go test）
  ↓
合計時間: ファイル作成は並列、テスト実行は直列
```

**v4.1スコープ（将来）**: タスク分割による複数Coder並列実行

```
[v4.1] タスク分割（将来実装）:

タスク: 「3つのマイクロサービスを設計して」
  ↓
Mio → タスク分割判定 → [並列実行]
               ├─ [CPU1] Aka: サービスAの設計
               ├─ [CPU2] Ao: サービスBの実装
               └─ [CPU3] Gin: サービスCの実装
  ↓
合計時間: max(Aka, Ao, Gin) = 10秒
v3.0では: Aka + Ao + Gin = 30秒
```

**注意**: v4.0では1タスク=1Coderの原則を維持する。タスク分割ロジック（どこで分割するか、分割結果の統合方法）はv4.1で設計する。

#### 1.2.2.1 Type-Based Phased Execution Model（v4.0）

Worker内PatchCommand並列実行の依存関係は、**コマンドType別の3フェーズ実行モデル**で管理する。

```
Phase 1: file_edit（並列実行）
  ├─ [goroutine] create hello.go
  ├─ [goroutine] create hello_test.go
  ├─ [goroutine] mkdir internal/pkg
  └─ sync.WaitGroup.Wait()  ← 全file_edit完了を待機
         ↓
Phase 2: shell_command（順次実行）
  ├─ run "go fmt ./..."
  └─ run "go test ./..."
         ↓
Phase 3: git_operation（順次実行）
  ├─ add -A
  └─ commit -m "..."
```

**設計原則**:

1. **Phase 1（file_edit）**: 全`file_edit`コマンドをgoroutineで並列実行。ファイルI/Oは独立しているため安全に並列化できる。ただし同一ファイルへの複数操作（create後にappend等）がある場合は、**同一Targetのコマンドは出現順に直列化**する。
2. **Phase 2（shell_command）**: `shell_command`は副作用が予測不可能（プロセス起動、ファイル読み書き等）のため、**常に順次実行**する。Phase 1の全完了後に開始。
3. **Phase 3（git_operation）**: `git_operation`は排他的操作（`.git/index` ロック）のため、**常に順次実行**する。Phase 2の全完了後に開始。

**フェーズ分割の実装**:

```go
// partitionByPhase はPatchCommandをフェーズ別に分割する
func partitionByPhase(commands []patch.PatchCommand) (
	fileEdits []patch.PatchCommand,
	shellCmds []patch.PatchCommand,
	gitOps []patch.PatchCommand,
) {
	for _, cmd := range commands {
		switch cmd.Type {
		case patch.TypeFileEdit:
			fileEdits = append(fileEdits, cmd)
		case patch.TypeShellCommand:
			shellCmds = append(shellCmds, cmd)
		case patch.TypeGitOperation:
			gitOps = append(gitOps, cmd)
		}
	}
	return
}

// groupByTarget は同一Targetのfile_editをグループ化する
// 同一ファイルへの複数操作（create→append等）を順序保証する
func groupByTarget(fileEdits []patch.PatchCommand) map[string][]patch.PatchCommand {
	groups := make(map[string][]patch.PatchCommand)
	for _, cmd := range fileEdits {
		groups[cmd.Target] = append(groups[cmd.Target], cmd)
	}
	return groups
}
```

**StopOnErrorとの相互作用**:
- `StopOnError=true`の場合: Phase 1で1つでもエラーが発生したら、他のgoroutineの完了を待った後、Phase 2/3をスキップする
- `StopOnError=false`の場合: 全Phaseを実行し、エラーを集約して報告する

**AutoCommitとの相互作用**:
- Git auto-commit（実行前）は Phase 1 開始前に実行（v3と同じ）
- Git auto-commit（実行後）は Phase 3 完了後に実行（v3と同じ）
- Phase 3 に明示的なgit_operationがある場合、auto-commitとの競合を防ぐため、auto-commitは Phase 3 の**後**に実行する

#### 1.2.3 スケーラビリティ

| スケール要因 | v3.0 | v4.0 |
|------------|------|------|
| マシン追加 | ✗ 効果なし | ✓ Agent数に比例して性能向上 |
| CPU追加 | ✗ 効果なし | ✓ 並列度向上 |
| メモリ追加 | △ 単一マシン限界 | ✓ 分散でメモリ使用量削減 |

#### 1.2.4 独立性

**Agent単位の管理**:
- ✓ Agentごとにバージョン管理可能
- ✓ Agentごとに再起動可能（他のAgentは継続稼働）
- ✓ 障害時の影響範囲が1Agentのみ

### 1.3 設計原則（不変）

**v3.0からの継承**:
1. **Clean Architecture 4層構造**: Domain → Application → Infrastructure → Adapter
2. **責務分離**: Mio（全体）、Shiro（Coderオーケストレーター）、Coder（実装）
3. **日本語会話**: Agent間通信は日本語メッセージ（JSONパラメータ）
4. **DDD**: エンティティ、値オブジェクト、リポジトリパターン

**v4.0の追加設計原則**:
1. **Transport層抽象化**: Local/Remote透過的切り替え
2. **疎結合**: Agent間依存は通信インターフェースのみ
3. **v3.0互換性**: 設定フラグで既存動作を維持
4. **段階的移行**: Phase 1-3で段階的に機能追加

### 1.4 Agent名称の統一定義

ドキュメント・設定・コードで使用するAgent名称を以下のように統一定義する。

| Transport ID（通信用） | 設定キー（config.yaml） | ニックネーム（ドキュメント用） | v3コード上の変数名 | 役割 |
|------------------------|------------------------|-------------------------------|-------------------|------|
| `"Mio"` | `mio` | Mio | `mio` | Chat/ルーティング |
| `"Worker"` | `worker` | Shiro | `shiro` | 実行/オーケストレーション |
| `"Coder1"` | `coder1` | Aka | `coder1` | 仕様設計（DeepSeek） |
| `"Coder2"` | `coder2` | Ao | `coder2` | 実装（OpenAI） |
| `"Coder3"` | `coder3` | Gin | `coder3` | 高品質コーディング（Claude） |

**使い分けルール**:
- **`Message.From` / `Message.To`**: Transport ID を使用（例: `"Worker"`, `"Coder3"`）
- **`config.yaml` の `distributed.transports`**: 設定キーを使用（例: `worker`, `coder3`）
- **ドキュメント本文・アーキテクチャ図**: ニックネーム使用可だが、初出時にTransport IDを併記

**注意**: v3コードの `shiro` 変数名は変更しない（後方互換性維持）。Transport IDのみ `"Worker"` を正とする。

### 1.5 非機能要件

| 項目 | 要件 | 検証方法 |
|------|------|---------|
| **後方互換性** | v3.0機能が100%動作 | `distributed.enabled: false` で全テスト通過 |
| **パフォーマンス** | v3.0比でレイテンシ増加20%以内 | ベンチマークテスト |
| **可用性** | Agent障害時も他のAgentは継続稼働 | 障害注入テスト |
| **セキュリティ** | SSH公開鍵認証必須 | セキュリティ監査 |
| **透明性** | すべての通信をログに記録 | ログ完全性チェック |

---

## 2. アーキテクチャ概要

### 2.1 v3.0（現在）の構成

```
[LINE/Slack] → [Adapter] → [Application (Orchestrator)]
                              ↓（直接関数呼び出し）
                            [Domain (Mio/Shiro/Coder)]
                              ↓
                            [Infrastructure (LLM Provider)]
```

**特徴**:
- すべてのAgentが同一プロセス内で動作
- Agent間通信はGo関数呼び出し（`mio.DecideAction()`, `shiro.Execute()`等）
- ローカル変数としてTask、Proposalを受け渡し

### 2.2 v4.0（将来）の構成

```
[LINE/Slack] → [Adapter] → [Application (Orchestrator)]
                              ↓（Transport経由）
[CPU1: Mio]  ←→ [Transport Layer (Local/SSH)]
                              ↓
[CPU2: Shiro] ←→ [Transport Layer]
                              ↓
[CPU3: Gin]   ←→ [Transport Layer]
```

**特徴**:
- Agent間通信はTransport層経由（Local or SSH）
- Agent間メッセージはJSON形式
- Agentは物理的に別CPU/マシンで動作可能

### 2.3 Transport層の責務

**Transport層**（`internal/infrastructure/transport/`）:
1. **通信経路の抽象化**: Local（同一プロセス）とRemote（SSH）の透過的切り替え
2. **メッセージ送受信**: JSON形式のメッセージをAgent間で送受信
3. **接続管理**: SSH接続の確立、再接続、切断
4. **エラーハンドリング**: タイムアウト、再接続、ログ記録
5. **ルーティング**: Agent名に基づくメッセージ配送

**Transport インターフェース**:
```go
type Transport interface {
    Send(ctx context.Context, msg Message) error
    Receive(ctx context.Context) (Message, error)
    Close() error
    IsHealthy() bool
}
```

### 2.4 既存コードとの互換性

**不変（v3.0と同じ）**:
- ✅ `internal/domain/agent/interfaces.go` - Agent インターフェース定義
- ✅ `internal/domain/agent/mio.go`, `shiro.go`, `coder.go` - Agent エンティティ
- ✅ `internal/domain/proposal/proposal.go` - Proposal値オブジェクト
- ✅ `internal/domain/patch/patch.go` - Patch値オブジェクト

**追加（v4.0）**:
- ➕ `internal/domain/transport/` - Transport抽象化（Domain層）
- ➕ `internal/infrastructure/transport/` - Transport実装（Local/SSH）
- ➕ `cmd/picoclaw-agent/` - Agentスタンドアロンモード
- ➕ `config.yaml` の `distributed` セクション
- ➕ `internal/application/orchestrator/distributed_orchestrator.go` - 分散実行用オーケストレーター（新規）

**変更（中規模）**:
- 🔧 `internal/application/orchestrator/message_orchestrator.go` - Transport層との統合（§2.4.1参照）
- 🔧 `internal/application/service/worker_execution_service.go` - PatchCommand並列実行対応（§2.4.2参照）
- 🔧 `internal/adapter/config/config.go` - DistributedConfig追加（§4.4参照）

#### 2.4.1 Orchestrator移行設計

**課題**: v3の`MessageOrchestrator`は同期的な関数呼び出し（`MioAgent.DecideAction()`, `ShiroAgent.Execute()`, `CoderAgent.Generate()`）で設計されている。v4のTransport層は非同期メッセージング（`Send()`/`Receive()`）であり、設計パラダイムが根本的に異なる。

**移行戦略**: Adapter パターンによる段階的移行

```
[v3 MessageOrchestrator]
  └─ MioAgent.DecideAction(ctx, task) → (Decision, error)   ← 同期関数呼び出し
  └─ ShiroAgent.Execute(ctx, task) → (string, error)         ← 同期関数呼び出し
  └─ CoderAgent.Generate(ctx, task, prompt) → (string, error) ← 同期関数呼び出し

[v4 DistributedOrchestrator]
  └─ Transport.Send(ctx, msg) → error                        ← 非同期メッセージング
  └─ Transport.Receive(ctx) → (Message, error)               ← ブロッキング受信
```

**実装方針**:

1. **v3 MessageOrchestrator は維持**（`distributed.enabled: false` 時に使用）
2. **v4 DistributedOrchestrator を新規作成**（`distributed.enabled: true` 時に使用）
3. **共通インターフェースで切り替え**:

```go
// Orchestrator は v3/v4 共通のインターフェース
type Orchestrator interface {
    ProcessMessage(ctx context.Context, req ProcessMessageRequest) (ProcessMessageResponse, error)
}

// v3: 同期関数呼び出し（既存）
type MessageOrchestrator struct { /* ... */ }

// v4: Transport層経由の非同期メッセージング（新規）
type DistributedOrchestrator struct {
    sessionRepo     SessionRepository
    router          *MessageRouter      // Transport層のルーター
    responseTimeout time.Duration       // レスポンスタイムアウト
}

// v4のProcessMessage: Send → Receive パターン
func (o *DistributedOrchestrator) ProcessMessage(ctx context.Context, req ProcessMessageRequest) (ProcessMessageResponse, error) {
    // 1. セッションをロードまたは作成
    sess, err := o.loadOrCreateSession(ctx, req.SessionID, req.Channel, req.ChatID)
    if err != nil {
        return ProcessMessageResponse{}, err
    }

    // 2. タスクメッセージを構築
    jobID := task.NewJobID()
    msg := transport.Message{
        From:      "Mio",
        To:        "Mio",  // 自分自身（ルーティング判定用）
        SessionID: sess.ID(),
        JobID:     jobID.String(),
        Message:   req.UserMessage,
    }

    // 3. Mio（ルーティング）→ Worker/Coder → 結果受信
    //    Transport層がメッセージを中継し、結果がReceive()で返る
    resultMsg, err := o.sendAndWaitForResult(ctx, msg)
    if err != nil {
        return ProcessMessageResponse{}, err
    }

    // 4. 結果をレスポンスに変換
    return o.buildResponse(resultMsg, jobID), nil
}
```

**影響範囲の見積もり**:

| 変更箇所 | 変更量 | 説明 |
|----------|--------|------|
| `message_orchestrator.go` | 小 | Orchestratorインターフェース抽出のみ |
| `distributed_orchestrator.go` | **新規（約300行）** | Transport層経由の非同期オーケストレーション |
| `cmd/picoclaw/main.go` | 中 | `distributed.enabled` に応じた切り替えロジック |
| DI/Wire | 中 | DistributedOrchestrator用の依存注入 |

**リスク**: v3とv4で同じ `ProcessMessageRequest`/`ProcessMessageResponse` を使うことで、アダプター層の外部互換性を保証する。

#### 2.4.2 Worker並列実行の実装設計

**課題**: v3の`workerExecutionService.ExecuteProposal()`はPatchCommandを`for`ループで順次実行している（`worker_execution_service.go:63-72`）。v4.0ではType-Based Phased Execution Model（§1.2.2.1）に基づく並列実行が必要。

**移行戦略**: 設定フラグによる切り替え

```go
// WorkerConfig に並列実行設定を追加
type WorkerConfig struct {
    // ... v3既存フィールド（AutoCommit, StopOnError, etc.）...

    // v4.0 追加
    ParallelExecution bool `yaml:"parallel_execution"` // true で並列実行モード
    MaxParallelism    int  `yaml:"max_parallelism"`     // file_edit並列数上限（デフォルト: 4）
}
```

**実装**: `worker_execution_service.go` に追加

```go
import (
    "sync"
    // ... 既存インポート ...
)

// ExecuteProposal は Proposal の Patch を解析・実行する（v4.0拡張版）
func (w *workerExecutionService) ExecuteProposal(
    ctx context.Context,
    jobID task.JobID,
    p *proposal.Proposal,
) (*patch.PatchExecutionResult, error) {
    // 1. Patchをパース
    commands, err := patch.ParsePatch(p.Patch())
    if err != nil {
        return nil, fmt.Errorf("patch parse error: %w", err)
    }

    // 2. 実行前サマリ表示
    if w.config.ShowExecutionSummary {
        w.showExecutionSummary(jobID, commands)
    }

    // 3. Git auto-commit（実行前）
    if w.config.AutoCommit {
        preCommitHash, err := w.autoCommitChanges(ctx, jobID, "Before patch execution")
        if err != nil {
            return nil, fmt.Errorf("pre-execution auto-commit failed: %w", err)
        }
        fmt.Printf("[Worker] Pre-commit succeeded: %s\n", preCommitHash)
    }

    // 4. 実行（並列 or 順次）
    var result *patch.PatchExecutionResult
    if w.config.ParallelExecution {
        result = w.executeParallel(ctx, jobID, commands)
    } else {
        result = w.executeSequential(ctx, jobID, commands)
    }

    // 5. Git auto-commit（実行後）
    if w.config.AutoCommit && result.ExecutedCmds > 0 {
        postCommitHash, err := w.autoCommitChanges(ctx, jobID,
            fmt.Sprintf("Patch execution: %d commands", result.ExecutedCmds))
        if err == nil {
            result = result.WithGitCommit(postCommitHash)
        }
    }

    // 6. サマリ生成
    summary := fmt.Sprintf("実行: %d 件, 成功: %d 件, 失敗: %d 件",
        len(commands), result.ExecutedCmds, result.FailedCmds)
    result = result.WithSummary(summary)

    return result, nil
}

// executeSequential は v3互換の順次実行（既存ロジック）
func (w *workerExecutionService) executeSequential(
    ctx context.Context,
    jobID task.JobID,
    commands []patch.PatchCommand,
) *patch.PatchExecutionResult {
    result := patch.NewPatchExecutionResult()
    for i, cmd := range commands {
        cmdResult := w.executeCommand(ctx, jobID, cmd, i)
        result.AddResult(cmdResult)
        if !cmdResult.Success && w.config.StopOnError {
            break
        }
    }
    return result
}

// executeParallel は Type-Based Phased Execution（v4.0新規）
func (w *workerExecutionService) executeParallel(
    ctx context.Context,
    jobID task.JobID,
    commands []patch.PatchCommand,
) *patch.PatchExecutionResult {
    result := patch.NewPatchExecutionResult()

    // Phase分割
    fileEdits, shellCmds, gitOps := partitionByPhase(commands)

    // === Phase 1: file_edit（並列実行）===
    if len(fileEdits) > 0 {
        phase1Results := w.executeFileEditsParallel(ctx, jobID, fileEdits)
        for _, r := range phase1Results {
            result.AddResult(r)
        }

        // StopOnError: Phase 1でエラー → Phase 2/3をスキップ
        if w.config.StopOnError && result.HasFailures() {
            return result
        }
    }

    // === Phase 2: shell_command（順次実行）===
    for i, cmd := range shellCmds {
        cmdResult := w.executeCommand(ctx, jobID, cmd, len(fileEdits)+i)
        result.AddResult(cmdResult)
        if !cmdResult.Success && w.config.StopOnError {
            return result
        }
    }

    // === Phase 3: git_operation（順次実行）===
    for i, cmd := range gitOps {
        cmdResult := w.executeCommand(ctx, jobID, cmd, len(fileEdits)+len(shellCmds)+i)
        result.AddResult(cmdResult)
        if !cmdResult.Success && w.config.StopOnError {
            return result
        }
    }

    return result
}

// executeFileEditsParallel はfile_editコマンドを並列実行する
// 同一Targetへの複数操作は出現順に直列化する
func (w *workerExecutionService) executeFileEditsParallel(
    ctx context.Context,
    jobID task.JobID,
    fileEdits []patch.PatchCommand,
) []patch.CommandResult {
    // 同一Targetのコマンドをグループ化
    groups := groupByTarget(fileEdits)

    // 結果を格納するスライス（順序保持のためインデックスマップを使用）
    allResults := make([]patch.CommandResult, len(fileEdits))
    indexMap := make(map[string][]int) // target → 元のインデックス配列

    idx := 0
    for target, cmds := range groups {
        indices := make([]int, len(cmds))
        for i := range cmds {
            indices[i] = idx
            idx++
        }
        indexMap[target] = indices
    }

    // 並列度制限付きgoroutine実行
    maxParallel := w.config.MaxParallelism
    if maxParallel <= 0 {
        maxParallel = 4 // デフォルト
    }
    sem := make(chan struct{}, maxParallel)

    var wg sync.WaitGroup

    for target, cmds := range groups {
        indices := indexMap[target]

        wg.Add(1)
        sem <- struct{}{} // セマフォ取得

        go func(target string, cmds []patch.PatchCommand, indices []int) {
            defer wg.Done()
            defer func() { <-sem }() // セマフォ解放

            // 同一Target内は順次実行（create→append等の順序保証）
            for i, cmd := range cmds {
                cmdResult := w.executeCommand(ctx, jobID, cmd, indices[i])
                allResults[indices[i]] = cmdResult

                // 同一Target内でエラー → 残りをスキップ
                if !cmdResult.Success && w.config.StopOnError {
                    break
                }
            }
        }(target, cmds, indices)
    }

    wg.Wait()
    return allResults
}

// partitionByPhase はPatchCommandをType別に3フェーズに分割する
func partitionByPhase(commands []patch.PatchCommand) (
    fileEdits []patch.PatchCommand,
    shellCmds []patch.PatchCommand,
    gitOps []patch.PatchCommand,
) {
    for _, cmd := range commands {
        switch cmd.Type {
        case patch.TypeFileEdit:
            fileEdits = append(fileEdits, cmd)
        case patch.TypeShellCommand:
            shellCmds = append(shellCmds, cmd)
        case patch.TypeGitOperation:
            gitOps = append(gitOps, cmd)
        }
    }
    return
}

// groupByTarget は同一Targetのfile_editをグループ化する
func groupByTarget(fileEdits []patch.PatchCommand) map[string][]patch.PatchCommand {
    groups := make(map[string][]patch.PatchCommand)
    for _, cmd := range fileEdits {
        groups[cmd.Target] = append(groups[cmd.Target], cmd)
    }
    return groups
}
```

**Config デフォルト値**（`config.go` の `setDefaults()` に追加）:

```go
// Worker並列実行デフォルト
if c.Worker.MaxParallelism == 0 {
    c.Worker.MaxParallelism = 4
}
// ParallelExecution はデフォルト false（v3互換）
```

**テスト計画**:

| テストケース | 内容 | 検証 |
|------------|------|------|
| `TestExecuteParallel_FileEditsOnly` | file_edit 5件を並列実行 | 全ファイル作成、実行時間が順次比で短縮 |
| `TestExecuteParallel_AllPhases` | file_edit 3件 + shell_command 2件 + git_operation 1件 | Phase順序の保証 |
| `TestExecuteParallel_SameTarget` | 同一ファイルへの create → append | 順序保証、内容の整合性 |
| `TestExecuteParallel_StopOnError` | Phase 1でエラー → Phase 2/3スキップ | エラー集約、Phase 2/3が未実行 |
| `TestExecuteParallel_Semaphore` | MaxParallelism=2 で file_edit 10件 | 同時実行数が2以下 |
| `TestExecuteParallel_FallbackSequential` | `ParallelExecution=false` | v3互換動作 |

---

## 3. Phase 1: ローカル通信層の確立

**期間**: 2週間
**目的**: Transport抽象化の導入、JSON形式のメッセージ送受信（ローカルでの検証）

### 3.1 Transport インターフェース定義

**ファイル**: `internal/domain/transport/transport.go`

**設計ポイント**:
- Clean Architectureに従い、Domain層に配置
- `Message` 構造体が `docs/Chat_Worker_Coder_アーキテクチャ.md` の仕様（531-546行目）に準拠
- Proposal、Resultの型は既存の `internal/domain/proposal/`、`internal/domain/patch/` を活用

**実装**:
```go
package transport

import (
	"context"
	"time"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/proposal"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/patch"
)

// Message は Agent間の通信メッセージ
type Message struct {
	From      string                      `json:"from"`       // 送信元Agent（"Mio", "Worker", "Coder3"）
	To        string                      `json:"to"`         // 送信先Agent
	SessionID string                      `json:"session_id"` // セッション識別子
	JobID     string                      `json:"job_id"`     // Job識別子
	Message   string                      `json:"message"`    // 日本語メッセージ
	Context   map[string]interface{}      `json:"context"`    // コンテキスト（履歴、パラメータ等）
	Proposal  *ProposalPayload            `json:"proposal,omitempty"` // Proposal（任意）
	Result    *ResultPayload              `json:"result,omitempty"`   // 実行結果（任意）
	Timestamp string                      `json:"timestamp"`  // タイムスタンプ（RFC3339）
}

// ProposalPayload は Proposal のペイロード
type ProposalPayload struct {
	Plan     string `json:"plan"`
	Patch    string `json:"patch"`     // JSON文字列
	Risk     string `json:"risk"`
	CostHint string `json:"costHint"`
}

// ResultPayload は 実行結果のペイロード
type ResultPayload struct {
	Status        string   `json:"status"`         // "success", "partial_success", "failed"
	CreatedFiles  []string `json:"created_files,omitempty"`
	ModifiedFiles []string `json:"modified_files,omitempty"`
	DeletedFiles  []string `json:"deleted_files,omitempty"`
	GitCommit     string   `json:"git_commit,omitempty"`
	ElapsedTime   string   `json:"elapsed_time"`
	ErrorMessage  string   `json:"error_message,omitempty"`
}

// Transport は Agent間通信の抽象化
type Transport interface {
	// Send はメッセージを送信
	Send(ctx context.Context, msg Message) error

	// Receive はメッセージを受信（ブロッキング）
	Receive(ctx context.Context) (Message, error)

	// Close はTransportを閉じる
	Close() error

	// IsHealthy はTransportの健全性を確認
	IsHealthy() bool
}
```

**バリデーション**:
```go
// Validate は Message の必須フィールドを検証
func (m *Message) Validate() error {
	if m.From == "" {
		return errors.New("from is required")
	}
	if m.To == "" {
		return errors.New("to is required")
	}
	if m.SessionID == "" {
		return errors.New("session_id is required")
	}
	if m.JobID == "" {
		return errors.New("job_id is required")
	}
	if m.Message == "" {
		return errors.New("message is required")
	}
	// タイムスタンプの形式チェック
	if _, err := time.Parse(time.RFC3339, m.Timestamp); err != nil {
		return fmt.Errorf("timestamp is invalid: %w", err)
	}
	return nil
}
```

### 3.2 LocalTransport 実装

**ファイル**: `internal/infrastructure/transport/local.go`

**設計ポイント**:
- チャネルベースの同一プロセス内通信
- スレッドセーフ（doneチャネル + sync.Mutex でデッドロック回避）
- バッファリング（チャネル容量100）

**実装**:
```go
package transport

import (
	"context"
	"errors"
	"sync"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// LocalTransport はローカル（同一プロセス内）通信の実装
//
// ロック戦略:
//   - closedフラグの読み書きのみ sync.Mutex で保護
//   - Send/Receive はロックを保持したままチャネル操作しない（デッドロック防止）
//   - チャネル自体はスレッドセーフなため、ロック不要
//   - Close()はdoneチャネルでSend/Receiveのブロッキングを解除
type LocalTransport struct {
	inbound  chan transport.Message
	outbound chan transport.Message
	done     chan struct{} // Close通知用（selectで使用）
	closed   bool
	mu       sync.Mutex // closedフラグ保護用（RWMutexは不要）
}

// NewLocalTransport は LocalTransport を作成
func NewLocalTransport() *LocalTransport {
	return &LocalTransport{
		inbound:  make(chan transport.Message, 100),
		outbound: make(chan transport.Message, 100),
		done:     make(chan struct{}),
		closed:   false,
	}
}

// Send はメッセージを送信
// ロックを保持したままチャネル操作しないため、デッドロックしない
func (t *LocalTransport) Send(ctx context.Context, msg transport.Message) error {
	select {
	case t.outbound <- msg:
		return nil
	case <-ctx.Done():
		return ctx.Err()
	case <-t.done:
		return errors.New("transport is closed")
	}
}

// Receive はメッセージを受信（ブロッキング）
// Close()が呼ばれるとdoneチャネル経由で即座にアンブロックされる
func (t *LocalTransport) Receive(ctx context.Context) (transport.Message, error) {
	select {
	case msg := <-t.inbound:
		return msg, nil
	case <-ctx.Done():
		return transport.Message{}, ctx.Err()
	case <-t.done:
		return transport.Message{}, errors.New("transport is closed")
	}
}

// Close はTransportを閉じる
// doneチャネルのcloseにより、ブロッキング中のSend/Receiveが即座に解除される
func (t *LocalTransport) Close() error {
	t.mu.Lock()
	defer t.mu.Unlock()

	if t.closed {
		return nil
	}

	t.closed = true
	close(t.done)     // Send/Receiveのブロッキングを解除
	close(t.inbound)  // forwardLoopへの終了通知
	close(t.outbound) // forwardLoopへの終了通知
	return nil
}

// IsHealthy はTransportの健全性を確認
func (t *LocalTransport) IsHealthy() bool {
	t.mu.Lock()
	defer t.mu.Unlock()
	return !t.closed
}

// GetOutboundChannel は outbound チャネルを返す（MessageRouter用）
func (t *LocalTransport) GetOutboundChannel() <-chan transport.Message {
	return t.outbound
}

// PutInboundMessage は inbound チャネルにメッセージを送る（MessageRouter用）
// doneチャネルでClose済みを検知するため、ロック不要
func (t *LocalTransport) PutInboundMessage(msg transport.Message) error {
	select {
	case t.inbound <- msg:
		return nil
	case <-t.done:
		return errors.New("transport is closed")
	}
}
```

### 3.3 MessageRouter 実装

**ファイル**: `internal/infrastructure/transport/router.go`

**設計ポイント**:
- Agent名 → Transport のマッピング管理
- スレッドセーフ（sync.RWMutex）
- 動的なAgent登録/削除
- **Agent毎に専用goroutineでoutboundを監視**（busy-loop回避）

**実装**:
```go
package transport

import (
	"context"
	"fmt"
	"log/slog"
	"sync"

	domaintransport "github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// MessageRouter は Agent間のメッセージをルーティング
type MessageRouter struct {
	transports map[string]*LocalTransport // Agent名 → Transport
	mu         sync.RWMutex
	ctx        context.Context
	cancel     context.CancelFunc
	wg         sync.WaitGroup // goroutine終了待ち
	logger     *slog.Logger
}

// NewMessageRouter は MessageRouter を作成
func NewMessageRouter(logger *slog.Logger) *MessageRouter {
	ctx, cancel := context.WithCancel(context.Background())
	return &MessageRouter{
		transports: make(map[string]*LocalTransport),
		ctx:        ctx,
		cancel:     cancel,
		logger:     logger,
	}
}

// RegisterAgent は Agent を登録し、専用の転送goroutineを起動
func (r *MessageRouter) RegisterAgent(name string, t *LocalTransport) {
	r.mu.Lock()
	defer r.mu.Unlock()
	r.transports[name] = t

	// Agent毎に1つのgoroutineでoutboundチャネルを監視
	// チャネル受信でブロックするため、CPU消費なし
	r.wg.Add(1)
	go r.forwardLoop(name, t)
}

// UnregisterAgent は Agent を削除（goroutineはctx.Done()で終了）
func (r *MessageRouter) UnregisterAgent(name string) {
	r.mu.Lock()
	defer r.mu.Unlock()
	delete(r.transports, name)
}

// Stop はルーティングを停止し、全goroutineの終了を待つ
func (r *MessageRouter) Stop() {
	r.cancel()
	r.wg.Wait()
}

// forwardLoop はAgent専用の転送ループ（1 Agent = 1 goroutine）
// チャネル受信でブロックするため、メッセージがないときCPUを消費しない
func (r *MessageRouter) forwardLoop(name string, src *LocalTransport) {
	defer r.wg.Done()
	outCh := src.GetOutboundChannel()

	for {
		select {
		case <-r.ctx.Done():
			return
		case msg, ok := <-outCh:
			if !ok {
				// チャネルがcloseされた（Transport終了）
				return
			}
			r.deliverMessage(msg)
		}
	}
}

// deliverMessage はメッセージを宛先Agentに配送
func (r *MessageRouter) deliverMessage(msg domaintransport.Message) {
	r.mu.RLock()
	targetTransport, ok := r.transports[msg.To]
	r.mu.RUnlock()

	if !ok {
		r.logger.Error("router.agent_not_found",
			"from", msg.From,
			"to", msg.To,
			"job_id", msg.JobID,
		)
		return
	}

	if err := targetTransport.PutInboundMessage(msg); err != nil {
		r.logger.Error("router.delivery_failed",
			"from", msg.From,
			"to", msg.To,
			"error", err,
		)
	}
}
```

**旧設計との差分**:

| 項目 | 旧（busy-loop） | 新（per-agent goroutine） |
|------|-----------------|--------------------------|
| CPU使用率 | 100%（defaultケースで即座にループ再開） | ~0%（チャネル受信でブロック） |
| ロック範囲 | mapイテレーション中にRLock/RUnlock交互 | deliverMessage内で最小限のRLock |
| map変更時の安全性 | イテレーション中のmap変更でpanicリスク | RLockはdeliverMessage内のみ、mapイテレーションなし |
| goroutine数 | 1（全Agent共有） | Agent数と同じ（通常5個以下） |

### 3.4 テスト計画（Phase 1）

#### 3.4.1 単体テスト

**`local_test.go`**: LocalTransport の送受信テスト
```go
func TestLocalTransport_SendReceive(t *testing.T) {
	transport := NewLocalTransport()
	defer transport.Close()

	msg := transport.Message{
		From: "Mio",
		To: "Worker",
		SessionID: "20260302-line-Utest001",
		JobID: "20260302-120000-a1b2c3d4",
		Message: "テストメッセージ",
		Timestamp: time.Now().Format(time.RFC3339),
	}

	ctx := context.Background()

	// Send
	err := transport.Send(ctx, msg)
	assert.NoError(t, err)

	// Receive
	received, err := transport.Receive(ctx)
	assert.NoError(t, err)
	assert.Equal(t, msg.From, received.From)
	assert.Equal(t, msg.Message, received.Message)
}
```

**`router_test.go`**: MessageRouter のルーティングテスト
```go
func TestMessageRouter_Route(t *testing.T) {
	router := NewMessageRouter()
	router.Start()
	defer router.Stop()

	mioTransport := NewLocalTransport()
	workerTransport := NewLocalTransport()
	router.RegisterAgent("Mio", mioTransport)
	router.RegisterAgent("Worker", workerTransport)

	msg := transport.Message{
		From: "Mio",
		To: "Worker",
		SessionID: "20260302-line-Utest001",
		JobID: "20260302-120000-a1b2c3d4",
		Message: "Workerに委譲",
		Timestamp: time.Now().Format(time.RFC3339),
	}

	ctx := context.Background()

	// Mio → Worker にメッセージ送信
	err := mioTransport.Send(ctx, msg)
	assert.NoError(t, err)

	// Worker がメッセージを受信
	received, err := workerTransport.Receive(ctx)
	assert.NoError(t, err)
	assert.Equal(t, "Mio", received.From)
	assert.Equal(t, "Worker", received.To)
}
```

#### 3.4.2 統合テスト

**`integration_local_test.go`**: ローカル通信フロー（Mio → Worker → Coder3）
```go
func TestIntegration_MioToWorkerToCoder(t *testing.T) {
	router := NewMessageRouter()
	router.Start()
	defer router.Stop()

	mioTransport := NewLocalTransport()
	workerTransport := NewLocalTransport()
	coder3Transport := NewLocalTransport()
	router.RegisterAgent("Mio", mioTransport)
	router.RegisterAgent("Worker", workerTransport)
	router.RegisterAgent("Coder3", coder3Transport)

	ctx := context.Background()

	// Mio → Worker
	msg1 := transport.Message{
		From: "Mio",
		To: "Worker",
		SessionID: "20260302-line-Uintegr01",
		JobID: "20260302-120100-e5f6a7b8",
		Message: "hello.goを作成してください",
		Timestamp: time.Now().Format(time.RFC3339),
	}
	mioTransport.Send(ctx, msg1)

	// Worker がメッセージ受信
	receivedByWorker, _ := workerTransport.Receive(ctx)
	assert.Equal(t, "Mio", receivedByWorker.From)

	// Worker → Coder3
	msg2 := transport.Message{
		From: "Worker",
		To: "Coder3",
		SessionID: "20260302-line-Uintegr01",
		JobID: "20260302-120100-e5f6a7b8",
		Message: "hello.goを作成してください",
		Timestamp: time.Now().Format(time.RFC3339),
	}
	workerTransport.Send(ctx, msg2)

	// Coder3 がメッセージ受信
	receivedByCoder3, _ := coder3Transport.Receive(ctx)
	assert.Equal(t, "Worker", receivedByCoder3.From)
	assert.Equal(t, "hello.goを作成してください", receivedByCoder3.Message)
}
```

### 3.5 成功基準（Phase 1）

- ✅ Transport インターフェースが定義され、全テストが通る
- ✅ LocalTransport で JSON メッセージが送受信できる
- ✅ MessageRouter で Agent間メッセージがルーティングできる
- ✅ 既存のv3.0実装と共存できる（破壊的変更なし）
- ✅ テストカバレッジ: Transport層 90%以上

---

## 4. Phase 2: リモート通信基盤

**期間**: 3週間
**目的**: SSH経由のリモート通信実装、Agent スタンドアロンモード実装

### 4.1 SSHTransport 実装

**ファイル**: `internal/infrastructure/transport/ssh.go`

**技術選択**:
- `golang.org/x/crypto/ssh` パッケージ使用
- SSH接続 → リモートで `picoclaw-agent --standalone` 起動
- stdin/stdout で JSON 送受信（1行1メッセージ）

**設計ポイント**:
- 再接続ロジック（3回リトライ、指数バックオフ）
- タイムアウト（接続: 10秒、通信: 30秒）
- ヘルスチェック（定期的なping/pong）
- エラーハンドリング（接続切断時の復旧）

**実装**:
```go
package transport

import (
	"context"
	"encoding/json"
	"errors"
	"fmt"
	"io"
	"net"
	"sync"
	"time"

	"golang.org/x/crypto/ssh"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// SSHTransport はSSH経由のリモート通信の実装
//
// ロック戦略:
//   - mu: closedフラグとconnect/reconnectの排他制御
//   - sendMu: encoder への排他アクセス（Send と reconnect の競合防止）
//   - decoder は Receive 内の goroutine でのみ使用（1つずつ順次実行）
type SSHTransport struct {
	host       string
	user       string
	privateKey string
	remoteCmd  string // リモートで実行するコマンド

	client  *ssh.Client
	session *ssh.Session
	stdin   io.WriteCloser
	stdout  io.Reader
	stderr  io.Reader

	decoder *json.Decoder
	encoder *json.Encoder

	closed bool
	mu     sync.Mutex  // closedフラグ・接続状態の保護
	sendMu sync.Mutex  // encoder への排他アクセス
}

// SSHConfig は SSHTransport の設定
type SSHConfig struct {
	Host          string
	User          string
	PrivateKey    string
	RemoteCommand string
	Timeout       time.Duration
}

// NewSSHTransport は SSHTransport を作成
func NewSSHTransport(cfg SSHConfig) (*SSHTransport, error) {
	t := &SSHTransport{
		host:       cfg.Host,
		user:       cfg.User,
		privateKey: cfg.PrivateKey,
		remoteCmd:  cfg.RemoteCommand,
		closed:     false,
	}

	if err := t.connect(); err != nil {
		return nil, fmt.Errorf("failed to connect: %w", err)
	}

	return t, nil
}

// connect はSSH接続を確立
func (t *SSHTransport) connect() error {
	// SSH鍵の読み込み
	key, err := ssh.ParsePrivateKey([]byte(t.privateKey))
	if err != nil {
		return fmt.Errorf("failed to parse private key: %w", err)
	}

	// SSH設定
	sshConfig := &ssh.ClientConfig{
		User: t.user,
		Auth: []ssh.AuthMethod{
			ssh.PublicKeys(key),
		},
		HostKeyCallback: ssh.InsecureIgnoreHostKey(), // TODO: 本番ではホスト鍵検証
		Timeout:         10 * time.Second,
	}

	// SSH接続
	client, err := ssh.Dial("tcp", t.host, sshConfig)
	if err != nil {
		return fmt.Errorf("failed to dial: %w", err)
	}
	t.client = client

	// セッション作成
	session, err := client.NewSession()
	if err != nil {
		return fmt.Errorf("failed to create session: %w", err)
	}
	t.session = session

	// stdin/stdout/stderr取得
	stdin, err := session.StdinPipe()
	if err != nil {
		return fmt.Errorf("failed to get stdin: %w", err)
	}
	t.stdin = stdin

	stdout, err := session.StdoutPipe()
	if err != nil {
		return fmt.Errorf("failed to get stdout: %w", err)
	}
	t.stdout = stdout

	stderr, err := session.StderrPipe()
	if err != nil {
		return fmt.Errorf("failed to get stderr: %w", err)
	}
	t.stderr = stderr

	// JSON エンコーダー/デコーダー
	t.encoder = json.NewEncoder(t.stdin)
	t.decoder = json.NewDecoder(t.stdout)

	// リモートコマンド実行
	if err := session.Start(t.remoteCmd); err != nil {
		return fmt.Errorf("failed to start remote command: %w", err)
	}

	return nil
}

// decodeResult は Receive のgoroutine間でデータ競合なく結果を受け渡すための型
type decodeResult struct {
	msg domaintransport.Message
	err error
}

// Send はメッセージを送信
// ロック戦略: sendMu でエンコーダへの排他アクセスを保証
func (t *SSHTransport) Send(ctx context.Context, msg domaintransport.Message) error {
	t.mu.Lock()
	if t.closed {
		t.mu.Unlock()
		return errors.New("transport is closed")
	}
	t.mu.Unlock()

	// エンコーダへの排他アクセス（reconnect()との競合防止）
	t.sendMu.Lock()
	defer t.sendMu.Unlock()

	done := make(chan error, 1)
	go func() {
		done <- t.encoder.Encode(msg)
	}()

	select {
	case err := <-done:
		return err
	case <-ctx.Done():
		return ctx.Err()
	}
}

// Receive はメッセージを受信
// データ競合の防止: goroutineの結果をdecodeResult構造体として
// チャネル経由で受け渡し、共有変数への並行アクセスを排除
func (t *SSHTransport) Receive(ctx context.Context) (domaintransport.Message, error) {
	t.mu.Lock()
	if t.closed {
		t.mu.Unlock()
		return domaintransport.Message{}, errors.New("transport is closed")
	}
	t.mu.Unlock()

	// 結果をチャネル経由で受け取り、共有変数への並行アクセスを排除
	resultCh := make(chan decodeResult, 1)
	go func() {
		var msg domaintransport.Message
		err := t.decoder.Decode(&msg)
		resultCh <- decodeResult{msg: msg, err: err}
	}()

	select {
	case result := <-resultCh:
		if result.err != nil {
			return domaintransport.Message{}, result.err
		}
		return result.msg, nil
	case <-ctx.Done():
		// 注意: goroutineはDecodeでブロックし続ける可能性がある。
		// 呼び出し側はClose()でstdinをcloseし、Decodeを強制終了させること。
		return domaintransport.Message{}, ctx.Err()
	}
}

// Close はTransportを閉じる
func (t *SSHTransport) Close() error {
	t.mu.Lock()
	defer t.mu.Unlock()

	if t.closed {
		return nil
	}

	t.closed = true

	if t.stdin != nil {
		t.stdin.Close()
	}
	if t.session != nil {
		t.session.Close()
	}
	if t.client != nil {
		t.client.Close()
	}

	return nil
}

// IsHealthy はTransportの健全性を確認
func (t *SSHTransport) IsHealthy() bool {
	t.mu.Lock()
	defer t.mu.Unlock()

	if t.closed || t.client == nil {
		return false
	}

	// ping/pong テスト（簡易ヘルスチェック）
	_, _, err := t.client.SendRequest("keepalive@openssh.com", true, nil)
	return err == nil
}
```

### 4.2 Agent スタンドアロンモード実装

**ファイル**: `cmd/picoclaw-agent/main.go`

**実装方針**:
- `--standalone` フラグで起動
- stdin から JSON を受信（`json.Decoder` 使用、バッファサイズ制限なし）
- Agent実行後、結果を stdout に JSON で出力
- 標準エラー（stderr）にログを出力（通信と分離）
- 設定ファイル（`config.yaml`）または環境変数から依存関係を初期化

#### 4.2.1 Standalone Agent初期化設計

各Agent種別の依存関係（v3 `cmd/picoclaw/main.go` の `buildDependencies()` から抽出）:

| Agent種別 | 必要な依存 | 環境変数 | 設定ファイルのセクション |
|-----------|-----------|---------|----------------------|
| **Worker** | OllamaProvider(worker-v1), ToolRunner, MCPClient, WorkerConfig | なし | `ollama`, `worker` |
| **Coder1** | DeepSeekProvider | `DEEPSEEK_API_KEY` | `deepseek` |
| **Coder2** | OpenAIProvider | `OPENAI_API_KEY` | `openai` |
| **Coder3** | ClaudeProvider | `ANTHROPIC_API_KEY` | `claude` |

**設定読み込み**: Standalone Agentは `--config` フラグまたは `PICOCLAW_CONFIG` 環境変数でconfig.yamlを読み込み、自分に必要なセクションのみを使用する。

#### 4.2.2 完全実装

```go
package main

import (
	"context"
	"encoding/json"
	"flag"
	"fmt"
	"log"
	"os"
	"os/signal"
	"syscall"
	"time"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/adapter/config"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/application/service"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/agent"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/patch"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/proposal"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/task"
	domaintransport "github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/infrastructure/llm/claude"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/infrastructure/llm/deepseek"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/infrastructure/llm/ollama"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/infrastructure/llm/openai"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/infrastructure/mcp"
	"github.com/Nyukimin/picoclaw_multiLLM/internal/infrastructure/tools"
)

// AgentHandler はメッセージを処理するAgent共通インターフェース
type AgentHandler interface {
	HandleMessage(ctx context.Context, msg domaintransport.Message) (domaintransport.Message, error)
}

func main() {
	// フラグ定義
	standalone := flag.Bool("standalone", false, "Run in standalone mode (stdin/stdout JSON)")
	agentType := flag.String("agent", "", "Agent type: worker, coder1, coder2, coder3")
	configPath := flag.String("config", "", "Config file path (default: PICOCLAW_CONFIG env or ./config.yaml)")
	flag.Parse()

	if !*standalone {
		log.Fatal("This command requires --standalone flag")
	}
	if *agentType == "" {
		log.Fatal("Agent type is required (--agent=worker|coder1|coder2|coder3)")
	}

	// ログを stderr に出力（stdout は通信用）
	log.SetOutput(os.Stderr)
	log.Printf("Starting picoclaw-agent in standalone mode (agent=%s)", *agentType)

	// 設定読み込み
	cfgPath := *configPath
	if cfgPath == "" {
		cfgPath = os.Getenv("PICOCLAW_CONFIG")
	}
	if cfgPath == "" {
		cfgPath = "./config.yaml"
	}
	cfg, err := config.LoadConfig(cfgPath)
	if err != nil {
		log.Fatalf("Failed to load config: %v", err)
	}

	// Agent初期化
	handler, err := initAgent(*agentType, cfg)
	if err != nil {
		log.Fatalf("Failed to init agent: %v", err)
	}
	log.Printf("Agent %s initialized successfully", *agentType)

	// シグナルハンドリング（Graceful Shutdown）
	sigCh := make(chan os.Signal, 1)
	signal.Notify(sigCh, syscall.SIGTERM, syscall.SIGINT)

	// stdin/stdout でJSON通信ループ
	// json.Decoder を使用（bufio.Scanner の 64KB 制限を回避）
	decoder := json.NewDecoder(os.Stdin)
	encoder := json.NewEncoder(os.Stdout)

	// メッセージ処理ループ（1メッセージずつ直列処理 → Sessionスレッドセーフ性を保証）
	doneCh := make(chan struct{})
	go func() {
		defer close(doneCh)
		for decoder.More() {
			var msg domaintransport.Message
			if err := decoder.Decode(&msg); err != nil {
				log.Printf("ERROR: Failed to decode message: %v", err)
				continue
			}

			log.Printf("Received: from=%s to=%s job_id=%s", msg.From, msg.To, msg.JobID)

			// メッセージ処理
			ctx, cancel := context.WithTimeout(context.Background(), 300*time.Second)
			result, err := handler.HandleMessage(ctx, msg)
			cancel()

			if err != nil {
				log.Printf("ERROR: HandleMessage failed: %v", err)
				// エラー応答を返す
				result = domaintransport.Message{
					From:      msg.To,
					To:        msg.From,
					SessionID: msg.SessionID,
					JobID:     msg.JobID,
					Message:   fmt.Sprintf("エラー: %v", err),
					Result: &domaintransport.ResultPayload{
						Status:       "failed",
						ErrorMessage: err.Error(),
						ElapsedTime:  "0s",
					},
					Timestamp: time.Now().UTC().Format(time.RFC3339),
				}
			}

			if err := encoder.Encode(result); err != nil {
				log.Printf("ERROR: Failed to encode result: %v", err)
			}
		}
	}()

	// シグナルまたはstdin EOF を待機
	select {
	case sig := <-sigCh:
		log.Printf("Received signal: %s, shutting down...", sig)
	case <-doneCh:
		log.Println("stdin closed, shutting down...")
	}

	log.Println("picoclaw-agent finished")
}

// initAgent はAgent種別に応じた初期化を行う
func initAgent(agentType string, cfg *config.Config) (AgentHandler, error) {
	switch agentType {
	case "worker":
		return initWorker(cfg)
	case "coder1":
		return initCoder1(cfg)
	case "coder2":
		return initCoder2(cfg)
	case "coder3":
		return initCoder3(cfg)
	default:
		return nil, fmt.Errorf("unknown agent type: %s", agentType)
	}
}

// === Worker ===

type workerHandler struct {
	shiroAgent       *agent.ShiroAgent
	executionService service.WorkerExecutionService
}

func initWorker(cfg *config.Config) (*workerHandler, error) {
	// Worker はOllama(worker-v1) + ToolRunner + MCPClient + WorkerExecutionService
	ollamaProvider := ollama.NewOllamaProvider(cfg.Ollama.BaseURL, cfg.Ollama.WorkerModel)
	toolRunner := tools.NewToolRunner()
	mcpClient := mcp.NewMCPClient()
	shiroAgent := agent.NewShiroAgent(ollamaProvider, toolRunner, mcpClient)
	executionService := service.NewWorkerExecutionService(cfg.Worker)

	return &workerHandler{
		shiroAgent:       shiroAgent,
		executionService: executionService,
	}, nil
}

func (h *workerHandler) HandleMessage(ctx context.Context, msg domaintransport.Message) (domaintransport.Message, error) {
	start := time.Now()

	// Proposalが含まれている場合 → Worker即時実行
	if msg.Proposal != nil {
		// Proposal からドメインオブジェクトに変換
		p := proposal.Reconstruct(msg.Proposal.Plan, msg.Proposal.Patch, msg.Proposal.Risk, msg.Proposal.CostHint)
		if p == nil || !p.IsValid() {
			return domaintransport.Message{}, fmt.Errorf("invalid proposal in message")
		}

		// JobID復元
		jobID, err := task.ParseJobID(msg.JobID)
		if err != nil {
			return domaintransport.Message{}, fmt.Errorf("invalid job_id: %w", err)
		}

		// Worker即時実行
		result, err := h.executionService.ExecuteProposal(ctx, jobID, p)
		if err != nil {
			return domaintransport.Message{}, fmt.Errorf("execution failed: %w", err)
		}

		// 結果をメッセージに変換
		return domaintransport.Message{
			From:      "Worker",
			To:        msg.From,
			SessionID: msg.SessionID,
			JobID:     msg.JobID,
			Message:   fmt.Sprintf("実行完了: %d件成功, %d件失敗", result.ExecutedCmds, result.FailedCmds),
			Result:    convertToResultPayload(result),
			Timestamp: time.Now().UTC().Format(time.RFC3339),
		}, nil
	}

	// Proposalなし → ShiroAgent.Execute()
	t := task.NewTask(task.JobID{}, msg.Message, "", "")
	response, err := h.shiroAgent.Execute(ctx, t)
	if err != nil {
		return domaintransport.Message{}, err
	}

	return domaintransport.Message{
		From:      "Worker",
		To:        msg.From,
		SessionID: msg.SessionID,
		JobID:     msg.JobID,
		Message:   response,
		Result: &domaintransport.ResultPayload{
			Status:      "success",
			ElapsedTime: time.Since(start).String(),
		},
		Timestamp: time.Now().UTC().Format(time.RFC3339),
	}, nil
}

// === Coder1 (DeepSeek) ===

type coderHandler struct {
	agentName  string
	coderAgent *agent.CoderAgent
}

func initCoder1(cfg *config.Config) (*coderHandler, error) {
	if cfg.DeepSeek.APIKey == "" {
		return nil, fmt.Errorf("DEEPSEEK_API_KEY is required for coder1")
	}
	provider := deepseek.NewDeepSeekProvider(cfg.DeepSeek.APIKey, cfg.DeepSeek.Model)
	coder := agent.NewCoderAgent(provider, nil, nil)
	return &coderHandler{agentName: "Coder1", coderAgent: coder}, nil
}

// === Coder2 (OpenAI) ===

func initCoder2(cfg *config.Config) (*coderHandler, error) {
	if cfg.OpenAI.APIKey == "" {
		return nil, fmt.Errorf("OPENAI_API_KEY is required for coder2")
	}
	provider := openai.NewOpenAIProvider(cfg.OpenAI.APIKey, cfg.OpenAI.Model)
	coder := agent.NewCoderAgent(provider, nil, nil)
	return &coderHandler{agentName: "Coder2", coderAgent: coder}, nil
}

// === Coder3 (Claude) ===

func initCoder3(cfg *config.Config) (*coderHandler, error) {
	if cfg.Claude.APIKey == "" {
		return nil, fmt.Errorf("ANTHROPIC_API_KEY is required for coder3")
	}
	provider := claude.NewClaudeProvider(cfg.Claude.APIKey, cfg.Claude.Model)
	coder := agent.NewCoderAgent(provider, nil, nil)
	return &coderHandler{agentName: "Coder3", coderAgent: coder}, nil
}

// HandleMessage はCoder共通のメッセージ処理
func (h *coderHandler) HandleMessage(ctx context.Context, msg domaintransport.Message) (domaintransport.Message, error) {
	// タスク構築
	t := task.NewTask(task.JobID{}, msg.Message, "", "")

	// Proposal生成
	p, err := h.coderAgent.GenerateProposal(ctx, t)
	if err != nil {
		return domaintransport.Message{}, fmt.Errorf("proposal generation failed: %w", err)
	}

	if p == nil {
		return domaintransport.Message{
			From:      h.agentName,
			To:        msg.From,
			SessionID: msg.SessionID,
			JobID:     msg.JobID,
			Message:   "Proposalを生成できませんでした",
			Timestamp: time.Now().UTC().Format(time.RFC3339),
		}, nil
	}

	// Proposal付きメッセージを返す
	return domaintransport.Message{
		From:      h.agentName,
		To:        msg.From,
		SessionID: msg.SessionID,
		JobID:     msg.JobID,
		Message:   "Proposal生成完了",
		Proposal: &domaintransport.ProposalPayload{
			Plan:     p.Plan(),
			Patch:    p.Patch(),
			Risk:     p.Risk(),
			CostHint: p.CostHint(),
		},
		Timestamp: time.Now().UTC().Format(time.RFC3339),
	}, nil
}

// === ユーティリティ ===

// convertToResultPayload は v3 PatchExecutionResult を v4 ResultPayload に変換する
func convertToResultPayload(r *patch.PatchExecutionResult) *domaintransport.ResultPayload {
	status := "success"
	if r.HasFailures() {
		if r.SuccessRate() > 0 {
			status = "partial_success"
		} else {
			status = "failed"
		}
	}

	// ファイル操作結果の分類
	var created, modified, deleted []string
	for _, cmdResult := range r.Results {
		if !cmdResult.Success || cmdResult.Command.Type != patch.TypeFileEdit {
			continue
		}
		switch cmdResult.Command.Action {
		case patch.ActionCreate, patch.ActionMkdir:
			created = append(created, cmdResult.Command.Target)
		case patch.ActionUpdate, patch.ActionAppend, patch.ActionRename, patch.ActionCopy:
			modified = append(modified, cmdResult.Command.Target)
		case patch.ActionDelete:
			deleted = append(deleted, cmdResult.Command.Target)
		}
	}

	errorMsg := ""
	if r.HasFailures() {
		errorMsg = r.Summary
	}

	return &domaintransport.ResultPayload{
		Status:        status,
		CreatedFiles:  created,
		ModifiedFiles: modified,
		DeletedFiles:  deleted,
		GitCommit:     r.GitCommit,
		ElapsedTime:   fmt.Sprintf("%ds", r.ExecutedCmds), // 簡易版
		ErrorMessage:  errorMsg,
	}
}
```

#### 4.2.3 ビルドとデプロイ

```bash
# ビルド
go build -o picoclaw-agent ./cmd/picoclaw-agent/

# リモートホストに配置
scp picoclaw-agent picoclaw@192.168.1.100:~/bin/

# リモートホストでの起動確認
ssh picoclaw@192.168.1.100 "~/bin/picoclaw-agent --standalone --agent=worker --config=~/config.yaml"
```

**SSHTransportからの起動コマンド**（TransportFactory §4.3参照）:
```go
RemoteCommand: fmt.Sprintf("picoclaw-agent --standalone --agent=%s --config=%s", agentName, remoteCfgPath),
```

### 4.3 TransportFactory 実装

**ファイル**: `internal/infrastructure/transport/factory.go`

**設計ポイント**:
- 設定ファイル（`config.yaml`）に基づき、Local/SSH を透過的に切り替え
- SSH鍵の読み込み（秘密鍵パス指定）
- ホスト鍵検証（`known_hosts` 活用）

**実装**:
```go
package transport

import (
	"fmt"
	"os"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// TransportType は Transport の種類
type TransportType string

const (
	TransportTypeLocal TransportType = "local"
	TransportTypeSSH   TransportType = "ssh"
)

// AgentTransportConfig は Agent ごとの Transport 設定
type AgentTransportConfig struct {
	Type       TransportType
	RemoteHost string
	RemoteUser string
	SSHKeyPath string
}

// TransportFactory は Transport を生成
type TransportFactory struct {
	configs map[string]AgentTransportConfig // Agent名 → 設定
}

// NewTransportFactory は TransportFactory を作成
func NewTransportFactory(configs map[string]AgentTransportConfig) *TransportFactory {
	return &TransportFactory{
		configs: configs,
	}
}

// CreateTransport は Agent名に応じた Transport を生成
func (f *TransportFactory) CreateTransport(agentName string) (transport.Transport, error) {
	cfg, ok := f.configs[agentName]
	if !ok {
		return nil, fmt.Errorf("no config for agent: %s", agentName)
	}

	switch cfg.Type {
	case TransportTypeLocal:
		return NewLocalTransport(), nil

	case TransportTypeSSH:
		// SSH鍵の読み込み
		privateKey, err := os.ReadFile(cfg.SSHKeyPath)
		if err != nil {
			return nil, fmt.Errorf("failed to read SSH key: %w", err)
		}

		sshCfg := SSHConfig{
			Host:          cfg.RemoteHost,
			User:          cfg.RemoteUser,
			PrivateKey:    string(privateKey),
			RemoteCommand: fmt.Sprintf("picoclaw-agent --standalone --agent=%s", agentName),
		}

		return NewSSHTransport(sshCfg)

	default:
		return nil, fmt.Errorf("unknown transport type: %s", cfg.Type)
	}
}
```

### 4.4 設定ファイル拡張

**ファイル**: `config.yaml`

**追加内容**:
```yaml
# 既存設定（v3.0）
llm:
  ollama:
    endpoint: "http://localhost:11434"
    # ...

# 新規追加（v4.0 分散実行）
distributed:
  enabled: true  # false で v3.0 モード

  transports:
    mio:
      type: local  # Mio はメインプロセス（常にローカル）

    worker:  # Transport ID: "Worker"（v3コード上は shiro）
      type: ssh
      remote_host: "192.168.1.100:22"
      remote_user: picoclaw
      ssh_key_path: /home/user/.ssh/id_rsa_picoclaw

    coder3:  # Transport ID: "Coder3"（ニックネーム: Gin）
      type: ssh
      remote_host: "192.168.1.101:22"
      remote_user: picoclaw
      ssh_key_path: /home/user/.ssh/id_rsa_picoclaw

    coder1:  # Transport ID: "Coder1"（ニックネーム: Aka）
      type: local  # または ssh

    coder2:  # Transport ID: "Coder2"（ニックネーム: Ao）
      type: local  # または ssh
```

**Config構造体の拡張**:

v3の実際のConfig構造体（`internal/adapter/config/config.go`）に`Distributed`フィールドを追加する。

```go
// Config はアプリケーション全体の設定
// v3既存フィールドをそのまま維持し、v4.0で Distributed を追加
type Config struct {
	// === v3.0 既存フィールド（変更なし）===
	Server   ServerConfig   `yaml:"server"`
	Ollama   OllamaConfig   `yaml:"ollama"`
	Claude   ClaudeConfig   `yaml:"claude"`
	DeepSeek DeepSeekConfig `yaml:"deepseek"`
	OpenAI   OpenAIConfig   `yaml:"openai"`
	Session  SessionConfig  `yaml:"session"`
	Worker   WorkerConfig   `yaml:"worker"`
	Log      LogConfig      `yaml:"log"`

	// === v4.0 追加フィールド ===
	Distributed DistributedConfig `yaml:"distributed"`
}

// DistributedConfig は分散実行設定
// YAML に distributed セクションがない場合、ゼロ値（Enabled=false）でv3互換動作
type DistributedConfig struct {
	Enabled    bool                       `yaml:"enabled"`    // false で v3.0 モード（デフォルト）
	Transports map[string]TransportConfig `yaml:"transports"` // キーは設定キー（§1.4参照: mio, worker, coder1, coder2, coder3）
}

// TransportConfig は Agent別のTransport設定
type TransportConfig struct {
	Type       string `yaml:"type"`        // "local" or "ssh"
	RemoteHost string `yaml:"remote_host"` // SSH接続先（例: "192.168.1.100:22"）
	RemoteUser string `yaml:"remote_user"` // SSHユーザー名
	SSHKeyPath string `yaml:"ssh_key_path"`// SSH秘密鍵パス
}

// WorkerConfig はWorker実行設定（v4.0拡張版）
type WorkerConfig struct {
	// === v3.0 既存フィールド ===
	AutoCommit           bool     `yaml:"auto_commit"`
	CommitMessagePrefix  string   `yaml:"commit_message_prefix"`
	CommandTimeout       int      `yaml:"command_timeout"`       // 秒（デフォルト: 300）
	GitTimeout           int      `yaml:"git_timeout"`           // 秒（デフォルト: 30）
	StopOnError          bool     `yaml:"stop_on_error"`
	Workspace            string   `yaml:"workspace"`
	ProtectedPatterns    []string `yaml:"protected_patterns"`
	ActionOnProtected    string   `yaml:"action_on_protected"`   // "error", "skip", "log"
	ShowExecutionSummary bool     `yaml:"show_execution_summary"`

	// === v4.0 追加フィールド ===
	ParallelExecution bool `yaml:"parallel_execution"` // true で並列実行（デフォルト: false）
	MaxParallelism    int  `yaml:"max_parallelism"`    // 並列度上限（デフォルト: 4）
}
```

**後方互換性の保証**:

```yaml
# v3.0の設定ファイル（distributed セクションなし）→ そのまま動作
server:
  port: 8080
ollama:
  base_url: "http://localhost:11434"
  chat_model: chat-v1
  worker_model: worker-v1
session:
  storage_dir: /tmp/picoclaw
worker:
  auto_commit: true
  stop_on_error: false
# distributed: が存在しない → Enabled=false（ゼロ値）でv3モード
```

```yaml
# v4.0の設定ファイル（distributed セクション追加）
server:
  port: 8080
ollama:
  base_url: "http://localhost:11434"
  # ...
worker:
  auto_commit: true
  stop_on_error: false
  parallel_execution: true   # v4.0 追加
  max_parallelism: 4         # v4.0 追加
distributed:                 # v4.0 追加セクション
  enabled: true
  transports:
    mio:
      type: local
    worker:
      type: ssh
      remote_host: "192.168.1.100:22"
      remote_user: picoclaw
      ssh_key_path: /home/user/.ssh/id_picoclaw
    # ...
```

**setDefaults() への追加**（`config.go`）:

```go
// v4.0 Worker並列実行デフォルト
if c.Worker.MaxParallelism == 0 {
    c.Worker.MaxParallelism = 4
}
// c.Worker.ParallelExecution はデフォルト false（ゼロ値）

// v4.0 Distributed デフォルト
// c.Distributed.Enabled はデフォルト false（ゼロ値）
// Transports が nil の場合は初期化不要（Enabled=false で参照されない）
```

**Validate() への追加**（`config.go`）:

```go
// v4.0 Distributed設定検証
if c.Distributed.Enabled {
    if len(c.Distributed.Transports) == 0 {
        return fmt.Errorf("distributed.enabled=true requires at least one transport")
    }
    for name, tc := range c.Distributed.Transports {
        if tc.Type != "local" && tc.Type != "ssh" {
            return fmt.Errorf("distributed.transports.%s.type must be 'local' or 'ssh', got '%s'", name, tc.Type)
        }
        if tc.Type == "ssh" {
            if tc.RemoteHost == "" {
                return fmt.Errorf("distributed.transports.%s.remote_host is required for ssh type", name)
            }
            if tc.RemoteUser == "" {
                return fmt.Errorf("distributed.transports.%s.remote_user is required for ssh type", name)
            }
            if tc.SSHKeyPath == "" {
                return fmt.Errorf("distributed.transports.%s.ssh_key_path is required for ssh type", name)
            }
        }
    }
}
```

### 4.5 テスト計画（Phase 2）

**`ssh_test.go`**: SSHTransport の送受信テスト（モックSSHサーバー使用）
**`standalone_test.go`**: スタンドアロンモードの動作確認
**`factory_test.go`**: TransportFactory のテスト
**`e2e_remote_test.go`**: リモート通信 E2E テスト

### 4.6 成功基準（Phase 2）

- ✅ SSH経由でメッセージを送受信できる
- ✅ Agent スタンドアロンモードで起動・実行できる
- ✅ 設定ファイルで Local/SSH を切り替え可能
- ✅ 接続エラー時に適切なエラーハンドリング（再接続、タイムアウト）
- ✅ 既存のv3.0機能が動作する（`distributed.enabled: false`）

---

## 5. Phase 3: 分散環境対応

**期間**: 2週間
**目的**: Session/Memory の分散管理、通信ログ・モニタリング、パフォーマンス最適化

### 5.1 Session 同期

**ファイル**: `internal/domain/session/distributed.go`

**設計ポイント**:
- 各 Agent が独立した Memory を持つ
- 階層的な会話ログ伝播（Coder → Worker → Mio）
- Mioが全体の会話ログを統合して把握

**実装**:
```go
package session

import (
	"sync"
	"time"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// AgentMemory は各Agentが持つ独立したメモリ
type AgentMemory struct {
	AgentID       string
	Conversations []transport.Message
	Context       map[string]interface{}
	SyncPoint     time.Time
	mu            sync.RWMutex
}

// NewAgentMemory は AgentMemory を作成
func NewAgentMemory(agentID string) *AgentMemory {
	return &AgentMemory{
		AgentID:       agentID,
		Conversations: []transport.Message{},
		Context:       make(map[string]interface{}),
		SyncPoint:     time.Now(),
	}
}

// AddConversation は会話を追加
func (m *AgentMemory) AddConversation(msg transport.Message) {
	m.mu.Lock()
	defer m.mu.Unlock()
	m.Conversations = append(m.Conversations, msg)
}

// GetConversations は会話ログを取得
func (m *AgentMemory) GetConversations() []transport.Message {
	m.mu.RLock()
	defer m.mu.RUnlock()
	return m.Conversations
}

// CentralMemory は Mioが持つ全体統合メモリ
type CentralMemory struct {
	SessionID     string
	AgentMemories map[string]*AgentMemory // Agent名 → Memory
	mu            sync.RWMutex
}

// NewCentralMemory は CentralMemory を作成
func NewCentralMemory(sessionID string) *CentralMemory {
	return &CentralMemory{
		SessionID:     sessionID,
		AgentMemories: make(map[string]*AgentMemory),
	}
}

// RegisterAgent は Agentのメモリを登録
func (c *CentralMemory) RegisterAgent(agentID string, memory *AgentMemory) {
	c.mu.Lock()
	defer c.mu.Unlock()
	c.AgentMemories[agentID] = memory
}

// GetAgentMemory は Agent のメモリを取得
func (c *CentralMemory) GetAgentMemory(agentID string) (*AgentMemory, bool) {
	c.mu.RLock()
	defer c.mu.RUnlock()
	memory, ok := c.AgentMemories[agentID]
	return memory, ok
}

// GetAllConversations はすべての会話ログを統合して取得
func (c *CentralMemory) GetAllConversations() []transport.Message {
	c.mu.RLock()
	defer c.mu.RUnlock()

	var all []transport.Message
	for _, memory := range c.AgentMemories {
		all = append(all, memory.GetConversations()...)
	}

	// タイムスタンプでソート
	sort.Slice(all, func(i, j int) bool {
		return all[i].Timestamp < all[j].Timestamp
	})

	return all
}
```

### 5.2 通信ログとモニタリング

**ファイル**: `internal/infrastructure/transport/logger.go`

**設計ポイント**:
- Transport のログラッパー（Decorator パターン）
- すべての送受信をログに記録（`slog`使用）
- メッセージのプレビュー（機密情報マスキング）

**実装**:
```go
package transport

import (
	"context"
	"log/slog"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// LoggingTransport は Transport のログラッパー
type LoggingTransport struct {
	inner  transport.Transport
	logger *slog.Logger
}

// NewLoggingTransport は LoggingTransport を作成
func NewLoggingTransport(inner transport.Transport, logger *slog.Logger) *LoggingTransport {
	return &LoggingTransport{
		inner:  inner,
		logger: logger,
	}
}

// Send はメッセージを送信（ログ記録）
func (t *LoggingTransport) Send(ctx context.Context, msg transport.Message) error {
	t.logger.Info("transport.send",
		"from", msg.From,
		"to", msg.To,
		"session_id", msg.SessionID,
		"job_id", msg.JobID,
		"message_preview", truncate(msg.Message, 50),
		"timestamp", msg.Timestamp,
	)

	err := t.inner.Send(ctx, msg)

	if err != nil {
		t.logger.Error("transport.send.failed",
			"from", msg.From,
			"to", msg.To,
			"error", err,
		)
	}

	return err
}

// Receive はメッセージを受信（ログ記録）
func (t *LoggingTransport) Receive(ctx context.Context) (transport.Message, error) {
	msg, err := t.inner.Receive(ctx)

	if err != nil {
		t.logger.Error("transport.receive.failed",
			"error", err,
		)
		return msg, err
	}

	t.logger.Info("transport.receive",
		"from", msg.From,
		"to", msg.To,
		"session_id", msg.SessionID,
		"job_id", msg.JobID,
		"message_preview", truncate(msg.Message, 50),
		"timestamp", msg.Timestamp,
	)

	return msg, nil
}

// Close はTransportを閉じる
func (t *LoggingTransport) Close() error {
	return t.inner.Close()
}

// IsHealthy はTransportの健全性を確認
func (t *LoggingTransport) IsHealthy() bool {
	return t.inner.IsHealthy()
}

// truncate は文字列を文字数（rune単位）で切り詰め
// UTF-8マルチバイト文字（日本語等）の途中で切断されないことを保証する
func truncate(s string, maxRunes int) string {
	runes := []rune(s)
	if len(runes) <= maxRunes {
		return s
	}
	return string(runes[:maxRunes]) + "..."
}
```

### 5.3 パフォーマンス最適化

**ファイル**: `internal/infrastructure/transport/pool.go`

**最適化策**:
1. **接続プーリング**: SSH接続を使い回す
2. **ヘルスチェック**: 定期的な接続確認、事前の再接続
3. **圧縮**: 大きなメッセージを圧縮（gzip、オプション）

**実装**:
```go
package transport

import (
	"context"
	"sync"

	"github.com/Nyukimin/picoclaw_multiLLM/internal/domain/transport"
)

// TransportPool は Transport の接続プール
type TransportPool struct {
	transports map[string]transport.Transport // Agent名 → Transport
	factory    *TransportFactory
	mu         sync.RWMutex
}

// NewTransportPool は TransportPool を作成
func NewTransportPool(factory *TransportFactory) *TransportPool {
	return &TransportPool{
		transports: make(map[string]transport.Transport),
		factory:    factory,
	}
}

// GetTransport は Agent名に対応するTransportを取得（プール管理）
func (p *TransportPool) GetTransport(agentName string) (transport.Transport, error) {
	p.mu.RLock()
	t, ok := p.transports[agentName]
	p.mu.RUnlock()

	if ok && t.IsHealthy() {
		return t, nil
	}

	// 新規作成または再作成
	p.mu.Lock()
	defer p.mu.Unlock()

	// 既存のTransportを削除
	if ok {
		t.Close()
		delete(p.transports, agentName)
	}

	// 新規作成
	newTransport, err := p.factory.CreateTransport(agentName)
	if err != nil {
		return nil, err
	}

	p.transports[agentName] = newTransport
	return newTransport, nil
}

// CloseAll はすべてのTransportを閉じる
func (p *TransportPool) CloseAll() {
	p.mu.Lock()
	defer p.mu.Unlock()

	for _, t := range p.transports {
		t.Close()
	}
	p.transports = make(map[string]transport.Transport)
}
```

### 5.4 テスト計画（Phase 3）

**`distributed_session_test.go`**: Session同期のテスト
**`logger_test.go`**: ログ記録のテスト
**`pool_test.go`**: 接続プールのテスト
**`performance_test.go`**: パフォーマンステスト（ベンチマーク）

### 5.5 成功基準（Phase 3）

- ✅ 複数Agentの会話履歴がMioに集約される
- ✅ すべての通信がログに記録される
- ✅ SSH接続が安定している（再接続機能動作）
- ✅ パフォーマンス劣化が許容範囲内（v3.0比で20%以内）
- ✅ 本番移行準備完了（ドキュメント、運用手順）

---

## 6. 通信フォーマット仕様

### 6.1 基本メッセージ構造

**JSON Schema**（付録Aに詳細）:

```json
{
  "from": "Worker",
  "to": "Coder3",
  "session_id": "20260302-line-U123456",
  "job_id": "20260302-153005-abc12345",
  "message": "hello.goを作成してください",
  "context": {
    "user_request": "hello.goを作成して",
    "history": [
      {"role": "user", "content": "..."},
      {"role": "assistant", "content": "..."}
    ]
  },
  "proposal": null,
  "result": null,
  "timestamp": "2026-03-02T15:30:00Z"
}
```

**必須フィールド**:
- `from` (string): 送信元Agent名（"Mio", "Worker", "Coder1", "Coder2", "Coder3"）
- `to` (string): 送信先Agent名
- `session_id` (string): セッション識別子（形式: `YYYYMMDD-channel-chatID`、例: `20260302-line-U123456`）
- `job_id` (string): Job識別子（形式: `YYYYMMDD-HHMMSS-uuid8`、例: `20260302-153005-abc12345`）
- `message` (string): 日本語メッセージ（自然言語）
- `timestamp` (string): タイムスタンプ（RFC3339形式）

**任意フィールド**:
- `context` (object): コンテキスト情報（ユーザーリクエスト、会話履歴等）
- `proposal` (object): Proposal（Coderのみ）
- `result` (object): 実行結果（Workerのみ）

### 6.2 Proposalメッセージ

**Coder → Worker への Proposal 送信**:

```json
{
  "from": "Coder3",
  "to": "Worker",
  "session_id": "20260302-line-U123456",
  "job_id": "20260302-153005-abc12345",
  "message": "以下のplanとpatchを確認してください",
  "proposal": {
    "plan": "ステップ1: pkg/test/ディレクトリの確認\nステップ2: hello.go作成\nステップ3: テスト追加",
    "patch": "[{\"action\": \"create\", \"path\": \"pkg/test/hello.go\", \"content\": \"package test\\n\\nfunc Hello() string {\\n\\treturn \\\"Hello World\\\"\\n}\"}]",
    "risk": "既存ファイルとの衝突の可能性（低）",
    "costHint": "見積もり: 5分"
  },
  "timestamp": "2026-03-02T15:30:05Z"
}
```

**Proposalフィールド**:
- `plan` (string): 実装計画（Markdownテキスト）
- `patch` (string): 差分（JSON文字列、PatchCommand配列）
- `risk` (string): リスク評価
- `costHint` (string): 工数見積もり

### 6.3 Resultメッセージ

**Worker → Mio への実行結果報告**:

```json
{
  "from": "Worker",
  "to": "Mio",
  "session_id": "20260302-line-U123456",
  "job_id": "20260302-153005-abc12345",
  "message": "hello.goの作成が完了しました",
  "result": {
    "status": "success",
    "created_files": ["pkg/test/hello.go"],
    "modified_files": [],
    "deleted_files": [],
    "git_commit": "abc123def456",
    "elapsed_time": "3.2s",
    "error_message": ""
  },
  "timestamp": "2026-03-02T15:30:10Z"
}
```

**Resultフィールド**:
- `status` (string): 実行状態（"success", "partial_success", "failed"）
- `created_files` (array): 作成されたファイル一覧
- `modified_files` (array): 変更されたファイル一覧
- `deleted_files` (array): 削除されたファイル一覧
- `git_commit` (string): Git commit hash（auto-commit有効時）
- `elapsed_time` (string): 実行時間
- `error_message` (string): エラーメッセージ（失敗時）

### 6.4 Contextフィールド

**会話履歴の伝播**:

```json
{
  "context": {
    "user_request": "hello.goを作成して",
    "routing_decision": "CODE3",
    "history": [
      {
        "role": "user",
        "content": "hello.goを作成して",
        "timestamp": "2026-03-02T15:29:50Z"
      },
      {
        "role": "mio",
        "content": "承知しました。Workerに依頼します",
        "timestamp": "2026-03-02T15:29:55Z"
      },
      {
        "role": "worker",
        "content": "了解しました。Ginに依頼します",
        "timestamp": "2026-03-02T15:30:00Z"
      }
    ],
    "metadata": {
      "priority": "normal",
      "timeout": 300
    }
  }
}
```

**Contextフィールドの構成**:
- `user_request` (string): 元のユーザーリクエスト
- `routing_decision` (string): ルーティング決定（CHAT/PLAN/CODE等）
- `history` (array): 会話履歴（role + content + timestamp）
- `metadata` (object): メタデータ（優先度、タイムアウト等）

### 6.5 エラーメッセージ

**通信エラー時のフォーマット**:

```json
{
  "from": "Worker",
  "to": "Mio",
  "session_id": "20260302-line-U123456",
  "job_id": "20260302-153005-abc12345",
  "message": "実行に失敗しました",
  "result": {
    "status": "failed",
    "error_message": "ファイル作成時にエラー: permission denied",
    "error_code": "ERR_FILE_PERMISSION",
    "stack_trace": "..."
  },
  "timestamp": "2026-03-02T15:30:15Z"
}
```

**エラーコード一覧**:
- `ERR_CONNECTION_LOST`: 接続切断
- `ERR_TIMEOUT`: タイムアウト
- `ERR_FILE_PERMISSION`: ファイル権限エラー
- `ERR_GIT_CONFLICT`: Git衝突
- `ERR_PROTECTED_FILE`: 保護ファイル操作試行

---

## 7. Session/Memory分散管理

### 7.1 Session管理の設計

**v3.0（単一プロセス）**:
```
[Mio/Worker/Coder] → [共有Session] → [共有Memory]
```

**v4.0（分散実行）**:
```
[Mio]   → [MioMemory]
[Worker] → [WorkerMemory]
[Coder3] → [Coder3Memory]
  ↓
階層的に伝播
  ↓
[CentralMemory（Mio）] ← すべての会話ログを統合
```

### 7.1.1 Sessionのスレッドセーフ性

**課題**: v3の`Session`構造体（`internal/domain/session/session.go`）にはmutexが存在しない。v3では単一goroutineからのアクセスを前提としていたが、v4では以下の理由でスレッドセーフ性が必要になる。

- Transport層のReceive goroutineが複数のメッセージを並行処理する可能性
- Worker内のPatchCommand並列実行中にSessionの更新が発生する可能性
- 会話ログの伝播（§7.2）中にSessionへの読み書きが競合する可能性

**設計方針**: Agent単位のSession所有権

```
[v4.0 設計]
各AgentがローカルにSessionを所有し、書き込みは所有者のみ行う。
他AgentからのSessionデータはメッセージ（Context）経由で読み取り専用で受信する。

結果:
- Session構造体にmutexは追加しない（単一所有者の原則）
- Agentプロセス内では、メッセージ処理を直列化（1メッセージずつ処理）
- 並列アクセスが必要になった場合のみ、sync.RWMutex を導入
```

**実装ガイドライン**:

1. **v4.0初期**: Agentごとに1つのメッセージ処理goroutineを維持（直列処理）
   - `Receive()` → 処理 → `Send()` のループを1 goroutineで実行
   - Session への競合アクセスは発生しない

2. **v4.0後期（必要に応じて）**: sync.RWMutex 導入
   ```go
   type Session struct {
       mu        sync.RWMutex           // v4.0後期: 並行アクセス保護
       id        string
       channel   string
       chatID    string
       history   []task.Task
       memory    map[string]interface{}
       createdAt time.Time
       updatedAt time.Time
   }
   ```

3. **原則**: mutex導入は「計測してボトルネックが確認された場合のみ」とする（YAGNI）

### 7.2 階層的な会話ログ伝播

**伝播フロー**:
1. **Coder3 → Worker**: Coder3の会話ログがWorkerに伝播
2. **Worker → Mio**: Worker、Coder3の会話ログがMioに伝播
3. **Mioが全体把握**: すべての会話ログを受け取り、モニタリング

**実装例**:

```go
// Worker がメッセージを受信したとき、Coderの会話ログを取得
func (w *WorkerAgent) ReceiveFromCoder(msg transport.Message) {
    // Coderの会話ログを自分のMemoryに追加
    w.memory.AddConversation(msg)

    // Coderのメモリも取得（任意）
    if msg.Context != nil {
        if coderHistory, ok := msg.Context["coder_history"].([]interface{}); ok {
            for _, h := range coderHistory {
                w.memory.AddExternalConversation(h)
            }
        }
    }
}

// Worker が Mioに報告するとき、自分とCoderの会話ログを含める
func (w *WorkerAgent) SendToMio(msg transport.Message) {
    msg.Context["worker_history"] = w.memory.GetConversations()
    msg.Context["coder_history"] = w.memory.GetExternalConversations()

    w.transport.Send(context.Background(), msg)
}
```

### 7.3 Memory永続化

**設計ポイント**:
- 各AgentのMemoryはローカルファイルに保存
- 日次カットオーバーで古いMemoryをアーカイブ
- CentralMemory（Mio）は全体の会話ログを統合保存

**ファイル構成**:
```
$HOME/.picoclaw/memory/
  ├── mio/
  │   ├── 20260302-line-U123456.json
  │   ├── 20260302-slack-C789012.json
  │   └── central_memory_20260302.json  ← 統合ログ
  ├── worker/
  │   └── 20260302-line-U123456.json
  ├── coder3/
  │   └── 20260302-line-U123456.json
  └── archive/
      └── 2026-03-01/
```

**JSON形式**:
```json
{
  "session_id": "20260302-line-U123456",
  "agent_id": "Worker",
  "conversations": [
    {
      "from": "Mio",
      "to": "Worker",
      "message": "hello.goを作成してください",
      "timestamp": "2026-03-02T15:30:00Z"
    },
    {
      "from": "Worker",
      "to": "Coder3",
      "message": "hello.goを作成してください",
      "timestamp": "2026-03-02T15:30:05Z"
    }
  ],
  "last_update": "2026-03-02T15:30:10Z"
}
```

### 7.4 競合解決

**問題**: 複数Agentが同時に同じファイルを編集する場合

**解決策**:
1. **楽観的ロック**: 各AgentがファイルのETagを保持し、書き込み前にチェック
2. **ワークスペース分離**: Agent別のワークスペースディレクトリ（実験的）
3. **Git auto-commit**: 各編集をcommitし、衝突時はユーザーに通知

**実装例**:
```go
// Worker の patch 実行前にファイル状態を確認
func (w *WorkerExecutionService) ExecutePatch(ctx context.Context, patch []PatchCommand) error {
    for _, cmd := range patch {
        // ファイルのハッシュ値を計算（楽観的ロック）
        expectedHash := cmd.ExpectedHash
        if expectedHash != "" {
            actualHash := computeFileHash(cmd.Path)
            if actualHash != expectedHash {
                return fmt.Errorf("file conflict: %s (expected %s, got %s)", cmd.Path, expectedHash, actualHash)
            }
        }

        // patch実行
        if err := executePatchCommand(cmd); err != nil {
            return err
        }
    }

    return nil
}
```

---

## 8. セキュリティとエラーハンドリング

### 8.1 SSH セキュリティ

#### 8.1.1 公開鍵認証

**必須要件**:
- ✅ パスワード認証は禁止
- ✅ SSH鍵ペア（RSA 4096bit以上 または Ed25519）
- ✅ 秘密鍵はパスフレーズ保護（推奨）

**鍵生成手順**:
```bash
# Ed25519鍵の生成（推奨）
ssh-keygen -t ed25519 -f ~/.ssh/id_picoclaw -C "picoclaw-agent"

# 公開鍵をリモートホストに配置
ssh-copy-id -i ~/.ssh/id_picoclaw.pub picoclaw@192.168.1.100
```

**設定ファイル**:
```yaml
distributed:
  transports:
    worker:
      type: ssh
      remote_host: "192.168.1.100:22"
      remote_user: picoclaw
      ssh_key_path: /home/user/.ssh/id_picoclaw
      ssh_key_passphrase_env: PICOCLAW_SSH_PASSPHRASE  # 環境変数から読み込み
```

#### 8.1.2 ホスト鍵検証

**問題**: `ssh.InsecureIgnoreHostKey()` は本番環境で危険

**解決策**: `known_hosts` ファイルを使用

```go
// ホスト鍵検証（本番環境）
func createHostKeyCallback() (ssh.HostKeyCallback, error) {
    knownHostsPath := filepath.Join(os.Getenv("HOME"), ".ssh", "known_hosts")
    return knownhosts.New(knownHostsPath)
}

// SSH接続（ホスト鍵検証あり）
sshConfig := &ssh.ClientConfig{
    User: t.user,
    Auth: []ssh.AuthMethod{
        ssh.PublicKeys(key),
    },
    HostKeyCallback: createHostKeyCallback(), // ← 本番環境で必須
    Timeout:         10 * time.Second,
}
```

**初回接続時の登録**:
```bash
# リモートホストの鍵を known_hosts に登録
ssh-keyscan -H 192.168.1.100 >> ~/.ssh/known_hosts
```

#### 8.1.3 秘密鍵の保護

**ベストプラクティス**:
1. **環境変数**: 秘密鍵のパスを環境変数で指定
2. **パーミッション制限**: 秘密鍵ファイルは `chmod 600`
3. **鍵管理**: シークレットストア（HashiCorp Vault、AWS Secrets Manager等）

```go
// 環境変数から秘密鍵パスを取得
sshKeyPath := os.Getenv("PICOCLAW_SSH_KEY_PATH")
if sshKeyPath == "" {
    sshKeyPath = filepath.Join(os.Getenv("HOME"), ".ssh", "id_picoclaw")
}

// パーミッション確認
fileInfo, err := os.Stat(sshKeyPath)
if err != nil {
    return fmt.Errorf("failed to stat SSH key: %w", err)
}
if fileInfo.Mode().Perm() != 0600 {
    return fmt.Errorf("SSH key must have 0600 permissions, got %o", fileInfo.Mode().Perm())
}
```

### 8.2 メッセージ検証

#### 8.2.1 必須フィールドチェック

```go
// Message.Validate() の実装（再掲）
func (m *Message) Validate() error {
    if m.From == "" {
        return errors.New("from is required")
    }
    if m.To == "" {
        return errors.New("to is required")
    }
    if m.SessionID == "" {
        return errors.New("session_id is required")
    }
    if m.JobID == "" {
        return errors.New("job_id is required")
    }
    if m.Message == "" {
        return errors.New("message is required")
    }

    // タイムスタンプの形式チェック
    if _, err := time.Parse(time.RFC3339, m.Timestamp); err != nil {
        return fmt.Errorf("timestamp is invalid: %w", err)
    }

    return nil
}
```

#### 8.2.2 インジェクション対策

**問題**: メッセージ内の悪意あるコマンド

**対策**:
1. **ホワイトリスト検証**: Agent名、SessionID、JobIDのフォーマットチェック
2. **サニタイゼーション**: メッセージ内のシェルメタ文字のエスケープ

```go
// Agent名の検証
var validAgentNames = map[string]bool{
    "Mio":    true,
    "Worker": true,
    "Coder1": true,
    "Coder2": true,
    "Coder3": true,
}

func (m *Message) ValidateAgentNames() error {
    if !validAgentNames[m.From] {
        return fmt.Errorf("invalid from: %s", m.From)
    }
    if !validAgentNames[m.To] {
        return fmt.Errorf("invalid to: %s", m.To)
    }
    return nil
}

// SessionID/JobIDのフォーマット検証
func (m *Message) ValidateIDs() error {
    // SessionID形式: YYYYMMDD-{channel}-{chatID}（例: "20260301-line-U123456"）
    // v3既存形式を維持: 日付(8桁)-チャネル名(小文字)-チャットID
    sessionIDPattern := regexp.MustCompile(`^[0-9]{8}-[a-z]+-\S+$`)
    if !sessionIDPattern.MatchString(m.SessionID) {
        return fmt.Errorf("invalid session_id format: %s (expected YYYYMMDD-channel-chatID)", m.SessionID)
    }

    // JobID形式: YYYYMMDD-HHMMSS-{UUID先頭8文字}（例: "20260302-153005-abc12345"）
    // v3既存形式を維持: task.NewJobID() が生成するフォーマット
    jobIDPattern := regexp.MustCompile(`^[0-9]{8}-[0-9]{6}-[0-9a-f]{8}$`)
    if !jobIDPattern.MatchString(m.JobID) {
        return fmt.Errorf("invalid job_id format: %s (expected YYYYMMDD-HHMMSS-uuid8)", m.JobID)
    }

    return nil
}
```

### 8.3 エラーハンドリング

#### 8.3.1 接続エラー（再接続）

**戦略**: 指数バックオフで3回リトライ

```go
// 再接続ロジック
// reconnect はSSH接続を再確立する
// mu と sendMu の両方を取得し、接続状態とエンコーダ/デコーダを安全に再構築する
func (t *SSHTransport) reconnect() error {
    t.mu.Lock()
    t.sendMu.Lock()
    defer t.sendMu.Unlock()
    defer t.mu.Unlock()

    maxRetries := 3
    baseDelay := 1 * time.Second

    for i := 0; i < maxRetries; i++ {
        delay := baseDelay * time.Duration(1<<uint(i)) // 指数バックオフ

        log.Printf("Reconnecting (attempt %d/%d) after %v...", i+1, maxRetries, delay)
        time.Sleep(delay)

        if err := t.connect(); err == nil {
            log.Printf("Reconnection successful")
            return nil
        }
    }

    return fmt.Errorf("failed to reconnect after %d attempts", maxRetries)
}

// SendWithRetry は接続エラー時に再接続を試みてリトライする
func (t *SSHTransport) SendWithRetry(ctx context.Context, msg domaintransport.Message) error {
    err := t.Send(ctx, msg)
    if err != nil && isConnectionError(err) {
        // 再接続試行
        if reconnectErr := t.reconnect(); reconnectErr == nil {
            // リトライ
            return t.Send(ctx, msg)
        }
    }
    return err
}
```

#### 8.3.2 タイムアウト

**設定**:
- **接続タイムアウト**: 10秒
- **通信タイムアウト**: 30秒（通常）、300秒（Coder実行）

```go
// タイムアウト設定（コンテキスト使用）
func (t *SSHTransport) Send(ctx context.Context, msg transport.Message) error {
    // デフォルト30秒、Coder実行時は300秒
    timeout := 30 * time.Second
    if msg.To == "Coder1" || msg.To == "Coder2" || msg.To == "Coder3" {
        timeout = 300 * time.Second
    }

    ctx, cancel := context.WithTimeout(ctx, timeout)
    defer cancel()

    done := make(chan error, 1)
    go func() {
        done <- t.encoder.Encode(msg)
    }()

    select {
    case err := <-done:
        return err
    case <-ctx.Done():
        return fmt.Errorf("send timeout after %v", timeout)
    }
}
```

#### 8.3.3 部分的失敗

**問題**: 複数のPatchCommand実行中、途中で失敗した場合

**対策**: トランザクション的な実行（ロールバック）

```go
// Worker の patch 実行（トランザクション）
func (w *WorkerExecutionService) ExecutePatchWithRollback(ctx context.Context, patch []PatchCommand) error {
    executedCommands := []PatchCommand{}

    for i, cmd := range patch {
        if err := executePatchCommand(cmd); err != nil {
            // ロールバック
            log.Printf("ERROR: Failed to execute command %d, rolling back...", i)
            for j := len(executedCommands) - 1; j >= 0; j-- {
                rollbackCommand(executedCommands[j])
            }
            return fmt.Errorf("patch execution failed at step %d: %w", i, err)
        }
        executedCommands = append(executedCommands, cmd)
    }

    return nil
}
```

---

## 9. 実装プラン

### 9.1 8週間実装計画

#### Week 1-2: Phase 1（ローカル通信層）

| Week | タスク | 担当 | 成果物 |
|------|--------|------|--------|
| Week 1 | Transport インターフェース定義 | チームA | `internal/domain/transport/transport.go` |
| Week 1 | LocalTransport 実装 | チームA | `internal/infrastructure/transport/local.go` |
| Week 1 | MessageRouter 実装 | チームA | `internal/infrastructure/transport/router.go` |
| Week 2 | 単体テスト | チームA | `*_test.go`（カバレッジ90%以上） |
| Week 2 | 統合テスト | チームB | `integration_local_test.go` |
| Week 2 | v3.0共存確認 | チームB | `distributed.enabled: false` で全テスト通過 |

**マイルストーン（Week 2終了時）**:
- ✅ Transport抽象化完成
- ✅ ローカル通信でJSON送受信動作確認
- ✅ v3.0との共存確認完了

#### Week 3-5: Phase 2（リモート通信基盤）

| Week | タスク | 担当 | 成果物 |
|------|--------|------|--------|
| Week 3 | SSHTransport 実装 | チームA | `internal/infrastructure/transport/ssh.go` |
| Week 3 | Agent スタンドアロンモード実装 | チームB | `cmd/picoclaw-agent/main.go` |
| Week 4 | TransportFactory 実装 | チームA | `internal/infrastructure/transport/factory.go` |
| Week 4 | 設定ファイル拡張 | チームA | `config.yaml`、Config構造体 |
| Week 5 | SSH通信テスト | チームB | `ssh_test.go`, `e2e_remote_test.go` |
| Week 5 | E2Eシナリオテスト | チームB | Mio → Worker → Coder3 SSH通信 |

**マイルストーン（Week 5終了時）**:
- ✅ SSH通信動作確認
- ✅ Agent スタンドアロンモード動作確認
- ✅ E2Eテスト成功

#### Week 6-8: Phase 3（分散環境対応）

| Week | タスク | 担当 | 成果物 |
|------|--------|------|--------|
| Week 6 | Session/Memory分散管理 | チームA | `internal/domain/session/distributed.go` |
| Week 6 | 階層的ログ伝播 | チームA | AgentMemory、CentralMemory |
| Week 7 | 通信ログ・モニタリング | チームB | `internal/infrastructure/transport/logger.go` |
| Week 7 | 接続プーリング | チームB | `internal/infrastructure/transport/pool.go` |
| Week 8 | パフォーマンステスト | チームB | ベンチマーク、最適化 |
| Week 8 | ドキュメント作成 | 全員 | 運用手順、トラブルシューティング |

**マイルストーン（Week 8終了時）**:
- ✅ Session同期動作確認
- ✅ 通信ログ記録確認
- ✅ パフォーマンス目標達成（v3.0比20%以内）
- ✅ 本番移行準備完了

### 9.2 本番移行（4週間）

#### Week 9: 開発環境でv4.0有効化

```yaml
# config.yaml（開発環境）
distributed:
  enabled: true
  transports:
    mio:
      type: local
    worker:
      type: ssh
      remote_host: "dev-worker.local:22"
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw_dev
```

**確認項目**:
- [ ] SSH接続成功
- [ ] メッセージ送受信成功
- [ ] ログ記録確認
- [ ] エラーハンドリング確認

#### Week 10-11: ステージング環境で段階的有効化

**4ステップ移行**:
1. **ステップ1**: Mioのみローカル、他はSSH
2. **ステップ2**: Worker追加でSSH
3. **ステップ3**: Coder1/2/3をSSH
4. **ステップ4**: 全AgentをSSH（Mioのみローカル）

**確認項目**:
- [ ] 各ステップで全機能動作確認
- [ ] 負荷テスト（10並列リクエスト）
- [ ] 障害注入テスト（Agent再起動、ネットワーク切断）

#### Week 12: 本番環境で段階的移行

**ブルーグリーンデプロイ**:
- **ブルー（v3.0）**: `distributed.enabled: false`
- **グリーン（v4.0）**: `distributed.enabled: true`
- トラフィック切り替え: 10% → 50% → 100%

**ロールバック手順**:
1. 設定ファイルを `distributed.enabled: false` に変更
2. サービス再起動
3. v3.0モードで動作確認

---

## 10. テスト戦略

### 10.1 単体テスト

**対象**: Transport層の各コンポーネント

**テストケース**:

#### LocalTransport
```go
func TestLocalTransport_SendReceive(t *testing.T) { /* ... */ }
func TestLocalTransport_Close(t *testing.T) { /* ... */ }
func TestLocalTransport_Concurrent(t *testing.T) { /* 並行送受信テスト */ }
```

#### SSHTransport
```go
func TestSSHTransport_Connect(t *testing.T) { /* SSH接続テスト */ }
func TestSSHTransport_Reconnect(t *testing.T) { /* 再接続テスト */ }
func TestSSHTransport_Timeout(t *testing.T) { /* タイムアウトテスト */ }
```

#### MessageRouter
```go
func TestMessageRouter_Route(t *testing.T) { /* ルーティングテスト */ }
func TestMessageRouter_AgentNotFound(t *testing.T) { /* Agent未登録エラー */ }
```

**カバレッジ目標**: Transport層 90%以上

### 10.2 統合テスト

**対象**: Agent間通信フロー

**テストケース**:

#### ローカル通信統合テスト
```go
func TestIntegration_MioToWorkerToCoder_Local(t *testing.T) {
    // Mio → Worker → Coder3 の通信フロー（ローカル）
    // 期待: 各Agentがメッセージを正しく受信
}
```

#### リモート通信統合テスト
```go
func TestIntegration_MioToWorkerToCoder_SSH(t *testing.T) {
    // Mio → Worker → Coder3 の通信フロー（SSH）
    // 期待: SSH経由で正しく通信
}
```

#### Session同期テスト
```go
func TestIntegration_SessionSync(t *testing.T) {
    // Coder3 → Worker → Mio の会話ログ伝播
    // 期待: Mio が Coder3 の会話ログを受信
}
```

### 10.3 E2Eテスト

**対象**: ユーザーリクエスト → 最終応答の完全フロー

**シナリオ1: ファイル作成**:
```
ユーザー: 「hello.goを作成して」
  ↓
Mio: ルーティング決定（CODE3）
  ↓
Worker: Ginに委譲
  ↓
Gin: Proposal生成
  ↓
Worker: patch実行
  ↓
Mio: ユーザーに返信「hello.goを作成しました」
```

**期待結果**:
- ✅ `pkg/test/hello.go` が作成される
- ✅ Git commit が作成される（auto-commit有効時）
- ✅ すべての通信がログに記録される

**シナリオ2: Worker内PatchCommand並列実行（v4.0）**:
```
ユーザー: 「file1.go、file2.go、file3.goを同時に作成して」
  ↓
Mio: ルーティング決定（CODE3）
  ↓
Worker: Coder3に委譲
  ↓
Coder3: Proposal生成（3ファイルのPatchCommand配列）
  ↓
Worker: PatchCommand並列実行
  ├─ [goroutine1] file_edit/create: file1.go
  ├─ [goroutine2] file_edit/create: file2.go
  └─ [goroutine3] file_edit/create: file3.go
  ↓
Worker: すべての結果を統合 → git auto-commit
  ↓
Mio: ユーザーに返信「3つのファイルを作成しました」
```

**期待結果**:
- ✅ 3ファイルがすべて作成される
- ✅ ファイル作成部分が並列実行により短縮される（v3.0比20-30%）
- ✅ git_operation コマンドは全file_edit完了後に順次実行

### 10.4 パフォーマンステスト

**ベンチマーク**:

```go
func BenchmarkLocalTransport_Send(b *testing.B) {
    transport := NewLocalTransport()
    defer transport.Close()

    msg := createTestMessage()
    ctx := context.Background()

    b.ResetTimer()
    for i := 0; i < b.N; i++ {
        transport.Send(ctx, msg)
    }
}

func BenchmarkSSHTransport_Send(b *testing.B) {
    transport := setupSSHTransport(b)
    defer transport.Close()

    msg := createTestMessage()
    ctx := context.Background()

    b.ResetTimer()
    for i := 0; i < b.N; i++ {
        transport.Send(ctx, msg)
    }
}
```

**目標**:
- **LocalTransport**: 10,000 msg/sec
- **SSHTransport**: 100 msg/sec
- **E2Eレイテンシ**: v3.0比 +20%以内

### 10.5 障害注入テスト

**テストケース**:

1. **SSH接続切断**:
   - 期待: 再接続成功、メッセージ再送
2. **Agent再起動**:
   - 期待: 他のAgentは継続稼働
3. **ネットワーク遅延**:
   - 期待: タイムアウト後にエラー返却
4. **不正なメッセージ**:
   - 期待: バリデーションエラー、正常拒否

---

## 11. 移行パス

### 11.1 v3.0との互換性

**設計原則**:
- ✅ **v3.0機能が100%動作**
- ✅ **設定フラグで切り替え**（`distributed.enabled`）
- ✅ **既存コードの破壊的変更なし**

**互換性確認**:
```bash
# v3.0モードで全テスト実行
export PICOCLAW_DISTRIBUTED_ENABLED=false
go test ./...

# v4.0モードで全テスト実行
export PICOCLAW_DISTRIBUTED_ENABLED=true
go test ./...
```

### 11.2 段階的移行（4ステップ、12週間）

#### ステップ1: ローカルのみ（Week 1-2）

```yaml
# config.yaml
distributed:
  enabled: true
  transports:
    mio:
      type: local
    worker:
      type: local
    coder3:
      type: local
    coder1:
      type: local
    coder2:
      type: local
```

**確認項目**:
- [ ] ローカル通信で全機能動作
- [ ] v3.0と同等の性能

#### ステップ2: Workerのみリモート（Week 3-5）

```yaml
# config.yaml
distributed:
  enabled: true
  transports:
    mio:
      type: local
    worker:
      type: ssh
      remote_host: "192.168.1.100:22"
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw
    coder3:
      type: local
    coder1:
      type: local
    coder2:
      type: local
```

**確認項目**:
- [ ] Mio → Worker SSH通信成功
- [ ] Worker → Coder（ローカル）通信成功

#### ステップ3: Coder1/2/3リモート（Week 6-8）

```yaml
# config.yaml
distributed:
  enabled: true
  transports:
    mio:
      type: local
    worker:
      type: ssh
      remote_host: "192.168.1.100:22"
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw
    coder3:
      type: ssh
      remote_host: "192.168.1.101:22"
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw
    coder1:
      type: ssh
      remote_host: "192.168.1.102:22"
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw
    coder2:
      type: ssh
      remote_host: "192.168.1.103:22"
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw
```

**確認項目**:
- [ ] Worker → Coder SSH通信成功
- [ ] 並列実行による性能向上確認

#### ステップ4: 本番環境展開（Week 9-12）

**ブルーグリーンデプロイ**:
1. **Week 9**: 開発環境でv4.0有効化
2. **Week 10-11**: ステージング環境で段階的有効化
3. **Week 12**: 本番環境で段階的移行（10% → 50% → 100%）

### 11.3 ロールバック手順

**トリガー**:
- 重大なバグ発見
- パフォーマンス劣化（v3.0比 +30%以上）
- SSH接続障害（復旧不可能）

**手順**:
1. **設定ファイル変更**:
   ```yaml
   distributed:
     enabled: false  # v3.0モードに戻す
   ```

2. **サービス再起動**:
   ```bash
   sudo systemctl restart picoclaw
   ```

3. **動作確認**:
   ```bash
   # v3.0モードで全機能確認
   curl -X POST http://localhost:8080/api/chat \
     -H "Content-Type: application/json" \
     -d '{"message": "hello"}'
   ```

4. **ログ確認**:
   ```bash
   # v3.0モードで動作しているか確認
   journalctl -u picoclaw | grep "distributed.enabled=false"
   ```

**所要時間**: 5分以内

**データロス**: なし（v3.0のMemory形式を維持）

### 11.4 移行チェックリスト

#### 事前準備

- [ ] SSH鍵ペア生成（Ed25519）
- [ ] リモートホストへの公開鍵配置
- [ ] `known_hosts` にホスト鍵登録
- [ ] 設定ファイル作成（`config.yaml`）
- [ ] バックアップ取得（Memory、設定ファイル）

#### Phase 1（ローカル通信層）

- [ ] Transport インターフェース実装
- [ ] LocalTransport 実装
- [ ] MessageRouter 実装
- [ ] 単体テスト通過（カバレッジ90%以上）
- [ ] v3.0共存確認

#### Phase 2（リモート通信基盤）

- [ ] SSHTransport 実装
- [ ] Agent スタンドアロンモード実装
- [ ] TransportFactory 実装
- [ ] 設定ファイル拡張
- [ ] E2Eテスト成功

#### Phase 3（分散環境対応）

- [ ] Session/Memory分散管理実装
- [ ] 通信ログ実装
- [ ] 接続プーリング実装
- [ ] パフォーマンステスト合格
- [ ] ドキュメント作成完了

#### 本番移行

- [ ] ステップ1完了（ローカルのみ）
- [ ] ステップ2完了（Workerリモート）
- [ ] ステップ3完了（Coderリモート）
- [ ] ステップ4完了（本番環境展開）
- [ ] 監視設定（Prometheus、Grafana等）

---

## 付録A: JSON Schema定義

### A.1 Message Schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PicoClaw Transport Message",
  "type": "object",
  "required": ["from", "to", "session_id", "job_id", "message", "timestamp"],
  "properties": {
    "from": {
      "type": "string",
      "enum": ["Mio", "Worker", "Coder1", "Coder2", "Coder3"],
      "description": "送信元Agent名"
    },
    "to": {
      "type": "string",
      "enum": ["Mio", "Worker", "Coder1", "Coder2", "Coder3"],
      "description": "送信先Agent名"
    },
    "session_id": {
      "type": "string",
      "pattern": "^[0-9]{8}-[a-z]+-\\S+$",
      "description": "セッション識別子（形式: YYYYMMDD-channel-chatID、例: 20260301-line-U123456）"
    },
    "job_id": {
      "type": "string",
      "pattern": "^[0-9]{8}-[0-9]{6}-[0-9a-f]{8}$",
      "description": "Job識別子（形式: YYYYMMDD-HHMMSS-uuid8、例: 20260302-153005-abc12345）"
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "description": "日本語メッセージ"
    },
    "context": {
      "type": "object",
      "description": "コンテキスト情報",
      "properties": {
        "user_request": { "type": "string" },
        "routing_decision": { "type": "string" },
        "history": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "role": { "type": "string" },
              "content": { "type": "string" },
              "timestamp": { "type": "string", "format": "date-time" }
            }
          }
        },
        "metadata": { "type": "object" }
      }
    },
    "proposal": {
      "type": "object",
      "description": "Proposal（Coderのみ）",
      "properties": {
        "plan": { "type": "string" },
        "patch": { "type": "string" },
        "risk": { "type": "string" },
        "costHint": { "type": "string" }
      }
    },
    "result": {
      "type": "object",
      "description": "実行結果（Workerのみ）",
      "properties": {
        "status": {
          "type": "string",
          "enum": ["success", "partial_success", "failed"]
        },
        "created_files": {
          "type": "array",
          "items": { "type": "string" }
        },
        "modified_files": {
          "type": "array",
          "items": { "type": "string" }
        },
        "deleted_files": {
          "type": "array",
          "items": { "type": "string" }
        },
        "git_commit": { "type": "string" },
        "elapsed_time": { "type": "string" },
        "error_message": { "type": "string" }
      }
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "タイムスタンプ（RFC3339形式）"
    }
  }
}
```

### A.2 Proposal Schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PicoClaw Proposal",
  "type": "object",
  "required": ["plan", "patch"],
  "properties": {
    "plan": {
      "type": "string",
      "description": "実装計画（Markdownテキスト）"
    },
    "patch": {
      "type": "string",
      "description": "差分（JSON文字列、PatchCommand配列）"
    },
    "risk": {
      "type": "string",
      "description": "リスク評価"
    },
    "costHint": {
      "type": "string",
      "description": "工数見積もり"
    }
  }
}
```

### A.3 Result Schema

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PicoClaw Execution Result",
  "type": "object",
  "required": ["status", "elapsed_time"],
  "properties": {
    "status": {
      "type": "string",
      "enum": ["success", "partial_success", "failed"],
      "description": "実行状態"
    },
    "created_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "作成されたファイル一覧"
    },
    "modified_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "変更されたファイル一覧"
    },
    "deleted_files": {
      "type": "array",
      "items": { "type": "string" },
      "description": "削除されたファイル一覧"
    },
    "git_commit": {
      "type": "string",
      "description": "Git commit hash（auto-commit有効時）"
    },
    "elapsed_time": {
      "type": "string",
      "description": "実行時間"
    },
    "error_message": {
      "type": "string",
      "description": "エラーメッセージ（失敗時）"
    }
  }
}
```

---

## 付録B: SSH接続設定例

### B.1 SSH鍵生成

```bash
# Ed25519鍵の生成（推奨）
ssh-keygen -t ed25519 -f ~/.ssh/id_picoclaw -C "picoclaw-agent"

# または RSA 4096bit
ssh-keygen -t rsa -b 4096 -f ~/.ssh/id_picoclaw -C "picoclaw-agent"

# パーミッション設定
chmod 600 ~/.ssh/id_picoclaw
chmod 644 ~/.ssh/id_picoclaw.pub
```

### B.2 公開鍵の配置

```bash
# リモートホストに公開鍵をコピー
ssh-copy-id -i ~/.ssh/id_picoclaw.pub picoclaw@192.168.1.100

# または手動で配置
cat ~/.ssh/id_picoclaw.pub | ssh picoclaw@192.168.1.100 \
  "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### B.3 known_hosts の設定

```bash
# ホスト鍵を事前に登録
ssh-keyscan -H 192.168.1.100 >> ~/.ssh/known_hosts
ssh-keyscan -H 192.168.1.101 >> ~/.ssh/known_hosts
ssh-keyscan -H 192.168.1.102 >> ~/.ssh/known_hosts
ssh-keyscan -H 192.168.1.103 >> ~/.ssh/known_hosts
```

### B.4 SSH設定ファイル（オプション）

**`~/.ssh/config`**:

```
Host picoclaw-worker
    HostName 192.168.1.100
    User picoclaw
    IdentityFile ~/.ssh/id_picoclaw
    StrictHostKeyChecking yes
    ServerAliveInterval 60
    ServerAliveCountMax 3

Host picoclaw-coder1
    HostName 192.168.1.101
    User picoclaw
    IdentityFile ~/.ssh/id_picoclaw
    StrictHostKeyChecking yes
    ServerAliveInterval 60
    ServerAliveCountMax 3

Host picoclaw-coder2
    HostName 192.168.1.102
    User picoclaw
    IdentityFile ~/.ssh/id_picoclaw
    StrictHostKeyChecking yes
    ServerAliveInterval 60
    ServerAliveCountMax 3

Host picoclaw-coder3
    HostName 192.168.1.103
    User picoclaw
    IdentityFile ~/.ssh/id_picoclaw
    StrictHostKeyChecking yes
    ServerAliveInterval 60
    ServerAliveCountMax 3
```

**使用例**:
```yaml
# config.yaml
distributed:
  transports:
    worker:
      type: ssh
      remote_host: "picoclaw-worker:22"  # SSH設定ファイルの Host名
      remote_user: picoclaw
      ssh_key_path: ~/.ssh/id_picoclaw
```

### B.5 リモートホストの準備

**リモートホストで実行**:

```bash
# picoclaw-agent バイナリの配置
scp ./bin/picoclaw-agent picoclaw@192.168.1.100:/usr/local/bin/

# 実行権限付与
ssh picoclaw@192.168.1.100 "chmod +x /usr/local/bin/picoclaw-agent"

# 動作確認
ssh picoclaw@192.168.1.100 "picoclaw-agent --version"
```

---

## 付録C: 用語集

### C.1 Transport層

| 用語 | 説明 |
|------|------|
| **Transport** | Agent間通信を抽象化したインターフェース |
| **LocalTransport** | 同一プロセス内通信の実装（チャネルベース） |
| **SSHTransport** | SSH経由のリモート通信の実装 |
| **MessageRouter** | Agent名に基づくメッセージルーティング |
| **TransportFactory** | Transport生成のファクトリ |
| **TransportPool** | Transport接続プール |

### C.2 メッセージ

| 用語 | 説明 |
|------|------|
| **Message** | Agent間通信の基本メッセージ |
| **Proposal** | Coderが生成する実装提案（plan/patch/risk） |
| **Result** | Workerの実行結果 |
| **Context** | メッセージのコンテキスト情報 |
| **SessionID** | セッション識別子（形式: `YYYYMMDD-channel-chatID`、例: `20260302-line-U123456`） |
| **JobID** | Job識別子（形式: `YYYYMMDD-HHMMSS-uuid8`、例: `20260302-153005-abc12345`） |

### C.3 Session/Memory

| 用語 | 説明 |
|------|------|
| **AgentMemory** | 各Agentが持つ独立したメモリ |
| **CentralMemory** | Mioが持つ全体統合メモリ |
| **会話ログ伝播** | Coder → Worker → Mio の階層的なログ伝播 |
| **日次カットオーバー** | 日次でSessionをリセットし、古いMemoryをアーカイブ |

### C.4 セキュリティ

| 用語 | 説明 |
|------|------|
| **公開鍵認証** | SSHの認証方式（パスワード認証は禁止） |
| **ホスト鍵検証** | `known_hosts` を使用したSSHホスト鍵検証 |
| **楽観的ロック** | ファイル編集時のETagチェック |
| **ワークスペース分離** | Agent別のワークスペースディレクトリ（実験的） |

### C.5 その他

| 用語 | 説明 |
|------|------|
| **v3.0** | Clean Architectureベースの単一プロセス実装 |
| **v4.0** | 分散実行対応版（Transport層追加） |
| **Phase 1-3** | v4.0の段階的実装フェーズ |
| **ブルーグリーンデプロイ** | v3.0/v4.0の並行稼働と段階的切り替え |
| **指数バックオフ** | 再接続時の待機時間を指数的に増加させる戦略 |

---

**ドキュメント作成完了**: 2026-03-02
**バージョン**: v4.0
**次のステップ**:
1. `docs/README.md` に v4.0 ドキュメントへのリンクを追加
2. `docs/実装仕様_v3.md` に v4.0 への参照を追加
3. `CLAUDE.md` に v4.0 ドキュメントを参照リンクに追加
4. Phase 1 実装開始
